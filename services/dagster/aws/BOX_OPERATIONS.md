# EC2 Box Operations — session reference

**Read this before any session that touches the box (pipeline / serving / ingestion / dbt / deploy).** It consolidates the operational mechanics that sessions kept re-discovering. Authoritative as of 2026-06-30; if a file contradicts this doc, the file wins — and fix the doc.

The "box" = a self-hosted **Dagster OSS** orchestrator on a single **AWS EC2** instance, run via **Docker Compose** under `services/dagster/aws/`. It replaced Railway (INC-16). It is the SOLE live orchestrator.

> 🚨 Pair this with the **"RECURRING LANDMINES"** block in `CLAUDE.md` (inline Snowflake key, boto3 instance-role, DuckDB region, binary-parquet-timestamp, VALUE:-case, box-aware memory, the runtime gate). This doc is the "how it works"; CLAUDE.md is the "what bites you."

---

## 1. Box facts (`infrastructure/aws_resources.md`)
| | |
|---|---|
| Instance ID | `i-07594af1679f81c38` |
| Type | `r6g.large` (arm64, 2 vCPU, **16 GB RAM**) — resized from `t4g.medium` (4 GB) on INC-22 after DuckDB OOM-killed the host |
| Elastic IP | `100.57.225.242` (stable — FanGraphs `cf_clearance` is bound to this IP) |
| Region | **us-east-1** (the instance). ⚠️ but DuckDB S3 reads of `baseball-betting-ml-artifacts` need **`us-east-2`** explicitly |
| OS | Amazon Linux 2023 arm64 |
| IAM | role `credence-dagster-ec2-role` — **no static AWS keys on the box**; S3/DynamoDB via the instance role (`credential_chain`); `AWS_ACCESS_KEY_ID` is UNSET |
| Public dagit | `https://dagster.credencesports.com` (Caddy HTTPS + basic-auth) |
| Cost | ~$73/mo |

**Access:** prefer **SSM** `aws ssm start-session --target i-07594af1679f81c38` (no SSH needed). SSH fallback: `ssh -i ~/.ssh/credence-dagster-key.pem ec2-user@100.57.225.242`. Repo on the box is at `~/app`.

---

## 2. Docker Compose layout (`services/dagster/aws/docker-compose.yml`)
Six core services on the `credence-dagster` bridge network:

| Service | Role |
|---|---|
| `dagster-postgres` | Dagster run/event/schedule metadata ONLY (not serving cache; regenerable) |
| `dagster-codeloc` | gRPC code server + run worker; needs the full op env (`env_file: .env`) incl. `DBT_RUNNER_URL` + `FLARESOLVERR_URL` |
| `dagster-daemon` | scheduler + sensors + run queue + heartbeat |
| `dagster-webserver` | dagit UI, bound `127.0.0.1:3000` (public via Caddy only) |
| `dbt-runner` | out-of-process dbt execution at `http://dbt-runner:8080` (see §4) |
| `flaresolverr` | FanGraphs Cloudflare solver at `http://flaresolverr:8191/v1` (see §5) |
| `caddy` | HTTPS reverse-proxy + basic-auth for dagit (Let's Encrypt) |
| `autoheal` | restarts containers Docker has marked `unhealthy` (label-scoped: byparr only — 2026-08-03) |

**Named volumes (the only state that survives a deploy):** `dagster_pg_data` (run history + every schedule's on/off state), `caddy_data` / `caddy_config` (ACME certs), and **`sports_duckdb` → `/var/lib/credence/sports` on `dagster-codeloc`** (NF-INFRA1: the sports NCAAF/NFL DuckDB — see §10b `SPORTS_DUCKDB_PATH`). ⛔ **Never `docker compose down -v` on the box** — that deletes all four, i.e. run history, schedule on/off state, the TLS cert and the sports database in one command.

**Run a script in a container (the common one):**
```bash
docker compose -f services/dagster/aws/docker-compose.yml exec dagster-codeloc \
  python scripts/<script>.py <args>
```
`-T` (no TTY) for non-interactive/cron contexts; add `-e AWS_DEFAULT_REGION=us-east-2` when the script does a DuckDB S3 read.

**Bring up / rebuild:**
```bash
docker compose -f services/dagster/aws/docker-compose.yml up -d --build
docker compose -f services/dagster/aws/docker-compose.yml --profile capture build
```

**Capture profile** = run-once cron images (NOT started by `up`): `odds-capture`, `schedule-capture`, `derivative-capture`, `weather-capture`. Fired by host cron (see §3), invoked `... run --rm <svc>`.

---

## 3. Host crontab / captures (`services/dagster/aws/capture.crontab`)
The intraday data captures run as **host cron**, not Dagster schedules (they were re-homed off Dagster+ to avoid the metered run-minutes).

| Schedule | Job | Tier |
|---|---|---|
| `*/30` | `odds-capture` → `mlb_odds_raw` (h2h/totals, all books) | **HALT** (serving-critical odds) |
| `*/30` | `schedule-capture` → intraday schedule + lineup-staging dbt rebuild (self-guards 14:00–03:00 UTC) | WARN |
| `*/30` | `derivative-capture` → derivative odds (team_totals/F5/…) | WARN |
| `0 * * * *` | `weather-capture` (self-guards 10:00–02:00 UTC) | WARN |
| `0 13` | MLB player-prop forward catch-up (`backfill_multisport_props_to_s3.py … --player-props-only`) | WARN |
| `30 12,17` | `check_data_freshness.py` (pages via SNS) | WARN |
| `*/5` | `healthcheck.sh` (core containers up; 1h cooldown) | ALERT |

Logs: `/home/ec2-user/capture-cron.log`. Install: `crontab ~/app/services/dagster/aws/capture.crontab` (deploy.sh reinstalls it — see §6). ⚠️ **INC-23 (2026-06-30)** hardens this to reconcile on EVERY deploy + ALERT if the odds-capture line is missing (a box resize/reprovision had silently dropped it → 5.6h stale odds). ⚠️ Note: an **intraday schedule-capture also exists inside Dagster** now (Option 2, INC-22, gated `SCHEDULE_LAKEHOUSE_INTRADAY=1`) because the lean host-cron only refreshed Snowflake views, never the parquet prod serves — don't double-run the two.

---

## 4. dbt on the box (out-of-process — MANDATORY)
**Never run `dbtf` in-process inside a Dagster op.** All dbt goes through `pipeline/ops/_dbt_exec._run_dbt`, which POSTs to the `dbt-runner` service (`DBT_RUNNER_URL=http://dbt-runner:8080`), polls `/status`, 30-min hard timeout, single-tenant (409 = busy → retry). If `DBT_RUNNER_URL` is unset it falls back to a local `dbtf` subprocess (dev/CI only) and that skip must be LOUD (INC-5/ALERT-tier).

**Local validation a session must run before handoff:**
```bash
uv run pytest -m "not slow" -n auto      # fast gate (~15s)
uv run pytest -m slow -n auto            # slow gate (~95s) if you touched @slow tests
scripts/dbt_state.sh build --select state:modified+ --target dev   # ⬅ use the wrapper
dbtf compile                              # full compile check
```
⚠️ **Always use `scripts/dbt_state.sh` for `state:modified+`** — plain `dbtf … state:modified+` without `--state` makes fusion 404 against dbt Platform (we're self-managed) and the selection breaks (E11.16). The wrapper injects `--state dbt/state --defer`, mirroring CI.

---

## 5. Flaresolverr (FanGraphs Cloudflare)
FanGraphs sits behind a Cloudflare JS challenge → direct requests 403. `flaresolverr` runs headless Chrome to solve it. Its `cf_clearance` is **bound to the box's egress IP + TLS fingerprint**, so it MUST be co-located on the box (this is why the EIP is stable). Scripts reach it via `FLARESOLVERR_URL=http://flaresolverr:8191/v1`; client = `scripts/utils/fangraphs_client.py` (`fetch_leaderboard`/`fetch_projections`, sends the full URL with `cmd: request.get`). FanGraphs ingests are **WARN-tier** (`pipeline/ops/daily_ingestion_ops.py`) — a Cloudflare/flaresolverr outage degrades quietly (Stuff+ is a nullable LEFT JOIN with a Statcast fallback). Validate: `... exec dagster-codeloc python services/dagster/aws/validate_flaresolverr.py`.

---

## 6. Deploy / CD (`services/dagster/aws/deploy.sh` + `.github/workflows/orchestration_cd.yml`)
CD = GitHub Actions OIDC → SSM RunCommand → `deploy.sh` on the box (auto on merge to main for `dagster/**`/`scripts/**`; or run manually via SSM). Flow, atomic with auto-rollback:
1. `git pull origin main` (FIRST — so env-parity validates the new env)
2. env-parity check vs `env.required` (every key present + non-empty)
3. snapshot images → `:rollback`
4. graceful drain (≤600s for in-flight runs)
5. **`docker compose up -d --build` + `--profile capture build`**
6. reconcile host crontab (capture.crontab)
7. verify (daemon up / defs import / dbt-runner health / PEM materialized / IMDS hop-2) → **rollback on any failure**

🚨 **BAKED-IMAGE DRIFT (the recurring gotcha):** a `git pull` updates the working tree but the running containers keep the OLD image — code is COPY'd in at build. **Only `up -d --build` ships new code.** A "successful" deploy without `--build` silently runs stale code.

🟥 **The merge bar for box code is NOT CI-green** (CI mocks all IO). For pipeline / serving / writers / sensors / date-tz / boto3 / DuckDB-on-box changes: **CI green AND the relevant op actually RAN once on the box** (a scoped run is fine — skip the stable W1 pitch rebuild, run the targeted chain). See CLAUDE.md "RUNTIME GATE".

---

## 7. Snowflake private key on the box (INLINE, not a file)
Compose `env_file:` can't carry real PEM newlines, so the key arrives as **`SNOWFLAKE_PRIVATE_KEY`** (raw / base64 / `\n`-escaped), NOT a file. The shared resolver (in `pipeline/resources/__init__.py`, `scripts/utils/snowflake_loader.py`, `betting_ml/utils/data_loader.py`, `scripts/ingest_statsapi.py`) resolves in order: **`SNOWFLAKE_PRIVATE_KEY_PATH` if the file exists → inline `SNOWFLAKE_PRIVATE_KEY` (normalized + written to `/tmp/snowflake_rsa_key.pem` 0600) → `SNOWFLAKE_PASSWORD`**. ⚠️ Older code that reads `SNOWFLAKE_PRIVATE_KEY_PATH` raw FAILS on the box — use the resolver pattern, or stay pure DuckDB/S3.

---

## 8. DuckDB memory on the box (box-aware)
`scripts/run_w1_lakehouse.py::_safe_memory_limit_gb()` = `clamp(0.6 × physical_RAM, 2, 11)` (≈9.6 GB on the 16 GB box). **Never hardcode `memory_limit` above physical RAM** — a hardcoded 11 GB on the old 4 GB box never spilled → kernel OOM-killed the HOST incl. Dagster (INC-22). Heavy flatten tiers also set `threads=2` to keep the working set spillable.

---

## 9. Quick "how do I…" index
- **Run a one-off script on the box:** `docker compose -f services/dagster/aws/docker-compose.yml exec -T dagster-codeloc python scripts/X.py …` (add `-e AWS_DEFAULT_REGION=us-east-2` for DuckDB S3).
- **Re-serve a specific date:** `… exec dagster-codeloc python scripts/write_serving_store.py --date YYYY-MM-DD --picks …`.
- **Debug a downstream (feature/serving) change fast:** SKIP `run_w1_lakehouse`'s full W1 pitch rebuild (~10 min); run only the targeted chain (`--w8a-only` etc.) — safe per the INC-21 recovery.
- **Run/validate dbt:** `scripts/dbt_state.sh build --select state:modified+ --target dev` locally; on the box it goes through `dbt-runner`.
- **Check the box / containers:** SSM in → `docker compose -f ~/app/services/dagster/aws/docker-compose.yml ps`; logs at `~/capture-cron.log`.
- **Ship a code change to the box:** merge to main (CD) OR SSM in + `deploy.sh` — and remember it needs `--build`.

---

## 10. Flag / schedule / sensor INTENDED-STATE table (E11.23 — the cutover-runtime-landmine cure)
The E11.1 cutover left a class of RUNTIME failures CI can't see (it mocks all IO): **intraday refresh jobs shipped GATED-OFF** and **serving-critical sensors/schedules that boot STOPPED** → they silently NEVER RUN (odds froze 3 days with NO alert; the lineup monitor was dead 2 days; K-page empty). This section is the source of truth for what MUST be on. Two structural cures back it up:
- **Self-start in code:** every serving-critical sensor + the primary schedules now declare `default_status=RUNNING`, so they auto-start on the box and after any Dagster-DB reset / re-host (the INC-16 class) instead of booting STOPPED.
- **A heartbeat detector:** `check_monitors_healthy_op` runs inside every `daily_ingestion_job` and ALARMS (email CRITICAL, ALERT-tier — never HALT) if a critical sensor/schedule is manually STOPPED or a permanently-on intraday flag is unset. Its critical sets + required flags mirror the tables below — **extend both together.**

> 🟥 **RUNTIME GATE:** the "Intended" column is the target; the box's ACTUAL state is only verifiable ON the box. After any change here, confirm on the box (`docker compose … exec -T dagster-codeloc env | grep <FLAG>`, and Dagit → Sensors/Schedules) — CI cannot see it.

> ⚖️ **TABLE HYGIENE (E11.24 target 6, 2026-08-05 — this table had CONTRADICTED ITSELF, and that is how an UNARMED slate got reported as armed).** The `E11_24_UMPIRE_REBUILD_GATE` row read **Intended `0`** in its own column while an appended note in the same cell read **"✅ …`=1` … LIVE + ARMED"**. A reader could take either as the truth, so "is the gate on?" had no answer here — and the FU-1 measurement was written up against the wrong one. **THREE RULES, now binding on every row:**
> 1. **The Intended column is the SOLE source of truth for intended state.** If a flip lands, EDIT THE COLUMN — ⛔ never leave the column stale and record the new state only in an appended note. A note that disagrees with its own column is a defect in this table, not a footnote.
> 2. **An observation is not an intention.** A "✅ verified" note records what the box read AT AN INSTANT and MUST carry that instant (UTC) — the flag alone is meaningless without it, because **a flag flip is INERT until the EXECUTING container is recreated** (FU-1, fired 3×). Date every such claim against `docker inspect -f '{{.State.StartedAt}}' <container>` and the run's own timestamp, or it is unfalsifiable.
> 3. **⛔ An `exec`-based read (`printenv`, `/proc/1/environ`) NEVER establishes what a PAST run saw** — it reports the container you just entered, which may post-date the run. The only restart-proof evidence is the **Postgres EVENT LOG** (`DagsterInstance.all_logs(run_id)` from inside the container): `context.log.info(...)` persists there indefinitely, while raw stdout is `LocalComputeLogManager` = ephemeral disk, WIPED on container recreation. ⇒ design every gate to **LOG ON BOTH BRANCHES**, so "it ran and decided X" is distinguishable from "it never ran."

### 10a. Serving-critical env flags — must be permanently `1` on the box
These are enforced by `check_monitors_healthy_op` (an unset one = a silently-gated-off refresh):

| Flag | Intended | Why (incident) |
|---|---|---|
| `SCHEDULE_LAKEHOUSE_INTRADAY` | **`1`** | INC-22: the in-Dagster intraday schedule/game-state capture that refreshes the S3 parquet prod serves. Off → game-state stuck in "Preview", stale lineups. Also START `intraday_schedule_capture_daytime/_overnight` AND disable the lean host-cron `schedule-capture` (don't double-ingest). |
| `LINEUP_INTRADAY_S3_REBUILD` | **`1`** | The intraday lineup-confirm re-score reaches the S3 W8b feature parquet (the 824819 loop). Off → a confirmed lineup never re-scores. |
| `W8A_LAKEHOUSE_S3` | **`1`** | Feature layer + EB posteriors served from S3 (cut over 2026-06-30). |
| `W8B_LAKEHOUSE_S3` | **`1`** | Serving pregame aggregator served from S3 (cut over 2026-06-30). |
| `LINEUP_MONITOR_S3` | **`1`** | E11.20 phase-2a: lineup-monitor detection tick reads S3+DynamoDB, not Snowflake (kills the ~10-min COMPUTE_WH wake). Flipped + converged 2026-07-20. Off → detection wakes the warehouse every tick. ⚠️ Rollback (a soak regression) = set `0` AND remove from the enforced set, else this heartbeat false-pages. |
| `W6_LAKEHOUSE_INTRADAY` | **`1`** | E11.20 phase-2a step 2 prereq: gates the intraday W6 odds-mart rebuild AND `W7B_INTRADAY_S3`'s `--book-odds --s3` read. Off → intraday serving silently falls back to Snowflake (stale odds / clobber). Cut over; enforced 2026-07-26. |
| `W7B_INTRADAY_S3` | **`1`** | E11.20 phase-2a step 2: intraday serving/predict (`write_serving_store_intraday`, post_lineup re-score) read S3 instead of Snowflake. Flipped ~00:40Z 7/25, verified on the 7/25 slate (coverage 1.000). Off → intraday serving reads SF. |
| `TICK_SF_FREE` | **`1`** | E11.20 phase-2a step 4 (the cost win): the 30-min `intraday_schedule_job` tick SKIPS `refresh_w1_external_tables` (~60-table refresh) + the dbt lineup rebuild = the #1 warehouse waker (~80% of the wake burn). Flipped 2026-07-26 ~04:48 UTC. Requires `SCHEDULE_LAKEHOUSE_INTRADAY=1` (inert+loud otherwise). ⚠️ In its first soak — rollback = set `0` AND remove from the enforced set (else the heartbeat false-pages). |

### 10b. Other cutover / gating flags — intended state (NOT auto-enforced; confirm per cutover)
| Flag | Intended | Note |
|---|---|---|
| `SPORTS_DUCKDB_PATH` | **`/var/lib/credence/sports/sports.duckdb`** (exact) | **NF-INFRA1.** The ONE path the whole football (NCAAF/NFL) build chain writes and every consumer reads — the Sleeper ingest, the board-publish job, both game-day gates, and dbt's `profiles.yml`. It is a PATH, not a `0/1` flag, so it is not in the §10a heartbeat set; it IS in `env.required`, so **a deploy FAILS while it is missing or empty**. ⚠️ **A `git pull` never touches the box's `.env` — the operator must add this key by hand, once, before the next deploy.** 🗄️ It must sit inside the `sports_duckdb` NAMED VOLUME (mounted at `/var/lib/credence/sports` on `dagster-codeloc`): `*.duckdb` is gitignored ⇒ absent from the `COPY . .` image, `/tmp` is wiped by every deploy, `/app` is replaced by the image — so every other location is deploy-ephemeral. 🚨 **Both halves of this are the NF-FRESH1 root cause**: with the var unset, four owners each fell back to a different default (`/tmp/sports_ncaaf.duckdb`, `/tmp/sports_nfl.duckdb`, `/app/…/sports.duckdb`, profiles.yml's), so the NFL build wrote one file while the NFL game-day gate read one nothing ever created (permanently fail-open) and the Sleeper op died in 114ms behind a bare `except` — **19 consecutive green runs over a 19-day-old Delta commit**. Materialize the file once with `sports_nfl_dbt_build_job`; verify with `scripts/check_sports_delta_freshness.py`. Single owner in code: `betting_ml/utils/sports_duckdb.py`. |
| `W6_LAKEHOUSE_INTRADAY` | `1` **post-cutover** | Gates the intraday MART rebuild (served-price freshness). The RAW odds S3 mirror export is UNGATED (always runs), so odds don't freeze even with this off; flip to `1` once the W6 external tables exist + parity is validated. NOT in the enforced set (avoids false pages pre-cutover). |
| `W7A_LAKEHOUSE_S3`, `W7B_LAKEHOUSE_S3` | `1` | Matchup consumers + prediction path off SF (cut over). `*_PARALLEL` variants are validation-only. |
| `W9_LAKEHOUSE_S3` | `0` | W9 signal-store SF writes STAY; only the output-mirror ships. `W9_LAKEHOUSE_S3_READS` follows the W-plan. |
| `ODDS_COVERAGE_STRICT` | `0` → `1` | ALERT today; flip to `1` (HALT on a current-slate odds freeze) once confirmed it doesn't false-fire. |
| `FEATURE_COVERAGE_STRICT` | `0` → `1` | ALERT today; flip to `1` after the W11b umpire cutover restores the block. |
| `PROPS_DAILY_INGEST` | `0` | Redundant with the host-cron `0 13` props line — enable EXACTLY ONE (else double-pay Odds API credits). |
| `W11_RAW_WRITE_MODE` / `LAKEHOUSE_RAW_WRITE_MODE` | per-cutover (`snowflake`→`both`→`s3`) | Tier-A raw writers' dual-write mode; advance per the W11 cutover, not blindly. |
| `W11_W4W5_NIGHTLY`, `W11B_UMPIRE_NIGHTLY`, `W11C_WEATHER_NIGHTLY`, `W11D_PUBLIC_BETTING_NIGHTLY`, `W11_W3PRE_DAILY`, `W11_BATTER_PITCHES_SF_RETIRED` | per-cutover | Nightly-rebuild / SF-retire gates; each flips with its wave's box cutover. |
| `E11_24_ELO_SF_FREE`, `E11_24_WEATHER_SF_FREE`, `E11_24_STATCAST_CATCHUP_GATE` | **all `1`** (live 2026-07-31) | E11.24 stage-1 cost levers, ALL FLIPPED. 1 = `compute_elo` reads `mart_game_results` from S3; 2 = `ingest_weather` slate/venue+dedup reads from S3 (⚠️ the WRITE leg is `W11_RAW_WRITE_MODE`, **not** `LAKEHOUSE_RAW_WRITE_MODE` — setting the wrong one is a silent no-op); 1b = `catchup_ingest_statcast` yields no output on a fire that landed no pitches, so Dagster SKIPS the chain (a skip is not a failure; fail-OPEN). **In `env.required` for PRESENCE only — no value is pinned, so a deliberate rollback cannot false-page.** Measured effect of 1+2+3: `COMPUTE_WH` awake-minutes 167 (7/28) → 141 (7/30), −16%, while RESUMES stayed flat at 44→43 — ⭐ a lever that removes an evenly-spread 24/7 poller deletes awake-MINUTES, not wakes, so judge these on active-minutes. **1b flipped 2026-07-31 ~19:45 UTC — ✅ VERIFIED on its first post-flip window (2026-08-01)**: in the 08–13 UTC band the catch-up chain fired in **2 distinct hours vs 5–6 every morning for the prior nine days**, hours 09 and 11 completely silent, and **RESUMES 22 (7/28) → 11**, waits 24 → 15, active-min 86 → 37. ⭐ **Judge 1b on the FIRE COUNT (distinct hours the chain ran), not executions** — 8/1 was an INC-37 day whose total executions were down ~60% anyway, so a volume metric would have credited the gate with an outage's work. 1b is the first lever here to move RESUMES (it removes a BURST; weather removed a POLLER and moved only active-minutes). ⚠️ one day only — re-read 8/2–8/3 (`uv run python scripts/report_e11_24_wake_census.py`) before booking it closed. ⏭️ Promote to the §10a enforced (`=='1'`) set only after each soaks; do not promote a first-soak flag. |
| `E11_24_UMPIRE_REBUILD_GATE` | ⛔ **RETIRED 2026-08-05 — the flag no longer exists in code** | E11.24 **target 6**. Superseded by the **table→VIEW flip** of the four ext-table-COPY models (`stg_statsapi_umpire_game_log`, `feature_pregame_umpire_features`, `feature_pregame_{starter,lineup}_features`): on the Snowflake target each is literally `select * from lakehouse_ext.<model>`, so rebuilding them is now `create or replace view` — **metadata-only DDL that NEVER resumes `COMPUTE_WH`**. A conditional SKIP of a free operation saves nothing, and the view removes the wake for **every caller at once** rather than the ~1-in-3 ticks a gate could catch. The gate was also verified **INERT** on the 2026-08-04 armed slate (0 skips of 3 real opportunities; its S3 marker object had never existed, so every tick took the fail-OPEN path). Module, op branch, test, and both flag registrations are deleted; guard `betting_ml/tests/test_e11_24_target6_ext_copy_views.py` stops it coming back. 🧹 **No box action required** — `deploy.sh` env-parity only checks that keys listed in `env.required` are PRESENT in `.env`, never that `.env` has no extras, so a leftover `E11_24_UMPIRE_REBUILD_GATE=1` on the box is harmless; delete it at leisure. |
| `E11_24_BULLPEN_S3_READ` | **`0` today; flip to `1` in a QUIET WINDOW** | E11.24 **target 6**. `update_team_posteriors` reads its bullpen observations from the S3 lakehouse via DuckDB instead of Snowflake, which also removes the ordering dependency forcing the w8b aggregator to mirror a one-slate-stale `team_sequential_posteriors`. In `env.required` for **PRESENCE only** (no value pinned), so a rollback cannot false-page. ⚠️ **NOT fail-open by design** — it feeds a non-idempotent, strictly-ordered chain, so a silent `[]` would masquerade as "source not ready" and stall the metric; it raises instead. ⛔ **Flip OUTSIDE a live slate** (before ~06:00 or after ~23:30 CST) — INC-36 made mid-slate deploys the risk window, and this is the serving/predict path where the E11.20 false-abstain class recurs. 🔒 **Verification is a REAL box run, not CI** (CI mocks all IO): confirm `predict_today` shows no new abstains and no new absent/all-null contracts vs the pre-flip slate, then measure resumes AND active-minutes in the 14–23 band (`uv run python scripts/report_e11_24_wake_census.py`). ⭐ Read the census **PER-DAY**, never off a window aggregate that straddles the flip date — executions HOLDING while waits → 0 is a working gate; both collapsing is a dead job or an outage day. ⏭️ Do NOT promote to the §10a enforced (`=='1'`) set on its first soak. ✅ Checked 2026-08-05: the box reads `BULLPEN_S3_READ=0` — the 14:21:11Z container recreation did NOT flip this non-fail-open lever live. |
| `W11TX_TRANSACTIONS_NIGHTLY` | `1` **once cut over** | INC-32 hygiene: gates the nightly `--w11tx-only` rebuild + `--w11tx` ext refresh that keeps `lakehouse_ext.stg_statsapi_transactions` (the injury-status chain) fresh from the live raw mirror. MUST be `1` before the SF raw `player_transactions` is dropped, else the ext table FREEZES on drop (`decommission_w11_abcd_drop.py`). Flip on only after `W11_RAW_WRITE_MODE=both\|s3` + the ext table exists + a box-validated `--w11tx-only` run (per-ROW ext fetch = the runtime gate). |

### 10c. Sensors — ALL self-start (`default_status=RUNNING`); the critical set is heartbeat-checked
All 11 sensors carry `default_status=RUNNING`. `check_monitors_healthy_op` alarms if any of these is manually STOPPED: `run_failure_alert_sensor`, `odds_current_rebuild_sensor`, `odds_freshness_alert_sensor`, `schedule_freshness_alert_sensor`, `statcast_freshness_sensor`, `lineup_monitor_sensor`, `pregame_alert_sensor`, `conviction_pick_alert_sensor`, `morning_watchdog_sensor`, `clv_alert_sensor`, `model_health_alert_sensor`.

**INC-32 (2026-07-18) — tick-STALENESS heartbeat:** the STOPPED check above is blind to a sensor that is still nominally RUNNING but whose evaluations have *stalled* (the sensor-daemon wedged mid-slate — 7/17, all evals stopped ~21:30Z after `lineup_monitor.py` hung the daemon thread). `check_monitors_healthy_op` now ALSO pages if any critical sensor's most-recent tick is older than `SENSOR_TICK_STALE_SECONDS` (default 60 min) — the daemon ticks every RUNNING sensor continuously (even a SkipReason is a tick), so a stale tick = a wedged daemon. Structural cure: every sensor subprocess (op AND the `_evaluate_lineup_monitor` sensor-eval path) now has a hard subprocess timeout so a wedge can't block the daemon in the first place.

### 10d. Schedules — intended boot state
| Schedule | Intended | Note |
|---|---|---|
| `daily_ingestion_job_schedule` | **RUNNING** (self-start) | the primary serving pipeline; heartbeat-checked. |
| `odds_clv_rebuild_daily` | **RUNNING** (self-start) | daily CLV / line-movement rebuild; heartbeat-checked. |
| `artifact_freshness_daytime` / `_overnight` | **RUNNING** (self-start) | INC-41. The off-cycle serving-artifact freshness SLA (`artifact_freshness_job` → `scripts/check_artifact_freshness.py`): asserts each registered serving-critical parquet has ADVANCED, using a CONTENT timestamp *inside* the data (never the S3 mtime — `aws s3 ls` prints shell-local time, and PR #638's atomic server-side copy refreshes the mtime even when the data is unchanged, so an mtime check reads GREEN through the very incident). Crons mirror `intraday_schedule_capture_*` (`*/30 14-23` + `0,30 0-3` UTC) because the CHECK cadence must match the WRITE cadence; lag is counted only across those active hours, so the deliberate 10.5h overnight gap is not a breach. **Heartbeat-checked (`CRITICAL_SCHEDULES`)** — unlike the capture schedules below there is no double-ingest hazard to gate on (it only reads S3 and pages), so a STOPPED one is never expected and always means the INC-41 blind spot is reopened. This is the ONLY thing that catches an INTRADAY artifact freeze between daily runs — INC-41's exact shape (lineups froze 20:08Z, hours after the daily job finished green, 6.5h / ~40 ticks, 3 games unscored, every op reporting SUCCESS). ALERT tier: pages via `send_alert`, never HALTs. Snowflake-free, so a 30-min tick cannot wake `COMPUTE_WH` (E11.24 soak-safe). |
| `lineup_monitor_schedule_daytime` / `_overnight` | **STOPPED** (manual fallback) | INC-32: DEMOTED 2026-07-18. The `lineup_monitor_sensor` (10-min, un-wedgeable, staleness-heartbeat-checked) is now the SOLE driver of `lineup_monitor_job`; running these too double-fired the job (check-then-act race → 2 runs per confirmed lineup → doubled dbt-runner 409 contention). NOT critical / not heartbeat-checked — a STOPPED one is expected. Re-enabling one reintroduces the double-fire. |
| `intraday_schedule_capture_daytime` / `_overnight` | operator-gated STOPPED | START only WITH `SCHEDULE_LAKEHOUSE_INTRADAY=1` AND after disabling the lean host-cron `schedule-capture` (double-ingest). NOT self-start / not heartbeat-checked. |
| `intraday_public_betting_daytime` / `_overnight` | operator-gated STOPPED | START only with `W11_RAW_WRITE_MODE=s3\|both` (paid ActionNetwork capture opt-in). |
| `weekly_ml_job_schedule`, `weekly_meta_model_job_schedule`, `clv_monitoring_job_schedule`, `magnitude_monitor_job_schedule` | STOPPED (optional) | heavy/optional; operator toggles as needed. |
| `weekly_player_profiles_job_schedule` | **RUNNING** (operator-toggled 2026-06-27 16:47 UTC — *not* self-start) | ⚠️ **CORRECTED 2026-08-15** — this row sat in the "STOPPED (optional)" group above for ~7 weeks while the schedule was in fact RUNNING (measured on the box: a persisted `InstigatorStatus.RUNNING` row, `start_timestamp` 2026-06-27 16:47:03 UTC, daemon evaluating it 33 min before the reading). The job def carries **no `default_status=RUNNING`**, so it runs only because someone toggled it on — meaning it survives a restart but **NOT** a Dagster-DB reset / re-host (the INC-16 class), and a future operator "tidying up" per the old row would stop it. ⭐ **THAT NOW HAS A CONSEQUENCE IT DID NOT HAVE IN JUNE:** since the E11.24 Bundle (2026-08-14) this is the ONLY job that runs `reexport_player_profiles_op`, so it is the sole writer keeping the `player_profiles_raw` S3 mirror current — and `check_data_freshness` reads that mirror (`source="s3"`, 192h). Stop this schedule and the entry drifts past its threshold in ~1 day and stays there, with the leaf never firing. The 47-day frozen mirror the Bundle fixed dates to **2026-06-28**, the day AFTER this schedule was enabled: someone turned the job on, hand-ran `export_w4_raw_to_s3.py` once, and nothing re-ran it — which is exactly the writer gap the Bundle closed. ⛔ **NOT heartbeat-checked, deliberately:** adding it to `CRITICAL_SCHEDULES` would page CRITICAL for a WARN-tier, non-blocking freshness entry on a weekly cadence — the "over-paging monitor gets muted" mode E11.30 warns about. Its failure surfaces as a STALE line in the freshness summary, which is the designed tier. If it is ever stopped on purpose, flip `player_profiles_raw` back to `source="snowflake"` in `scripts/check_data_freshness.py` in the SAME change (that re-opens the Snowflake connection and re-adds this script to the `COMPUTE_WH` wake queue — #679 — so it is a real cost decision, and it is guarded by `test_e11_24_bundle_freshness_reexports.py`). |
| `w1_parity_schedule` | STOPPED (one-shot legacy) | fires only on its pinned parity date; leave off. |
| `sports_ncaaf_dbt_schedule` / `sports_nfl_dbt_schedule` | **STOPPED until the 2026 season opens** — then **RUNNING** | NCAAF-P1.1. Game-day-GATED rebuilds of the `sports_dbt` NCAAF/NFL marts (dbt-duckdb; no warehouse, no API credits). Cron fires daily inside the season months (NCAAF Aug–Jan, NFL Sep–Feb) at 11:00 PT and `betting_ml/monitoring/sports_game_day_gate.py` SKIPs when no game was played the prior day — so ~2–3 runs/week, not 7. ⛔ Shipped STOPPED on purpose: there are no live football games until **Aug 2026 (NCAAF) / Sep 2026 (NFL)**. ⏰ **ACTION: turn BOTH ON in Dagit before the openers** — while STOPPED, the NCAAF/NFL marts (incl. P0.4 roster-continuity + P0.5 coaching-change) only rebuild when an operator launches the job by hand, which is the "silently rot" state P1.1 exists to end. NOT heartbeat-checked (nothing serving-critical depends on them yet); revisit if a live NCAAF/NFL serving surface ships. The gate FAILS OPEN — an unreadable/stale mart rebuilds rather than skipping. ⛔ **NF-INFRA1 follow-up (2026-08-15): deliberately NOT added to `CRITICAL_SCHEDULES` yet** — both are intentionally STOPPED until the NCAAF-on-box card turns them on; adding them to the required-RUNNING set now would false-page every heartbeat evaluation. They join it in that story. |
| `sports_ncaaf_roll_forward_schedule` | **STOPPED → turn ON NOW (pre-season), well before the Aug-29 2026 opener** | NCAAF-P0.7. The ANNUAL pre-season season roll-forward: weekly (Mon 06:00 PT, **Feb–Aug**) it refreshes the **upcoming** season's SCHEDULE + covariate priors (`ROLL_FORWARD_SOURCES`) for the clock-derived `current_season()`, then rebuilds the NCAAF marts (`sports_ncaaf_roll_forward_job`: ingest → mart rebuild) — the game-day `sports_ncaaf_dbt_schedule` SKIPs pre-season, so this is the ONLY thing that lands the 2026 schedule before kickoff. ⛔ Ships STOPPED (operator-gated, same E11.23 exception as the sports dbt schedules): needs `CFBD_API_KEY` in the box env. ⏰ **ACTION: enable in Dagit as soon as the box has the CFBD key** — this is the launch-critical "2026 data is live" gate (with P0.6b). ~8 cheap CFBD calls/week; idempotent Delta overwrite; 0-row covariates (not yet published) are logged loud, never fail. Re-runnable next August unchanged (season auto-advances). After it lands, the season's ONE-TIME P1.2 re-fit + board render are the operator laptop steps (P0.7 handoff). NOT heartbeat-checked. |
| `sports_nfl_roll_forward_schedule` | **RUNNING** — ⚠️ this row said STOPPED until 2026-08-15 and was WRONG: NF-FRESH1 read three consecutive Monday Delta commits at 13:15:2x UTC across four tables (a second-level match to the declared cron), and the operator confirmed it RUNNING on the box. Verify a claimed schedule state against the box run list, never against this table alone. | NF-D1. The NFL analog of NCAAF-P0.7: weekly (Mon 06:15 PT, **Mar → the following Feb** — one full season cycle; ⚠️ **NF-FRESH2 P0 widened this from `3-8` on 2026-08-15**, because a Mar–Aug window froze every NFL raw feed on 09-01, through the opener and the whole season, while the Sep–Feb mart rebuild kept running over raw that had stopped advancing) it refreshes the **upcoming** season's rosters/weekly_rosters/schedule/depth_charts/injuries/nflverse_draft_picks/nflverse_combine (`ROLL_FORWARD_SOURCES`) for the clock-derived `current_season()`, then rebuilds the NFL marts (`sports_nfl_roll_forward_job`: ingest → mart rebuild) — landing `schedules` is what unblocks the `stg_nfl_depth_charts` 2025+-branch ASOF week-map (0-mapped without a schedule to bucket daily ESPN snapshots into), sharpening MVP-1's fantasy-projection role signal beyond the prior-season carryover. ⛔ Ships STOPPED: UNLIKE NCAAF, the ingest step needs NO API key (nflverse is free/public) — it's gated purely on the shared `sports_nfl_dbt_build_job` box-readiness prereq (dbt-duckdb + S3 instance-role read) the rebuild step depends on. ⏰ **ACTION: enable in Dagit as soon as the box has dbt-duckdb** — this sharpens the live 2026 fantasy draft-tool board (NF-FASTPATH/NF-C1-lite/NF-C2). 7 unauthenticated nflverse reads/week; idempotent Delta overwrite; 0-row feeds (not yet published, e.g. `injuries` pre-camp) are logged loud, never fail. Re-runnable next spring unchanged (season auto-advances). Verified live 2026-07-25: rosters/weekly_rosters/schedules/depth_charts/nflverse_draft_picks/nflverse_combine already published for 2026 (2,930/2,930/272/365,771/257/319 rows); `injuries` not yet (0 rows, expected pre-camp). NOT heartbeat-checked. |
| `sports_nfl_sleeper_injuries_schedule` | **RUNNING** — self-start (`default_status=RUNNING`) as of the NF-INFRA1 follow-up (2026-08-15) **and heartbeat-checked** (`CRITICAL_SCHEDULES`). ✅ the 19-green-runs break is FIXED IN CODE by NF-INFRA1; the operator has confirmed `SPORTS_DUCKDB_PATH` is set on the box and the DuckDB materialized (§10b). | NF-D5. Continues NF-D2 slice 5: DAILY (not weekly, 06:30 PT, **Mar–Aug**) refresh of Sleeper's free `v1/players/nfl` snapshot (`sports_nfl_sleeper_injuries_job`: land `nfl/raw/sleeper_injuries` → refresh just the `stg_nfl_sleeper_injuries` staging model) — surfaces offseason PUP/IR/surgery designations to `load_forward_roster_status` (COALESCEd over nflverse's roster status, Sleeper preferred) MONTHS earlier than nflverse's roster `status`/injury report catches up. Both ops are WARN-tier (advisory/non-serving — `load_forward_roster_status` falls back cleanly to nflverse-only if this schedule stays off or a run fails). Verified live 2026-07-26: landed 2,499 rows (100% gsis-matched via the native id + the deterministic name/pos crosswalk fallback), coalesced into a real board re-run and flipped nflverse-only's 8 RES/0 PUP → 10 RES/10 PUP (George Kittle, Zach Charbonnet, Isaac Guerendo, Tim Patrick, Tyler Conklin among the newly-surfaced PUP cases). ⏰ **Mar–Aug widened to Mar → the following Feb by NF-FRESH2 P0** (same seasonal-cliff fix as the roll-forward; availability designations churn hardest in-season). 🚨 **THE NF-FRESH1 BREAK (2026-08-15): 19 consecutive daily runs reported SUCCESS while `nfl/raw/sleeper_injuries` held ONE 19-day-old Delta commit.** The ingest op died at `duckdb.connect(read_only=True)` in ~114ms — the sports DuckDB is gitignored, so it was absent from the `COPY . .` image and `/tmp` is wiped by every deploy — and its bare `except` returned SUCCESS. Widening the cron did not fix it. ✅ **NF-INFRA1 fixed all three layers:** the file now lives on the `sports_duckdb` NAMED VOLUME at one authoritative `SPORTS_DUCKDB_PATH` (§10b); the ingest op PAGES AND RAISES on a missing DuckDB, a fetch failure, a zero-row write, or a land whose crosswalk collapsed (⛔ it REFUSES that write rather than overwriting the good partition with a native-gsis-only frame — measured at 16.7% of rostered / 22.1% of flagged, i.e. strictly worse than a loud break); and a third op reads the commit timestamp from INSIDE `_delta_log` and pages if the artifact did not advance (INC-41). ⭐ **The proof of life is a NEW COMMIT VERSION, never a green run** — `SPORTS_LAKE_REGION=us-east-2 uv run python scripts/check_sports_delta_freshness.py --strict`. 🚨 **NF-INFRA1 FOLLOW-UP (2026-08-15):** this schedule's ON state used to live ONLY in the Dagster Postgres (no `default_status=RUNNING`) — a volume reset / box re-host would have silently reverted it to STOPPED (Sleeper goes dark again) with nothing paging. Now self-starts AND is in `check_monitors_healthy_op`'s required-RUNNING set, so a revert PAGES CRITICAL instead of going silent. |
| `sports_nfl_board_publish_schedule` | **RUNNING** — self-start (`default_status=RUNNING`) as of the NF-INFRA1 follow-up (2026-08-15) **and heartbeat-checked** (`CRITICAL_SCHEDULES`). NF-INFRA1 landed the prereq (the `sports_duckdb` volume + one authoritative `SPORTS_DUCKDB_PATH`) and the operator confirmed the DuckDB materialized + toggled this ON in Dagit. | NF-FRESH2 P2/P4. The cadence that makes the SERVED NFL draft board MOVE — before it, the board was a static hand-published S3 blob (last publish 2026-08-10, market inputs ~3 weeks old, depth-chart view ~12 days old). Fires `sports_nfl_board_publish_job`: a daily depth-chart/roster ingest slice + mart rebuild → refresh ADP/ECR → rebuild the NF1.5 projection → rebuild the 14 league boards → export → `--publish` → **verify the artifact that shipped**. One cron (07:15 PT, **no month range** — deliberately no seasonal cliff) that decides its own cadence: DAILY Aug 1 – Sep 15, WEEKLY (Mondays) otherwise. ⭐ The ingest→publish ORDER is a graph EDGE, not a cron offset (INC-25): the 2026-08-10 board was generated 7h42m BEFORE that Monday's ingest landed. ⚖️ It can never blank the product (a failure leaves the previous board serving) but it PAGES AND RAISES rather than reporting a green run that published nothing — the explicit inverse of the Sleeper row above. ⛔ **PREREQ (NF-INFRA1):** the build chain reads the box's sports DuckDB, which is gitignored and absent from the image until `sports_nfl_dbt_build_job` has materialized it into the `sports_duckdb` volume. `SPORTS_DUCKDB_PATH` now has exactly ONE owner (`betting_ml/utils/sports_duckdb.py`) and is deploy-gated via `env.required`, so the build op and every consumer can no longer disagree about which file they mean. Enabling before the materialize produces a daily CRITICAL page (by design). 🚨 **NF-INFRA1 FOLLOW-UP (2026-08-15):** this schedule's ON state used to live ONLY in the Dagster Postgres (no `default_status=RUNNING`) — a volume reset / box re-host would have silently reverted it to STOPPED (the board freezes, the exact failure NF-FRESH2/NF-INFRA1 exist to prevent, one level up) with nothing paging. Now self-starts AND is in `check_monitors_healthy_op`'s required-RUNNING set, so a revert PAGES CRITICAL instead of going silent. |
| `sports_ncaaf_odds_capture_schedule` | **STOPPED → turn ON alongside the roll-forward schedule, before the Aug-29 2026 opener** | NCAAF-P0.6b. The recurring IN-SEASON closing-line catch-up: weekly (Mon 08:00 PT, **Aug–Jan**) `odds_recurring_capture.py` detects which CFBD week(s) of the clock-derived `current_season()` are newly kicked-off + not yet ≥80%-covered in the lake, pulls ONLY those via the PAID Odds-API `/historical` endpoint (leakage-safe K−buffer snapshot, same as the P0.6 backfill), and READ-MERGE-WRITEs them into the `odds_ncaaf_historical` Delta partition (a plain overwrite would delete every prior week — see the module docstring) — bridging P0.6's one-time 2020-2025 backfill into an ongoing 2026+ feed so the CLV benchmark keeps extending for P1.4's vs-market eval. ⛔ Ships STOPPED (operator-gated, same E11.23 exception): needs `ODDS_API_KEY` (the PAID main tier — `/historical` is not on the starter tier) **and** `CFBD_API_KEY` in the box env. ⏰ **ACTION: enable in Dagit once both keys are confirmed on the box** — there is nothing to capture until 2026 games actually kick off, so this is safe to enable early (it no-ops for 0 credits every week until then). ~1,800-2,100 credits for a normal week (bounded to newly-covered weeks only, never the whole season); WARN tier — a failed/missed week just catches up on the next fire, merge-safe, no data lost. `verify_odds_historical.py --seasons <season>` is the post-capture acceptance check (its own default range stops at the last COMPLETED season, so pass the in-progress season explicitly). NOT heartbeat-checked. |
| `sports_ncaaf_prediction_snapshot_schedule` | **STOPPED → turn ON only AFTER the close-to-kickoff P1.2 re-fit, before the Aug-29 2026 opener** | NCAAF-PS. The weekly PRE-KICKOFF prediction snapshot: Tue 09:00 PT, **Aug–Jan**, `sports_ncaaf_prediction_snapshot_job` runs the SERVED P1.4 model over every FBS-vs-FBS game kicking off in the next 7 days and appends one immutable row per `(game_id, snapshot_ts)` to `ncaaf/derived/game_prediction_snapshots`, then fans out to a weekly `ncaaf/derived/futures_board_snapshots` of the P1.5 board. ⭐ **THIS IS THE FORWARD TRACK RECORD** — a backtest can always be re-derived, so only a row written BEFORE kickoff shows what we would actually have said; **a missed week is NOT recoverable** (unlike the merge-safe P0.6b catch-up), which is why the game op RAISES on failure rather than logging. 💸 **Costs nothing**: no CFBD key, no Odds-API key, no credits, no warehouse — it reads the raw `games` + derived `team_strength_week` Deltas over S3 and the two COMMITTED served JSON artifacts, and deliberately touches NO gitignored/deploy-ephemeral file (`sports.duckdb`, the strength/matrix parquet) so it cannot run green over a frozen input (NF-INFRA1/NF-FRESH1). ⛔ Ships STOPPED for a reason beyond the usual operator gate: until the operator's once-per-season **P1.2 re-fit** lands (fall-camp covariates), the strength mart carries the pre-season COLD START (2025 carry-forward — the 2026 board has Indiana leading), and a snapshot is immutable by design, so an early fire would freeze that into a record that can never be rewritten. ⏰ **ACTION: re-fit P1.2 → smoke `--dry-run` → enable in Dagit, before 8/29.** Leakage gate is DATE-based + HALT (`snapshot_ts < commence_time` on every row, refusing the whole write on any violation); the writer READ-MERGE-WRITEs so a re-run can never delete a prior week. `best_alpha=0` — probabilities + intervals only, no pick/edge claim. NOT heartbeat-checked. |
| `sports_ncaaf_serving_write_schedule` | **STOPPED → turn ON right after `sports_ncaaf_prediction_snapshot_schedule`, before the Aug-29 2026 opener** | NCAAF-P3.1. The daily in-season SERVING publish: 06:00 PT, **Aug–Jan**, `sports_ncaaf_serving_write_job` reads the latest NCAAF-PS per-game snapshots + the P1.5 futures-board snapshot from the lake and writes the app-facing blobs (`ncaaf/manifest`, `ncaaf/slate/{LA-date}`, `ncaaf/game/{id}`, `ncaaf/futures/board`) to **DynamoDB `credence-prod-serving-cache` (primary) + S3 `credence-prod-s3-api-cache` under `ncaaf-cache/` (fallback)** — the store the public FastAPI `/ncaaf/*` routes read. ⭐ **THE AUTHORITATIVE WRITE IS CHAINED INSIDE THE SNAPSHOT JOB** (`ncaaf_serving_write_after_snapshot_op`, INC-25: a publish that only ran on its own schedule would serve LAST week's vintage on the morning the new one lands); this schedule is the daily top-up, because the manifest's `current_game_day` rolls every day and a market line can land for a kickoff that has since passed its capture instant. 🚦 HALT tier — it raises if NEITHER store took a blob (the app would have no board); the market-line join inside it is WARN and the futures board ALERT-continue. 💸 **Costs nothing**: no CFBD key, no Odds-API credits, no warehouse — two Delta reads over S3 plus a few dozen small key writes; **no new IAM grant** (the box role already holds table-wide DynamoDB RW + bucket-wide S3 Get/Put on exactly these two resources). ⛔ Ships STOPPED (the usual NCAAF operator gate): there is nothing to publish until the first snapshot has landed. ⏰ **ACTION: enable in Dagit AFTER the first `sports_ncaaf_prediction_snapshot_job` run, before 8/29.** ⚠️ **The API half deploys SEPARATELY** — `infrastructure/lambda/deploy.sh` ships the routers and each `/ncaaf/*` route needs its API-Gateway authorizer set to NONE (`infrastructure/aws_resources.md`); "merged" never means "served". `best_alpha=0` — probabilities + intervals only, no pick or edge claim. NOT heartbeat-checked. |
| `sports_nfl_pit_weather_schedule` | **RUNNING** (self-starts; ships RUNNING on purpose) | NF-W0a. HOURLY Sep–Feb NFL weather-forecast ladder `[120,72,48,24,3,1]`h → the append-only `nfl/pit/weather` Delta store. **FREE** (Open-Meteo, no API key) and Snowflake-free. ⭐ **Ships RUNNING deliberately, unlike the paid captures**: a forecast is only point-in-time-honest if CAPTURED AT THE TIME (the Open-Meteo archive returns OBSERVATIONS, not the forecast that stood on a historical Tuesday), so a missed rung is PERMANENT data loss — and a schedule that boots STOPPED silently never runs (E11.23), which here would be discovered in January with the season's Tuesday-build weather already unrecoverable. Between "it quietly costs a few free API calls" and "it quietly costs the season", RUNNING is the only defensible default. Off-season the cron's month gate means it never fires; in-season a fire with no game at a rung is a cheap no-op. Hourly is the MINIMUM cadence the ladder needs (tightest rung gap 3h→1h, match window ±0.75h). WARN/ALERT tier — never fails the run, but PAGES CRITICAL via `send_alert` (dedup key `nfl_pit_capture:weather`) when eligible games produced ZERO captures. NOT heartbeat-checked. |
| `sports_nfl_pit_metadata_schedule` | **RUNNING** (self-starts) | NF-W0a. Tue+Fri 09:00 PT, Sep–Feb: injury reports stamped with **OUR OWN `capture_timestamp`** + a per-ingest nflverse SCHEMA SNAPSHOT → `nfl/pit/injuries` + `nfl/pit/schema_snapshot`. **FREE**, Snowflake-free. The injury leg exists because nflverse DELETED `injuries.date_modified` in 2025 (NF-W0 defect #2) — the only as-of timestamp left is one we make, and it cannot be manufactured after the fact. The schema leg exists because our own lake structurally CANNOT see an upstream deletion (`schema_mode='merge'` backfills the dropped column with NULLs so it still reads present) while the vendor's release file can; it already reproduces both 2025 breaks (`injuries.date_modified` missing, `depth_charts` `week`/`depth_team`/`position` missing). ALERT tier — pages ERROR on a watched column going missing/all-NULL or an unreadable asset. NOT heartbeat-checked. |
| `sports_nfl_pit_market_schedule` | **STOPPED → ⏰ TURN ON BEFORE THE 2026-09-09 OPENER** | NF-W0a. Tue+Fri 09:15 PT, Sep–Feb: a **point-in-time NFL market board** (live `/odds`) → `nfl/pit/market`. ⛔ Ships STOPPED (the same operator-gated PAID-capture exception `sports_ncaaf_odds_capture_schedule` takes): needs `ODDS_API_KEY`. ⏰ **THE DEADLINE IS REAL — enable before the opener or NO Tue/Fri market feature is EVER backtestable**, because the Odds-API history retains only CLOSING lines (that is exactly what `odds_nfl_historical` captures, by design, for CLV) and a Tuesday-build market feature can only ever come from having LOOKED on Tuesday. **Safe to enable early**: verified live 2026-08-05 that the 2026 board is ALREADY fully priced (272 events × 10 books) 35 days pre-opener, so enabling now also captures pre-season line movement at no extra cost. **Cost: ~30 credits/snapshot ≈ 1,300/season** (10 × 3 markets × 1 region). ⚠️ Player PROPS are a SEPARATE opt-in — `NFL_PIT_CAPTURE_PROPS=1`, default OFF, at ~120 credits PER EVENT ≈ 1,700/slate snapshot ≈ **~75,000 credits/season**; confirm the balance before setting it. ALERT tier — pages CRITICAL on a zero-event capture. NOT heartbeat-checked. |
