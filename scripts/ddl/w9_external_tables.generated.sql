-- =============================================================================
-- w9_external_tables.generated.sql — GENERATED by generate_w9_external_tables.py
-- E11.1-W9: external tables over the S3 parquet for the 5 sub-model signal STORES
-- (mart_sub_model_signals + offense_v1/offense_v2/starter_suppression/starter_ip
-- _signals). Reuses the lakehouse_ext schema + s3_lakehouse stage + parquet_snappy
-- file format (W1d). Run AFTER export_w9_signals_to_s3.py has written
-- lakehouse/<name>/data.parquet, and BEFORE the parity check / any W8 source repoint.
-- Refresh: scripts/refresh_w1_external_tables.py --w9 (W9_TABLES). AUTO_REFRESH=FALSE.
-- DO NOT hand-edit — regenerate from the parquet if a store's schema changes.
-- =============================================================================

-- ── mart_sub_model_signals  (14 columns) ──
CREATE OR REPLACE EXTERNAL TABLE baseball_data.lakehouse_ext.mart_sub_model_signals (
    GAME_PK                          NUMBER(38,0)   AS (VALUE:game_pk::NUMBER(38,0)),
    SIDE                             VARCHAR        AS (VALUE:side::VARCHAR),
    SIGNAL_NAME                      VARCHAR        AS (VALUE:signal_name::VARCHAR),
    SUB_MODEL_NAME                   VARCHAR        AS (VALUE:sub_model_name::VARCHAR),
    SUB_MODEL_VERSION                VARCHAR        AS (VALUE:sub_model_version::VARCHAR),
    SIGNAL_VALUE                     FLOAT          AS (VALUE:signal_value::FLOAT),
    UNCERTAINTY                      FLOAT          AS (VALUE:uncertainty::FLOAT),
    SIGNAL_AVAILABLE                 BOOLEAN        AS (VALUE:signal_available::BOOLEAN),
    INPUT_FEATURE_HASH               VARCHAR        AS (VALUE:input_feature_hash::VARCHAR),
    COMPUTED_AT                      TIMESTAMP_NTZ  AS (VALUE:computed_at::TIMESTAMP_NTZ),
    VALID_FROM                       TIMESTAMP_NTZ  AS (VALUE:valid_from::TIMESTAMP_NTZ),
    VALID_TO                         TIMESTAMP_NTZ  AS (VALUE:valid_to::TIMESTAMP_NTZ),
    IS_CURRENT                       BOOLEAN        AS (VALUE:is_current::BOOLEAN),
    RECORD_HASH                      VARCHAR        AS (VALUE:record_hash::VARCHAR)
)
WITH LOCATION = @baseball_data.lakehouse_ext.s3_lakehouse/mart_sub_model_signals/
FILE_FORMAT = baseball_data.lakehouse_ext.parquet_snappy
AUTO_REFRESH = FALSE
COMMENT = 'E11.1-W9: mart_sub_model_signals signal store from S3 lakehouse parquet';
-- Preserve the feature/reader grant on (re)create (INC-18 band-aid class):
GRANT SELECT ON EXTERNAL TABLE baseball_data.lakehouse_ext.mart_sub_model_signals TO ROLE CREDENCE_API_RO;

-- ── offense_v1_signals  (8 columns) ──
CREATE OR REPLACE EXTERNAL TABLE baseball_data.lakehouse_ext.offense_v1_signals (
    GAME_PK                          VARCHAR        AS (VALUE:game_pk::VARCHAR),
    SIDE                             VARCHAR        AS (VALUE:side::VARCHAR),
    GAME_DATE                        DATE           AS (VALUE:game_date::DATE),
    GAME_YEAR                        NUMBER(38,0)   AS (VALUE:game_year::NUMBER(38,0)),
    PRED_RUNS_RAW                    FLOAT          AS (VALUE:pred_runs_raw::FLOAT),
    RUNS_INDEX                       FLOAT          AS (VALUE:runs_index::FLOAT),
    MODEL_VERSION                    VARCHAR        AS (VALUE:model_version::VARCHAR),
    INGESTION_TS                     TIMESTAMP_NTZ  AS (VALUE:ingestion_ts::TIMESTAMP_NTZ)
)
WITH LOCATION = @baseball_data.lakehouse_ext.s3_lakehouse/offense_v1_signals/
FILE_FORMAT = baseball_data.lakehouse_ext.parquet_snappy
AUTO_REFRESH = FALSE
COMMENT = 'E11.1-W9: offense_v1_signals signal store from S3 lakehouse parquet';
-- Preserve the feature/reader grant on (re)create (INC-18 band-aid class):
GRANT SELECT ON EXTERNAL TABLE baseball_data.lakehouse_ext.offense_v1_signals TO ROLE CREDENCE_API_RO;

-- ── offense_v2_signals  (10 columns) ──
CREATE OR REPLACE EXTERNAL TABLE baseball_data.lakehouse_ext.offense_v2_signals (
    GAME_PK                          VARCHAR        AS (VALUE:game_pk::VARCHAR),
    SIDE                             VARCHAR        AS (VALUE:side::VARCHAR),
    GAME_DATE                        DATE           AS (VALUE:game_date::DATE),
    GAME_YEAR                        NUMBER(38,0)   AS (VALUE:game_year::NUMBER(38,0)),
    PRED_RUNS_MU                     FLOAT          AS (VALUE:pred_runs_mu::FLOAT),
    PRED_RUNS_DISPERSION             FLOAT          AS (VALUE:pred_runs_dispersion::FLOAT),
    PRED_RUNS_RAW                    FLOAT          AS (VALUE:pred_runs_raw::FLOAT),
    UNCERTAINTY                      FLOAT          AS (VALUE:uncertainty::FLOAT),
    MODEL_VERSION                    VARCHAR        AS (VALUE:model_version::VARCHAR),
    INGESTION_TS                     TIMESTAMP_NTZ  AS (VALUE:ingestion_ts::TIMESTAMP_NTZ)
)
WITH LOCATION = @baseball_data.lakehouse_ext.s3_lakehouse/offense_v2_signals/
FILE_FORMAT = baseball_data.lakehouse_ext.parquet_snappy
AUTO_REFRESH = FALSE
COMMENT = 'E11.1-W9: offense_v2_signals signal store from S3 lakehouse parquet';
-- Preserve the feature/reader grant on (re)create (INC-18 band-aid class):
GRANT SELECT ON EXTERNAL TABLE baseball_data.lakehouse_ext.offense_v2_signals TO ROLE CREDENCE_API_RO;

-- ── starter_suppression_signals  (10 columns) ──
CREATE OR REPLACE EXTERNAL TABLE baseball_data.lakehouse_ext.starter_suppression_signals (
    GAME_PK                          VARCHAR        AS (VALUE:game_pk::VARCHAR),
    SIDE                             VARCHAR        AS (VALUE:side::VARCHAR),
    GAME_DATE                        DATE           AS (VALUE:game_date::DATE),
    GAME_YEAR                        NUMBER(38,0)   AS (VALUE:game_year::NUMBER(38,0)),
    STARTER_SUPPRESSION_MU           FLOAT          AS (VALUE:starter_suppression_mu::FLOAT),
    STARTER_SUPPRESSION_SIGMA        FLOAT          AS (VALUE:starter_suppression_sigma::FLOAT),
    STARTER_SUPPRESSION_SIGNAL       FLOAT          AS (VALUE:starter_suppression_signal::FLOAT),
    UNCERTAINTY                      FLOAT          AS (VALUE:uncertainty::FLOAT),
    MODEL_VERSION                    VARCHAR        AS (VALUE:model_version::VARCHAR),
    INGESTION_TS                     TIMESTAMP_NTZ  AS (VALUE:ingestion_ts::TIMESTAMP_NTZ)
)
WITH LOCATION = @baseball_data.lakehouse_ext.s3_lakehouse/starter_suppression_signals/
FILE_FORMAT = baseball_data.lakehouse_ext.parquet_snappy
AUTO_REFRESH = FALSE
COMMENT = 'E11.1-W9: starter_suppression_signals signal store from S3 lakehouse parquet';
-- Preserve the feature/reader grant on (re)create (INC-18 band-aid class):
GRANT SELECT ON EXTERNAL TABLE baseball_data.lakehouse_ext.starter_suppression_signals TO ROLE CREDENCE_API_RO;

-- ── starter_ip_signals  (13 columns) ──
CREATE OR REPLACE EXTERNAL TABLE baseball_data.lakehouse_ext.starter_ip_signals (
    GAME_PK                          VARCHAR        AS (VALUE:game_pk::VARCHAR),
    SIDE                             VARCHAR        AS (VALUE:side::VARCHAR),
    GAME_DATE                        DATE           AS (VALUE:game_date::DATE),
    GAME_YEAR                        NUMBER(38,0)   AS (VALUE:game_year::NUMBER(38,0)),
    STARTER_IP_MU                    FLOAT          AS (VALUE:starter_ip_mu::FLOAT),
    STARTER_IP_DISPERSION            FLOAT          AS (VALUE:starter_ip_dispersion::FLOAT),
    STARTER_IP_SIGNAL                FLOAT          AS (VALUE:starter_ip_signal::FLOAT),
    STARTER_IP_P80_OUTS              FLOAT          AS (VALUE:starter_ip_p80_outs::FLOAT),
    STARTER_IP_P20_OUTS              FLOAT          AS (VALUE:starter_ip_p20_outs::FLOAT),
    UNCERTAINTY                      FLOAT          AS (VALUE:uncertainty::FLOAT),
    IS_BULK_USAGE                    BOOLEAN        AS (VALUE:is_bulk_usage::BOOLEAN),
    MODEL_VERSION                    VARCHAR        AS (VALUE:model_version::VARCHAR),
    INGESTION_TS                     TIMESTAMP_NTZ  AS (VALUE:ingestion_ts::TIMESTAMP_NTZ)
)
WITH LOCATION = @baseball_data.lakehouse_ext.s3_lakehouse/starter_ip_signals/
FILE_FORMAT = baseball_data.lakehouse_ext.parquet_snappy
AUTO_REFRESH = FALSE
COMMENT = 'E11.1-W9: starter_ip_signals signal store from S3 lakehouse parquet';
-- Preserve the feature/reader grant on (re)create (INC-18 band-aid class):
GRANT SELECT ON EXTERNAL TABLE baseball_data.lakehouse_ext.starter_ip_signals TO ROLE CREDENCE_API_RO;

-- Verification (run after CREATE):
-- SELECT count(*) FROM baseball_data.lakehouse_ext.mart_sub_model_signals;
-- SELECT count(*) FROM baseball_data.lakehouse_ext.offense_v1_signals;
-- SELECT count(*) FROM baseball_data.lakehouse_ext.offense_v2_signals;
-- SELECT count(*) FROM baseball_data.lakehouse_ext.starter_suppression_signals;
-- SELECT count(*) FROM baseball_data.lakehouse_ext.starter_ip_signals;
