# NCAAF — Roadmap (per-sport, its own doc)

**Status:** v0.1 — activated 2026-07-08 (operator: each sport gets its OWN roadmap + prompts). Kickoff at the MLB All-Star break; NCAAF season opens ~late-Aug 2026.
**Parents:** `../../multi_sport_roadmap.md` (program-level) · `../../sport_data_platform.md` (the shared lean lakehouse — INSTANTIATE it, don't reinvent) · MLB `../../baseball/edge_program/` (the reference implementation).
**Sibling docs (this folder):** `ncaaf_guide.md` (the methodology stub) · `ncaaf_story_prompts.md` (the runnable prompts) · `ncaaf_data_inventory.md` (**to be created by Phase 0** — the master data file).

> **Session model (operator 2026-07-08):** post-ASB the two concurrent Opus sessions split — **1 baseball (continuation) + 1 DEDICATED NCAAF.** This doc + `ncaaf_story_prompts.md` drive the NCAAF session.

---

## 0. The two value cases (why NCAAF is in scope — read first)
1. **NCAAF's own betting/analytics product — a STRETCH** on the fall timeline (huge talent disparity, scheme/pace variance, but *soft markets on smaller programs* = where a real edge is most plausible). Phase it; don't over-invest before it earns.
2. **The NFL FEEDER — the real near-term value.** College production + combine/draft data → **college→NFL translation factors** → NFL rookie projections (fantasy-dynasty + rookie props where the market is otherwise priors-only). This is the football analog of MLB's MiLB→MLB MLEs (Edge **E7**). ⇒ **NCAAF *data* + the translation model are worth building even if NCAAF *betting* slips.** Data-first is the right posture either way.

## 1. Platform (instantiate, don't reinvent)
Follow `../../sport_data_platform.md`: free APIs → **Lambda + EventBridge cron → S3 Parquet lake** (`s3://<bucket>/ncaaf/raw/<source>/season=YYYY/…`) → **`dbt-duckdb` over S3** (staging→marts) → serving.
- ⚠️ **STALENESS FIX (the platform doc predates INC-16):** it says "serve on Railway PG" — **Railway is DECOMMISSIONED.** NCAAF serves on the CURRENT stack: **DynamoDB (primary) → S3 (fallback)**, same as MLB post-decommission. Do NOT stand up Railway.
- ⭐ **NCAAF inherits E11.20's platform choices** (Delta-on-path + Polars + the decomposed/incremental build + retention discipline) — this is the "get it right ONCE before the ×3 scale-out" payoff. Weekly-batch cadence ⇒ naturally cheap.

## 2. 🎯 DATA — exactly what we're looking for (the crux; drives Phase 0)
**Minimum bar = full box scores (team + per-player game logs). Reach = position-level ADVANCED metrics.** The honest availability map (grounds the Phase-0 source decision):

**Tier A — FREE / near-free via CFBD (CollegeFootballData; free tier 1,000 calls/mo, or Patreon Tier-3 ~$10/mo for 75k calls + GraphQL + live):** box scores (team + player), **play-by-play** (→ derive EPA/play, success rate, explosiveness, usage/share ourselves), advanced **TEAM** metrics (SP+, havoc rate, line-yards, stuff rate, sack rate, finishing/field-position), rosters, **recruiting/talent ratings**, results/schedule. **The Odds API** covers NCAAF game lines + props + scores. ⇒ this alone gets us box scores + team-advanced + PBP-derived player-advanced.

**Tier B — PAID charting (PFF College, or Sports Info Solutions):** the individual GRADES + tracking we can't derive from PBP — OL pass-block/run-block grades & pressures allowed, DB coverage grades & completion/passer-rating-allowed, WR separation & yards-per-route-run & contested-catch, RB yards-after-contact & forced missed tackles, QB CPOE/air-yards/time-to-throw. **This is the "incredibly useful if available" tier.** Decision (Phase 0): is the PFF College cost justified pre-profit, or do we ship on Tier-A + PBP-derived proxies first and add PFF only if the model earns it? (Default lean: Tier-A first; PFF is a later, edge-gated buy.)

**By position — the wishlist (✅ Tier-A derivable · 💰 Tier-B/PFF · ⛔ genuine gap):**
- **QB:** box (cmp/att/yds/TD/INT/sacks/rush) ✅ · EPA/play, success rate, pressure→sack (team-derived) ✅ · CPOE, air yards/ADOT, time-to-throw, big-time-throw/TWP grades 💰
- **RB:** box (rush/rec lines, fumbles) ✅ · EPA/rush, success rate, explosive-run rate, stuff rate, snap/usage share ✅ · yards-after-contact, forced missed tackles, YPRR 💰
- **WR/TE:** box (rec/yds/TD) ✅ · **targets ⇒ target share** ✅ (⚠️ *not* in the box score — only via CFBD `/plays/stats` `statType='Target'`, 2013+) · EPA/target ✅ · **aDOT 💰** (⚠️ **corrected by P0.1**: air yards are NOT charted free anywhere — this was listed ✅-from-PBP and is actually PFF-only) · separation, YAC over expected, contested-catch %, yards-per-route-run, drop rate 💰
- **OL (linemen):** ⛔ **the hardest gap — NO free individual-OL production exists anywhere.** TEAM proxies ✅ (line yards, stuff rate, sack rate allowed, havoc allowed, adjusted-line-yards) · individual pass-block win rate / pressures-allowed / run-block grades 💰 (PFF only). ⇒ model OL at the UNIT level from Tier-A; individual OL is a PFF-gated reach.
- **Defense:** team (havoc, stuff rate, PPA/EPA & success allowed, explosiveness allowed) ✅ · individual box (tackles/TFL/sacks/QB-hurries/INT/PBU/FF) ✅ · individual pressure rate, pass-rush win rate, coverage grade / completion & passer-rating allowed in coverage, missed-tackle rate 💰

**⇒ Phase-0 conclusion to reach (not assume):** Tier-A (CFBD + Odds API) delivers box scores + team-advanced + PBP-derived player metrics for QB/RB/WR/TE/DEF at $0–$10/mo; **OL-individual + all position grades are PFF-paid** — decide the buy after the Tier-A model exists. Ground every "available/derivable" claim against the live CFBD v2 endpoints (the free tier's 1,000-call cap likely forces the $10 Patreon tier for a multi-season backfill — a Phase-0 cost call).

> ### ✅ RESOLVED by **NCAAF-P0.1** (2026-07-13) — see **`ncaaf_data_inventory.md`** (the master data file; every claim ground-truthed on live endpoints)
> The conclusion above **held**, with four corrections that change downstream work:
> 1. **BUY CFBD Patreon Tier 3 — $10/mo, 75k calls.** The free tier's 1,000/mo cap is **confirmed live** (the `X-Calllimit-Remaining` response header). The backfill needs **~15,800 calls**; Tier 3 clears it in one month with ~5× headroom. **The only Phase-0 cost.**
> 2. **⚠️ Player-advanced data starts 2014, not 2004** (`/ppa/players/games` and `/plays/stats` return **zero** before 2013/14). Team/box/PBP reach back to 2004. **⇒ backfill window = 2014–2025.**
> 3. **⚠️ aDOT / air yards / CPOE are NOT free** (corrected above). **Snap counts have no college equivalent** (proxy = `/player/usage`). The **OL-individual gap is CONFIRMED** — `/games/players` has no OL category at all.
> 4. **⭐ The NFL-feeder spine is solved and is NOT an ID join** — CFBD and nflverse share **no player ID**. The **draft slot `(season, overall pick)`** is deterministic: **99.7% of CFBD draft picks (2015–25) resolve to an NFL `gsis_id`**. P0.3 is now a much smaller job (fuzzy matching only for UDFAs).
>
> Odds API covers NCAAF fully (11 books incl. **Bovada**; h2h/spreads/totals; **historical floor 2020**; props exist but are **thin** — marquee games/top players only). **PFF is not even a clean buy:** its $119.99/yr product is a **website subscription, not an API/bulk licence** — so it's an edge-gated *licensing* project, never a Phase-0 line item.

## 3. Phased plan
- **Phase 0 — DATA (start here):** evaluate + lock the sources (CFBD tier decision, Odds API NCAAF, draft/combine) → build `ncaaf_data_inventory.md` (every source → endpoint → fields → the by-position coverage map above, marking ✅/💰/⛔) + the **college↔NFL player-ID xref** (the spine of the feeder) + the lean-lakehouse scaffold (`ingest/` per `sport_data_platform.md §2`). *(prompts: P0.1 sources+inventory, P0.2 scaffold, P0.3 xref)*
- **Phase 1A — the NFL feeder (parallel, highest near-term ROI):** the **college→NFL translation** (E7 analog: college production + combine → NFL rookie projections; feeds the NFL vertical). Keyed on the P0.3 draft-slot spine.
- **Phase 1B — the NCAAF GAME MODEL (operator-scoped 2026-07-13 — plan it fully BEFORE building; the "don't become a mess" discipline).** Strict dependency order — each story gates the next:
  1. **P0.4 — NIL + TRANSFER-PORTAL data** (a data-source add, do EARLY, feeds the features): the transfer portal + NIL are re-shaping lower-conference power → we need it as an input. Source eval like P0.1: transfer moves are **derivable free from CFBD roster year-over-year deltas** (+ `/player/portal` if live); **NIL $ valuations are PAID/scraped (On3/Rivals) → treat like PFF (a gated buy, not Phase-0)** — ship on the FREE transfer/roster-continuity signal first (returning production, talent in/out, portal churn), add $-valuations only if they earn it.
  1b. **P0.5 — COACHING-CHANGE data ✅ DONE (box-verified & merged 2026-07-19):** shipped `coaches` ingest (25th source) → `stg_ncaaf_coaches` → mart `ncaaf_team_coaching_change` (1,555 rows) — HC continuity + tenure + change flags + ⭐ the coach's PRIOR SP+ off/def track record + first-time-HC/censoring, all leakage-safe. OC/DC deferred (no free source, gated like NIL-$). Feeds P1.3. 🚩 needs P1.1's box Dagster job (hand-built today). Orig scope ↓ — **HEAD COACH is FREE via CFBD `/coaches`** (coach↔school↔year history + per-coach-year **SP+ off/def splits** ⇒ not just a change flag but the coach's PRIOR scheme/quality profile). **OC/DC (coordinators) are a GAP — NOT in CFBD;** the tracker is **FootballScoop** (+ ESPN/CBS), no free API ⇒ scrape / maintain a small table, treat as a best-effort gated add (like NIL-$). ⇒ features: new-HC/OC/DC flags, first-year-at-school, + the new coach's prior SP+ off/def profile.
  1c. **P0.6 — HISTORICAL CLOSING-LINE ODDS ✅✅ DONE (FULL 2020–2025 backfill verified 2026-07-19):** `odds_ncaaf_historical` (26th source, `on_demand`) + `ingest/odds_backfill.py` + `ingest/verify_odds_historical.py` (exit 0/1, CI-gateable) — paid Odds-API `/historical` CLOSING game lines, **10,394 rows / 5,531 games across SIX seasons (2020–2025)**, the leakage-safe CLV benchmark that GATES P1.4's vs-market leg + all of Phase 2. Game-lines-only port of NFL-N0.4: kickoffs from CFBD `/games.startDate` (UTC), snapshot at K−buffer + `_snapshot_ts`<`commence_time` guard, below-2020 clean skip, `--dry-run` credit gate, on_demand-excluded from the default backfill. Verified: spreads/totals 100%/season + h2h 95.5–99.6%, 27 books incl. Bovada (Fanatics from 2025), 5,529/5,531 games retain a pre-kickoff close (the 2 misses = a CFBD-vs-OddsAPI kickoff disagreement, 0.04%). ~100–120k credits (4.83M remain). Run on the LAPTOP (I/O-bound). ⚠️ landmine: season defaults must be CLOCK-DERIVED (`last_completed_season()`), not pinned — a pinned 2020–2024 was stale-by-a-season the day it merged (the 2025 gap). ⇒ P1.4's vs-market leg + Phase 2 UNBLOCKED. NOT a blocker for P1.1–P1.3 or the feeder.
  1d. **P0.6b — RECURRING 2026+ CLOSING-LINE CAPTURE ⏰ (NEW 2026-07-19; NOT yet built — a build-AHEAD-of-kickoff item, not a live gap):** P0.6 is the one-time 2020–2025 backfill; there's no recurring capture for 2026+ yet — which is EXPECTED (the 2026 season hasn't started, so there's nothing to capture). The work: stand up the recurring closing-line capture BEFORE the Aug-2026 kickoff so the CLV feed keeps extending. Decision to make explicitly: (A) periodic `/historical` catch-up (reuse `odds_backfill.py` on a weekly in-season cron — recommended, low-risk, cheap at NCAAF's weekly cadence) vs (B) snapshot the live `odds_ncaaf` feed at commence−buffer. Prompt ready in `ncaaf_story_prompts.md`. Independent of P1.1–P1.4; sequence before Aug 2026.
  2. **P1.1 — ANALYTIC DATA MODELING ✅✅ DONE + BOX-VERIFIED (2026-07-20; `sports_ncaaf_dbt_build_job` ran green on the EC2 box in 2m59s — RUNTIME GATE CLOSED).** 4 dims + 4 facts + 3 rollups + 6 new staging models in `sports_dbt`, all FBS-filtered + sport-tagged, documented in **`ncaaf_mart_inventory.md`**. SCD-2 `dim_ncaaf_team` (realignment verified — Texas/Oklahoma→SEC 2024, UCLA→Big Ten, BYU→Big 12 2023) + SCD-2 `dim_ncaaf_player` (payload = team+position; class_year EXCLUDED — it was 2:1 version noise). Facts: 18,124 team-games · 539k player-games · 214k drives · 1.55M plays. ⭐ `rollup_ncaaf_team_week_asof` is THE pregame surface; `rollup_ncaaf_team_week_opponent_adjusted` applies a 2-pass schedule adjustment whose 2024 season-end top-5 (Ohio State/Notre Dame/Texas/Oregon/Ole Miss) reproduces the actual CFP result with no ranking input. **🚨 CAUGHT + FIXED A LIVE LEAK:** CFBD restarts `week` at 1 for the POSTSEASON, so raw `week` sorted January's title game before September's week 2 — 2024 Ohio State had FIVE games at `week<=1` and every as-of row from wk2 on absorbed them; cured by `season_order_week` (the only column that may order a season) + 3 singular leakage gates, one deliberately DATE-based because a week-based test re-uses the broken ordering and passes green. **Box job `sports_ncaaf_dbt_build_job` wired** (whole NCAAF DAG incl. P0.4 + P0.5 marts; staging serial; run=HALT / test=WARN per INC-6) + the first NCAAF CI slice. ⚠️ Pre-existing P0.3 failure surfaced (not mine): `not_null_xref_college_nfl_players_gsis_id` — 8 drafted players never played an NFL snap. ⏭️ unblocks P1.2 / P1.2b / P1.3 / P1A.
  3. **P1.2 — CONFERENCE / TEAM-STRENGTH MIXED-EFFECTS MODEL ✅ DONE (code-complete + validated on a REAL 2014-2025 build, 2026-07-20).** A hierarchical partial-pooling model (**team nested in conference**) → opponent-adjusted strength with honest posterior uncertainty, emitted **week-by-week point-in-time** at the `rollup_ncaaf_team_week_asof` grain. Shipped: `models/hierarchical.py` (a sport-agnostic penalized-Gaussian/mixed-effects solver — closed-form Gaussian posterior + marginal-likelihood variance components, so ~200 leakage-safe refits run in minutes instead of the hours NUTS would cost), `models/team_strength.py` (the NCAAF estimator), `models/run_team_strength.py` (CLI + gates + report), the `ncaaf_team_strength_week` mart + `assert_team_strength_is_point_in_time` gate, and 20 fast-gate tests. ⭐ `strength_margin` = neutral-field points above an average FBS team, decomposed into three ADDITIVE, auditable pieces (conference pooling level + pre-season covariates + this season's games), plus an offense/defense scoring split for P1.4's totals leg. The NIL/transfer signal enters as a **covariate on the prior mean** (`covariate_component_roster_flux` makes its contribution readable per team). **Leakage:** fit window is `season_order_week < W` STRICTLY (never raw `week`), hyperparameters come from STRICTLY PRIOR seasons, and **2014 is the un-emitted seed** (it exists only to give 2015 a season-prior) — so every emitted row has out-of-sample hyperparameters. Gates are DATE-based, not week-based (the P1.1 lesson: a week-based test re-uses the broken ordering and passes green). 🔬 **Two real bugs caught and fixed during the build, both worth remembering:** (1) ML variance components genuinely COLLAPSE to zero on a thin fit — the likelihood peaks at `tau_team = 0`, which silently deletes the team level of a team-strength model; cured with a boundary-avoiding Gamma(2,·) prior on each tau + multi-start, and a regression test pins it. (2) 🚨 **`strength_offense - strength_defense` is a trap** — defense is signed as points PREVENTED, so net strength is their SUM; subtracting returns ~0 for every team. ⚠️ Honest framing: a strength PRIOR, not an edge claim — it is a candidate FEATURE that **P1.4** ablation-tests under §0.5. `best_alpha = 0` holds. ⏭️ unblocks P1.3.
   📊 **VALIDATION (added from the session recap; vs REALITY, walk-forward OOS — never vs a market):** **MAE 13.0** vs 16.4 home-field-only baseline · **71.7% winner accuracy** · stable across all 11 seasons (12.8–14.1) · 2024 final top-3 = **Ohio State/Notre Dame/Texas** = the actual CFP semifinal field with **zero ranking input** · posterior sd tightens **monotonically 6.7 → 2.7** pts wk1→season-end · ✅ the required **NIL/transfer sanity check PASSED** (biggest pre-season roster-flux adjustments land on plausibly-churned teams — 2021 UMass, 2019 UConn, 2019 Rutgers — not noise). Reproduced on the operator's laptop with identical numbers (deterministic).
   🚩🚩 **CRITICAL CARRY-FORWARD FOR P1.4 — `strength_margin_sd` is PARAMETER uncertainty, NOT a calibrated predictive interval: it is ~1.5× TOO TIGHT to price a game with.** P1.4 MUST recalibrate it on HELD-OUT data (the MLB E13.6 pattern) before deriving any interval/probability; until then use `strength_margin` as a POINT feature and the sd as a RELATIVE confidence signal only. (Injected into the P1.3 + P1.4 prompts.)
   🐞 **TWO MORE REAL BUGS (beyond the 2 above) — 3 of the 4 were SILENT; CI could not have caught any (it mocks all IO):** (3) a **"flat" prior that was secretly a 1,000-point prior AND leaked** → one barely-supported covariate reported ±913 pts of uncertainty (2021 New Mexico State); cured by scaling priors to the data + a plausibility gate, accuracy unchanged. (4) a **recency-weighting bug** that quietly ran a "4-season" fit on the last few weeks — it surfaced **only as MIScalibration** (intervals covering half their claim), never as an error; fixing it improved calibration AND accuracy. ⇒ ⭐ **LESSON: model quality gates here are BEHAVIORAL, not green-checkmark — the real-data run + calibration/plausibility checks are the gate.** ✅ and the leakage gate was **verified to actually FAIL on a tampered row**, so its green means something — make that the standard. ⭐ **`hierarchical.py` is reusable UNCHANGED by NFL + NCAAB** (same problem shape: many entities, few games, sparse schedule) — cross-sport leverage, recorded in `../../sport_data_platform.md`.
  3b. **P1.2b — FRESHMAN-PRODUCTION PROJECTION ✅✅ DONE + VALIDATED ON THE REAL 2014–2025 BUILD (2026-07-21; operator ran it laptop-side, priors landed in S3).** 18,306 recruit↔production pairs → **16,541 priors across 11 classes (2015–2025)** + 1,424 (season, team) rows. All HALT gates green. 📊 **REAL RESULT — a genuine but WEAK signal (the honest outcome for a freshman prior):** winner `gbm@200-2-0.05` beats the position-mean null (OOS MAE **0.7011 < 0.7164**, ~2% skill) **robustly — PBO = 0.000** (the small edge holds across every CSCV split; partial-pool/OLS collapse toward the null because rating is a weak predictor). ⚠️ **DSR = 0.821 (< 0.95)** — the effect is real but small enough it doesn't clear the strict live-grade deflation bar; PBO/DSR are REPORTED not HALTed (E2.1-r reasoning — for a candidate FEATURE feeding a downstream bake-off, robust-but-modest is acceptable; P1.4 decides). Per-group proj↔realized corr: best where expected (TE 0.30 / DL 0.28 / WR 0.27), ALL 0.20, ambiguous ATH ~0.02. Face-valid: top 2025 projections = Elijah Griffin (5★ DL) / Keelon Russell + Bryce Underwood (5★ QB); top classes = Texas/Georgia/Alabama/Ohio State — from a model that never saw a ranking. ⇒ recruiting rating is a genuine-but-noisy predictor of immediate freshman production (opportunity/depth-chart drives freshman snaps as much as talent). `best_alpha = 0` holds. The recruiting-level analog of the college→NFL feeder (MLB E7 MLE, one rung down): a true freshman has NO college snaps, so the recruiting rating becomes the prior. Shipped: `stg_ncaaf_recruiting_players` + `stg_ncaaf_roster.recruit_ids` (staging), `ncaaf_recruit_production_pairs` (the recruit→first-FBS-season-production substrate), `models/freshman_projection.py` (the **§0.5 BAKE-OFF** — 4 pre-registered candidate classes, NOT a single architecture: ⭐ partial-pooling via the P1.2 `hierarchical.py` solver REUSED UNCHANGED — a random-slopes model, EB-shrunk thin position cells toward a global rating→production line, inheriting the boundary-avoiding tau prior that stops variance collapse; position-stratified OLS foil; learned GBM; and a **position-mean NULL FLOOR**), `models/run_freshman_projection.py` (CLI + gates + report), the `ncaaf_freshman_priors` (per-recruit) + `ncaaf_team_freshman_prior` (⭐ the **P1.3 join contract**, grain (season, team)) views, and **14 fast-gate tests**. **Selection:** leave-one-CLASS-out expanding-window CV on held-out standardized-production MAE, PBO/DSR-deflated, with an **oracle-floor** sanity gate (E2.1-r). **Leakage** is class-based (fit only on strictly-prior classes; 2014 the un-emitted seed) and **verified to FAIL on a tampered training class** (green means something). **Uncertainty is PARAMETER uncertainty** (a RELATIVE confidence signal, ~like P1.2's sd — a pricing consumer MUST recalibrate). **🔧 Corrected a live data-inventory error:** the recruit↔college bridge is `roster.recruit_ids ↔ recruiting.id` (→ **18,306 recruit↔production pairs** on the real mart), NOT `athleteId ↔ recruitIds` (7 matches in 12 seasons — effectively dead). ⚠️ Honest: a freshman PRIOR, not an edge claim; `best_alpha = 0`. Report: `ablation_results/ncaaf_p1_2b_freshman_projection.md`. ⏭️ Feeds P1.3 (fills the true-freshman gap in roster features).
  4. **P1.3 — FEATURE ENGINEERING ✅✅ DONE + BOX-VERIFIED ON REAL 2014–2025 (2026-07-21).** Shipped `feature_ncaaf_pregame_matrix` (dbt-duckdb mart, cached parquet + S3 `ncaaf/derived/feature_pregame_matrix` Delta) — **9,086 FBS-vs-FBS games × 200 cols, PER-MATCHUP grain** (`home_*`/`away_*` side by side, matches P1.4's joint-distribution design), **12 families** (strength · efficiency raw+opp-adj · pace · line/trench unit · drive quality · roster/portal/talent · freshman · coaching HC-only · QB continuity · situational · travel/altitude), 6 `label_*` targets. Two join grains verified on the real lake (week-grained 1:1 on `(season, team_id, season_order_week)`; season-grained broadcast on `(season, team)`) — count == the exact `dim_ncaaf_game` FBS universe, no fan-out/drop. **DATE-based leakage gate `assert_pregame_matrix_is_point_in_time` (HALT, proven-to-fail)** + a per-family coverage report + 11 tests; NCAAF HALT-leakage set now 5 gates (P1.2 strength gate promoted to HALT). Honest scope: NULL kept NULL; QB continuity-only (no CFB injury source); HC-only coaching; no is_rivalry (dropped, not guessed). 📝 **CORRECTED my earlier "no venue" gap — venue lat/long ARE staged → travel/altitude shipped (coverage-flagged); the "no venue" assumption was wrong (carry to NFL/NCAAB).** `best_alpha=0` — a feature matrix, no edge claim. ⏭️ unblocks P1.4 (reads it directly, zero setup). Orig scope ↓ —
  5. **P1.4 — THE GAME-MODEL BAKE-OFF (3 TARGETS: H2H moneyline · spread/margin · total points) 🟢 CODE-COMPLETE + SMOKE-VERIFIED (2026-07-22; heavy §0.5 search + CLV leg are operator-run).** Same §0.5 discipline as MLB (≥3 model classes, Optuna, feature-ablation, purged/embargoed CV, **PBO<0.2 / DSR≥0.95 / FDR**, honest `best_alpha=0` until proven + CLV as the leading indicator). ⭐ **DESIGN LEAN: model the JOINT scoring distribution (margin + total, or home/away points) ONCE → DERIVE all 3 markets** (H2H = P(margin>0), spread = P(margin>line), total = P(total>line)) — mirrors MLB's per-side→derive-everything (E2); more coherent than 3 unlinked target models, and it makes the sides↔totals correlation honest. The bake-off picks joint-vs-separate on the metric. **SHIPPED:** `models/ncaaf_game_distribution.py` (pure joint (margin,total) core — 4 forms gaussian/student_t/native/count, held-out σ/ρ/dof/r calibration [the E13.6 recalibration of P1.2's too-tight `strength_margin_sd`], derive-3-markets, oracle-floor guard) + `models/bakeoff_ncaaf_game.py` (assemble one-pull cache + the CLV odds↔matrix `game_id` staging join, season-forward DATE-purged folds [`season_order_week`, never raw `week`], 5 learners × 4 contracts [incl. `strength_only` = the P1.2 prior FOIL the full matrix must beat] × 4 forms, Optuna, PBO/DSR decide, finalize + PIT gate + vs-close CLV eval) + 11 fast-gate guards. ⭐ **NCAAF selection-metric-hygiene finding: unlike discrete F5 baseball, NCAAF margin/total are WIDE-support → a correct oracle covers ≈0.80 (NO inclusive-integer inflation) → the calib_80 floor carries a small sampling tolerance (deflationary wobble), metric stays PIT-only.** 2-fold smoke: reference strength-prior competitive with full-LGBM (a trustworthy-null is a valid outcome); finalize gate PIT-flat, calib PASS. 🖥️ off the MLB serving lane (SF-free). Design/run-plan: `ablation_results/ncaaf_p1_4_game_model.md`.
- (optional/stretch alongside 1B) the E10.1 parlay CALCULATOR + CLV transparency (transfer directly from MLB, no new modeling).
- **Phase 2 — gated edge (post-kickoff, optional):** NCAAF sharp-anchor / totals / spreads under the SAME deflation gate as MLB (**PBO<0.2, DSR≥0.95, FDR**; game-level correlated-quote collapse). ⭐ this is where MLB's now-hardened instruments earn out. (Props are THIN per P0.1 — lean on sides/totals/microstructure, not an MLB-style prop engine.)

## 4. ⭐ Why NCAAF is where the closed-MLB tools get their real test
MLB returned **9 mechanism nulls** (H2H, totals, props, derivatives, cross-market, microstructure, 6 model classes) *because MLB main markets are efficient*. **College markets are thinner + softer-lined** → the weaker-efficiency prior where an edge is actually plausible. Re-point the hardened, deflation-honest MLB instruments here:
- **E13.16 microstructure/CLV harness** (leakage guard + stale-quote control + working negative control + realized-ROI) → NCAAF line movement, esp. smaller programs.
- **E1.12 regime-conditioning** → talent-mismatch regimes (huge in CFB) are a natural specialization axis.
- **E13.14 cross-market** → sides↔totals↔props constellation on softer lines.
All under the same PBO/DSR/FDR deflation — the clock must NOT manufacture a mirage (the MLB discipline transfers wholesale).

## 5. Cost posture (pre-profit)
CFBD $0 (free tier) → ~$10/mo (Patreon Tier-3, likely needed for the backfill) · Odds API (existing sub) · PFF College = a LATER edge-gated buy, not a Phase-0 cost · compute = Lambda + DuckDB-over-S3 (pennies) + the existing AWS box/S3. No new Snowflake. Weekly in-season cadence keeps it cheap.

## 6. Timeline
- **Now → ASB:** Phase 0 prompts ready (this doc + `ncaaf_story_prompts.md`); the dedicated NCAAF session starts at the ASB.
- **ASB → late-Aug:** Phase 0 data + scaffold + the feeder xref land; Phase 1 feeder projections.
- **Season (late-Aug on):** honest surfaces live; Phase 2 edge gated + optional.

---
_Data-source facts current as of 2026-07-08. Phase 0 re-verifies against live CFBD v2 endpoints before committing._
