# NCAAF — Story Prompts (per-sport catalog)

**Parent roadmap:** `ncaaf_roadmap.md` · **Platform:** `../../sport_data_platform.md` · **Reference impl:** MLB `../../baseball/edge_program/` (esp. Edge **E7** MiLB→MLB for the feeder).
**Global conventions (inherit from MLB §0):** `dbtf` not `dbt`; `uv run python`; Snowflake via MCP fully-qualified, no `USE` (but NCAAF is DuckDB/S3-native — Snowflake only if a port-up earns it); **do NOT `git commit`/`push`** (operator does git — provide a copy-pasteable `git add <paths>` at closeout); operator provisions all secrets/API keys (never enter credentials); hand >1-min scripts to the operator with **laptop-vs-box + the full copy-pasteable command**.
**⚠️ Serving = DynamoDB→S3 (NOT Railway — decommissioned INC-16); the platform doc's "Railway PG" line is stale.**

---
```
✅ DONE 2026-07-13 — NCAAF-P0.1 delivered `ncaaf_data_inventory.md` (the master data file; 42 live CFBD v2 endpoints probed,
   Odds API + nflverse ground-truthed). HEADLINES: buy CFBD Patreon Tier 3 ($10/mo, 75k calls — free tier's 1,000/mo cap
   confirmed live; backfill needs ~15.8k calls). Backfill window = 2014–2025 (player-advanced is ZERO before 2013/14).
   NFL-feeder spine SOLVED: CFBD⇄nflverse share NO player ID, but the draft slot (season, overall pick) matches 99.7%.
   OL-individual gap CONFIRMED; aDOT/CPOE/snap-counts are NOT free. PFF = a licensing project, not a $120 buy → deferred.
▶ NCAAF-P0.1 — DATA-SOURCE EVALUATION + `ncaaf_data_inventory.md` (START HERE — data-first)   [Data · 🧭 OPUS · Phase 0]
🎯 GOAL: lock the viable data sources + produce the master data file. The MINIMUM bar is full BOX SCORES (team + per-player game logs); the REACH is position-level ADVANCED metrics for QB / RB / WR / TE / OL / DEF. Ground every claim on the LIVE CFBD v2 API — do not assume.
Read: `ncaaf_roadmap.md` §2 (the exact by-position wishlist + the ✅Tier-A / 💰PFF / ⛔gap map) + `../../sport_data_platform.md` + `../../multi_sport_roadmap.md` §4 (the NFL feeder).
DO:
 1. **CFBD (CollegeFootballData) — the free/near-free backbone:** enumerate the v2 endpoints that actually deliver, per the wishlist: box scores (team + player game stats), PLAY-BY-PLAY (the raw material for EPA/success/explosiveness/usage we DERIVE ourselves), advanced TEAM stats (SP+, havoc, line yards, stuff/sack rate), rosters, recruiting/talent, results/schedule, drives, win-prob. ⚠️ **the FREE tier is ~1,000 calls/mo — almost certainly too tight for a multi-season backfill → price the Patreon Tier-3 (~$10/mo, 75k calls + GraphQL + live) and make the buy recommendation** (this is the one likely Phase-0 cost; flag it for the operator to provision the key).
 2. **The Odds API (existing sub):** confirm NCAAF game lines + totals + spreads + props + scores coverage + the historical depth (for CLV/backtests later).
 3. **Draft/combine data** (for the NFL feeder spine): identify the source (CFBD has some; combine data via nflverse/`nfl_data_py` or a public combine set) — needed for the college→NFL translation.
 4. **The PFF/SIS (paid-charting) DECISION:** map EXACTLY which wishlist items are ONLY available paid — the individual GRADES: OL pass-block/run-block + pressures-allowed, DB coverage grade / completion & passer-rating allowed, WR separation / YPRR / contested-catch, RB yards-after-contact / forced-missed-tackles, QB CPOE / air-yards / time-to-throw. **RECOMMEND: ship Tier-A + PBP-derived proxies FIRST; treat PFF College as a LATER edge-gated buy, not a Phase-0 cost** (state the price if findable + what it would unlock). ⛔ **Call out the OL-individual gap explicitly: there is NO free individual-offensive-line production — model OL at the UNIT level from Tier-A team line-metrics; individual OL is PFF-only.**
 5. **WRITE `ncaaf_data_inventory.md`** — the master file: per source → endpoint/dataset → fields → grain (team/player/play) → season coverage → the by-position ✅/💰/⛔ map from roadmap §2, resolved against what's ACTUALLY live → the recommended Phase-0 source set + the cost line + the identified gaps.
GATE/AC: `ncaaf_data_inventory.md` exists + is ground-truthed against live endpoints (a real sample pull per key source, not doc-reading); the by-position coverage map is resolved (every wishlist item marked available-free / paid / gap with the source named); the CFBD tier + PFF buy recommendations are explicit with costs; the NFL-feeder data (draft/combine + the ID-xref source) is identified. ⇒ P0.2 (scaffold) + P0.3 (xref) can start from a locked source set. Operator handoff: which API keys to provision + `git add`.
```
```
✅ DONE 2026-07-15 — NCAAF-P0.2 stood up `football/ncaaf/ingest/` (Delta-native, sport-agnostic scaffold) + the SHARED
   new-sports `sports_dbt/` dbt-duckdb project (separate from MLB's Snowflake dbt, per the cross-sport decision).
   LIVE runtime-gate PASSED off-box (laptop): CFBD /games (231, 85 FBS) + Odds API NCAAF (2,640 outcomes / 11 books incl.
   Bovada) + nflverse draft_picks (257) → local Delta → dbt-duckdb `stg_ncaaf_games`/`stg_ncaaf_odds` build + tests GREEN.
   Registry covers all 24 §8 tables; P0.1 landmines encoded (CFBD 200-text/html JSON guard, /plays/stats per-gameId 2000-cap,
   year-only no-team-loop, box/advanced id=, nflverse-as-parquet). 16 fast-gate tests. ⏳ OPERATOR: create the
   `credence-sports-lakehouse` S3 bucket + instance-role grant, provision the CFBD Tier-3 key, then run the S3 backfill
   (same code path, proven local). Serving = DynamoDB→S3. See `football/ncaaf/ingest/README.md`.
✅ DONE 2026-07-16 (box-verified, merged) — the NCAAF lean lakehouse scaffold is LIVE: `football/ncaaf/ingest/` (CFBD/Odds/nflverse → season-partitioned **Delta** on `s3://credence-sports-lakehouse/ncaaf/raw/<source>/season=YYYY/`) + a NEW separate `quant_sports_intel_models/sports_dbt/` dbt-duckdb project (disjoint from MLB's Snowflake dbt) with `stg_ncaaf_games`/`stg_ncaaf_odds`; full 2014–2025 backfill landed + read-verified (games/play_stats 12/12 seasons). Cost = **$10/mo CFBD Tier-3** (the one recurring spend). Infra durable: bucket `credence-sports-lakehouse` (us-east-2) + IAM (box RW, Lambda RO); Tier-3 key in the box `.env` only.
🚩 **DOWNSTREAM FLAGS (P0.3 / P1.1+ MUST heed — from the P0.2 recap):**
 • ⚠️ **`odds_ncaaf` is CURRENT odds, NOT historical closing lines** (936 rows, fetched live, mis-tagged `season=YYYY`). Real historical NCAAF closing lines = inventory #21 `odds_ncaaf_historical` (paid Odds-API `/historical`) — **NOT built → a NEW story (P0.6) gating any NCAAF market-benchmark / CLV / edge work** (incl. P1.4's vs-market eval + all of Phase 2). Do NOT treat the 936 current rows as closing lines.
 • ⚠️ **`games` lands ALL divisions** (1,586–3,831/season); FBS-vs-FBS ≈ 760–808. **FBS filter is in dbt (`is_fbs_matchup`), NOT at ingest → every downstream model MUST apply it** or it pulls in FCS/D-II junk. (Only `play_stats` is FBS-gated at ingest, for CFBD budget.)
 • **Backfills run ON THE BOX, not the laptop** (in-region multipart PUT + instance-role write; laptop IAM is RO). Reuse the single-client + adaptive-throttle path (`CFBD_THROTTLE_SECONDS`, ×1.5 on 429) + `--skip-existing`; never hand-roll a client per season. Boto3/delta-rs: reuse `s3io.storage_options()` (the AKID cure) — never `aws_access_key_id=os.environ.get(...)`.
 • **dbt-duckdb is NOT on the box yet** (`sports_dbt` runs on the laptop) + **`sports_dbt` has NO CI** → **P1.1 must budget for: wiring the NCAAF dbt build into a Dagster job on the box + standing up the first NCAAF CI gate** (the MLB gates don't cover it).
 • CFBD quirks are encoded (`cfbd_client` asserts `application/json` so a wrong-path 200-HTML RAISES); still, each of the ~21 non-smoked fetchers gets a live ground-truth pull the FIRST time it's consumed (P0.1 discipline).
✅ UNBLOCKED: P0.3 (xref) + P1.1 (dims/facts — absorbs the box-dbt wiring + first CI gate). (Orig prompt ↓.)
▶ NCAAF-P0.2 — LEAN LAKEHOUSE SCAFFOLD (instantiate `sport_data_platform.md`)   [Data/Infra · 🧭 OPUS · Phase 0 · after P0.1 locks the sources]
🎯 GOAL: stand up the `ncaaf/` ingest→S3→dbt-duckdb pipeline per the SHARED platform pattern — do NOT reinvent it; copy MLB's proven lakehouse-write patterns. Weekly-batch, cheap.
Read: `../../sport_data_platform.md` (esp. §1 pattern + §2 repo layout) + `ncaaf_data_inventory.md` (P0.1) + MLB's `scripts/utils/lakehouse_raw_writer.py` + `run_w1_lakehouse` pattern (the DuckDB-over-S3 build) + ⚠️ **the E11.20 Delta/Polars patterns (NCAAF should be Delta-native from day one — inherit, don't rebuild raw parquet).**
DO: build `ncaaf/ingest/` per §2 — `s3io.py` (DataFrame→partitioned Parquet/**Delta** in S3; reuse MLB's), `sources.py` (the CFBD/Odds registry: source→fetch fn→season col→table), `handler.py` (Lambda entrypoint), `backfill.py` (off-Lambda full-history runner), `Dockerfile`. Land raw → `s3://<bucket>/ncaaf/raw/<source>/season=YYYY/…`; a first `dbt-duckdb` staging layer. **Box-safe:** instance-role S3 auth (never inline keys — the AKID landmine); DuckDB region + memory box-aware; TZ = the game-day helper class. Serving target = DynamoDB→S3 (NOT Railway).
⚠️ **P0.1 LANDMINES — do NOT rediscover these (all in `ncaaf_data_inventory.md` §1):**
 • **A WRONG CFBD PATH RETURNS `200 text/html` (the Swagger page), NOT a 404** — the v1-style singular paths (`/play/stats`) silently serve an HTML doc bundle. **Every fetcher must assert `Content-Type: application/json` + that the body parses — status 200 is NOT a success signal.** v2 paths are PLURAL (`/plays/stats`).
 • **`/plays/stats` is hard-capped at 2,000 rows/response ⇒ MUST be pulled per `gameId`** (~960 calls/season — the dominant cost). **`/plays` REQUIRES `week`.** But `/roster`, `/player/usage`, `/stats/player/season`, `/ppa/players/season`, `/recruiting/players`, `/player/returning` all accept **year-only — do NOT loop 136 teams (a 136× budget trap).** `/game/box/advanced` takes `id=`, not `gameId=`.
 • **`nfl_data_py` IS ABANDONED** — it pins `pandas==1.5.3`, which **will not build on py3.12**. `sport_data_platform.md §4/§10` still recommends it; **that guidance is STALE.** Read the **nflverse release Parquet directly** via DuckDB `read_parquet('https://github.com/nflverse/nflverse-data/releases/download/<asset>/<asset>.parquet')` — dependency-free and lakehouse-native.
 • The **24 locked lake tables + partitioning + cadence are already specified** in `ncaaf_data_inventory.md` **§8** — build straight from that table. Backfill window **2014–2025** (§2.7). Requires the **Tier-3 key** (§6) before the backfill runs.
GATE/AC: a live ingest of ≥1 CFBD source + Odds API NCAAF → S3 Parquet/Delta (a real Lambda or off-box run, per the runtime gate — CI can't see it); a `dbt-duckdb` staging model reads it; the backfill runner does a multi-season pull; repo layout matches `sport_data_platform.md §2`; costs stated. Operator handoff: box-vs-laptop run-order + `git add`.
```
```
✅ DONE 2026-07-16 — NCAAF-P0.3 built the college↔NFL player-ID xref (the NFL-feeder spine; MLB Edge-E7 analog, feeds P1A).
   KEY = the DRAFT SLOT, not an ID: `CFBD /draft/picks (year, overall)` ⇄ `nflverse draft_picks (season, pick)` (99.7% →
   gsis_id); combine attaches nflverse-internally on the cfb_player_id slug. Deliverable = a Python DuckDB-over-Delta builder
   `football/ncaaf/feeder/xref.py` (mirrors MLB run_w1_lakehouse; PARITY — same code on the S3 box + local Delta fixtures) that
   lands `ncaaf/marts/xref_college_nfl_players` as a versioned Delta mart, + `feeder/name_norm.py` (shared suffix/apostrophe/
   accent normalisation, Python+SQL, parity-tested) + dbt views (`stg_ncaaf_cfbd_draft_picks`, `stg_nflverse_draft_picks`, mart
   VIEW `xref_college_nfl_players` + schema tests). match_method source-stamped: `deterministic_slot` (high, score 1.0) |
   `fuzzy_udfa` (medium/low, Jaro-Winkler name+school+position). Residuals handled: UDFAs (fuzzy nflverse players⇄CFBD roster),
   transfers (slot is school-agnostic; collegeAthleteId stable across schools), name-norm (the 2–8% surname disagreement).
   ⚠️ `target_*` (car_av/w_av/games/…) are the P1A TARGET (POST-draft) — prefixed so they can't be fed as features (leakage-safe).
   ANTI-CARTESIAN: SQL joins are immune to the pandas NaN-to-NaN trap, but the builder STILL drops null keys + dedups 1-per-key +
   ASSERTS row counts after every join + raises `XrefValidationError` on inflation/dup-gsis. Offline session (no AWS/GitHub net) →
   validated on local Delta fixtures: `betting_ml/tests/test_ncaaf_xref.py` (14 fast-gate tests, 0.8s) proves slot baseline 100%
   reproduced + no cartesian + NaN-slug no-multiply + Jr.-surname agrees + UDFA resolve/exclude-drafted/no-false-match + transfer
   stable id + unique gsis_id; the full dbt build+tests over the fixture are GREEN (18 nodes).
   ⏳ OPERATOR (the box runtime gate — CI/laptop can't see S3): run `docker compose -f services/dagster/aws/docker-compose.yml
   exec -T -e AWS_DEFAULT_REGION=us-east-2 dagster-codeloc python -m quant_sports_intel_models.football.ncaaf.feeder.xref
   --draft-seasons 2015-2025 --write` → must reproduce ~99.7% slot baseline with NO cartesian inflation (the CLI prints per-class
   %). On first real read, confirm `nflverse_players` carries college_name/draft_number/entry_year/display_name (else the UDFA
   residual degrades to 0 gracefully; the slot spine is unaffected). ✅ UNBLOCKS P1A (college→NFL translation).
▶ NCAAF-P0.3 — COLLEGE↔NFL PLAYER-ID XREF (the NFL-feeder spine)   [Data/Model · 🧭 OPUS · Phase 0 · the highest-ROI Phase-0 asset]
🎯 GOAL: build the college↔pro player-ID crosswalk — the spine of the NFL feeder (college production → NFL rookie projections; the E7 MiLB→MLB analog). Do this EARLY; everything downstream keys on it.
Read: `../../multi_sport_roadmap.md` §4 (draft-continuity) + MLB Edge **E7** (the MiLB→MLB MLE pattern — the direct analog) + `ncaaf_data_inventory.md`.
⭐ **P0.1 ALREADY SOLVED THE SPINE — this story is now MUCH smaller than written. Read `ncaaf_data_inventory.md` §4 FIRST.**
 • **There is NO shared player ID between CFBD and nflverse** (CFBD `collegeAthleteId`=ESPN numeric `4431611`; nflverse `cfb_player_id`=a sports-reference SLUG `caleb-williams-3`; CFBD `nflAthleteId` ∩ nflverse `espn_id` = **0 of 257**). **Any plan that assumes an ID join is wrong.**
 • **The deterministic key is the DRAFT SLOT:** `CFBD /draft/picks (year, overall)` ⇄ `nflverse draft_picks (season, pick)` (nflverse `pick` IS the overall pick). **Verified 2,813/2,821 = 99.7% of 2015–25 CFBD picks resolve to an NFL `gsis_id`**, independently validated at 92–100% surname agreement. Combine measurables then attach nflverse-internally on the `cfb_id` slug (81.8% of picks; 65.7% have a 40-time).
 • **The feeder's TARGET variable is already in `nflverse draft_picks`** (`car_av`, `w_av`, `games`, `seasons_started`, `probowls`, career pass/rush/rec lines) — no extra source needed.
 • 💥 **pandas merges NaN-to-NaN** — coercing the slug column with `to_numeric` yields all-NaN and a **cartesian explosion that fabricates a bogus ~100% match** (this bit the P0.1 session). **Drop null join keys on BOTH sides and assert `len(merged) == len(left)`.**
DO: land the xref as a versioned mart built on the **draft-slot key** (not fuzzy matching) for drafted players. The REAL remaining work is the three residuals: **(a) UDFAs** — no draft slot ⇒ genuinely need fuzzy `name + school + position + year`; **(b) transfers** — a player's CFBD production spans schools (use `/player/portal` + `/player/search.teamStints`); **(c) name normalisation** (suffixes/`Jr.`/apostrophes = the 2–8% surname disagreement). Source-stamp match confidence (deterministic-slot vs fuzzy), leakage-safe. First-pass college→NFL translation (the E7 analog) begins once the xref exists (its own Phase-1 prompt).
GATE/AC: a college↔NFL ID xref mart with match-confidence, validated on a known draft class (the 99.7% slot baseline must be REPRODUCED, no cartesian inflation — assert row counts); UDFA/transfer/duplicate cases handled; feeds the NFL guide's rookie projections. Operator handoff + `git add`.
```
```
✅ DONE 2026-07-19 — NCAAF-P0.4 shipped the FREE transfer/roster-continuity signal as the derived dbt mart
`ncaaf_team_roster_continuity` (`sports_dbt/models/ncaaf/marts/`; one row per season+team, FBS, 2014–2025, 1,555 rows) +
4 new staging views-as-tables (`stg_ncaaf_returning_production` / `_transfer_portal` / `_talent` / `_roster`). The signal
combines: **returning production %** (CFBD `/player/returning`, the headline continuity metric + pass/rec/rush splits + usage),
**transfer-portal flux** (`/player/portal` — in/out counts, star- & 247-rating-weighted, blue-chip, attrition; portal era 2021+,
flagged `portal_data_covered`), **roster year-over-year continuity** (`/roster` head-count overlap, N∩N-1; NULL at the 2014 floor
= unknown, not 0), and **247 talent level + YoY delta** (`/talent`, 2015+). ⭐ Leakage-safe / point-in-time (every input set
pre-season; joined by season as-of kickoff). **Zero new ingest/cost** — pure dbt over the P0.2-locked §8 raw feeds. Feeds
**P1.2** (strength covariate) + **P1.3** (features). NIL-$ documented + DEFERRED (inventory §10 — On3/Rivals = PFF-class,
no public bulk API, NOT ground-truthed, edge-gated, never a Phase-0 dep). Validated: dbtf compile + `dbtf build … --target dev`
GREEN (10 tests inc. grain-unique), values ground-truthed on real CFBD pulls (SMU-2024 etc.). ✅ UNBLOCKS the P0.4 covariate in
P1.2 + the roster features in P1.3.
🩹 LANDMINE (added to the pile): `returning` is a **reserved keyword** in DuckDB/dbt-fusion — a CTE named `with returning as (…)`
fails with a MIS-SURFACED "Error while decoding UTF-8 … 1:11" from fusion (not a parse error) → renamed `ret_prod`. The
multi-`delta_scan`-stacking mart hits the N0.3 "DeltaScan serialization not implemented" limit → the 4 new staging models are
`materialized='table'` (read each Delta once) so the mart plan holds no `delta_scan`.
▶ NCAAF-P0.4   ✅ **DONE 2026-07 (prod; SQL-ONLY derivation, $0 new — every raw feed was already in P0.2).** New mart **`ncaaf_team_roster_continuity`** (season×team, FBS, 2014–2025, 1,555 rows): returning-production % (`/player/returning`) + transfer-portal flux (`/player/portal` — in/out counts, star/247-weighted, blue-chip, attrition; **`portal_data_covered` flag, era 2021+**) + roster YoY overlap (`/roster`) + 247 talent + YoY delta (`/talent`, 2015+); 4 staging models. Leakage-safe by construction (pre-season inputs; 2014 continuity honest-NULL). **NIL-$ DEFERRED** (documented `ncaaf_data_inventory.md §10` — no public bulk API = scrape/enterprise like PFF; NOT ground-truthed since nothing to pull; revisit only if a Phase-1 ablation shows residual error where roster-$ would explain beyond portal-flux + returning-production). CI `dbtf compile --target ci` 134✅. ⏭️ unblocks P1.2 (strength covariate) + P1.3 (roster-flux feature block). 🚩 **FLAGS the downstream inherits:** (1) ⚠️ **NO Dagster job for the NCAAF dbt models yet → the mart is rebuilt MANUALLY until P1.1 wires the box job (P1.1's scope) — don't let it silently rot**; (2) **respect `portal_data_covered` — pre-2021 portal=0 is UNKNOWN, not "no churn"** (P1.2/P1.3 must not treat 0 as a real value); (3) 🧰 **dbt-fusion preview-196 SEGFAULTS building 2+ delta-scan models in ONE invocation → build staging SERIALLY** (workaround; resolves on a fusion upgrade — bites P1.1 which builds many NCAAF models). (Orig prompt ↓.)
▶ NCAAF-P0.4 — NIL + TRANSFER-PORTAL DATA (source eval + ingest — a data-source add; do EARLY, feeds the features)   [Data · 🧭 OPUS · Phase 0/1 boundary]   (operator-scoped 2026-07-13)
🎯 WHY: the transfer portal + NIL money are RE-SHAPING lower-conference power (talent moves fast now) — the game model needs a roster-continuity / talent-flux signal or it will misprice teams whose roster turned over. Data-first + source-honest, like P0.1.
DO: (1) **Transfer portal (mostly FREE):** derive portal moves from **CFBD roster year-over-year deltas** (who left / who arrived) + `/player/portal` if live + `teamStints`; build a per-team **roster-continuity / returning-production** signal (returning starters, returning production %, talent in vs out). (2) **NIL $ valuations (PAID/scraped — treat like PFF):** On3 NIL valuations / Rivals are NOT a clean API → **do NOT gate the model on NIL $; ship on the FREE transfer/roster-continuity signal first**, add $-valuations only as an edge-gated later buy (state the source + cost if found). (3) land both as versioned lake tables (sport-tagged), point-in-time (as-of the season/week, leakage-safe — a team's transfer class is known pre-season). Ground-truth every "available" claim on a real pull (the P0.1 discipline; watch the CFBD 200-text/html gotcha).
GATE/AC: a free transfer/roster-continuity signal per team-season landed + validated; the NIL-$ availability/cost documented + explicitly deferred (not a Phase-0 dep); leakage-safe (as-of); feeds P1.2 (strength covariate) + P1.3 (features). Operator handoff + `git add`.
```
```
▶ NCAAF-P0.5 — COACHING-CHANGE DATA (HC free + OC/DC gated)   ✅✅ DONE — box-verified & merged to main 2026-07-19
✅ SHIPPED: `coaches` ingest (25th NCAAF source, CFBD `/coaches`, 1 call/season, Delta-partitioned, backfilled 2014–2025) → `stg_ncaaf_coaches` (exploded to coach-school-season grain, carries SP+ splits) → mart **`ncaaf_team_coaching_change`** (1,555 rows, season×team FBS, same returning-production spine as P0.4; box build 3 models + 10/10 tests green over real S3).
✅ THE SIGNAL P1.3 GETS (join season×team): HC identity + `hc_tenure_years` + `is_first_year_at_school`; `hc_change_from_prev` (NULL at 2014 floor); `hc_midseason_change` + `n_coaches_in_season` (coach-of-record = most games); ⭐ the coach's PRIOR SP+ track record (`hc_prior_sp_overall/offense/defense_avg` career-to-date + `hc_recent_sp_*` most-recent-prior + `hc_prior_seasons` + `is_first_time_hc`) — the differentiator vs a binary flag; `is_hc_history_censored` (2014 left-censor). Leakage-safe: identity/change/tenure known pre-season, prior-SP+ reads strictly `year < season`, 0 future-touch rows; ground-truthed (Rhule/Deion/Frost→Joseph).
✅ OC/DC DECISION: no free CFBD coordinator endpoint; editorial (FootballScoop/ESPN) or paid (SportsDataIO) only → **DEFERRED, gated like NIL-$** — revisit only if a Phase-1 ablation shows residual a coordinator change would explain beyond HC + returning-production + roster flux. Not a blocker.
🚩 **ROUTED TO P1.1 (now the highest-priority downstream item — from BOTH P0.4 and P0.5):** NO Dagster box-job exists for ANY NCAAF dbt model yet → `ncaaf_team_coaching_change` (and P0.4's `stg_ncaaf_returning_production`, its parent) are HAND-BUILT today and can silently rot. P1.1 wiring the full NCAAF staging+mart box-job resolves both. (Already flagged in P1.1's scope §39/§41 — this CONFIRMS it's the top P1.1 line.)
🧨 REUSABLE LANDMINES (added to the pile): `coaches` has NO coach ID → `coach_name` is the only cross-school key; build `delta_scan` staging **serially** (fusion-preview-196 segfaults on 2+ delta parents per invocation → `--select +model` can't auto-pull two delta parents); `rows`/`returning` are reserved words. CI green on main: `test_ncaaf_scaffold.py` 24 passed (registry 24→25); `dbt compile --target ci` 150/150.
🔓 UNBLOCKS P1.3 (coaching feature block). Independent of P0.6 / P1.1 / P1.2. (Orig prompt ↓.)
─────────────────────────────────────────
▶ NCAAF-P0.5 — COACHING-CHANGE DATA (HC free + OC/DC gated)   [Data · 🧭 OPUS · Phase 0/1 boundary · with P0.4]   (operator-scoped 2026-07-13)
🎯 WHY: a new HC/OC/DC can flip a team's scheme + scoring profile overnight (~63–71 new Power-4 coordinators in a single 2026 cycle) → the model needs a coaching-continuity signal.
DO: (1) **HEAD COACH — FREE via CFBD `/coaches`** (verify live, the P0.1 200-text/html discipline): coach↔school↔year history → **year-over-year HC-change flags per team** + ⭐ **the coach's PRIOR SP+ off/def profile** (the endpoint carries per-coach-year SP+ splits → a new HC's expected impact ≈ their track record, not just a binary flag). (2) **OC/DC (coordinators) — a GAP, NOT in CFBD → best-effort gated add:** the tracker is **FootballScoop** (ESPN/CBS also) — no free API ⇒ scrape their OC/DC change trackers OR maintain a small season table (SportsDataIO is a paid alt). Ship HC-continuity FREE first; OC/DC is a scraped/manual best-effort, gated like NIL-$. (3) land coaching tenure + change flags as a versioned team-season table (sport-tagged, point-in-time — a coaching hire is known pre-season, leakage-safe).
GATE/AC: HC change/tenure + prior-SP+-profile landed FREE from CFBD `/coaches` (verified live); OC/DC source + cost documented + shipped best-effort-or-deferred (not a blocker); leakage-safe; feeds P1.3. Operator handoff + `git add`.
```
```
▶ NCAAF-P0.6 — HISTORICAL NCAAF CLOSING-LINE ODDS (paid Odds-API `/historical`) — GATES all market/CLV/edge work   ✅✅ DONE — FULL 2020–2025 backfill complete & verified on the laptop 2026-07-19 (one follow-up commit pending on the open PR)
✅ SHIPPED + LANDED: `odds_ncaaf_historical` source (26th, `on_demand=True`) + `ncaaf/ingest/odds_backfill.py` (game-lines-only port of NFL-N0.4) + `ncaaf/ingest/verify_odds_historical.py` (re-runnable acceptance check, **exit 0/1 → CI/handoff-gateable**). Kickoffs read from CFBD `/games.startDate` (already UTC); leakage-safe snapshot at K−buffer with ±30min commence window + `_snapshot_ts`/`_requested_snapshot` stamps; below-floor (<2020) clean skip; `--dry-run` credit estimate (reads CFBD schedule, NO paid Odds call); default backfill EXCLUDES it (handler `_resolve_sources` drops on_demand → no accidental credit burn); **run on the LAPTOP not the box (I/O-bound, kept off live serving — baked into the runner docstring).** **BACKFILL DONE — SIX seasons 2020–2025** to `s3://credence-sports-lakehouse/ncaaf/raw/odds_ncaaf_historical/`: **10,394 rows / 5,531 distinct games.** FBS coverage by season: 2020 94.4% (COVID-short) · 2021 121.5% · 2022 128.8% · 2023 98.0% · 2024 100.5% · 2025 101.1% (>100% = the feed also prices some FCS games the FBS count omits). Markets: spreads/totals **100% every season**, h2h 95.5–99.6%. **27 US books incl. Bovada** (7,506 rows, the target book); Fanatics from 2025. **5,529 / 5,531 games retain a valid pre-kickoff close** (~74–76% of rows already pre-kickoff; the ~25% post-commence rows are redundant neighbour-window captures P1.4 filters via `snapshot_ts < commence_time`). The 2 exceptions (Texas A&M–UTSA, Georgia–Austin Peay, both 2025) = a CFBD-vs-OddsAPI kickoff disagreement (CFBD `startDate` off by 10/60 min → the ±30-min anchor missed true kickoff); 0.04% of games, P1.4 simply has no close for those two; +15 inert non-FBS orphans outside the modelling universe. Cost ≈ **100–120k Odds credits** (~16–20k/season — NCAAF has far more distinct kickoff windows than NFL's ~90); **4.83M credits remain.** ⚠️ **LANDMINES CAUGHT (both worth carrying — see the reusable note below): (1)** season defaults were **PINNED (2020–2024), not clock-derived → stale by a full season the day it merged** = the 2025 gap; now clock-derived via `last_completed_season()` (January handled conservatively so a default run never pulls an in-progress season). **(2)** `--skip-existing` silently PROTECTED a 3-event `--max-events 3` verify STUB in 2024 → caught only by the coverage check → re-pull the season WITHOUT the flag. ⇒ UNBLOCKS P1.4 vs-market leg + Phase 2.
🎯 WHY: P0.2's `odds_ncaaf` (936 rows) is a CURRENT-odds feed, NOT historical closing lines (mis-tagged `season=YYYY`, fetched live). ⚠️ **without real historical closing lines there is NO market benchmark + NO CLV** → this GATES P1.4's vs-market eval + all of Phase 2 (sharp-anchor/microstructure). Inventory table #21 `odds_ncaaf_historical` (paid Odds-API `/historical`) is the source, NOT built.
⭐ **REUSE THE NFL-N0.4 PROVEN PATTERN (just built this exact Odds-API `/historical` ingest for NFL):** copy `nfl/ingest/odds_backfill.py` — the **`--dry-run` free credit estimate** (reads schedules, no API calls, before any paid run), **on-demand gating** (paid per-event sources excluded from the default backfill → no accidental credit burn), the **closing snapshot at commence − buffer** + **below-floor season skip** (pre-floor seasons skip cleanly, no 422 storm). Same `sources.py` typed/JSON routing.
DO: build the `odds_ncaaf_historical` ingest (paid Odds-API `/historical` endpoint, existing sub — confirm credits; **game-line floor 2020, per P0.1 + N0.4-confirmed**; ⚠️ **props have a HARDER 2023 vendor floor if ever wanted, but NCAAF props are THIN → game lines h2h/spread/total only here**) → season-partitioned Delta on `credence-sports-lakehouse`. Capture CLOSING lines (h2h/spread/total, 11 books incl. Bovada) with commence-time → leakage-safe closing snapshot (enforce `snapshot_ts < commence_time`, the N0.4 belt-and-suspenders). Cost-aware (`--dry-run` first). ⚠️ do NOT conflate with the live `odds_ncaaf` feed — this is the backtest/CLV source.
GATE/AC: `odds_ncaaf_historical` closing lines landed (2020+, h2h/spread/total, commence-time for leakage-safe close), Delta on the lake, credits stated; ⇒ unblocks P1.4's market-relative eval + Phase 2. Operator handoff + `git add`. **SEQUENCE: before P1.4's vs-market leg / Phase 2 — NOT a blocker for P1.1–P1.3 (which train on game outcomes) or the feeder.**
```
```
▶ NCAAF-P0.6b — RECURRING 2026+ CLOSING-LINE CAPTURE (bridge the one-time backfill → an ongoing CLV feed)   [Data/Ops · 🧭 OPUS/SONNET · ⏰ TIME-SENSITIVE — NCAAF season opens Aug 2026]   (NEW 2026-07-19, from the P0.6 handoff)
🎯 WHY: P0.6 backfilled 2020–2025 closing lines. There's no recurring capture for 2026+ yet — which is EXPECTED, not a gap: **the 2026 season hasn't started, so there's nothing to capture.** This is a **build-AHEAD-of-kickoff** item — stand up the recurring closing-line capture BEFORE the Aug-2026 opener so the CLV benchmark keeps extending season-over-season (else, once 2026 games start completing, the P1.4/Phase-2 vs-market eval would be stuck on a frozen 2020–2025 window). Time-boxed by the Aug-2026 kickoff, not urgent today.
🧭 THE DECISION (make it explicitly, don't default): **(A) periodic `/historical` catch-up** — reuse `odds_backfill.py` on a weekly/nightly cron pulling the just-completed slate's closes (paid Odds credits, but cheap — ~16–20k/season amortized; exact, same leakage-safe K−buffer snapshot as the backfill) VS **(B) capture near-kickoff from the LIVE `odds_ncaaf` feed** — snapshot the live feed at commence−buffer + persist as the close (no extra Odds-historical credits, but needs a scheduler at each game's kickoff + careful leakage stamping). Recommend **(A) periodic `/historical` catch-up** as the low-risk default — it reuses the proven, verified P0.6 path + `verify_odds_historical.py` as the acceptance gate, and NCAAF's weekly cadence makes a once-weekly catch-up trivially cheap; only go (B) if the credit cost proves material.
DO: pick A or B (state the rationale); wire the recurring capture (EventBridge/cron per `sport_data_platform.md`) writing to the SAME `odds_ncaaf_historical` Delta partition (season=2026…); clock-derived season default (`last_completed_season()` — the P0.6 landmine, don't pin); on-demand/paid gating preserved (no accidental credit burn on a routine pull); `verify_odds_historical.py` runs post-capture as the gate. ⚠️ do NOT conflate with the live `odds_ncaaf` current-odds feed — this persists the leakage-safe CLOSE.
GATE/AC: a recurring 2026+ closing-line capture live (A or B, rationale stated), writing leakage-safe closes to the `odds_ncaaf_historical` Delta, `verify_odds_historical.py` green on the first captured week, clock-derived season default, credit cost stated; ⇒ the CLV feed stays fresh into the 2026 season. Operator handoff + `git add`. **SEQUENCE: before the 2026 season opens (Aug 2026); independent of P1.1–P1.4.**
```
```
▶ NCAAF-P1.2b — FRESHMAN-PRODUCTION PROJECTION (recruit → college; the HS→college MLE)   [Model · 🧭 OPUS · Phase 1 · feeds P1.3]   (operator-scoped 2026-07-13)
🎯 WHY (operator): we must project TRUE FRESHMEN (no college snaps) somehow → use their recruiting rating as the prior. This is the recruiting-level analog of the college→NFL feeder (E7 pattern, one level down).
Read: MLB Edge **E7** (the MiLB→MLB MLE — the direct analog) + the P1.1 marts + `ncaaf_data_inventory.md` (recruiting endpoints).
DO: recruiting is FREE (CFBD `/recruiting/players` — composite/stars/position/school). Fit **recruit-rating → early-college production** on historical recruit↔production pairs (assembled from P1.1: a recruit's rating vs their fresh/soph output), position-specific, EB-shrunk for thin cells → a **freshman-production prior** (mean + uncertainty) for players with no college history. Leakage-safe (recruiting rating is pre-arrival). Honest: a prior, not a projection-of-edge.
GATE/AC: a position-specific recruit→production prior (mean + uncertainty), validated on a holdout recruit class; feeds P1.3 as the true-freshman feature. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.1 — ANALYTIC DATA MODELING   ✅ DONE 2026-07-20 (full build verified over the real S3 lake; box runtime gate pending operator)
📦 SHIPPED in `sports_dbt`: **6 new staging** (teams / game_team_stats / game_player_stats / drives / plays / game_advanced) → **4 DIMS** (`dim_ncaaf_conference`, SCD-2 `dim_ncaaf_team`, SCD-2 `dim_ncaaf_player`, `dim_ncaaf_game`) → **4 FACTS** (`fact_ncaaf_team_game` 18,124 · `fact_ncaaf_player_game` 539k · `fact_ncaaf_drive` 214k · `fact_ncaaf_play` 1.55M) → **3 ROLLUPS** (season, ⭐as-of-week point-in-time, ⭐opponent-adjusted). All FBS-filtered + sport-tagged. Full doc: **`ncaaf_mart_inventory.md`** (new).
🚨 **THE HEADLINE FINDING — a LIVE leak caught and fixed mid-story: CFBD RESTARTS `week` AT 1 FOR THE POSTSEASON.** Every bowl/CFP game carries `week=1` but is played in Dec/Jan, so ordering a season by raw `week` puts the national championship BEFORE regular-season week 2. Measured: 2024 Ohio State had **FIVE** games at `week<=1` (opener + 4 playoff games) and every as-of-week row from wk2 onward was silently absorbing them. **CURE = `season_order_week`** on `dim_ncaaf_game` (regular weeks as-is, postseason offset past them; monotone in game_date) — **the ONLY column that may order or window a season; raw `week` is reporting-only.** ⚠️ **AND THE LESSON FOR EVERY FUTURE POINT-IN-TIME MART:** the obvious leakage test (recompute with `week < W` and compare) is WORTHLESS — it re-uses the model's own ordering, so a wrong ORDERING still satisfies a right FILTER and the test passes GREEN. It did. The shipped gate `assert_asof_week_has_no_future_games` is therefore **DATE-based** (every contributing game must predate its own week's first kickoff), which cannot be fooled by an ordering bug.
🔒 3 singular leakage gates ship + run in the box job: `assert_asof_week_has_no_future_games` (date-based) · `assert_opponent_adjustment_is_point_in_time` (each opponent's rating read at the SAME as_of_week, not season-final — the classic opponent-adjustment leak) · `assert_season_order_week_is_monotone_in_date` (root-cause guard).
⭐ SCD-2 findings: realignment verified (Texas/Oklahoma Big12→SEC 2024, UCLA→Big Ten, BYU→Big12 2023, Cincinnati). **Idaho correctly has NO `is_current` row** (left FBS after 2017) — don't "fix" it. `dim_ncaaf_player` payload = team+position, **class_year EXCLUDED**: it advances every season by construction and produced 20,672 of 30,433 version breaks vs 9,761 real ones (2:1 noise).
⭐ Opponent adjustment sanity: 2-pass; corr(raw,adj)=0.95, mean shift 0.027 vs raw SD 0.107. 2024 season-end adjusted top-5 = **Ohio State, Notre Dame, Texas, Oregon, Ole Miss** — the actual CFP result, from a model that never sees a ranking. 2024 Indiana/Army (great record, soft schedule) correctly marked DOWN.
🏗️ **BOX JOB WIRED (the story's top line, routed here by BOTH P0.4 and P0.5):** `sports_ncaaf_dbt_build_job` builds the **WHOLE** NCAAF DAG incl. P0.4's `ncaaf_team_roster_continuity` + P0.5's `ncaaf_team_coaching_change` (no more hand-built marts silently rotting). Staging built **SERIALLY** — the P0.5 fusion-segfault landmine AND a measured OOM. `dbt run` = **HALT**, `dbt test` = **WARN-continue** (CLAUDE.md INC-6). First NCAAF CI slice added to `sports_dbt_ci.yml` incl. a non-empty-selector assertion (a selector matching nothing still exits 0 in dbt).
🧯 LANDMINES FOR DOWNSTREAM: (1) **memory** — `stg_ncaaf_game_player_stats` (4 chained UNNESTs → 5.2M rows) and `fact_ncaaf_play` pin `threads=1` + `preserve_insertion_order=false`; both OOM a 4 GB DuckDB otherwise. **Do NOT remove the pins when the box gets more RAM** — amplification scales with seasons. `stg_ncaaf_game_advanced` extracts the offense/defense sub-objects ONCE per row (100 full-document `json_extract_string` paths OOM'd 4 GB on a 25k-row table). (2) **NULL is unknown, keep it NULL** — wk-1 as-of rows, teams with no `/plays` coverage (2014 Washington wk1 = 0 plays), first-time HCs. (3) NCAAF staging default flipped to **table** (delta_scan-stacking cure, N0.3 pattern).
⚠️ **PRE-EXISTING P0.3 failure surfaced + RATCHETED (not waived):** `not_null_xref_college_nfl_players_gsis_id` was failing on 8 of 4,211 rows — drafted players who never took an NFL snap have no nflverse `gsis_id` (e.g. Bud Sasser, 2015 rd6). Those are LEGITIMATE nulls; P0.3's `not_null` was wrong about its own data. Now `warn_if:">0"` + `error_if:">8"` — **the 8 known warn, a 9th FAILS THE BUILD**, so the count can only drift DOWN silently and any growth (a slot-match regression, or a fresh draft class not yet on the field) surfaces. Verified both directions (8→WARN; simulated threshold 7→ERROR). ⚠️ Raise the threshold DELIBERATELY with a reason if it legitimately changes — don't delete the test or bump it reflexively.
🔒 **TEST TIERING (refined):** `dbt run` HALT → **the 3 leakage gates HALT** → the rest of `dbt test` WARN-continue. Leakage is carved OUT of the INC-6 WARN default on purpose — a warning nobody reads is exactly how the postseason week-1 collision survived into a shipped model.
⏭️ UNBLOCKS P1.2 (strength) · P1.2b (freshman priors) · P1.3 (features) · P1A (feeder). (Orig prompt ↓.)

▶ NCAAF-P1.1 — ANALYTIC DATA MODELING (the supportable dimensional structure — BEFORE features)   [Data/dbt · 🧭 OPUS · Phase 1 · the "don't become a mess" story]   (operator-scoped 2026-07-13)
🎯 GOAL: turn the raw lake (P0.2) into a PROPER conformed dimensional model so the structure is supportable + doesn't rot. Do this BEFORE feature engineering — features build on these marts.
Read: `ncaaf_data_inventory.md` §8 (the 24 lake tables) + MLB's `baseball_data_mart_inventory.md` + `scd2_convention.md` (the modeling conventions to mirror) + the `dbt-duckdb` new-sports project (per `sport_data_platform.md` cross-sport decisions — SEPARATE from MLB's Snowflake dbt).
🚩 **ABSORBS the P0.2 flags:** (a) ⚠️ **`games` lands ALL divisions → APPLY the `is_fbs_matchup` FBS filter** in the dims/facts (or downstream pulls in FCS/D-II junk); (b) ✅ **dbt-duckdb is now ON the box image + `sports_dbt_ci.yml` EXISTS (done by NFL-N0.3) → EXTEND that CI + the box job for the NCAAF models, don't stand them up** (the cross-sport infra is shared — mirror N0.3's NFL slice: `sports_nfl_dbt_build_job` + the offline parse+compile CI). ⚠️ baked-image-drift lesson from N0.3: any new dbt dep needs adding in 3 places (pyproject/lock + box Dockerfile pip list + `up -d --build`). ⚠️ **P0.4 flag: NO Dagster job exists for ANY NCAAF dbt model yet → this story wires the box job for ALL of them (P0.4's `ncaaf_team_roster_continuity` + the new dims/facts) so nothing rebuilds manually / silently rots.** 🧰 **P0.4 landmine: dbt-fusion preview-196 SEGFAULTS building 2+ delta-scan models in one invocation → build staging SERIALLY** (until a fusion upgrade).
DO: build the NCAAF marts in the new-sports `quant_sports_intel_models/sports_dbt/` project: **DIMS** `dim_team` / `dim_player` / `dim_game` / `dim_conference` (+ SCD-2 where an attribute drifts, e.g. conference realignment — the MLB `scd2_convention` pattern); **FACTS** `fact_team_game` / `fact_player_game` / `fact_drive` / `fact_play`; **ROLLUPS** season + week point-in-time (as-of-week, **leakage-safe** — never fold post-kickoff data into a pregame row) + **opponent-adjusted** efficiency (adjust raw for schedule strength). ⭐ every row **SPORT-TAGGED** + **FBS-filtered** (`is_fbs_matchup`). Grain + partition + cadence per inventory §8; Delta-native; read the lake via the `ncaaf_delta()` macro (P0.2).
🚩 **THE BOX-JOB IS THE TOP LINE OF THIS STORY (P0.4 AND P0.5 both routed it here):** NO Dagster box-job exists for ANY NCAAF dbt model → today `stg_ncaaf_returning_production` (P0.4) + `ncaaf_team_coaching_change` (P0.5) are HAND-BUILT on the laptop and can silently rot. The box job MUST materialize the FULL NCAAF staging+mart DAG (these two included), not just the new dims/facts. Build serially where delta parents are involved (P0.5 landmine: fusion-preview-196 segfaults on 2+ `delta_scan` per invocation → don't rely on `--select +model` to auto-pull two delta parents).
GATE/AC: the conformed dims+facts+rollups build in `sports_dbt` over the S3 lake, FBS-filtered + sport-tagged; point-in-time correctness proven (as-of-week row has NO post-kickoff info — leakage spot-check); opponent-adjustment applied; **the dbt build wired into a box Dagster job (dbt-duckdb on the box) covering the WHOLE NCAAF DAG incl. P0.4's `stg_ncaaf_returning_production` + P0.5's `ncaaf_team_coaching_change` + the first NCAAF CI gate stood up**; a real box build (runtime gate). Operator handoff + `git add`.
```
```
▶ NCAAF-P1.2 — CONFERENCE / TEAM-STRENGTH MIXED-EFFECTS MODEL (partial pooling)   [Model · 🧭 OPUS · Phase 1 · produces the strength feature]   (operator-scoped 2026-07-13)
🎯 WHY (operator): CFB has HUGE talent disparity + a SPARSE cross-conference schedule → raw records aren't comparable across conferences. A **hierarchical partial-pooling** model (team nested in conference) is the right tool — it shrinks thin-sample teams toward their conference mean + yields opponent-adjusted strength with honest uncertainty.
Read: MLB's Elo / sequential-posterior state models (the analog state-estimator pattern) + `ncaaf_data_inventory.md` + the P1.1 marts.
DO: fit a mixed-effects / hierarchical model — **random effect: team, nested in conference**; response = margin (and/or points-for/against) per game; covariates = home, rest, and the **P0.4 roster-continuity/NIL-flux signal** (so a gutted or portal-loaded roster moves the estimate) + recruiting/talent. Emit a per-team **strength estimate + its posterior uncertainty**, week-by-week, point-in-time (leakage-safe). This is a STRUCTURAL estimator (not a bake-off) — but the FEATURE it produces gets ablation-tested in P1.4. Honest: it's a strength PRIOR, not an edge claim.
GATE/AC: a week-by-week team-strength posterior (mean + uncertainty), partial-pooled by conference, leakage-safe; the NIL/transfer covariate materially moves plausibly-affected teams (a sanity check); feeds P1.3 as a feature. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.3 — FEATURE ENGINEERING (the broad pregame feature library — BEFORE model dev)   [Model/Data · 🧭 OPUS · Phase 1 · operator-scoped: "a good number of features ready"]   (2026-07-13)
🎯 GOAL: assemble a BROAD, leakage-safe, point-in-time pregame feature matrix so the P1.4 bake-off isn't feature-starved. Build on P1.1 marts + P1.2 strength + P0.4 NIL/transfer.
DO: engineer the pregame (as-of-kickoff) feature set — **team strength** (P1.2 estimate + uncertainty) · **efficiency** (EPA/play, success rate, explosiveness, finishing — offense/defense, opponent-adjusted) · **pace/style** (plays/game, seconds/play, pass rate, tempo) · **line/trench** (OL unit line-yards/stuff/sack-rate-allowed; DL havoc/pressure — the UNIT proxies, individual-OL is the confirmed gap) · **roster** (returning production, P0.4 portal/NIL flux, recruiting/talent composite, **P1.2b freshman priors** for true-freshman contributors) · **coaching** (P0.5 new-HC/OC/DC flags, first-year-at-school, + the new coach's prior SP+ off/def profile) · **situational** (home/away, rest days, travel distance, altitude, week, is-rivalry, is-conference) · **QB** (the pivotal position — starter continuity, EPA, injury/change flags). Leakage-safe (as-of-kickoff ONLY — never post-game), one-pull-to-parquet (the MLB cost-hygiene rule — assemble ONCE, cache, every downstream reads the cached parquet). Document each feature's source + grain + as-of semantics.
GATE/AC: a leakage-safe pregame feature matrix (documented; as-of-kickoff proven — no post-game leak) with a good breadth across the families above; cached to parquet for the bake-off; sport-tagged. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.4 — THE GAME-MODEL BAKE-OFF (3 TARGETS: H2H moneyline · spread/margin · total points)   [Model · 🧭 OPUS · Phase 1 · the §0.5 discipline, full deflation]   (operator-scoped 2026-07-13)
🎯 GOAL: the NCAAF game model, built with the SAME MLB bake-off discipline (§0.5) — NOT a single architecture. 3 markets: H2H (moneyline/win-prob), spread (margin), total (points).
Read: MLB §0.5 (the bake-off + deflation discipline) + E2 (the per-side JOINT distributional pattern) + the P1.3 feature parquet.
DO: ⭐ **DESIGN LEAN — model the JOINT scoring distribution ONCE, DERIVE all 3 markets** (predict the margin+total [or home/away points] distribution → H2H = P(margin>0), spread = P(margin>line), total = P(total>line)) — mirrors MLB E2 per-side→derive-everything, keeps the sides↔totals correlation honest, and is more coherent than 3 unlinked target models. The bake-off decides joint-vs-separate on the metric. **§0.5 DISCIPLINE (non-negotiable):** pre-register ≥3 candidate model classes per the target, Optuna-tune each, feature-ablate, pick on the metric under **purged/embargoed CV** (CFB weeks = the time axis), guard with **PBO<0.2 / DSR≥0.95 / FDR** counting every config. 🔒 **HONEST FRAME:** `best_alpha=0` until it clears the gate + a positive forward-CLV window (the MLB standard — no win-rate/edge claim on an unproven model; CLV is the leading indicator). A clean null is a valid deliverable. Calibration (PIT/reliability) is product value regardless of edge.
GATE/AC: the pre-registered ≥3-class bake-off per market (or the joint model + derivation), Optuna + ablation, on purged/embargoed CV, PBO<0.2/DSR≥0.95/FDR; calibrated 3-market probabilities (PIT-flat); honest framing (`best_alpha=0` unless the gate clears); the winner + a trustworthy null both acceptable outcomes. Operator handoff + `git add`.
```
```
▶ NCAAF-P1A — COLLEGE→NFL TRANSLATION MODEL (the NFL feeder — E7 MiLB→MLB analog)   [Model · 🧭 OPUS · Phase 1 · ⭐ the HIGHEST near-term ROI · parallel track to the P1B game-model chain]   (operator-scoped 2026-07-13)
✅ **UNBLOCKED — P0.3 DONE 2026-07-16, the xref spine is LIVE.** Read it FIRST: the mart is at `s3://credence-sports-lakehouse/ncaaf/marts/xref_college_nfl_players` (4,211 rows; box-verified) + `football/ncaaf/feeder/xref.py`/`README.md`. **START FROM THE XREF — don't rebuild the join.** Key facts P1A inherits: (1) ⭐ **TARGET already in the mart, `target_`-prefixed** (`target_car_av`/`target_w_av`/`target_games`/…) = the P1A modeling target; **NEVER use a `target_*` col as a FEATURE (post-draft = leakage)** — features are college-production + combine ONLY. (2) match provenance is stamped — `match_method` ∈ {`deterministic_slot` (2,796 drafted, high conf), `fuzzy_udfa` (1,390 UDFAs, med/low)}; **weight/segment by confidence** (UDFAs have NULL `target_*` = no draft outcome row → they're for the roster-continuity side, not the drafted-outcome training). (3) training-set size: ~2,796 drafted players 2015–25 resolve to `gsis_id` (99.11%); **combine attach 81.8%, `has_forty` 65.6%** → combine features are PARTIALLY present (impute/flag-missing, don't drop). (4) ⚠️ **P0.1 COLUMN-NAME CORRECTION (cost P0.3 a box bug — do NOT re-hit):** the real `nflverse_players` cols are **`rookie_season`/`draft_pick`/`draft_round`**, NOT the `entry_year`/`draft_number` the P0.1 notes implied. (5) ⚠️ **NaN hygiene: nflverse combine floats are `'NaN'` strings that `try_cast(... as double)` turns into a NON-NULL NaN** → use the P0.3 `_num()` helper (NaN→NULL) or `has_forty`-type counts over-report. (6) box discipline: build with `docker compose run --rm` + bind-mount (the P0.3 pattern) so you don't restart the live Dagster stack mid-E11.20-gate.
🎯 WHY: NCAAF's real near-term value is as the NFL FEEDER — college production + combine → **NFL rookie projections** that power the NFL vertical (fantasy-dynasty boards + rookie props, where the market is otherwise priors-only). This is the football analog of MLB's MiLB→MLB MLEs (Edge **E7**). Runs in PARALLEL with the P1B game-model chain (different consumer: NFL, not NCAAF betting).
🔒 PREREQS: **P0.3 (the draft-slot xref spine — SOLVED, 99.7%)** + P1.1 marts (college production) + P0.3's attached combine measurables. The TARGET is already in `nflverse draft_picks` (`car_av`/`w_av`/`games`/`seasons_started`/career lines — P0.1 §4) — no extra source.
Read: MLB Edge **E7** (the MiLB→MLB MLE — the DIRECT analog to mirror) + `ncaaf_data_inventory.md` §4 (the draft-slot spine + the combine attach) + the P1.1 marts + `../../multi_sport_roadmap.md` §4 + the NFL guide (the consumer).
DO: assemble per-drafted-player features = **college production** (final 1–2 seasons of opponent-adjusted efficiency/usage from P1.1, position-specific) + **combine/measurables** (P0.3 attach) + recruiting pedigree → keyed on the **draft-slot xref** to the NFL `gsis_id`; TARGET = NFL early-career production (from `nflverse draft_picks`). Fit a **position-specific translation** (the E7 pattern), **leakage-safe** (ONLY pre-draft college/combine data → predict the pro outcome — never fold in NFL production), **EB-shrunk** for thin position×archetype cells. Handle: **UDFAs** (no draft slot → the P0.3 fuzzy-match residual, lower-confidence) + the **2014 floor** (~12 draft classes = the training ceiling, per P0.1 §2). §0.5 bake-off discipline for the architecture choice (≥ a couple candidate classes, cross-validated by draft-class, honest). Emit **rookie projections + uncertainty**, position-specific. 🔒 HONEST: a projection/PRIOR (esp. valuable where the NFL market is priors-only), NOT an edge claim.
GATE/AC: a position-specific college→NFL translation validated on a HELD-OUT draft class (leakage-safe — no pro data in the features; predicts the holdout's early-career production with honest uncertainty); UDFA + 2014-floor handled; rookie projections + uncertainty emitted, keyed to `gsis_id`, ready for the NFL vertical (fantasy-dynasty + rookie props). Operator handoff + `git add`.
```
```
▶ NCAAF-Phase-2 (gated edge) — SPEC LATER (post-kickoff): re-point the hardened MLB instruments (E13.16 microstructure/CLV, E1.12 regime-conditioning, E13.14 cross-market) at the SOFTER college markets under PBO<0.2/DSR≥0.95/FDR + game-level correlated-quote collapse. **Props are THIN (P0.1) → sides/totals/microstructure ONLY, not an MLB-style prop engine.** Written from the P1.4 handoff once the game model + market coverage are known (mirror MLB's one-fresh-session-per-story flow).
```

---
_Created 2026-07-08. Phase 0 is data-first and re-verifies sources against live endpoints. Data-source facts (CFBD tiers, PFF-paid charting) current as of 2026-07-08._
