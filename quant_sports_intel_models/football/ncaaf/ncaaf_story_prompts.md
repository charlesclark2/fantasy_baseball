# NCAAF — Story Prompts (per-sport catalog)

**Parent roadmap:** `ncaaf_roadmap.md` · **Platform:** `../../sport_data_platform.md` · **Reference impl:** MLB `../../baseball/edge_program/` (esp. Edge **E7** MiLB→MLB for the feeder).
**Global conventions (inherit from MLB §0):** `dbtf` not `dbt`; `uv run python`; Snowflake via MCP fully-qualified, no `USE` (but NCAAF is DuckDB/S3-native — Snowflake only if a port-up earns it); **do NOT `git commit`/`push`** (operator does git — provide a copy-pasteable `git add <paths>` at closeout); operator provisions all secrets/API keys (never enter credentials); hand >1-min scripts to the operator with **laptop-vs-box + the full copy-pasteable command**.
**⚠️ Serving = DynamoDB→S3 (NOT Railway — decommissioned INC-16); the platform doc's "Railway PG" line is stale.**

---
```
✅ DONE 2026-07-13 — NCAAF-P0.1 delivered `ncaaf_data_inventory.md` (the master data file; 42 live CFBD v2 endpoints probed,
   Odds API + nflverse ground-truthed). HEADLINES: buy CFBD Patreon Tier 3 ($10/mo, 75k calls — free tier's 1,000/mo cap
   confirmed live; backfill needs ~15.8k calls). Backfill window = 2014–2025 (player-advanced is ZERO before 2013/14).
   NFL-feeder spine SOLVED: CFBD⇄nflverse share NO player ID, but the draft slot (season, overall pick) matches 99.7%.
   OL-individual gap CONFIRMED; aDOT/CPOE/snap-counts are NOT free. PFF = a licensing project, not a $120 buy → deferred.
▶ NCAAF-P0.1 — DATA-SOURCE EVALUATION + `ncaaf_data_inventory.md` (START HERE — data-first)   [Data · 🧭 OPUS · Phase 0]
🎯 GOAL: lock the viable data sources + produce the master data file. The MINIMUM bar is full BOX SCORES (team + per-player game logs); the REACH is position-level ADVANCED metrics for QB / RB / WR / TE / OL / DEF. Ground every claim on the LIVE CFBD v2 API — do not assume.
Read: `ncaaf_roadmap.md` §2 (the exact by-position wishlist + the ✅Tier-A / 💰PFF / ⛔gap map) + `../../sport_data_platform.md` + `../../multi_sport_roadmap.md` §4 (the NFL feeder).
DO:
 1. **CFBD (CollegeFootballData) — the free/near-free backbone:** enumerate the v2 endpoints that actually deliver, per the wishlist: box scores (team + player game stats), PLAY-BY-PLAY (the raw material for EPA/success/explosiveness/usage we DERIVE ourselves), advanced TEAM stats (SP+, havoc, line yards, stuff/sack rate), rosters, recruiting/talent, results/schedule, drives, win-prob. ⚠️ **the FREE tier is ~1,000 calls/mo — almost certainly too tight for a multi-season backfill → price the Patreon Tier-3 (~$10/mo, 75k calls + GraphQL + live) and make the buy recommendation** (this is the one likely Phase-0 cost; flag it for the operator to provision the key).
 2. **The Odds API (existing sub):** confirm NCAAF game lines + totals + spreads + props + scores coverage + the historical depth (for CLV/backtests later).
 3. **Draft/combine data** (for the NFL feeder spine): identify the source (CFBD has some; combine data via nflverse/`nfl_data_py` or a public combine set) — needed for the college→NFL translation.
 4. **The PFF/SIS (paid-charting) DECISION:** map EXACTLY which wishlist items are ONLY available paid — the individual GRADES: OL pass-block/run-block + pressures-allowed, DB coverage grade / completion & passer-rating allowed, WR separation / YPRR / contested-catch, RB yards-after-contact / forced-missed-tackles, QB CPOE / air-yards / time-to-throw. **RECOMMEND: ship Tier-A + PBP-derived proxies FIRST; treat PFF College as a LATER edge-gated buy, not a Phase-0 cost** (state the price if findable + what it would unlock). ⛔ **Call out the OL-individual gap explicitly: there is NO free individual-offensive-line production — model OL at the UNIT level from Tier-A team line-metrics; individual OL is PFF-only.**
 5. **WRITE `ncaaf_data_inventory.md`** — the master file: per source → endpoint/dataset → fields → grain (team/player/play) → season coverage → the by-position ✅/💰/⛔ map from roadmap §2, resolved against what's ACTUALLY live → the recommended Phase-0 source set + the cost line + the identified gaps.
GATE/AC: `ncaaf_data_inventory.md` exists + is ground-truthed against live endpoints (a real sample pull per key source, not doc-reading); the by-position coverage map is resolved (every wishlist item marked available-free / paid / gap with the source named); the CFBD tier + PFF buy recommendations are explicit with costs; the NFL-feeder data (draft/combine + the ID-xref source) is identified. ⇒ P0.2 (scaffold) + P0.3 (xref) can start from a locked source set. Operator handoff: which API keys to provision + `git add`.
```
```
✅ DONE 2026-07-15 — NCAAF-P0.2 stood up `football/ncaaf/ingest/` (Delta-native, sport-agnostic scaffold) + the SHARED
   new-sports `sports_dbt/` dbt-duckdb project (separate from MLB's Snowflake dbt, per the cross-sport decision).
   LIVE runtime-gate PASSED off-box (laptop): CFBD /games (231, 85 FBS) + Odds API NCAAF (2,640 outcomes / 11 books incl.
   Bovada) + nflverse draft_picks (257) → local Delta → dbt-duckdb `stg_ncaaf_games`/`stg_ncaaf_odds` build + tests GREEN.
   Registry covers all 24 §8 tables; P0.1 landmines encoded (CFBD 200-text/html JSON guard, /plays/stats per-gameId 2000-cap,
   year-only no-team-loop, box/advanced id=, nflverse-as-parquet). 16 fast-gate tests. ⏳ OPERATOR: create the
   `credence-sports-lakehouse` S3 bucket + instance-role grant, provision the CFBD Tier-3 key, then run the S3 backfill
   (same code path, proven local). Serving = DynamoDB→S3. See `football/ncaaf/ingest/README.md`.
✅ DONE 2026-07-16 (box-verified, merged) — the NCAAF lean lakehouse scaffold is LIVE: `football/ncaaf/ingest/` (CFBD/Odds/nflverse → season-partitioned **Delta** on `s3://credence-sports-lakehouse/ncaaf/raw/<source>/season=YYYY/`) + a NEW separate `quant_sports_intel_models/sports_dbt/` dbt-duckdb project (disjoint from MLB's Snowflake dbt) with `stg_ncaaf_games`/`stg_ncaaf_odds`; full 2014–2025 backfill landed + read-verified (games/play_stats 12/12 seasons). Cost = **$10/mo CFBD Tier-3** (the one recurring spend). Infra durable: bucket `credence-sports-lakehouse` (us-east-2) + IAM (box RW, Lambda RO); Tier-3 key in the box `.env` only.
🚩 **DOWNSTREAM FLAGS (P0.3 / P1.1+ MUST heed — from the P0.2 recap):**
 • ⚠️ **`odds_ncaaf` is CURRENT odds, NOT historical closing lines** (936 rows, fetched live, mis-tagged `season=YYYY`). Real historical NCAAF closing lines = inventory #21 `odds_ncaaf_historical` (paid Odds-API `/historical`) — **NOT built → a NEW story (P0.6) gating any NCAAF market-benchmark / CLV / edge work** (incl. P1.4's vs-market eval + all of Phase 2). Do NOT treat the 936 current rows as closing lines.
 • ⚠️ **`games` lands ALL divisions** (1,586–3,831/season); FBS-vs-FBS ≈ 760–808. **FBS filter is in dbt (`is_fbs_matchup`), NOT at ingest → every downstream model MUST apply it** or it pulls in FCS/D-II junk. (Only `play_stats` is FBS-gated at ingest, for CFBD budget.)
 • **Backfills run ON THE BOX, not the laptop** (in-region multipart PUT + instance-role write; laptop IAM is RO). Reuse the single-client + adaptive-throttle path (`CFBD_THROTTLE_SECONDS`, ×1.5 on 429) + `--skip-existing`; never hand-roll a client per season. Boto3/delta-rs: reuse `s3io.storage_options()` (the AKID cure) — never `aws_access_key_id=os.environ.get(...)`.
 • **dbt-duckdb is NOT on the box yet** (`sports_dbt` runs on the laptop) + **`sports_dbt` has NO CI** → **P1.1 must budget for: wiring the NCAAF dbt build into a Dagster job on the box + standing up the first NCAAF CI gate** (the MLB gates don't cover it).
 • CFBD quirks are encoded (`cfbd_client` asserts `application/json` so a wrong-path 200-HTML RAISES); still, each of the ~21 non-smoked fetchers gets a live ground-truth pull the FIRST time it's consumed (P0.1 discipline).
✅ UNBLOCKED: P0.3 (xref) + P1.1 (dims/facts — absorbs the box-dbt wiring + first CI gate). (Orig prompt ↓.)
▶ NCAAF-P0.2 — LEAN LAKEHOUSE SCAFFOLD (instantiate `sport_data_platform.md`)   [Data/Infra · 🧭 OPUS · Phase 0 · after P0.1 locks the sources]
🎯 GOAL: stand up the `ncaaf/` ingest→S3→dbt-duckdb pipeline per the SHARED platform pattern — do NOT reinvent it; copy MLB's proven lakehouse-write patterns. Weekly-batch, cheap.
Read: `../../sport_data_platform.md` (esp. §1 pattern + §2 repo layout) + `ncaaf_data_inventory.md` (P0.1) + MLB's `scripts/utils/lakehouse_raw_writer.py` + `run_w1_lakehouse` pattern (the DuckDB-over-S3 build) + ⚠️ **the E11.20 Delta/Polars patterns (NCAAF should be Delta-native from day one — inherit, don't rebuild raw parquet).**
DO: build `ncaaf/ingest/` per §2 — `s3io.py` (DataFrame→partitioned Parquet/**Delta** in S3; reuse MLB's), `sources.py` (the CFBD/Odds registry: source→fetch fn→season col→table), `handler.py` (Lambda entrypoint), `backfill.py` (off-Lambda full-history runner), `Dockerfile`. Land raw → `s3://<bucket>/ncaaf/raw/<source>/season=YYYY/…`; a first `dbt-duckdb` staging layer. **Box-safe:** instance-role S3 auth (never inline keys — the AKID landmine); DuckDB region + memory box-aware; TZ = the game-day helper class. Serving target = DynamoDB→S3 (NOT Railway).
⚠️ **P0.1 LANDMINES — do NOT rediscover these (all in `ncaaf_data_inventory.md` §1):**
 • **A WRONG CFBD PATH RETURNS `200 text/html` (the Swagger page), NOT a 404** — the v1-style singular paths (`/play/stats`) silently serve an HTML doc bundle. **Every fetcher must assert `Content-Type: application/json` + that the body parses — status 200 is NOT a success signal.** v2 paths are PLURAL (`/plays/stats`).
 • **`/plays/stats` is hard-capped at 2,000 rows/response ⇒ MUST be pulled per `gameId`** (~960 calls/season — the dominant cost). **`/plays` REQUIRES `week`.** But `/roster`, `/player/usage`, `/stats/player/season`, `/ppa/players/season`, `/recruiting/players`, `/player/returning` all accept **year-only — do NOT loop 136 teams (a 136× budget trap).** `/game/box/advanced` takes `id=`, not `gameId=`.
 • **`nfl_data_py` IS ABANDONED** — it pins `pandas==1.5.3`, which **will not build on py3.12**. `sport_data_platform.md §4/§10` still recommends it; **that guidance is STALE.** Read the **nflverse release Parquet directly** via DuckDB `read_parquet('https://github.com/nflverse/nflverse-data/releases/download/<asset>/<asset>.parquet')` — dependency-free and lakehouse-native.
 • The **24 locked lake tables + partitioning + cadence are already specified** in `ncaaf_data_inventory.md` **§8** — build straight from that table. Backfill window **2014–2025** (§2.7). Requires the **Tier-3 key** (§6) before the backfill runs.
GATE/AC: a live ingest of ≥1 CFBD source + Odds API NCAAF → S3 Parquet/Delta (a real Lambda or off-box run, per the runtime gate — CI can't see it); a `dbt-duckdb` staging model reads it; the backfill runner does a multi-season pull; repo layout matches `sport_data_platform.md §2`; costs stated. Operator handoff: box-vs-laptop run-order + `git add`.
```
```
✅ DONE 2026-07-16 — NCAAF-P0.3 built the college↔NFL player-ID xref (the NFL-feeder spine; MLB Edge-E7 analog, feeds P1A).
   KEY = the DRAFT SLOT, not an ID: `CFBD /draft/picks (year, overall)` ⇄ `nflverse draft_picks (season, pick)` (99.7% →
   gsis_id); combine attaches nflverse-internally on the cfb_player_id slug. Deliverable = a Python DuckDB-over-Delta builder
   `football/ncaaf/feeder/xref.py` (mirrors MLB run_w1_lakehouse; PARITY — same code on the S3 box + local Delta fixtures) that
   lands `ncaaf/marts/xref_college_nfl_players` as a versioned Delta mart, + `feeder/name_norm.py` (shared suffix/apostrophe/
   accent normalisation, Python+SQL, parity-tested) + dbt views (`stg_ncaaf_cfbd_draft_picks`, `stg_nflverse_draft_picks`, mart
   VIEW `xref_college_nfl_players` + schema tests). match_method source-stamped: `deterministic_slot` (high, score 1.0) |
   `fuzzy_udfa` (medium/low, Jaro-Winkler name+school+position). Residuals handled: UDFAs (fuzzy nflverse players⇄CFBD roster),
   transfers (slot is school-agnostic; collegeAthleteId stable across schools), name-norm (the 2–8% surname disagreement).
   ⚠️ `target_*` (car_av/w_av/games/…) are the P1A TARGET (POST-draft) — prefixed so they can't be fed as features (leakage-safe).
   ANTI-CARTESIAN: SQL joins are immune to the pandas NaN-to-NaN trap, but the builder STILL drops null keys + dedups 1-per-key +
   ASSERTS row counts after every join + raises `XrefValidationError` on inflation/dup-gsis. Offline session (no AWS/GitHub net) →
   validated on local Delta fixtures: `betting_ml/tests/test_ncaaf_xref.py` (14 fast-gate tests, 0.8s) proves slot baseline 100%
   reproduced + no cartesian + NaN-slug no-multiply + Jr.-surname agrees + UDFA resolve/exclude-drafted/no-false-match + transfer
   stable id + unique gsis_id; the full dbt build+tests over the fixture are GREEN (18 nodes).
   ⏳ OPERATOR (the box runtime gate — CI/laptop can't see S3): run `docker compose -f services/dagster/aws/docker-compose.yml
   exec -T -e AWS_DEFAULT_REGION=us-east-2 dagster-codeloc python -m quant_sports_intel_models.football.ncaaf.feeder.xref
   --draft-seasons 2015-2025 --write` → must reproduce ~99.7% slot baseline with NO cartesian inflation (the CLI prints per-class
   %). On first real read, confirm `nflverse_players` carries college_name/draft_number/entry_year/display_name (else the UDFA
   residual degrades to 0 gracefully; the slot spine is unaffected). ✅ UNBLOCKS P1A (college→NFL translation).
▶ NCAAF-P0.3 — COLLEGE↔NFL PLAYER-ID XREF (the NFL-feeder spine)   [Data/Model · 🧭 OPUS · Phase 0 · the highest-ROI Phase-0 asset]
🎯 GOAL: build the college↔pro player-ID crosswalk — the spine of the NFL feeder (college production → NFL rookie projections; the E7 MiLB→MLB analog). Do this EARLY; everything downstream keys on it.
Read: `../../multi_sport_roadmap.md` §4 (draft-continuity) + MLB Edge **E7** (the MiLB→MLB MLE pattern — the direct analog) + `ncaaf_data_inventory.md`.
⭐ **P0.1 ALREADY SOLVED THE SPINE — this story is now MUCH smaller than written. Read `ncaaf_data_inventory.md` §4 FIRST.**
 • **There is NO shared player ID between CFBD and nflverse** (CFBD `collegeAthleteId`=ESPN numeric `4431611`; nflverse `cfb_player_id`=a sports-reference SLUG `caleb-williams-3`; CFBD `nflAthleteId` ∩ nflverse `espn_id` = **0 of 257**). **Any plan that assumes an ID join is wrong.**
 • **The deterministic key is the DRAFT SLOT:** `CFBD /draft/picks (year, overall)` ⇄ `nflverse draft_picks (season, pick)` (nflverse `pick` IS the overall pick). **Verified 2,813/2,821 = 99.7% of 2015–25 CFBD picks resolve to an NFL `gsis_id`**, independently validated at 92–100% surname agreement. Combine measurables then attach nflverse-internally on the `cfb_id` slug (81.8% of picks; 65.7% have a 40-time).
 • **The feeder's TARGET variable is already in `nflverse draft_picks`** (`car_av`, `w_av`, `games`, `seasons_started`, `probowls`, career pass/rush/rec lines) — no extra source needed.
 • 💥 **pandas merges NaN-to-NaN** — coercing the slug column with `to_numeric` yields all-NaN and a **cartesian explosion that fabricates a bogus ~100% match** (this bit the P0.1 session). **Drop null join keys on BOTH sides and assert `len(merged) == len(left)`.**
DO: land the xref as a versioned mart built on the **draft-slot key** (not fuzzy matching) for drafted players. The REAL remaining work is the three residuals: **(a) UDFAs** — no draft slot ⇒ genuinely need fuzzy `name + school + position + year`; **(b) transfers** — a player's CFBD production spans schools (use `/player/portal` + `/player/search.teamStints`); **(c) name normalisation** (suffixes/`Jr.`/apostrophes = the 2–8% surname disagreement). Source-stamp match confidence (deterministic-slot vs fuzzy), leakage-safe. First-pass college→NFL translation (the E7 analog) begins once the xref exists (its own Phase-1 prompt).
GATE/AC: a college↔NFL ID xref mart with match-confidence, validated on a known draft class (the 99.7% slot baseline must be REPRODUCED, no cartesian inflation — assert row counts); UDFA/transfer/duplicate cases handled; feeds the NFL guide's rookie projections. Operator handoff + `git add`.
```
```
✅ DONE 2026-07-19 — NCAAF-P0.4 shipped the FREE transfer/roster-continuity signal as the derived dbt mart
`ncaaf_team_roster_continuity` (`sports_dbt/models/ncaaf/marts/`; one row per season+team, FBS, 2014–2025, 1,555 rows) +
4 new staging views-as-tables (`stg_ncaaf_returning_production` / `_transfer_portal` / `_talent` / `_roster`). The signal
combines: **returning production %** (CFBD `/player/returning`, the headline continuity metric + pass/rec/rush splits + usage),
**transfer-portal flux** (`/player/portal` — in/out counts, star- & 247-rating-weighted, blue-chip, attrition; portal era 2021+,
flagged `portal_data_covered`), **roster year-over-year continuity** (`/roster` head-count overlap, N∩N-1; NULL at the 2014 floor
= unknown, not 0), and **247 talent level + YoY delta** (`/talent`, 2015+). ⭐ Leakage-safe / point-in-time (every input set
pre-season; joined by season as-of kickoff). **Zero new ingest/cost** — pure dbt over the P0.2-locked §8 raw feeds. Feeds
**P1.2** (strength covariate) + **P1.3** (features). NIL-$ documented + DEFERRED (inventory §10 — On3/Rivals = PFF-class,
no public bulk API, NOT ground-truthed, edge-gated, never a Phase-0 dep). Validated: dbtf compile + `dbtf build … --target dev`
GREEN (10 tests inc. grain-unique), values ground-truthed on real CFBD pulls (SMU-2024 etc.). ✅ UNBLOCKS the P0.4 covariate in
P1.2 + the roster features in P1.3.
🩹 LANDMINE (added to the pile): `returning` is a **reserved keyword** in DuckDB/dbt-fusion — a CTE named `with returning as (…)`
fails with a MIS-SURFACED "Error while decoding UTF-8 … 1:11" from fusion (not a parse error) → renamed `ret_prod`. The
multi-`delta_scan`-stacking mart hits the N0.3 "DeltaScan serialization not implemented" limit → the 4 new staging models are
`materialized='table'` (read each Delta once) so the mart plan holds no `delta_scan`.
▶ NCAAF-P0.4   ✅ **DONE 2026-07 (prod; SQL-ONLY derivation, $0 new — every raw feed was already in P0.2).** New mart **`ncaaf_team_roster_continuity`** (season×team, FBS, 2014–2025, 1,555 rows): returning-production % (`/player/returning`) + transfer-portal flux (`/player/portal` — in/out counts, star/247-weighted, blue-chip, attrition; **`portal_data_covered` flag, era 2021+**) + roster YoY overlap (`/roster`) + 247 talent + YoY delta (`/talent`, 2015+); 4 staging models. Leakage-safe by construction (pre-season inputs; 2014 continuity honest-NULL). **NIL-$ DEFERRED** (documented `ncaaf_data_inventory.md §10` — no public bulk API = scrape/enterprise like PFF; NOT ground-truthed since nothing to pull; revisit only if a Phase-1 ablation shows residual error where roster-$ would explain beyond portal-flux + returning-production). CI `dbtf compile --target ci` 134✅. ⏭️ unblocks P1.2 (strength covariate) + P1.3 (roster-flux feature block). 🚩 **FLAGS the downstream inherits:** (1) ⚠️ **NO Dagster job for the NCAAF dbt models yet → the mart is rebuilt MANUALLY until P1.1 wires the box job (P1.1's scope) — don't let it silently rot**; (2) **respect `portal_data_covered` — pre-2021 portal=0 is UNKNOWN, not "no churn"** (P1.2/P1.3 must not treat 0 as a real value); (3) 🧰 **dbt-fusion preview-196 SEGFAULTS building 2+ delta-scan models in ONE invocation → build staging SERIALLY** (workaround; resolves on a fusion upgrade — bites P1.1 which builds many NCAAF models). (Orig prompt ↓.)
▶ NCAAF-P0.4 — NIL + TRANSFER-PORTAL DATA (source eval + ingest — a data-source add; do EARLY, feeds the features)   [Data · 🧭 OPUS · Phase 0/1 boundary]   (operator-scoped 2026-07-13)
🎯 WHY: the transfer portal + NIL money are RE-SHAPING lower-conference power (talent moves fast now) — the game model needs a roster-continuity / talent-flux signal or it will misprice teams whose roster turned over. Data-first + source-honest, like P0.1.
DO: (1) **Transfer portal (mostly FREE):** derive portal moves from **CFBD roster year-over-year deltas** (who left / who arrived) + `/player/portal` if live + `teamStints`; build a per-team **roster-continuity / returning-production** signal (returning starters, returning production %, talent in vs out). (2) **NIL $ valuations (PAID/scraped — treat like PFF):** On3 NIL valuations / Rivals are NOT a clean API → **do NOT gate the model on NIL $; ship on the FREE transfer/roster-continuity signal first**, add $-valuations only as an edge-gated later buy (state the source + cost if found). (3) land both as versioned lake tables (sport-tagged), point-in-time (as-of the season/week, leakage-safe — a team's transfer class is known pre-season). Ground-truth every "available" claim on a real pull (the P0.1 discipline; watch the CFBD 200-text/html gotcha).
GATE/AC: a free transfer/roster-continuity signal per team-season landed + validated; the NIL-$ availability/cost documented + explicitly deferred (not a Phase-0 dep); leakage-safe (as-of); feeds P1.2 (strength covariate) + P1.3 (features). Operator handoff + `git add`.
```
```
▶ NCAAF-P0.5 — COACHING-CHANGE DATA (HC free + OC/DC gated)   ✅✅ DONE — box-verified & merged to main 2026-07-19
✅ SHIPPED: `coaches` ingest (25th NCAAF source, CFBD `/coaches`, 1 call/season, Delta-partitioned, backfilled 2014–2025) → `stg_ncaaf_coaches` (exploded to coach-school-season grain, carries SP+ splits) → mart **`ncaaf_team_coaching_change`** (1,555 rows, season×team FBS, same returning-production spine as P0.4; box build 3 models + 10/10 tests green over real S3).
✅ THE SIGNAL P1.3 GETS (join season×team): HC identity + `hc_tenure_years` + `is_first_year_at_school`; `hc_change_from_prev` (NULL at 2014 floor); `hc_midseason_change` + `n_coaches_in_season` (coach-of-record = most games); ⭐ the coach's PRIOR SP+ track record (`hc_prior_sp_overall/offense/defense_avg` career-to-date + `hc_recent_sp_*` most-recent-prior + `hc_prior_seasons` + `is_first_time_hc`) — the differentiator vs a binary flag; `is_hc_history_censored` (2014 left-censor). Leakage-safe: identity/change/tenure known pre-season, prior-SP+ reads strictly `year < season`, 0 future-touch rows; ground-truthed (Rhule/Deion/Frost→Joseph).
✅ OC/DC DECISION: no free CFBD coordinator endpoint; editorial (FootballScoop/ESPN) or paid (SportsDataIO) only → **DEFERRED, gated like NIL-$** — revisit only if a Phase-1 ablation shows residual a coordinator change would explain beyond HC + returning-production + roster flux. Not a blocker.
🚩 **ROUTED TO P1.1 (now the highest-priority downstream item — from BOTH P0.4 and P0.5):** NO Dagster box-job exists for ANY NCAAF dbt model yet → `ncaaf_team_coaching_change` (and P0.4's `stg_ncaaf_returning_production`, its parent) are HAND-BUILT today and can silently rot. P1.1 wiring the full NCAAF staging+mart box-job resolves both. (Already flagged in P1.1's scope §39/§41 — this CONFIRMS it's the top P1.1 line.)
🧨 REUSABLE LANDMINES (added to the pile): `coaches` has NO coach ID → `coach_name` is the only cross-school key; build `delta_scan` staging **serially** (fusion-preview-196 segfaults on 2+ delta parents per invocation → `--select +model` can't auto-pull two delta parents); `rows`/`returning` are reserved words. CI green on main: `test_ncaaf_scaffold.py` 24 passed (registry 24→25); `dbt compile --target ci` 150/150.
🔓 UNBLOCKS P1.3 (coaching feature block). Independent of P0.6 / P1.1 / P1.2. (Orig prompt ↓.)
─────────────────────────────────────────
▶ NCAAF-P0.5 — COACHING-CHANGE DATA (HC free + OC/DC gated)   [Data · 🧭 OPUS · Phase 0/1 boundary · with P0.4]   (operator-scoped 2026-07-13)
🎯 WHY: a new HC/OC/DC can flip a team's scheme + scoring profile overnight (~63–71 new Power-4 coordinators in a single 2026 cycle) → the model needs a coaching-continuity signal.
DO: (1) **HEAD COACH — FREE via CFBD `/coaches`** (verify live, the P0.1 200-text/html discipline): coach↔school↔year history → **year-over-year HC-change flags per team** + ⭐ **the coach's PRIOR SP+ off/def profile** (the endpoint carries per-coach-year SP+ splits → a new HC's expected impact ≈ their track record, not just a binary flag). (2) **OC/DC (coordinators) — a GAP, NOT in CFBD → best-effort gated add:** the tracker is **FootballScoop** (ESPN/CBS also) — no free API ⇒ scrape their OC/DC change trackers OR maintain a small season table (SportsDataIO is a paid alt). Ship HC-continuity FREE first; OC/DC is a scraped/manual best-effort, gated like NIL-$. (3) land coaching tenure + change flags as a versioned team-season table (sport-tagged, point-in-time — a coaching hire is known pre-season, leakage-safe).
GATE/AC: HC change/tenure + prior-SP+-profile landed FREE from CFBD `/coaches` (verified live); OC/DC source + cost documented + shipped best-effort-or-deferred (not a blocker); leakage-safe; feeds P1.3. Operator handoff + `git add`.
```
```
▶ NCAAF-P0.6 — HISTORICAL NCAAF CLOSING-LINE ODDS (paid Odds-API `/historical`) — GATES all market/CLV/edge work   ✅✅ DONE — FULL 2020–2025 backfill complete & verified on the laptop 2026-07-19 (one follow-up commit pending on the open PR)
✅ SHIPPED + LANDED: `odds_ncaaf_historical` source (26th, `on_demand=True`) + `ncaaf/ingest/odds_backfill.py` (game-lines-only port of NFL-N0.4) + `ncaaf/ingest/verify_odds_historical.py` (re-runnable acceptance check, **exit 0/1 → CI/handoff-gateable**). Kickoffs read from CFBD `/games.startDate` (already UTC); leakage-safe snapshot at K−buffer with ±30min commence window + `_snapshot_ts`/`_requested_snapshot` stamps; below-floor (<2020) clean skip; `--dry-run` credit estimate (reads CFBD schedule, NO paid Odds call); default backfill EXCLUDES it (handler `_resolve_sources` drops on_demand → no accidental credit burn); **run on the LAPTOP not the box (I/O-bound, kept off live serving — baked into the runner docstring).** **BACKFILL DONE — SIX seasons 2020–2025** to `s3://credence-sports-lakehouse/ncaaf/raw/odds_ncaaf_historical/`: **10,394 rows / 5,531 distinct games.** FBS coverage by season: 2020 94.4% (COVID-short) · 2021 121.5% · 2022 128.8% · 2023 98.0% · 2024 100.5% · 2025 101.1% (>100% = the feed also prices some FCS games the FBS count omits). Markets: spreads/totals **100% every season**, h2h 95.5–99.6%. **27 US books incl. Bovada** (7,506 rows, the target book); Fanatics from 2025. **5,529 / 5,531 games retain a valid pre-kickoff close** (~74–76% of rows already pre-kickoff; the ~25% post-commence rows are redundant neighbour-window captures P1.4 filters via `snapshot_ts < commence_time`). The 2 exceptions (Texas A&M–UTSA, Georgia–Austin Peay, both 2025) = a CFBD-vs-OddsAPI kickoff disagreement (CFBD `startDate` off by 10/60 min → the ±30-min anchor missed true kickoff); 0.04% of games, P1.4 simply has no close for those two; +15 inert non-FBS orphans outside the modelling universe. Cost ≈ **100–120k Odds credits** (~16–20k/season — NCAAF has far more distinct kickoff windows than NFL's ~90); **4.83M credits remain.** ⚠️ **LANDMINES CAUGHT (both worth carrying — see the reusable note below): (1)** season defaults were **PINNED (2020–2024), not clock-derived → stale by a full season the day it merged** = the 2025 gap; now clock-derived via `last_completed_season()` (January handled conservatively so a default run never pulls an in-progress season). **(2)** `--skip-existing` silently PROTECTED a 3-event `--max-events 3` verify STUB in 2024 → caught only by the coverage check → re-pull the season WITHOUT the flag. ⇒ UNBLOCKS P1.4 vs-market leg + Phase 2.
🎯 WHY: P0.2's `odds_ncaaf` (936 rows) is a CURRENT-odds feed, NOT historical closing lines (mis-tagged `season=YYYY`, fetched live). ⚠️ **without real historical closing lines there is NO market benchmark + NO CLV** → this GATES P1.4's vs-market eval + all of Phase 2 (sharp-anchor/microstructure). Inventory table #21 `odds_ncaaf_historical` (paid Odds-API `/historical`) is the source, NOT built.
⭐ **REUSE THE NFL-N0.4 PROVEN PATTERN (just built this exact Odds-API `/historical` ingest for NFL):** copy `nfl/ingest/odds_backfill.py` — the **`--dry-run` free credit estimate** (reads schedules, no API calls, before any paid run), **on-demand gating** (paid per-event sources excluded from the default backfill → no accidental credit burn), the **closing snapshot at commence − buffer** + **below-floor season skip** (pre-floor seasons skip cleanly, no 422 storm). Same `sources.py` typed/JSON routing.
DO: build the `odds_ncaaf_historical` ingest (paid Odds-API `/historical` endpoint, existing sub — confirm credits; **game-line floor 2020, per P0.1 + N0.4-confirmed**; ⚠️ **props have a HARDER 2023 vendor floor if ever wanted, but NCAAF props are THIN → game lines h2h/spread/total only here**) → season-partitioned Delta on `credence-sports-lakehouse`. Capture CLOSING lines (h2h/spread/total, 11 books incl. Bovada) with commence-time → leakage-safe closing snapshot (enforce `snapshot_ts < commence_time`, the N0.4 belt-and-suspenders). Cost-aware (`--dry-run` first). ⚠️ do NOT conflate with the live `odds_ncaaf` feed — this is the backtest/CLV source.
GATE/AC: `odds_ncaaf_historical` closing lines landed (2020+, h2h/spread/total, commence-time for leakage-safe close), Delta on the lake, credits stated; ⇒ unblocks P1.4's market-relative eval + Phase 2. Operator handoff + `git add`. **SEQUENCE: before P1.4's vs-market leg / Phase 2 — NOT a blocker for P1.1–P1.3 (which train on game outcomes) or the feeder.**
```
```
▶ NCAAF-P0.6b — RECURRING 2026+ CLOSING-LINE CAPTURE (bridge the one-time backfill → an ongoing CLV feed)   [Data/Ops · 🧭 OPUS/SONNET · ⏰ TIME-SENSITIVE — NCAAF season opens Aug 2026]   (NEW 2026-07-19, from the P0.6 handoff)
🎯 WHY: P0.6 backfilled 2020–2025 closing lines. There's no recurring capture for 2026+ yet — which is EXPECTED, not a gap: **the 2026 season hasn't started, so there's nothing to capture.** This is a **build-AHEAD-of-kickoff** item — stand up the recurring closing-line capture BEFORE the Aug-2026 opener so the CLV benchmark keeps extending season-over-season (else, once 2026 games start completing, the P1.4/Phase-2 vs-market eval would be stuck on a frozen 2020–2025 window). Time-boxed by the Aug-2026 kickoff, not urgent today.
🧭 THE DECISION (make it explicitly, don't default): **(A) periodic `/historical` catch-up** — reuse `odds_backfill.py` on a weekly/nightly cron pulling the just-completed slate's closes (paid Odds credits, but cheap — ~16–20k/season amortized; exact, same leakage-safe K−buffer snapshot as the backfill) VS **(B) capture near-kickoff from the LIVE `odds_ncaaf` feed** — snapshot the live feed at commence−buffer + persist as the close (no extra Odds-historical credits, but needs a scheduler at each game's kickoff + careful leakage stamping). Recommend **(A) periodic `/historical` catch-up** as the low-risk default — it reuses the proven, verified P0.6 path + `verify_odds_historical.py` as the acceptance gate, and NCAAF's weekly cadence makes a once-weekly catch-up trivially cheap; only go (B) if the credit cost proves material.
DO: pick A or B (state the rationale); wire the recurring capture (EventBridge/cron per `sport_data_platform.md`) writing to the SAME `odds_ncaaf_historical` Delta partition (season=2026…); clock-derived season default (`last_completed_season()` — the P0.6 landmine, don't pin); on-demand/paid gating preserved (no accidental credit burn on a routine pull); `verify_odds_historical.py` runs post-capture as the gate. ⚠️ do NOT conflate with the live `odds_ncaaf` current-odds feed — this persists the leakage-safe CLOSE.
GATE/AC: a recurring 2026+ closing-line capture live (A or B, rationale stated), writing leakage-safe closes to the `odds_ncaaf_historical` Delta, `verify_odds_historical.py` green on the first captured week, clock-derived season default, credit cost stated; ⇒ the CLV feed stays fresh into the 2026 season. Operator handoff + `git add`. **SEQUENCE: before the 2026 season opens (Aug 2026); independent of P1.1–P1.4.**
```
```
▶ NCAAF-P1.2b — FRESHMAN-PRODUCTION PROJECTION (recruit → college; the HS→college MLE)   [Model · 🧭 OPUS · Phase 1 · feeds P1.3]   (operator-scoped 2026-07-13)
✅✅ **DONE + VALIDATED ON THE REAL 2014–2025 BUILD (2026-07-21; operator ran it laptop-side, 16,541 priors + 1,424 team rows landed in S3).** 📊 Real result — a genuine but WEAK signal (the honest freshman-prior outcome): winner `gbm@200-2-0.05` beats the position-mean null (OOS MAE 0.7011 < 0.7164) **robustly, PBO = 0.000**; ⚠️ **DSR = 0.821 (< 0.95)** — real but small enough it doesn't clear the strict deflation bar (PBO/DSR REPORTED not HALTed per E2.1-r — a candidate feature, P1.4 decides). Face-valid (top 2025 = Griffin 5★DL / Underwood + Russell 5★QB; top classes Texas/Georgia/Alabama). `best_alpha = 0`. Shipped exactly to all 5 PM additions: (1) §0.5 **bake-off** — 4 pre-registered candidate classes incl. ⭐ partial-pooling via `hierarchical.py` REUSED UNCHANGED + a position-mean NULL floor, leave-one-CLASS-out expanding-window CV, PBO/DSR, oracle-floor gate; (2) the **EB variance-collapse cure** inherited from the solver (boundary-avoiding tau + multi-start + a regression test that pins it) + the ±plausibility gate; (3) the **P1.3 join contract** = `ncaaf_team_freshman_prior` grain **(season, team)**, columns `n_incoming_freshmen`/`freshman_class_projected_production`/`freshman_class_avg_projected_production`/`freshman_class_top_projected_production`/`freshman_class_avg_rating`/`blue_chip_count`, broadcast to every `as_of_week` on `(season=arrival_season, team=arrival_team)`; (4) **uncertainty = PARAMETER uncertainty** (relative confidence, recalibrate to price); (5) **behavioral gates** — leakage is class-based + verified to FAIL on a tampered training class. 🔧 Corrected a live data-inventory error (bridge is `roster.recruit_ids ↔ recruiting.id`, not `athleteId`). Files: `stg_ncaaf_recruiting_players` · `stg_ncaaf_roster.recruit_ids` · `ncaaf_recruit_production_pairs` · `models/freshman_projection.py` · `models/run_freshman_projection.py` · `ncaaf_freshman_priors` + `ncaaf_team_freshman_prior` views · `betting_ml/tests/test_ncaaf_freshman_projection.py` (14 tests). Report: `ablation_results/ncaaf_p1_2b_freshman_projection.md`. **Original prompt + PM additions retained below for the validation run.**
🟢🟢 **PM READINESS PASS (2026-07-20) — CLEARED TO RUN NOW (operator pulled it ahead of P1.3). 5 additions the prompt below is missing:**
 **(1) 🔬 §0.5 BAKE-OFF IS REQUIRED — this is a MODEL, not a lookup.** The DO line ("fit … EB-shrunk") is a single architecture, which violates the standing discipline. **Pre-register ≥3 candidate forms + a foil**, e.g. (a) ⭐ **partial pooling via the P1.2 solver — REUSE `football/ncaaf/models/hierarchical.py`** (player nested in position, position nested in recruit-tier/school-tier); "position-specific + EB-shrunk for thin cells" IS a partial-pooling problem, and the sport-agnostic solver already exists + is validated — do NOT hand-roll a new estimator; (b) a simple **position-stratified regression on composite/stars** (the interpretable foil); (c) a **learned GBM** on rating + position + school-tier + class-size features. Optuna-tune; select on a **held-out RECRUIT CLASS** under purged CV; PBO<0.2 / DSR≥0.95. A single form's miss is NOT a null.
 **(2) 🚨 THE EB VARIANCE-COLLAPSE BUG IS LIVE FOR THIS STORY — P1.2 just hit it.** On thin fits the marginal likelihood genuinely peaks at `tau = 0`, which **silently deletes a level of the hierarchy** (here: "every recruit at a position is identical") — no error, no failing test. P1.2b's position×class cells are exactly this thin. **CURE (copy P1.2's): a boundary-avoiding `Gamma(2,·)` prior on each variance component + multi-start + a regression test that pins it.** Also copy P1.2's **prior-scale plausibility gate** (its "flat" prior was secretly a 1,000-point prior that leaked ±913 pts on one team).
 **(3) 📐 DEFINE THE OUTPUT GRAIN + THE P1.3 JOIN CONTRACT — P1.3 is holding a named slot for you.** The model is per-PLAYER (recruit), but **P1.3's matrix is `(season, team, as_of_week)`** → P1.2b must ALSO emit (or explicitly specify) the **team-level aggregation** P1.3 consumes — e.g. a team's true-freshman projected-contribution share / roster-weighted freshman prior. **State the exact column names + grain in the handoff** so P1.3's slot joins 1:1 without rework. (Do not leave the aggregation undefined — that's the whole point of the slot.)
 **(4) ⚠️ UNCERTAINTY SEMANTICS — say which kind you emit.** P1.2 shipped **parameter** uncertainty that is ~1.5× too tight to price with, and the consumer had to be warned. State plainly whether P1.2b's "uncertainty" is parameter-only or a calibrated predictive interval; if parameter-only, mark it a RELATIVE confidence signal and note that any pricing consumer must recalibrate on held-out data.
 **(5) ✅ BEHAVIORAL GATES, not green-checkmark** (the P1.2 lesson — CI mocks IO and cannot see this class): validate on the REAL 2014+ build with **calibration + plausibility checks**; make the leakage gate **DATE-based** and **verify it actually FAILS on a tampered row** so its green means something.
 📅 **DATA-WINDOW NOTE:** the binding floor is **production, not recruiting** — CFBD player-advanced starts 2014 ⇒ usable recruit↔production pairs are recruits **arriving 2014+** (~11 classes). Recruiting itself reaches back further but is unusable without the production side.
🔧 **POST-P1.1 UPDATE (2026-07-20):** the recruit↔production pairs come from `fact_ncaaf_player_game` / the player rollups (see `ncaaf_mart_inventory.md` §6); order/window early-college production by **`season_order_week`** (not raw `week`); the **2014 history floor** bounds the recruit-class training window; **NULL = unknown → keep NULL** (a recruit with no college snaps yet is the whole point — don't impute). Otherwise the prompt stands.
🎯 WHY (operator): we must project TRUE FRESHMEN (no college snaps) somehow → use their recruiting rating as the prior. This is the recruiting-level analog of the college→NFL feeder (E7 pattern, one level down).
Read: MLB Edge **E7** (the MiLB→MLB MLE — the direct analog) + the P1.1 marts + `ncaaf_data_inventory.md` (recruiting endpoints).
DO: recruiting is FREE (CFBD `/recruiting/players` — composite/stars/position/school). Fit **recruit-rating → early-college production** on historical recruit↔production pairs (assembled from P1.1: a recruit's rating vs their fresh/soph output), position-specific, EB-shrunk for thin cells → a **freshman-production prior** (mean + uncertainty) for players with no college history. Leakage-safe (recruiting rating is pre-arrival). Honest: a prior, not a projection-of-edge.
GATE/AC: a position-specific recruit→production prior (mean + uncertainty), validated on a holdout recruit class; feeds P1.3 as the true-freshman feature. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.1 — ANALYTIC DATA MODELING   ✅✅ DONE + BOX-VERIFIED 2026-07-20 (`sports_ncaaf_dbt_build_job` green on EC2 in 2m59s — RUNTIME GATE CLOSED)
📦 SHIPPED in `sports_dbt`: **6 new staging** (teams / game_team_stats / game_player_stats / drives / plays / game_advanced) → **4 DIMS** (`dim_ncaaf_conference`, SCD-2 `dim_ncaaf_team`, SCD-2 `dim_ncaaf_player`, `dim_ncaaf_game`) → **4 FACTS** (`fact_ncaaf_team_game` 18,124 · `fact_ncaaf_player_game` 539k · `fact_ncaaf_drive` 214k · `fact_ncaaf_play` 1.55M) → **3 ROLLUPS** (season, ⭐as-of-week point-in-time, ⭐opponent-adjusted). All FBS-filtered + sport-tagged. Full doc: **`ncaaf_mart_inventory.md`** (new).
🚨 **THE HEADLINE FINDING — a LIVE leak caught and fixed mid-story: CFBD RESTARTS `week` AT 1 FOR THE POSTSEASON.** Every bowl/CFP game carries `week=1` but is played in Dec/Jan, so ordering a season by raw `week` puts the national championship BEFORE regular-season week 2. Measured: 2024 Ohio State had **FIVE** games at `week<=1` (opener + 4 playoff games) and every as-of-week row from wk2 onward was silently absorbing them. **CURE = `season_order_week`** on `dim_ncaaf_game` (regular weeks as-is, postseason offset past them; monotone in game_date) — **the ONLY column that may order or window a season; raw `week` is reporting-only.** ⚠️ **AND THE LESSON FOR EVERY FUTURE POINT-IN-TIME MART:** the obvious leakage test (recompute with `week < W` and compare) is WORTHLESS — it re-uses the model's own ordering, so a wrong ORDERING still satisfies a right FILTER and the test passes GREEN. It did. The shipped gate `assert_asof_week_has_no_future_games` is therefore **DATE-based** (every contributing game must predate its own week's first kickoff), which cannot be fooled by an ordering bug.
🔒 3 singular leakage gates ship + run in the box job: `assert_asof_week_has_no_future_games` (date-based) · `assert_opponent_adjustment_is_point_in_time` (each opponent's rating read at the SAME as_of_week, not season-final — the classic opponent-adjustment leak) · `assert_season_order_week_is_monotone_in_date` (root-cause guard).
⭐ SCD-2 findings: realignment verified (Texas/Oklahoma Big12→SEC 2024, UCLA→Big Ten, BYU→Big12 2023, Cincinnati). **Idaho correctly has NO `is_current` row** (left FBS after 2017) — don't "fix" it. `dim_ncaaf_player` payload = team+position, **class_year EXCLUDED**: it advances every season by construction and produced 20,672 of 30,433 version breaks vs 9,761 real ones (2:1 noise).
⭐ Opponent adjustment sanity: 2-pass; corr(raw,adj)=0.958, mean shift 0.024 vs raw SD 0.104. 2024 season-end adjusted top-5 = **Ohio State, Notre Dame, Texas, Oregon, Ole Miss** — the actual CFP result, from a model that never sees a ranking. 2024 Indiana/Army (great record, soft schedule) correctly marked DOWN.
🏗️ **BOX JOB WIRED (the story's top line, routed here by BOTH P0.4 and P0.5):** `sports_ncaaf_dbt_build_job` builds the **WHOLE** NCAAF DAG incl. P0.4's `ncaaf_team_roster_continuity` + P0.5's `ncaaf_team_coaching_change` (no more hand-built marts silently rotting). Staging built **SERIALLY** — the P0.5 fusion-segfault landmine AND a measured OOM. `dbt run` = **HALT**, `dbt test` = **WARN-continue** (CLAUDE.md INC-6). First NCAAF CI slice added to `sports_dbt_ci.yml` incl. a non-empty-selector assertion (a selector matching nothing still exits 0 in dbt).
🧯 LANDMINES FOR DOWNSTREAM: (1) **memory** — `stg_ncaaf_game_player_stats` (4 chained UNNESTs → 5.2M rows) and `fact_ncaaf_play` pin `threads=1` + `preserve_insertion_order=false`; both OOM a 4 GB DuckDB otherwise. **Do NOT remove the pins when the box gets more RAM** — amplification scales with seasons. `stg_ncaaf_game_advanced` extracts the offense/defense sub-objects ONCE per row (100 full-document `json_extract_string` paths OOM'd 4 GB on a 25k-row table). (2) **NULL is unknown, keep it NULL** — wk-1 as-of rows, teams with no `/plays` coverage (2014 Washington wk1 = 0 plays), first-time HCs. (3) NCAAF staging default flipped to **table** (delta_scan-stacking cure, N0.3 pattern).
⚠️ **PRE-EXISTING P0.3 failure surfaced + RATCHETED (not waived):** `not_null_xref_college_nfl_players_gsis_id` was failing on 8 of 4,211 rows — drafted players who never took an NFL snap have no nflverse `gsis_id` (e.g. Bud Sasser, 2015 rd6). Those are LEGITIMATE nulls; P0.3's `not_null` was wrong about its own data. Now `warn_if:">0"` + `error_if:">8"` — **the 8 known warn, a 9th FAILS THE BUILD**, so the count can only drift DOWN silently and any growth (a slot-match regression, or a fresh draft class not yet on the field) surfaces. Verified both directions (8→WARN; simulated threshold 7→ERROR). ⚠️ Raise the threshold DELIBERATELY with a reason if it legitimately changes — don't delete the test or bump it reflexively.
🔒 **TEST TIERING (refined):** `dbt run` HALT → **the 3 leakage gates HALT** → the rest of `dbt test` WARN-continue. Leakage is carved OUT of the INC-6 WARN default on purpose — a warning nobody reads is exactly how the postseason week-1 collision survived into a shipped model.
⏭️ UNBLOCKS P1.2 (strength) · P1.2b (freshman priors) · P1.3 (features) · P1A (feeder). (Orig prompt ↓.)

▶ NCAAF-P1.1 — ANALYTIC DATA MODELING (the supportable dimensional structure — BEFORE features)   [Data/dbt · 🧭 OPUS · Phase 1 · the "don't become a mess" story]   (operator-scoped 2026-07-13)
🎯 GOAL: turn the raw lake (P0.2) into a PROPER conformed dimensional model so the structure is supportable + doesn't rot. Do this BEFORE feature engineering — features build on these marts.
Read: `ncaaf_data_inventory.md` §8 (the 24 lake tables) + MLB's `baseball_data_mart_inventory.md` + `scd2_convention.md` (the modeling conventions to mirror) + the `dbt-duckdb` new-sports project (per `sport_data_platform.md` cross-sport decisions — SEPARATE from MLB's Snowflake dbt).
🚩 **ABSORBS the P0.2 flags:** (a) ⚠️ **`games` lands ALL divisions → APPLY the `is_fbs_matchup` FBS filter** in the dims/facts (or downstream pulls in FCS/D-II junk); (b) ✅ **dbt-duckdb is now ON the box image + `sports_dbt_ci.yml` EXISTS (done by NFL-N0.3) → EXTEND that CI + the box job for the NCAAF models, don't stand them up** (the cross-sport infra is shared — mirror N0.3's NFL slice: `sports_nfl_dbt_build_job` + the offline parse+compile CI). ⚠️ baked-image-drift lesson from N0.3: any new dbt dep needs adding in 3 places (pyproject/lock + box Dockerfile pip list + `up -d --build`). ⚠️ **P0.4 flag: NO Dagster job exists for ANY NCAAF dbt model yet → this story wires the box job for ALL of them (P0.4's `ncaaf_team_roster_continuity` + the new dims/facts) so nothing rebuilds manually / silently rots.** 🧰 **P0.4 landmine: dbt-fusion preview-196 SEGFAULTS building 2+ delta-scan models in one invocation → build staging SERIALLY** (until a fusion upgrade).
DO: build the NCAAF marts in the new-sports `quant_sports_intel_models/sports_dbt/` project: **DIMS** `dim_team` / `dim_player` / `dim_game` / `dim_conference` (+ SCD-2 where an attribute drifts, e.g. conference realignment — the MLB `scd2_convention` pattern); **FACTS** `fact_team_game` / `fact_player_game` / `fact_drive` / `fact_play`; **ROLLUPS** season + week point-in-time (as-of-week, **leakage-safe** — never fold post-kickoff data into a pregame row) + **opponent-adjusted** efficiency (adjust raw for schedule strength). ⭐ every row **SPORT-TAGGED** + **FBS-filtered** (`is_fbs_matchup`). Grain + partition + cadence per inventory §8; Delta-native; read the lake via the `ncaaf_delta()` macro (P0.2).
🚩 **THE BOX-JOB IS THE TOP LINE OF THIS STORY (P0.4 AND P0.5 both routed it here):** NO Dagster box-job exists for ANY NCAAF dbt model → today `stg_ncaaf_returning_production` (P0.4) + `ncaaf_team_coaching_change` (P0.5) are HAND-BUILT on the laptop and can silently rot. The box job MUST materialize the FULL NCAAF staging+mart DAG (these two included), not just the new dims/facts. Build serially where delta parents are involved (P0.5 landmine: fusion-preview-196 segfaults on 2+ `delta_scan` per invocation → don't rely on `--select +model` to auto-pull two delta parents).
GATE/AC: the conformed dims+facts+rollups build in `sports_dbt` over the S3 lake, FBS-filtered + sport-tagged; point-in-time correctness proven (as-of-week row has NO post-kickoff info — leakage spot-check); opponent-adjustment applied; **the dbt build wired into a box Dagster job (dbt-duckdb on the box) covering the WHOLE NCAAF DAG incl. P0.4's `stg_ncaaf_returning_production` + P0.5's `ncaaf_team_coaching_change` + the first NCAAF CI gate stood up**; a real box build (runtime gate). Operator handoff + `git add`.
```
```
▶ NCAAF-P1.2 — CONFERENCE / TEAM-STRENGTH MIXED-EFFECTS MODEL (partial pooling)   [Model · 🧭 OPUS · Phase 1 · produces the strength feature]   (operator-scoped 2026-07-13)   ✅✅ DONE 2026-07-20 — validated end-to-end on real 2014–2025 data + reproduced on the operator's laptop (identical numbers — deterministic); on `dev`, PR open→`main`. ⏭️ UNBLOCKS P1.3.
📦 **OUTPUT (what P1.3 joins):** per `(season, team, as_of_week)` — **`strength_margin`** (points better than an average FBS team on a neutral field, pre-kickoff) + **`strength_margin_sd`**, at the SAME grain as `rollup_ncaaf_team_week_asof` → **1:1 join**. Decomposes into 3 additive auditable pieces (conference pooling level · pre-season covariates [talent, roster continuity, portal flux, coaching change, last-season finish] · this-season's games). A **second parallel fit splits offense/defense** for P1.4's totals leg.
📊 **VALIDATION (vs realized margins, walk-forward OOS — never vs a market):** **MAE 13.0** vs 16.4 home-field-only baseline; **71.7% winner accuracy**; stable across all 11 seasons (12.8–14.1). Face validity: 2024 final top-3 = **Ohio State, Notre Dame, Texas** = the actual CFP semifinal field, zero ranking input. Posterior uncertainty behaves: sd ~6.7 pts wk1 → **monotonically** ~2.7 by season end. ✅ the required NIL/transfer sanity check PASSED — biggest pre-season roster-flux adjustments land on plausibly-churned teams (2021 UMass, 2019 UConn, 2019 Rutgers), not noise.
🚩🚩 **TWO HONESTY CAVEATS THE PM + P1.3/P1.4 MUST CARRY:** **(1) this is a strength PRIOR, not an edge claim** — every number is vs REALITY, never vs a market price; no win-rate/ROI/bet implied; **`best_alpha=0` still holds**; whether it beats a closing line is P1.4's question under full bake-off discipline. **(2) ⚠️ `strength_margin_sd` is PARAMETER uncertainty, NOT a calibrated predictive interval — it is ~1.5× TOO TIGHT if used directly to price a game.** ⇒ **P1.4 MUST recalibrate it on held-out data (the MLB E13.6 pattern). Use `strength_margin` as a POINT feature and the sd as a RELATIVE confidence signal only — do NOT price intervals off it raw.**
⚙️ **ENGINEERING (2 deliberate calls):** **(a) closed-form Gaussian solver, NOT PyMC/NUTS** — the model refits ~200× (season × as-of-week, each on a leakage-safe window); NUTS = a multi-hour job nobody re-runs, closed form = **~2 min**. Tradeoff stated openly: an **empirical-Bayes plug-in for the variance components** (same posture as the MLB bullpen posteriors), not hidden. **(b) ⭐ THE SOLVER IS SPORT-AGNOSTIC (`hierarchical.py`)** — a general penalized-Gaussian/mixed-effects engine → **NFL + NCAAB reuse it UNCHANGED.** Real cross-sport leverage beyond this story.
🐞 **FOUR REAL BUGS CAUGHT ONLY BY REAL-DATA VALIDATION — THREE WERE SILENT (CI could NOT have caught them; it mocks all IO):** (1) **ML variance collapse** — on a thin fit the likelihood genuinely peaks at "every team in a conference is identical," **silently deleting the team level of a team-strength model** → fixed with a boundary-avoiding prior. (2) **a "flat" prior that was secretly a 1,000-point prior, and it LEAKED** — one barely-supported covariate made 2021 New Mexico State report ±913 points of uncertainty → fixed by scaling priors to the data + a plausibility gate (accuracy unchanged). (3) **a recency-weighting bug** that quietly ran a "4-season" fit on the last few weeks — surfaced NOT as an error but as **miscalibration** (intervals covering half their claim) → fixing it improved both calibration AND accuracy. (4) **a SIGN TRAP in the offense/defense split** — defense is "points PREVENTED," so net strength is the **SUM, not the difference** (documented at every consumer site). ⇒ **LESSON: model quality gates here are BEHAVIORAL, not green-checkmark** — the real-data run + calibration/plausibility checks are what caught these.
🔒 **GUARDRAILS SHIPPED:** a **date-based** (not week-based) leakage gate `assert_team_strength_is_point_in_time` — ⭐ **verified to actually FAIL on a tampered row, so its green MEANS something** (directly addresses the P1.1 postseason-week ordering leak); 20 fast-gate unit tests (partial-pooling behavior, leakage contract, NULL handling, + a regression per bug above); an **INC-25-class build-order note** — the mart reads a parquet the model script writes, so it MUST build after the script in the same run (documented + tagged so a default build can't break it).
🔧 **POST-P1.1 UPDATE (2026-07-20) — READY, but wire in these P1.1 facts (the prompt below predates P1.1):** (1) **the marts exist — read `ncaaf_mart_inventory.md` §6** for exact names/columns; the college-production inputs are `fact_ncaaf_team_game` + `rollup_ncaaf_team_week_asof` (⭐ THE pregame surface) + `rollup_ncaaf_team_week_opponent_adjusted`; the roster/coaching covariates are `ncaaf_team_roster_continuity` (P0.4) + `ncaaf_team_coaching_change` (P0.5). (2) 🚨 **ORDER/WINDOW BY `season_order_week`, NEVER raw `week`** — CFBD restarts `week`=1 for the postseason, so raw week sorts January's title game before September's wk2 (the live leak P1.1 caught); "week-by-week point-in-time" below MUST mean `season_order_week`. (3) **P1.1 already ships a 2-pass opponent adjustment** (`rollup_ncaaf_team_week_opponent_adjusted`) — the mixed-effects model can BUILD ON / cross-check it, not re-derive from scratch; note it's 2-pass/UNWEIGHTED (a refinement candidate). (4) **NULL = unknown, keep NULL** (wk-1 as-of rows, no-play-coverage teams) — partial pooling handles thin samples; don't impute-to-zero. (5) **2014 is the history floor** → the training window is 2014→present + no season-prior exists for 2014.
🎯 WHY (operator): CFB has HUGE talent disparity + a SPARSE cross-conference schedule → raw records aren't comparable across conferences. A **hierarchical partial-pooling** model (team nested in conference) is the right tool — it shrinks thin-sample teams toward their conference mean + yields opponent-adjusted strength with honest uncertainty.
Read: MLB's Elo / sequential-posterior state models (the analog state-estimator pattern) + `ncaaf_data_inventory.md` + the P1.1 marts.
DO: fit a mixed-effects / hierarchical model — **random effect: team, nested in conference**; response = margin (and/or points-for/against) per game; covariates = home, rest, and the **P0.4 roster-continuity/NIL-flux signal** (so a gutted or portal-loaded roster moves the estimate) + recruiting/talent. Emit a per-team **strength estimate + its posterior uncertainty**, week-by-week, point-in-time (leakage-safe). This is a STRUCTURAL estimator (not a bake-off) — but the FEATURE it produces gets ablation-tested in P1.4. Honest: it's a strength PRIOR, not an edge claim.
GATE/AC: a week-by-week team-strength posterior (mean + uncertainty), partial-pooled by conference, leakage-safe; the NIL/transfer covariate materially moves plausibly-affected teams (a sanity check); feeds P1.3 as a feature. Operator handoff + `git add`.
```

✅ DONE 2026-07-20 — NCAAF-P1.2 shipped the team-strength posterior.
  **Delivered:** `football/ncaaf/models/hierarchical.py` (a SPORT-AGNOSTIC penalized-Gaussian /
  mixed-effects solver — closed-form Gaussian posterior + marginal-likelihood variance components;
  NFL/NCAAB can reuse it unchanged), `models/team_strength.py` (the NCAAF estimator),
  `models/run_team_strength.py` (CLI + gates + backtest + report), the `ncaaf_team_strength_week`
  mart (§6.13 of the mart inventory) + `assert_team_strength_is_point_in_time`, and 20 fast-gate
  tests in `betting_ml/tests/test_ncaaf_team_strength.py`.
  **The model:** `margin = hfa + (theta_home - theta_away)`, `theta_t = mu_conf + Z_t.beta + u_t`
  — so strength decomposes into three ADDITIVE, auditable pieces (conference pooling level +
  pre-season covariates + this season's games) that sum to `strength_margin` exactly. A second,
  structurally identical fit on the team-game grain gives an offense/defense scoring split for
  P1.4's totals leg. The NIL/transfer signal enters as a covariate ON THE PRIOR MEAN, and
  `covariate_component_roster_flux` makes its per-team contribution readable — that is the AC's
  sanity check, expressed as a column rather than a claim.
  **Leakage:** window is `season_order_week < W` STRICTLY (never raw `week`); hyperparameters come
  from STRICTLY PRIOR seasons; **2014 is the un-emitted seed** (it only gives 2015 a season-prior),
  so every emitted row has out-of-sample hyperparameters. Gates are DATE-based, not week-based.
  **Real-data result (laptop, full 2014–2025):** posterior sd decays 6.7 → 2.7 pts across a
  season; 2024's final top three (Ohio State / Notre Dame / Texas) reproduces the actual CFP
  semifinal field with NO ranking input; walk-forward MAE ~13.1 pts vs ~15.7 home-field-only.
  🔬 **Two real bugs found and fixed — both worth carrying forward:**
   1. **ML variance components collapse to zero.** On a thin fit the marginal likelihood genuinely
      PEAKS at `tau_team = 0`, which silently deletes the team level of a team-strength model
      (verified: true within-conference sd 6.0, ML returned 0.03). Cured with a boundary-avoiding
      Gamma(2,·) prior on each tau + multi-start; a regression test pins it, and any component that
      still hits a bound is propagated to the run notes rather than swallowed.
   2. 🚨 **`strength_offense - strength_defense` is a trap.** Defense is signed as points PREVENTED,
      so net strength is their SUM; subtracting returns ~0 for every team. Documented in the module,
      the mart, the yml and a test.
  ⚡ Also: the likelihood objective originally recomputed `X'WX` on every evaluation, which made a
  full run take hours. Sufficient statistics are now hoisted out of the optimizer loop (+ triangular
  solves, capped optimizer budget) → ~1 min/season.
  ⚠️ **Honest framing:** a strength PRIOR, not an edge claim. It is a candidate FEATURE that P1.4
  ablation-tests under §0.5 against direct-learned foils. `best_alpha = 0` holds.
  ⏭️ **Unblocks P1.3.** ⚠️ Build order is load-bearing (INC-25 class): dbt marts →
  `run_team_strength.py` → `dbt run --select ncaaf_team_strength_week`.

```
▶ NCAAF-P1.3 — FEATURE ENGINEERING (the broad pregame feature library — BEFORE model dev)   [Model/Data · 🧭 OPUS · Phase 1 · operator-scoped: "a good number of features ready"]   (2026-07-13)
✅✅ **DONE + VALIDATED ON THE REAL 2014–2025 BUILD 2026-07-21 (operator ran it LAPTOP-side — the sports_dbt DAG reads the S3 lake RO + feeds no live serving, so no box needed).** 📊 REAL RESULT: `feature_ncaaf_pregame_matrix` = **9,086 FBS-vs-FBS games** (= the exact `dim_ncaaf_game` FBS universe, no drop/fan-out) × 200 cols; the HALT leakage gate PASSES on **18,172 real matchup-sides (0 violations)** + still proven to fail on a tampered row; landed 2014–2025 in S3 (`ncaaf/derived/feature_pregame_matrix`, 534 in COVID-2020, ~760–808 otherwise). **Per-family coverage all healthy (no dead join):** strength **2014=0% (not emitted, honest) → 100%** 2015+ (2020 96% = COVID opt-outs); season-broadcast families 95–99%; the rollup-derived families all 91% (shared week-1/no-coverage NULL); travel/altitude 86–88% (neutral-site NULL, by design). ⚠️ **P1.4 NOTE: portal_flux reads ~98% even pre-2021 because `portal_net_count` is a real 0 there, not NULL — the honest gate is the `{home,away}_portal_data_covered` boolean carried in the matrix.** Report: `ablation_results/ncaaf_p1_3_feature_matrix.md`.
📦 SHIPPED: the dbt mart **`feature_ncaaf_pregame_matrix`** (`sports_dbt/models/ncaaf/marts/`, tag
`ncaaf_p1_3`) — **one row per FBS-vs-FBS game, 200 cols**: home_/away_ features across **12 families**
(team strength P1.2 · efficiency raw + opponent-adjusted P1.1 · pace/style · line/trench UNIT proxies
· drive quality · roster continuity + portal flux + talent P0.4 · freshman prior P1.2b · coaching
HC-only P0.5 · QB continuity derived · situational · travel/altitude), 6 POST-KICKOFF `label_*`
targets (prefixed, never a feature), + headline diffs. Plus the DATE-based per-matchup leakage gate
**`assert_pregame_matrix_is_point_in_time`** (HALT-tier — wired into `NCAAF_LEAKAGE_GATES` + the CI
gate slice, 3→4), the driver **`models/run_feature_matrix.py`** (one-pull→cached parquet
`ncaaf/derived/feature_pregame_matrix`, join-coverage verification, per-family per-season coverage
report, proves the gate FAILS on a tampered row, `--s3`), and 11 fast-gate tests
`betting_ml/tests/test_ncaaf_feature_matrix.py`. Mart inventory §6.15.
🔑 **Banner A (verify joins on the real lake):** week-grained families join **1:1** on
`(season, team_id, as_of_week = season_order_week)`; season-grained families **BROADCAST** on
`(season, team-name)`. The mart is game-grained + all LEFT joins, so a fan-out DUPLICATES `game_id`
(the driver's grain guard raises) and a drop shrinks the row count vs the `dim_ncaaf_game` FBS
universe (the driver asserts equality). The per-family coverage report surfaces a silently-dead
family (F2/INC-31 class) at build time. **⚠️ the row-count/coverage asserts + the real gate run are
the OPERATOR box step below** — offline they're proven on synthetic + a full local mart execution.
✅ **Banner B (gate proven to fail):** DATE-based (per-matchup kickoff boundary, count-parity +
clock-sanity — NOT a `week<W` filter), and `test_ncaaf_feature_matrix.py` PROVES it fires on both a
back-dated row (clock) and a wrong-week snapshot (parity).
🔧 **PM-correction outcomes:** (2) coaching = HC-only ✅. (3a) QB injury DROPPED (no CFB source) —
kept the derivable half (starter continuity + trailing YPA/QBR). (3b) `is_rivalry` DROPPED (no
confirmed field — not guessed). (3c) `box_advanced` not staged → finishing/field-pos not added.
(4) situational thinned as expected. **⭐ ONE deliberate departure:** travel/altitude are BUILT
(non-neutral games) — the P1.1-update "drop travel/altitude" banner predates confirming
`venue_latitude`/`venue_longitude` ARE staged on `stg_ncaaf_teams`; verified from code, so
`away_travel_km` + `away_altitude_change_m` ship coverage-flagged for P1.4 to ablate (neutral-site
NULL by design). Documented honestly in the report.
🔒 Offline gates GREEN: dbt `parse` + `compile --target ci` (64 models, 4 leakage gates); the FULL
mart executes on synthetic upstreams (200 cols, values correct); the driver runs end-to-end
(all 5 gates incl. tamper-proof); fast gate `pytest -m "not slow"` **1665 passed**.
✅ **RUNTIME GATE CLOSED ON THE LAPTOP** (no box — the sports_dbt DAG reads the S3 lake RO and
serves nothing live; P1.2/P1.2b also ran laptop-side): `dbt run --select +feature_ncaaf_pregame_matrix`
(25 models, 1m56s) → `dbt test --select assert_pregame_matrix_is_point_in_time` (PASS) →
`run_feature_matrix.py --s3` (all 5 gates green, S3 landed 2014–2025). ⏭️ UNBLOCKS P1.4 (reads the
cached parquet / the S3 derived Delta). (Orig prompt + PM banners ↓.)
─────────────────────────────────────────
🟢🟢 **PM FINAL CONFIRM-PASS (2026-07-21) — ALL 3 DEPS DONE (P1.1 marts ✅ · P1.2 strength ✅ · P1.2b freshman ✅); CLEARED TO KICK OFF. Two additions from the P1.1/P1.2/P1.2b sessions — both apply BECAUSE P1.3 is the most JOIN-HEAVY, highest-leverage leakage surface in the chain:**
 **(A) 🔑 VERIFY EVERY JOIN KEY + ROW COVERAGE ON THE REAL LAKE (the P1.2b dead-bridge lesson — a documented key can be wrong + ship a silently-empty result, CI-green).** P1.3 fans ~6 marts into one matrix at **TWO grains**: WEEK-level 1:1 (`rollup_ncaaf_team_week_asof` + `_opponent_adjusted`, P1.2 `strength` per `(season, team, as_of_week)`) vs SEASON-level BROADCAST across `as_of_week` (P1.2b `ncaaf_team_freshman_prior`, P0.4 roster-continuity, P0.5 coaching, all `(season, team)`). ⚠️ **after EACH join, assert row count on the real build — a season-level join must BROADCAST (not fan-out or drop); a week-level join must stay 1:1.** Match-count each key on the lake before trusting the grain; do NOT code to a documented key. **Emit a per-family COVERAGE REPORT** (% non-null per feature family, per season) so a silently-dead family (the F2/INC-31 class — a whole block reading NULL) surfaces at build time, not in P1.4.
 **(B) ✅ THE LEAKAGE GATE MUST BE PROVEN TO FAIL (the P1.1/P1.2/P1.2b standard).** P1.3's AC below says "as-of-kickoff proven" — make that gate **DATE-based** (every contributing row predates the game's kickoff; NOT a `week<W` filter, which passes green on a bad ordering) **AND verify it actually FAILS on a tampered/future-dated row**, so its green means something. This is THE matrix every downstream model trains on — a silent leak here contaminates all of P1.4.
🟢 **PM READINESS PASS (2026-07-20) — CLEARED TO RUN, with 4 corrections to the DO list below. Read all three banners before building.**
 **(1) ✅ RESOLVED — `P1.2b` freshman priors are BUILT (2026-07-21).** Join the team aggregate **`ncaaf_team_freshman_prior`** on **`(season, team)`** (arrival_season/arrival_team), broadcast across every `as_of_week`; columns: `n_incoming_freshmen`, `freshman_class_projected_production`, `freshman_class_avg_projected_production`, `freshman_class_top_projected_production`, `freshman_class_avg_rating`, `blue_chip_count`. ⚠️ **it's a WEAK signal by measurement** (GBM beats null robustly PBO=0.000 but DSR=0.821 <0.95 — recruiting explains only ~0.2 of freshman-production variance; opportunity/depth-chart drives freshman snaps as much as talent) → **carry it, but don't expect it to move the needle; let P1.4's ablation decide its weight.** ⚠️ its `_sd`/uncertainty is **PARAMETER uncertainty, not a calibrated interval** (same as P1.2's `strength_margin_sd`) → relative-confidence only, recalibrate before any pricing use.
 **(2) 🚫 COACHING = HEAD COACH ONLY.** The DO list says "P0.5 new-HC/**OC/DC** flags" — **P0.5 DEFERRED coordinators** (no free source exists; editorial/paid only, gated like NIL-$). Available today: HC identity/change/tenure, `is_first_year_at_school`, `hc_midseason_change`, ⭐ the coach's **prior SP+ off/def track record**, `is_first_time_hc`, `is_hc_history_censored`. **Drop OC/DC from the feature list.**
 **(3) ⚠️ VERIFY-OR-DROP before promising these three** (each is unconfirmed in `ncaaf_data_inventory.md` — do NOT invent a proxy): **(a) QB "injury/change flags"** — ⚠️ college football has **no mandated injury report** (unlike the NFL) and no injury source was established in P0.1 → **most likely NOT available; drop the injury half** and keep the derivable half (starter continuity / QB change vs prior game, from the roster + player facts). **(b) "is-rivalry"** — confirm a CFBD field exists or derive it from a maintained pair list; don't assume. **(c) "finishing" / field-position efficiency** — may live in the **unstaged `box_advanced`** (§7 gap) → confirm it's staged before including.
 **(4) ✅ Sanity-check the surviving situational family.** After the P1.1 banner removes venue/travel/altitude/neutral-site, what genuinely remains is: **home/away, rest days (derive from `game_date` deltas), `season_order_week`, is-conference** (+ is-rivalry if (3b) confirms). That's a thinner situational block than the prompt implies — that's correct and honest, not a shortfall.
🎯 **NET: the buildable families are** team strength (P1.2) · efficiency/opp-adj (P1.1 rollups) · pace/style · line/trench UNIT proxies · roster (returning production + P0.4 portal/NIL flux + recruiting/talent) · coaching (HC-only) · situational (thinned per (4)) · QB (continuity/EPA, minus injury). **That is still a broad matrix — the bake-off will not be feature-starved.**
🔧 **POST-P1.2 UPDATE (2026-07-20) — the strength feature is READY, join it 1:1:** P1.2 emits **`strength_margin` + `strength_margin_sd`** per `(season, team, as_of_week)` at the SAME grain as `rollup_ncaaf_team_week_asof` → a clean 1:1 join (plus a parallel offense/defense split for P1.4's totals leg). ⚠️ **`strength_margin_sd` is PARAMETER uncertainty, NOT a calibrated predictive interval (~1.5× too tight)** → carry it as a **RELATIVE confidence feature only**; do NOT derive intervals/prices from it (P1.4 recalibrates). ⚠️ **SIGN TRAP if you touch the offense/defense split: defense = "points PREVENTED" → net strength is the SUM, not the difference.**
🔧 **POST-P1.1 UPDATE (2026-07-20) — READ BEFORE building; the prompt below predates P1.1 and OVERPROMISES some features the data doesn't have:** (1) **the pregame surface is `rollup_ncaaf_team_week_asof`** (+ `rollup_ncaaf_team_week_opponent_adjusted` for the already-computed opp-adj efficiency — USE it, don't re-derive). ⚠️ **`rollup_ncaaf_team_season` is NOT leakage-safe for its OWN season** (season-over-season priors only). Full column map: `ncaaf_mart_inventory.md` §6. (2) 🚨 **`season_order_week`, never raw `week`, for every window/filter/as-of** (the postseason week=1 leak). (3) 🚫 **DROP the features the §7 known-gaps say we CAN'T build: NO venue data → NO neutral-site flag, NO travel distance, NO altitude** (the prompt lists these — remove them or mark explicitly deferred until venue lands); **special teams NOT pivoted into the player fact** (no ST features yet); **`box_advanced` is unstaged** (some advanced metrics unavailable). (4) **leakage AC must be DATE-based** (every contributing game predates the row's kickoff — a `week<W` filter test passes green on a bad ordering; P1.1's lesson). (5) **NULL = unknown → keep NULL, do NOT impute-to-zero** in the matrix (wk-1, no-coverage teams, first-time HCs); let the P1.4 learners handle missingness. (6) **2014 history floor.**
🎯 GOAL: assemble a BROAD, leakage-safe, point-in-time pregame feature matrix so the P1.4 bake-off isn't feature-starved. Build on P1.1 marts + P1.2 strength + P0.4 NIL/transfer.
DO: engineer the pregame (as-of-kickoff) feature set — **team strength** (P1.2 estimate + uncertainty) · **efficiency** (EPA/play, success rate, explosiveness, finishing — offense/defense, opponent-adjusted) · **pace/style** (plays/game, seconds/play, pass rate, tempo) · **line/trench** (OL unit line-yards/stuff/sack-rate-allowed; DL havoc/pressure — the UNIT proxies, individual-OL is the confirmed gap) · **roster** (returning production, P0.4 portal/NIL flux, recruiting/talent composite, **P1.2b freshman priors** for true-freshman contributors) · **coaching** (P0.5 new-HC/OC/DC flags, first-year-at-school, + the new coach's prior SP+ off/def profile) · **situational** (home/away, rest days, travel distance, altitude, week, is-rivalry, is-conference) · **QB** (the pivotal position — starter continuity, EPA, injury/change flags). Leakage-safe (as-of-kickoff ONLY — never post-game), one-pull-to-parquet (the MLB cost-hygiene rule — assemble ONCE, cache, every downstream reads the cached parquet). Document each feature's source + grain + as-of semantics.
GATE/AC: a leakage-safe pregame feature matrix (documented; as-of-kickoff proven — no post-game leak) with a good breadth across the families above; cached to parquet for the bake-off; sport-tagged. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.4 — THE GAME-MODEL BAKE-OFF (3 markets)   ✅✅ DONE + merged 2026-07-22 — 🏁 CLOSES THE NCAAF PHASE-1 GAME-MODEL BUILD
📊 **VERDICT = `REFERENCE_STANDS` — a trustworthy TIED-FIELD NULL (125 deflated configs).** Modeled the JOINT `(margin, total)` ONCE → derived all 3 markets (H2H=P(margin>0)/spread=P(margin>line)/total=P(total>line)). **The full 180-feature matrix does NOT beat the P1.2 strength-prior alone:** tuned xgb (0.0192)/lgbm (0.0204) edge the strength reference (0.0242) on RAW score but **full-search PBO 0.648 / best DSR 0.007** → don't survive deflation. ⭐ **read per E2.1-r: a HIGH PBO over a TIED field (top-15 span 0.0192→0.0226, ~17%) IS THE NULL, not overfitting — "which tied learner wins" is noise → the strength-prior choice is now PROVEN, not assumed.** **SHIPPED MODEL: `ridge / strength_only / strength_posterior`** (the P1.2 prior + posterior-predictive σ), calibrated on 6,024 OOS games 2018–25: margin calib_80 0.800 PIT-flat ✅ (drives H2H+spread), total calib_80 0.802 (floor PASS) but PIT-flat mild FAIL (0.0218 vs ~0.02 — accepted for a market-blind product dist; flag for refit if P1.5 wants a sharper total tail), H2H Brier 0.1814. **EDGE = clean null: ATS 0.496 ≈ placebo 0.497, both < 0.5238 breakeven → `best_alpha=0` holds** (forward-CLV can't exist pre-season → P0.6b in-season follow-on).
✅ **BOTH PM INJECTIONS VALIDATED:** (1) posterior-predictive — ⭐ it FIXED A FAILURE THE AGGREGATE METRIC HID: homoscedastic Gaussian under-covered early season (wk1-2 calib 0.785 < floor) but 4,557 late games swamped 684 early → aggregate PIT looked fine; slicing by week exposed it, `strength_posterior` (σ_g²=σ₀²+k²·strength_var, k≈0.5–0.57 = real propagation) fixed it (wk1-3 PASS) at ZERO aggregate cost → **a small-N slice is now a first-class gate, and early season is where the NCAAF book is SOFTEST.** (2) cold-start VERIFIED (season-forward CV = E13.7 analog; 100% of wk-1 eval games carry NULL in-season features, intervals honestly ×1.06 wider — correct, not a weakness). (3) **P1.5 interface READY: `ncaaf_game_predictor.sample_matchup(...)`, served params carry σ₀ + k separately so the season sim draws strength once/season + calls `fixed_strength=True` (σ₀ only) → no double-count** (exactly the P1.5 once-per-season discipline).
🧭 **REUSABLE FINDINGS → NFL/NCAAB (propagated):** (A) ⭐ **WIDE-SUPPORT calib hygiene is the OPPOSITE of discrete F5 baseball:** the MLB inflation (inclusive-integer coverage inflates a correct LOW-MEAN count model's calib_80 to 0.82–0.86) is a LOW-MEAN effect; NCAAF margin/total are WIDE-support (σ≈16) → a correct oracle covers ~0.80 EXACTLY, no inflation → a STRICT ≥0.80 floor would REJECT a perfect oracle on sampling noise → add a small deflationary tolerance (`_CALIB_FLOOR_TOL=0.02`); metric stays PIT-only; oracle-floor guard verified. **⇒ NFL game-line (N1.1) margin/total are ALSO wide-support → use THIS nuance, NOT the F5-discrete one; NFL props (N1.2) stay discrete → F5 lesson there.** (B) small-N/early-season slice = a first-class gate (aggregate PIT hides it). (C) 🧵 **thread-cap native-C++ learners in a fan-out search** — CatBoost uncapped BLAS×learner turned a 20-min run into 5 HOURS; the caps existed (`NCAAF_P1_4_THREADS` + `--n-jobs`, from E2.1-r) but weren't set. Fast gate 1742✅. ⏭️ **UNBLOCKS P1.5 (futures — interface ready) + P0.6b (forward-CLV once the season opens).** (Orig prompt ↓.)
─────────────────────────────────────────
▶ NCAAF-P1.4 — THE GAME-MODEL BAKE-OFF (3 TARGETS: H2H moneyline · spread/margin · total points)   [Model · 🧭 OPUS · Phase 1 · the §0.5 discipline, full deflation]   (operator-scoped 2026-07-13)   ← ⭐ **THE NCAAF FINISH LINE; all 4 deps DONE (P1.1/P1.2/P1.2b/P1.3)**
🟢 **CODE-COMPLETE + SMOKE-VERIFIED (2026-07-22) — heavy §0.5 search + CLV leg are OPERATOR-RUN.** Shipped `models/ncaaf_game_distribution.py` (pure joint (margin,total) core: forms gaussian/student_t/native/count + held-out σ/ρ/dof/r calibration [recalibrates P1.2's too-tight `strength_margin_sd`, E13.6] + derive-3-markets + oracle-floor guard) and `models/bakeoff_ncaaf_game.py` (one-pull assemble + the CLV odds↔matrix `game_id` staging join done + verified; season-forward DATE-purged folds; 5 learners × 4 contracts [`strength_only` = the P1.2-prior FOIL] × 4 forms; Optuna; PBO/DSR decide; finalize + PIT gate + vs-close CLV eval) + `betting_ml/tests/test_ncaaf_game_distribution.py` (11 fast-gate guards, GREEN). ⭐ NCAAF-specific selection-metric-hygiene result: wide-support margin/total ⇒ a correct oracle covers ≈0.80 (NO discrete inflation) ⇒ the calib_80 floor carries a sampling tolerance (deflationary), metric stays PIT-only. **PM nudges folded in:** (a) POSTERIOR-PREDICTIVE form `strength_posterior` propagates the recalibrated per-game P1.2 strength posterior (σ_g²=σ0²+k²·strength_var) — fixes the early-season under-coverage the homoscedastic σ hid (wk1-2 margin calib 0.785→0.804); (b) COLD-START AC: Week 1–3 validated separately (floor PASS; wk-1 intervals honestly wider; 100% no current-season peeking — season-forward CV = E13.7 cold-start); (c) P1.5 FUTURES INTERFACE exposed via `models/ncaaf_game_predictor.py` (`sample_matchup(..., fixed_strength=)` + the strength-decomposition contract so a season sim draws strength once/season without double-counting). Real CLV: ATS 0.497=placebo / O/U 0.524, both < breakeven → clean game-line null, best_alpha=0. Design + operator run-plan: `ablation_results/ncaaf_p1_4_game_model.md`. **Operator: run stages 0→4 on the LAPTOP (SF-free, off the MLB lane); assemble needs `AWS_DEFAULT_REGION=us-east-2` + read creds.**
🟢🟢 **PM FINAL CONFIRM-PASS (2026-07-21) — CLEARED, with 4 additions the prompt below is missing:**
 **(1) 🕒 FORWARD-CLV CANNOT EXIST PRE-SEASON — do NOT read the gate as blocked on it.** NCAAF's 2026 season opens ~Aug 2026 → there is NO live slate to accrue forward-CLV on during the build. ⇒ **the SHIP bar for P1.4 is the OFFLINE deflated vs-close eval (2020–2025 historical CLV, PBO<0.2/DSR≥0.95/FDR) + calibration (PIT/reliability).** Forward-CLV is an IN-SEASON confirmation that accrues AFTER kickoff (and depends on P0.6b's recurring capture keeping the benchmark fresh) — it is NOT a pre-season deliverable. Build + gate + ship the honest calibrated model now; the forward-CLV window opens in the fall. (The MLB "positive forward-CLV window" language below is the STANDARD, but MLB is in-season — for NCAAF pre-season, historical-deflated-CLV + calibration is the gate, forward-CLV is the follow-on.)
 **(2) 🔑 THE ODDS↔MATRIX JOIN IS A CROSS-SOURCE JOIN — VERIFY THE KEY ON THE REAL LAKE (the P1.2b dead-bridge lesson).** You OWN building the CLV staging mart: `odds_ncaaf_historical` (P0.6, keyed by commence_time + teams/event) ⋈ `feature_ncaaf_pregame_matrix` (keyed by game_id / (season, home, away)). These are DIFFERENT number spaces → match-count the join on the real build before trusting it; a wrong key silently drops rows. Expect **2020–2025 coverage** (odds floor 2020) and the **2 known no-close games** (P0.6's CFBD-vs-OddsAPI kickoff disagreement) to drop — that's fine, not a bug. Enforce `snapshot_ts < commence_time` (leakage-safe close).
 **(3) 🎯 INCLUDE THE P1.2 STRENGTH MODEL ITSELF AS A BAKE-OFF FOIL/BASELINE — it's already a strong margin predictor** (MAE 13.0 / 71.7% winner acc, ranking-blind). The bar isn't "beat a naive baseline," it's "does the full feature matrix beat the strength prior alone (and strength + market)?" A candidate that doesn't beat strength-only isn't earning its complexity. (`hierarchical.py` is also reusable as a candidate class — noted below.)
 **(4) 🖥️ OFF-BOX / off the MLB serving lane** — reads the cached P1.3 parquet / S3 Delta (SF-free, dbt-duckdb `sports_dbt`), so it's parallel-safe with everything MLB/E11.20. Heavy fits (≥3 classes × Optuna × purged CV, but NCAAF is only ~9k games so lighter than MLB) → operator-run if >1-min; state laptop-vs-box per command.
🟢🟢 **P1.3 DELIVERED THE INPUT — the exact matrix contract (from the P1.3 handoff 2026-07-21; READ before building):**
 • **INPUT = `feature_ncaaf_pregame_matrix`** — a dbt-duckdb mart, cached to parquet + landed in **S3 `ncaaf/derived/feature_pregame_matrix`** (Delta, 2014–2025). **9,086 FBS-vs-FBS games × 200 cols; grain = PER-MATCHUP** (one row, `home_*`/`away_*` side by side — matches your JOINT-distribution design). Read the cached parquet / S3 Delta directly (the one-pull rule); zero setup.
 • **TARGET = the 6 `label_*` columns; NEVER feed a `label_*` as a FEATURE** (they're prefixed exactly so `select home_*/away_*` can't leak them). **Train on `label_is_completed = true` ONLY** — unplayed rows carry NULL labels + are for serving.
 • 🚨 **CV TIME AXIS = `season_order_week`, NOT raw `week`** (already flagged below — the single most important carry-over; postseason `week`=1 collision breaks the embargo silently).
 • **EFFECTIVE TRAINING WINDOW = 2015+ for features** (2014 has NO strength — P1.2 emits 2015+; 2015 is thinly calibrated `_hyper_prior_seasons=1` → down-weight or drop) **AND 2020+ for the vs-market/CLV leg** (P0.6 odds floor is 2020). So the FORWARD-CLV eval window is 2020–2025; the outcome-only training can use 2015+.
 • **NULL = unknown, kept NULL (never imputed to 0)** — the imputation choice is YOURS (the learners'). ⚠️ **`{home,away}_portal_data_covered` boolean is the honest pre-2021 gate — `portal_net_count` is a REAL 0 pre-2021 (feed didn't exist), NOT null → do NOT read a 0 as "no churn"; respect the covered-boolean.**
 • **`strength_margin` for the MARGIN leg (already sign-correct); the `strength_offense`/`strength_defense` split feeds the TOTALS leg (SIGN TRAP: defense = points PREVENTED → net = SUM, not difference — see below).** `strength_margin_sd` = parameter uncertainty (recalibrate; below). **Freshman prior is WEAK (P1.2b DSR 0.821)** — let ablation weight it.
 • 🔒 **the matrix is leakage-safe BY CONSTRUCTION + gated** (`assert_pregame_matrix_is_point_in_time`, HALT, date-based, proven-to-fail) — but P1.4's OWN CV folds are where a NEW leak enters (the CV-axis point above); the matrix gate does NOT protect your fold construction.
 • 📝 **DOC CORRECTION (supersedes my earlier P1.1-update "drop travel/altitude"): venue lat/long ARE staged** → P1.3 shipped `away_travel_km` + `away_altitude_change_m` (coverage-flagged; neutral-site geo NULL). Travel/altitude ARE available — the "no venue" gap was wrong. (Same for NFL/NCAAB: verify venue geo on the real lake, don't inherit the "no venue" assumption.)
📏 **SELECTION-METRIC HYGIENE — CARRY FROM MLB E2.1-r (2026-07-20; P1.4's TOTALS leg is a DISCRETE count predictive, so this bites here):** ⚠️ **interval-coverage targets are BIASED for discrete predictives** — inclusive integer interval bounds inflate a CORRECTLY-specified points model's coverage to ~0.82–0.86, so a metric like `|calib_80 − 0.80|` silently **REWARDS UNDER-DISPERSION** (it briefly ranked an under-dispersed candidate #1 in MLB). ⇒ **gate the totals/margin distributions on RANDOMIZED-PIT FLATNESS (max-decile-deviation), and keep `calib_80 ≥ 0.80` as a FLOOR, never a target to minimize distance to.** ⭐ **AND SANITY-CHECK THE SELECTION METRIC AGAINST AN ORACLE FLOOR** — score a known-correct oracle and assert nothing beats it; a candidate scoring better than the oracle is mathematically impossible and is the tell that the metric is inverted (guard: `test_oracle_is_the_scoring_floor`). 🧮 **Also read PBO correctly:** if the top candidates genuinely TIE, a HIGH PBO is the NULL (noise over which tied model wins), not a failed search — the discriminator is the SPREAD, not the PBO alone.
🔧 **POST-P1.2 UPDATE (2026-07-20) — P1.4 OWNS a recalibration P1.2 deliberately did NOT do:** ⚠️⚠️ **`strength_margin_sd` is PARAMETER uncertainty, NOT a calibrated predictive interval — ~1.5× TOO TIGHT if used directly to price a game. P1.4 MUST recalibrate it on HELD-OUT data (the MLB E13.6 pattern) before any interval/probability is derived from it.** Use `strength_margin` as a point feature; treat the raw sd as relative confidence only. ⚠️ **SIGN TRAP: the P1.2 offense/defense split (which feeds the TOTALS leg) defines defense as "points PREVENTED" → net strength is the SUM, not the difference.** ✅ P1.2's own validation is vs REALITY only (MAE 13.0 / 71.7% winner acc, walk-forward OOS) — **whether the strength feature beats a CLOSING LINE is P1.4's question**, under full §0.5 deflation. `best_alpha=0` holds until this story's gate says otherwise. ⭐ P1.2's solver `hierarchical.py` is sport-agnostic — reusable if P1.4 wants a hierarchical candidate in the bake-off.
🔧 **POST-P1.1 UPDATE (2026-07-20) — one correctness-critical wiring + two facts:** (1) 🚨🚨 **the purged/embargoed CV TIME AXIS = `season_order_week`, NOT raw `week`** — the prompt says "CFB weeks = the time axis"; if the CV folds/embargo order by raw `week`, the postseason-week=1 collision puts January's playoff games in the SAME fold as September's week 2 → the embargo is broken and the deflation gate is computed on leaked folds (silently). This is the single most important P1.1 carry-over for P1.4. (2) the market benchmark (P0.6 `odds_ncaaf_historical`, 2020–2025 closing lines) + its P1.4-owned CLV staging mart (the `snapshot_ts < commence_time` filter) is the vs-market/CLV leg — build that flatten here. (3) **2014 history floor** bounds the training window; the P1.3 feature matrix is the input (read its cached parquet, one-pull rule).
🎯 GOAL: the NCAAF game model, built with the SAME MLB bake-off discipline (§0.5) — NOT a single architecture. 3 markets: H2H (moneyline/win-prob), spread (margin), total (points).
Read: MLB §0.5 (the bake-off + deflation discipline) + E2 (the per-side JOINT distributional pattern) + the P1.3 feature parquet.
DO: ⭐ **DESIGN LEAN — model the JOINT scoring distribution ONCE, DERIVE all 3 markets** (predict the margin+total [or home/away points] distribution → H2H = P(margin>0), spread = P(margin>line), total = P(total>line)) — mirrors MLB E2 per-side→derive-everything, keeps the sides↔totals correlation honest, and is more coherent than 3 unlinked target models. The bake-off decides joint-vs-separate on the metric. **§0.5 DISCIPLINE (non-negotiable):** pre-register ≥3 candidate model classes per the target, Optuna-tune each, feature-ablate, pick on the metric under **purged/embargoed CV** (CFB weeks = the time axis), guard with **PBO<0.2 / DSR≥0.95 / FDR** counting every config. 🔒 **HONEST FRAME:** `best_alpha=0` until it clears the gate + a positive forward-CLV window (the MLB standard — no win-rate/edge claim on an unproven model; CLV is the leading indicator). A clean null is a valid deliverable. Calibration (PIT/reliability) is product value regardless of edge.
GATE/AC: the pre-registered ≥3-class bake-off per market (or the joint model + derivation), Optuna + ablation, on purged/embargoed CV, PBO<0.2/DSR≥0.95/FDR; calibrated 3-market probabilities (PIT-flat); honest framing (`best_alpha=0` unless the gate clears); the winner + a trustworthy null both acceptable outcomes. Operator handoff + `git add`.
```
```
▶ NCAAF-P1.5 — FUTURES / SEASON-SIMULATION: National Championship + Conference titles (⭐ where full-season MC is the RIGHT tool)   [Model + product · 🧭 OPUS · Phase 1 · downstream of P1.4 · operator-requested 2026-07-21 (Bovada offers these)]
✅✅ **DONE + HELD-OUT-CALIBRATED (2026-07-24; the whole build ran LAPTOP-side in ~6s — vectorized, well under the 1-min rule).** Shipped `models/season_simulation.py` (the PURE posterior-predictive season MC — no IO, fully unit-tested), `models/run_season_simulation.py` (driver: strengths+schedule from the lake → board + held-out calibration), and `betting_ml/tests/test_ncaaf_season_simulation.py` (10 fast-gate guards, GREEN; fast gate 1770✅). **THE METHOD (all 4 steps, exactly as scoped):** (1) draw each team's strength ONCE/season from its P1.2 week-1 posterior (reused across the schedule = the honest correlation); (2) simulate every game via `sample_matchup(..., fixed_strength=True)` — σ₀ ONLY, NO double-count; (3) conference standings → a simulated neutral CCG between the top-2 → the **2026 12-team CFP (STRAIGHT SEEDING — the 2025-26 rule change, VERIFIED current: 5 champ AQs [4 Power champs + best G5], top-4 byes, 5v12…8v9)** simulated to a champion; (4) count frequencies → P(conf)/P(bye)/P(CFP)/P(final)/P(natty)/E[W] per team. ⭐ **THE MEAN MAP is faithful to the served ridge/strength_only model** (verified on the real matrix: residual sd 16.4 vs served σ 16.1, 72.3% winner acc): `μ_margin = HFA + (sm_home−sm_away)`, `μ_total = 2·base + (off_h+off_a) − (def_h+def_a)` (⚠️ SUM-not-difference for defense). 📊 **HELD-OUT CALIBRATION (2016–2025 pre-season, 2015 dropped = P1.2 thin-seed noise):** **9/10 champions were pre-season TOP-4** (only miss = the historic 2025 Indiana shock, rank ~18) from a MARKET-BLIND board with no ranking input; expected-wins MAE **1.64** (bias ~0); conference-title Brier-skill **+0.05** (>0 skillful), reliability well-calibrated with mild mid-bin over-confidence → a `--strength-sd-scale≈1.3` E13.6 knob marginally improves it (natty prefers 1.0 → **1.0 ships as the honest default = draw straight from the posterior**). **HONEST FRAME: `best_alpha=0` — calibrated title odds are PRODUCT value; the de-vig-vs-Bovada edge leg is a SCAFFOLD (`--futures-csv`) because historical futures odds were never captured (P0.6 = game lines only), and futures CLV is season-long → verdict is a multi-season backtest, not a quick read.** ⚠️ **NO LIVE 2026 BOARD YET** — the 2026 schedule + 2026 week-1 strengths don't exist until the season nears (lake has games/strengths only 2015–2025); re-run `--season 2026` when they land (nothing else changes). ⭐ **`season_simulation.py` is SPORT-AGNOSTIC** (posteriors + schedule + `CfpFormat` + game-dist params) → **N1.1 Super Bowl / division / conference futures = the same engine on the NFL game model** (noted in N1.1). Report: `ablation_results/ncaaf_p1_5_season_simulation.md`. Operator: heavy runs are actually cheap here (`--calibrate --season 2024 --n-sims 20000` ≈ 6s). Orig prompt ↓ (still current for the 2026 re-point).
🟢🟢 **UNBLOCKED — P1.4 DONE 2026-07-22 delivered the exact interface: `ncaaf_game_predictor.sample_matchup(...)` with the served params carrying σ₀ + k SEPARATELY, so the season sim calls `fixed_strength=True` (σ₀ only) after drawing each team's strength ONCE per simulated season → no double-count** (exactly this story's once-per-season discipline). The game model is `ridge/strength_only/strength_posterior` (margin calib 0.800 PIT-flat; ⚠️ the TOTAL leg has a mild PIT-flat residual — fine for win/loss sims, but if a total-derived futures market wants a sharp tail, flag P1.4 for a total-leg refit). ⏰ **TIMELY: pre-season championship/conference futures are LIVE on Bovada NOW → a pre-season board off the P1.4 priors is the near-term product moment.** Prompt below is current. ⚠️ NCAAF is STRICTLY a betting surface (operator 2026-07-22) → this is a betting/product futures story, not fantasy.
🎯 WHY: Bovada (+ others) post **National-Championship winner** + **each Conference-championship winner** futures. These are SEASON-LONG outcome probabilities — you CANNOT derive P(win the conf/natty) analytically (schedule structure + standings + tiebreakers + CFP selection) → **a full-season Monte-Carlo on top of the P1.4 game model IS the tool.** ⭐ Unlike a game-level bottom-up sim (E13.2 = NULL), SEASON simulation is the genuinely-right MC application: the P1.4 per-matchup predictive distribution is the engine, run the whole season thousands of times, count championship frequencies. Product value (a pre-season + weekly-updated title-odds board = high-search evergreen content, the honest-analytics + Action-Network-competitor wedge) AND a gated edge probe.
🔬 THE METHOD (posterior-predictive season sim): (1) DRAW each team's strength ONCE per simulated season from its P1.4/P1.2 POSTERIOR (⚠️ **once per season, reused across ALL that team's games — NOT re-drawn per game**; this is what makes the futures distribution honest — a team that's genuinely good in a given sim draw wins more of its schedule that sim, which is the real correlation structure). (2) SIMULATE every remaining game with the P1.4 posterior-predictive game model → win/loss + scores. (3) APPLY the deterministic bookkeeping: conference standings + **tiebreakers** + the conference championship game, then the **12-team CFP bracket + selection** (committee behavior is fuzzy → approximate with a transparent seeding heuristic; state the assumption). (4) COUNT frequencies over N sims → P(win conf) / P(win natty) per team. Re-run from the CURRENT state each week → live-updating futures as strength tightens (Week-1 board runs off pre-season priors — honestly wide — per the P1.4 cold-start capability).
🔒 HONEST FRAME + EDGE DISCIPLINE: futures carry a **HIGH hold (20–40% on a big board)** + are brand/public-shaped → `best_alpha=0` applies HARD: our sim P(win) vs the DE-VIGGED futures price; only a deflation-gate survivor (PBO/DSR/FDR over teams×markets×seasons) is an "edge." ⭐ known soft spot to TEST (not assume): **favorite-longshot bias** (longshots systematically overbet). Futures CLV is season-long → the honest verdict is a multi-season historical backtest + forward-track, NOT a quick CLV read. **Calibrated title odds are PRODUCT value regardless of edge** — ship the board honestly framed either way.
🖥️ Off-box modeling; reads P1.4's game model + strength posteriors (so P1.4 MUST expose the posteriors + a per-matchup predictive-sample fn — see the P1.4 injection). ≥N-sim runs are >1-min → operator-run. ⚠️ needs the CFP/conference structure encoded (2026 12-team CFP + the current conference alignment post-realignment — verify on the real `dim_ncaaf_conference`).
🔎 **TWO PRESENT-DAY-FACT VERIFICATIONS BEFORE BUILDING (don't code from this prompt's or your own memory — both changed recently):** (1) ⚙️ **the exact `models/ncaaf_game_predictor.py` signature** — this prompt names `sample_matchup(...)`/`fixed_strength=True` from the P1.4 recap, but READ the actual module first (the fn name/args/param layout of the served `ridge/strength_only/strength_posterior` params may differ) and code to what's there. (2) 🏆 **the CURRENT 2026 CFP format + SEEDING rules** — the 12-team bracket's seeding was CHANGED after its first year (the "5 highest-ranked conference champions auto-qualify + straight-seeding vs top-4-bye" question moved between 2024→2025→2026); the bracket/bye/selection bookkeeping is CORE to the sim, so confirm the rules that apply to the 2026 season from a current source, don't assume the inaugural format. State the encoded ruleset + the committee-seeding heuristic as an explicit, swappable assumption.
📏 **CALIBRATION-VALIDATION REALITY (set expectations): champions are RARE events** — ~10 held-out seasons gives only ~10 natty outcomes, too thin to calibrate the title market alone. Calibrate P(win) across the FULL teams × markets × seasons bucket set (every team's conf-title + playoff-berth + natty probability vs its realized 0/1), lean on the denser conference/berth markets, and treat the natty-specific read as directional. This is the honest-uncertainty framing, not a weakness to hide.
GATE/AC: a season-sim producing calibrated P(win conf)/P(win natty) per team from pre-season AND any-week state; validated on held-out seasons (does the sim's title-odds calibrate vs realized outcomes); vs-Bovada de-vig comparison under the deflation gate (honest edge verdict, null acceptable); the title-odds board shippable as an honest product surface. ⭐ **NFL analog = the same season-sim on N1.1 → Super Bowl / division / conference futures** (noted in N1.1). Operator handoff + `git add`.
```
```
▶ NCAAF-P1A — COLLEGE→NFL TRANSLATION MODEL (the NFL feeder — E7 MiLB→MLB analog)   [Model · 🧭 OPUS · Phase 1 · ⭐ the HIGHEST near-term ROI · parallel track to the P1B game-model chain]   (operator-scoped 2026-07-13)
✅ **DONE + RAN END-TO-END ON THE LAPTOP (2026-07-22).** 3,804 projections (draft classes 2016–2026, incl. the freshly-ingested 2026 incoming class) landed in `s3://credence-sports-lakehouse/ncaaf/derived/nfl_rookie_projections`; dbt view `ncaaf_nfl_rookie_projections` built; all gates green (grain unique, PBO 0.000, DSR 0.994). 📊 HONEST VERDICT: college→NFL is a REAL-but-WEAK signal (beats the position-mean null OOS 0.7901<0.8107, consistently) but the **draft slot DECISIVELY beats it (0.6417 vs 0.7901)** → NOT a standalone rookie board; its value is the RESIDUAL (college prod vs draft slot) for N1.2/N1.3 to COMBINE with slot. Signal concentrates at skill positions (RB/TE/QB/WR); DL/LB/DB ≈0; combine+pedigree add no signal. 🗓️ The 2026 draft was ingested this session (raw feeds had stopped at 2025) — see the ANNUAL-REFRESH chain in mart-inventory §6.16. Mirrors the P1.2b sibling exactly. SHIPPED: substrate mart `ncaaf_draft_college_production_pairs` (xref ⋈ P1.1 college production on `college_athlete_id = fact.player_id` ⋈ combine ⋈ recruiting pedigree; leakage-safe, `target_*` = LABEL) · harness `models/college_nfl_translation.py` (§0.5 bake-off: ⭐partial-pool via `hierarchical.py` REUSED / stratified-OLS / GBM / position-mean null + a reported-not-selected draft-slot benchmark; leave-one-DRAFT-CLASS-out expanding CV; PBO/DSR; oracle-floor; EB variance-collapse cure inherited) · runner `models/run_college_nfl_translation.py` (emits `gsis_id`-keyed `projected_nfl_z`+`_sd`; ⭐ prints the xref→college-production JOIN-COVERAGE per PM #4) · output view `ncaaf_nfl_rookie_projections` (`ncaaf_p1a` tag) · fast-gate `test_ncaaf_college_nfl_translation.py` (17 tests, incl. leakage-by-class verified to FAIL on tamper). TARGET=`target_w_av` standardized within (position, draft class); UDFAs excluded-from-train-but-emitted; 2015 seed un-emitted. Uncertainty = PARAMETER (N1.2 MUST recalibrate). ⏭️ Operator (⭐ ON THE LAPTOP — off-season, keep it off the box; the bake-off is pure laptop compute + the sports lake is a separate S3 bucket, so zero MLB-pipeline contention). 🚨 Build with **dbt-core (`python -m dbt.cli.main`), NOT `dbtf`** — fusion segfaults on 2+ delta_scan models. All `SPORTS_LAKE_REGION=us-east-2`: `python -m dbt.cli.main run --select ncaaf.staging --threads 1` → `python -m dbt.cli.main run --select ncaaf.marts --exclude tag:ncaaf_p1a --threads 1` → `run_college_nfl_translation.py --duckdb quant_sports_intel_models/sports_dbt/sports.duckdb --s3` → `python -m dbt.cli.main run --select tag:ncaaf_p1a --threads 1`. See §6.16 + `ablation_results/ncaaf_p1a_college_nfl_translation.md` (generated by the run).
🟢🟢 **PM FINAL CONFIRM-PASS (2026-07-22) — CLEARED TO RUN NOW (unblocked, independent of the in-flight P1.4; it GATES NFL N1.2/N1.3). 6 lessons from its two SIBLING feeder-MLE stories (E7.3 MiLB-MLE + NCAAF-P1.2b freshman) that post-date this prompt — P1A is the SAME model class, so they apply directly:**
 **(1) 🔬 THE §0.5 BAKE-OFF IS UNDERSPECIFIED BELOW ("≥ a couple candidates" violates the standard) — pre-register ≥3 + a foil, and ⭐ REUSE `hierarchical.py` as the LEAD candidate.** "Position-specific + EB-shrunk for thin position×archetype cells" IS a partial-pooling problem, and P1.2/P1.2b already proved the sport-agnostic `hierarchical.py` solver on exactly this shape → do NOT hand-roll an estimator. The P1.2b candidate set to mirror: (a) ⭐ partial-pooling via `hierarchical.py` (player nested in position, nested in draft-tier), (b) a position-stratified regression foil, (c) a learned GBM, (d) a **position-mean NULL floor**. Optuna-tune; select on a **held-out DRAFT CLASS** under purged CV; PBO<0.2/DSR≥0.95; oracle-floor sanity gate.
 **(2) 🚨 THE EB VARIANCE-COLLAPSE BUG IS LIVE FOR P1A (P1.2 hit it, P1.2b inherited the cure) — thin position×draft-tier cells → the likelihood peaks at `tau=0` = "every player at a position is identical," silently deleting a hierarchy level, NO error.** Copy the cure: boundary-avoiding `Gamma(2,·)` prior on each variance component + multi-start + a regression test that pins it, plus the prior-scale plausibility gate (P1.2's "flat" prior was secretly 1,000-scale + leaked).
 **(3) ⚠️ UNCERTAINTY SEMANTICS — state which kind you emit, because N1.2 (rookie-prop PRICING) is a direct consumer.** P1.2/P1.2b both shipped PARAMETER uncertainty that is too tight to price with. Say plainly whether P1A's "rookie projection uncertainty" is parameter-only (→ a RELATIVE confidence signal; N1.2 MUST recalibrate on held-out before pricing rookie props) or a calibrated predictive interval.
 **(4) 🔑 VERIFY THE COLLEGE-PRODUCTION↔XREF JOIN COVERAGE ON THE REAL LAKE (the P1.2b dead-bridge lesson).** The P0.3 xref join is already built + match-counted (4,211 rows) — but P1A joins P1.1 college-production TO the xref: match-count that on the real build (does every drafted player in the xref carry P1.1 college production? a silently-thin join under-trains the model).
 **(5) ✅ BEHAVIORAL VALIDATION, not green-checkmark:** held-out draft class + a FACE-VALIDITY spot-check (do the top projected rookies match actual early-career stars, the P1.2b Griffin/Underwood + P1.2 CFP-top-5 pattern?) + the oracle-floor gate. Leakage gate verified to FAIL on a tampered row.
 **(6) 📉 EXPECT A ROBUST-BUT-WEAK SIGNAL, and that's a VALID + VALUABLE deliverable (the P1.2b DSR-0.821 precedent) — the NFL draft is famously noisy.** If it's robust (PBO low) but DSR<0.95, REPORT it honestly (don't force/HALT — it's a candidate PRIOR feeding downstream, like P1.2b), because ⭐ **even a weak feeder is high-value HERE: the NFL rookie market is priors-only, so a modest honest projection beats no projection.** `best_alpha=0`.
🔧 **POST-P1.1 UPDATE (2026-07-20):** the college-production features now come from concrete P1.1 marts — `fact_ncaaf_player_game` + `rollup_ncaaf_team_week_opponent_adjusted` (opponent-adjusted efficiency/usage; `ncaaf_mart_inventory.md` §6); window/order final-college-seasons by **`season_order_week`** (not raw `week`); the **2014 floor** is BOTH the draft-class ceiling AND the college-history floor (already noted below). P1A is otherwise the most-current P1 prompt (P0.3-updated).
✅ **UNBLOCKED — P0.3 DONE 2026-07-16, the xref spine is LIVE.** Read it FIRST: the mart is at `s3://credence-sports-lakehouse/ncaaf/marts/xref_college_nfl_players` (4,211 rows; box-verified) + `football/ncaaf/feeder/xref.py`/`README.md`. **START FROM THE XREF — don't rebuild the join.** Key facts P1A inherits: (1) ⭐ **TARGET already in the mart, `target_`-prefixed** (`target_car_av`/`target_w_av`/`target_games`/…) = the P1A modeling target; **NEVER use a `target_*` col as a FEATURE (post-draft = leakage)** — features are college-production + combine ONLY. (2) match provenance is stamped — `match_method` ∈ {`deterministic_slot` (2,796 drafted, high conf), `fuzzy_udfa` (1,390 UDFAs, med/low)}; **weight/segment by confidence** (UDFAs have NULL `target_*` = no draft outcome row → they're for the roster-continuity side, not the drafted-outcome training). (3) training-set size: ~2,796 drafted players 2015–25 resolve to `gsis_id` (99.11%); **combine attach 81.8%, `has_forty` 65.6%** → combine features are PARTIALLY present (impute/flag-missing, don't drop). (4) ⚠️ **P0.1 COLUMN-NAME CORRECTION (cost P0.3 a box bug — do NOT re-hit):** the real `nflverse_players` cols are **`rookie_season`/`draft_pick`/`draft_round`**, NOT the `entry_year`/`draft_number` the P0.1 notes implied. (5) ⚠️ **NaN hygiene: nflverse combine floats are `'NaN'` strings that `try_cast(... as double)` turns into a NON-NULL NaN** → use the P0.3 `_num()` helper (NaN→NULL) or `has_forty`-type counts over-report. (6) box discipline: build with `docker compose run --rm` + bind-mount (the P0.3 pattern) so you don't restart the live Dagster stack mid-E11.20-gate.
🎯 WHY: NCAAF's real near-term value is as the NFL FEEDER — college production + combine → **NFL rookie projections** that power the NFL vertical (fantasy-dynasty boards + rookie props, where the market is otherwise priors-only). This is the football analog of MLB's MiLB→MLB MLEs (Edge **E7**). Runs in PARALLEL with the P1B game-model chain (different consumer: NFL, not NCAAF betting).
🔒 PREREQS: **P0.3 (the draft-slot xref spine — SOLVED, 99.7%)** + P1.1 marts (college production) + P0.3's attached combine measurables. The TARGET is already in `nflverse draft_picks` (`car_av`/`w_av`/`games`/`seasons_started`/career lines — P0.1 §4) — no extra source.
Read: MLB Edge **E7** (the MiLB→MLB MLE — the DIRECT analog to mirror) + `ncaaf_data_inventory.md` §4 (the draft-slot spine + the combine attach) + the P1.1 marts + `../../multi_sport_roadmap.md` §4 + the NFL guide (the consumer).
DO: assemble per-drafted-player features = **college production** (final 1–2 seasons of opponent-adjusted efficiency/usage from P1.1, position-specific) + **combine/measurables** (P0.3 attach) + recruiting pedigree → keyed on the **draft-slot xref** to the NFL `gsis_id`; TARGET = NFL early-career production (from `nflverse draft_picks`). Fit a **position-specific translation** (the E7 pattern), **leakage-safe** (ONLY pre-draft college/combine data → predict the pro outcome — never fold in NFL production), **EB-shrunk** for thin position×archetype cells. Handle: **UDFAs** (no draft slot → the P0.3 fuzzy-match residual, lower-confidence) + the **2014 floor** (~12 draft classes = the training ceiling, per P0.1 §2). §0.5 bake-off discipline for the architecture choice (≥ a couple candidate classes, cross-validated by draft-class, honest). Emit **rookie projections + uncertainty**, position-specific. 🔒 HONEST: a projection/PRIOR (esp. valuable where the NFL market is priors-only), NOT an edge claim.
GATE/AC: a position-specific college→NFL translation validated on a HELD-OUT draft class (leakage-safe — no pro data in the features; predicts the holdout's early-career production with honest uncertainty); UDFA + 2014-floor handled; rookie projections + uncertainty emitted, keyed to `gsis_id`, ready for the NFL vertical (fantasy-dynasty + rookie props). Operator handoff + `git add`.
```
```
▶ NCAAF-P2.1 — GAME-MODEL STRUCTURAL REFINEMENT: a PRE-REGISTERED hypothesis battery (P1.4 was MVP-level; this is the Phase-2 model-quality revisit)   [Model · 🧭 OPUS · 🔬 §0.5 full deflation · post-P1.4]   (operator stress-test 2026-07-23)
🎯 WHY: P1.4's `REFERENCE_STANDS` null is trustworthy WITHIN the conventional search (learner × feature-subset × parametric form) but did NOT try richer MODEL STRUCTURE. The operator stress-tested the model + wants these as a deflation-gated Phase-2 agenda. ⚠️ **VERIFY each vs `ncaaf_game_distribution.py` FIRST — these are read off the model FORMULA (`margin = hfa + (θ_home−θ_away)`, `θ=μ_conf+Zβ+u`, offense/def split), not the code.**
🔒 **DISCIPLINE (non-negotiable — the E1.11/E13.16 mirage lesson): this is a HYPOTHESIS BATTERY → PRE-REGISTER the full set BEFORE any eval; every hypothesis × config counts toward PBO<0.2 / DSR≥0.95 / FDR (a wide battery MUST be deflated or it manufactures a mirage); purged/embargoed CV on `season_order_week`; the gate = beat the P1.4 reference on CALIBRATION (PIT-flat, wide-support floor+tolerance) AND, for any edge claim, clear the deflated vs-close CLV gate.** HONEST PRIOR: MOST WILL BE NULL (P1.4 + MLB efficiency). ⭐ but CFB is genuinely softer than pro markets → the highest-EV places to look = (i) structural gaps the market prices but the model doesn't [HFA, matchup], (ii) where the book is structurally LAZY [early season per P1.4's small-N finding, smaller programs, situational spots]. A clean null is a valid deliverable.
🔬 **THE PRE-REGISTERED HYPOTHESES (tiered by prior EV):**
 **TIER 1 — structural gaps the model provably lacks (highest prior):**
  • **H1 TEAM/VENUE-SPECIFIC HFA** — replace the single constant `hfa` with a partial-pooled team/venue HFA (altitude [Wyoming/AFA/Colorado], crowd, dome, VISITOR travel). The market prices HFA per-venue; a constant misprices the extremes.
  • **H2 MATCHUP-AWARE SPREAD** — the SPREAD/ML uses NET `strength_margin` (offense+defense blended) → misses the offense-vs-defense STYLISTIC matchup (the TOTALS leg already uses the split). Model the spread through the off/def decomposition OR explicit matchup-interaction terms. ⭐ the sharpest market-mispricing seam.
  • **H2b UNIT-LEVEL matchup (finer H2)** — rush-offense vs rush-defense + pass-offense vs pass-defense separately (not just overall off/def), from the P1.1 efficiency splits.
 **TIER 2 — CFB-specific situational spots (the market's lazy seams):**
  • **H3 REST / BYE differential** (off-a-bye vs short-week); **H4 LOOKAHEAD / LETDOWN** (big rival next week; coming off a huge win); **H5 RIVALRY variance** (rivalry games closer/higher-variance than strength implies); **H6 BOWL regime** (opt-outs / coaching changes / motivation asymmetry — the reg-season-fit strength may NOT transfer to bowls → a bowl-specific adjustment or wider intervals).
 **TIER 3 — dynamic / in-season non-stationarity:**
  • **H7 QB-AVAILABILITY regime change** (a team without its starter is a different team — an abrupt strength shift the gradual update misses; ties the P1.3 QB-continuity feature); **H8 RECENCY-WEIGHTED strength** (a team trending up/down — weight late-season games more).
 **TIER 4 — environment / style:**
  • **H9 PACE / TEMPO** (fast-vs-slow controls possessions → total variance + game script); **H10 WEATHER** (wind/cold suppresses passing → lowers totals + disadvantages pass-heavy teams — confirm venue weather is available first).
 **TIER 5 — noise / regression + distribution shape:**
  • **H11 TURNOVER-LUCK regression** (turnover margin is noisy → a turnover-lucky team regresses); **H12 GARBAGE-TIME adjustment** (efficiency/margin distorted by garbage time — is win-prob-gated exclusion applied? P1.1's NFL flag was WP∉[0.05,0.95]); **H13 SPECIAL-TEAMS strength component**; **H14 KEY-NUMBER-aware margin** (football margins mass at 3/7/10/14 → a discrete/mixture margin dist could find spread value the continuous form misses — ties the P1.4 total-tail PIT residual); **H15 HETEROSCEDASTIC/SKEWED margin by strength gap** (big favorites have fatter, skewed tails); **H16 PRESEASON-PRIOR tuning** (the preseason component drives the SOFTEST early-season market → tune its weighting specifically — highest-leverage for the soft window).
📓 Read: P1.2 (`hierarchical.py` + `team_strength.py`) + P1.4 (`ncaaf_game_distribution.py` / `bakeoff_ncaaf_game.py`) + MLB §0.5 + E1.11 (pre-registration discipline) + E13.16 (the mirage/negative-control lesson) + the `ablation_results/ncaaf_p1_4_game_model.md` dossier.
GATE/AC: the pre-registered hypothesis set (BEFORE eval); each tested under purged CV with the FULL battery counted in PBO/DSR/FDR; calibration-vs-P1.4-reference + (for any edge claim) deflated vs-close CLV; an honest dossier — survivors ship (retrain + P1.5 re-point), nulls recorded (a trustworthy null is valid). ⭐ **survivors are SPORT-AGNOSTIC structure → carry H1 (venue HFA) + H2 (matchup spread) to NFL-N1.1 + NCAAB.** Operator handoff + `git add`.
```
```
▶ NCAAF-P2.2 (gated edge — market instruments) — SPEC LATER (post-kickoff): re-point the hardened MLB instruments (E13.16 microstructure/CLV, E1.12 regime-conditioning, E13.14 cross-market) at the SOFTER college markets under PBO<0.2/DSR≥0.95/FDR + game-level correlated-quote collapse. **Props are THIN (P0.1) → sides/totals/microstructure ONLY, not an MLB-style prop engine.** Written from the P1.4/P2.1 handoff once the game model + market coverage are known (mirror MLB's one-fresh-session-per-story flow).
```

---
_Created 2026-07-08. Phase 0 is data-first and re-verifies sources against live endpoints. Data-source facts (CFBD tiers, PFF-paid charting) current as of 2026-07-08._
