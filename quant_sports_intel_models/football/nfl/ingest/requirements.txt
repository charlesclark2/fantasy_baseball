# NFL lakehouse ingest — container deps (NFL-N0.2).
# NOTE: NOT nfl_data_py — it is abandoned (pins pandas==1.5.3, won't build on py3.12).
# nflverse is read as release Parquet DIRECTLY via DuckDB (nfl_data_inventory.md §1).
# Delta pins MATCH the repo root pyproject.toml (E11.20) so train≠serve format drift can't creep in:
# deltalake==1.6.1. (Same pins as the NCAAF-P0.2 ingest image.)
requests>=2.31.0
pyarrow>=14.0.0
duckdb>=1.1.0
deltalake==1.6.1
boto3>=1.34.0
pandas>=2.0.0
python-dotenv>=1.0.0   # CLI loads .env for standalone laptop runs (uv run doesn't auto-load it)
