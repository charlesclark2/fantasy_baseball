# NFL — Story Prompts (per-sport catalog)

**Story-ID scheme: `N<phase>.<story>` (N = NFL — distinct from NCAAF's `P…` and MLB's `E…` so numbers don't collide across sports).**
**Parent roadmap:** `nfl_roadmap.md` · **Guide (the detailed PORT PLAN):** `nfl_guide.md` · **Platform:** `../../sport_data_platform.md` · **Reference impl:** MLB `../../baseball/edge_program/` + the PROVEN NCAAF instantiation (`../ncaaf/`).
**Global conventions:** `uv run python`; DuckDB/S3-native (Snowflake `FOOTBALL_DATA` = reference-for-logic ONLY, never the runtime target); **do NOT `git commit`/`push`** (operator does git — provide `git add <paths>` at closeout); operator provisions secrets/keys; hand >1-min scripts to the operator with **laptop-vs-box + full copy-pasteable command**.
**🚩 TWO MANDATORY CORRECTIONS to the guide's port plan (nfl_roadmap §2):** (1) **NO `nfl_data_py`** (abandoned, won't build py3.12) → read **nflverse release Parquet via DuckDB `read_parquet(<github release url>)`** (validated by the NCAAF feeder). (2) **Orchestrate on the existing Dagster EC2, NOT Lambda+EventBridge.**
**♻️ REUSE the proven NCAAF stack (do NOT reinvent):** the `credence-sports-lakehouse` bucket (`nfl/` prefix), the `quant_sports_intel_models/sports_dbt/` dbt-duckdb project, the `football/ncaaf/ingest/` pattern (`s3io.py` etc. — sport-agnostic), Delta-native, `s3io.storage_options()` for the AKID-safe S3 auth.
**🎯 STRATEGIC FRAME:** NFL = PRODUCT/FANTASY/FEEDER (efficient market → props/CLV/fantasy, NOT head-on edge); NCAAF is the edge play. Data-first.

---
```
▶ N0.1 — DATA-SOURCE EVAL + REFRESH `nfl_data_inventory.md` (START HERE — verify live, correct the staleness)   [Data · 🧭 OPUS · Phase 0]  ✅ DONE 2026-07-17 — inventory v1.0 live-verified (25 nflverse assets + Odds API); source→lake plan LOCKED (§7); by-position free-vs-PFF map (§4); $0 new cost.
🎯 GOAL: lock the NFL sources against the LIVE nflverse releases + Odds API, and REFRESH `nfl_data_inventory.md` (currently reconstructed-from-stale-Snowflake, NOT a live scan). Faster than NCAAF-P0.1: nflverse is already validated (the feeder read it) + the inventory draft exists.
Read: `nfl_guide.md` (the source list + the port plan) + `nfl_data_inventory.md` (the existing 16-table stack to re-home) + `../ncaaf/ncaaf_data_inventory.md` (the format to mirror) + the NCAAF-P0.1 landmines (200-text/html, NaN-to-NaN, the nflverse column-name reality).
DO: (1) enumerate the nflverse release assets we need + confirm each reads via DuckDB `read_parquet(<github release url>)` (a real sample pull, not doc-reading): **player_stats/weekly, rosters, depth_charts, snap_counts, schedules, NGS (passing/rushing/receiving), combine, draft_picks, injuries, PBP (nflfastR), QBR, PFR advanced, FTN**. ⚠️ ground the ACTUAL column names live (the feeder found the NCAAF-P0.1 notes' `entry_year`/`draft_number` were wrong = `rookie_season`/`draft_pick`/`draft_round`; NaN floats need the `_num()` NaN→NULL cure). (2) **The Odds API NFL** (existing sub) — confirm game lines + the DEEP prop markets (passing/rushing/receiving yds/TDs/receptions) + scores + historical depth (for CLV). (3) map the by-position ADVANCED coverage — ⭐ **NFL's advantage: NGS + PFR give the advanced tracking FREE** (separation/CPOE/time-to-throw/YAC) that NCAAF gates behind PFF; note what's free vs any gap. (4) REFRESH `nfl_data_inventory.md`: per source → nflverse asset/endpoint → fields (live) → grain → season coverage → the target lake table; mark the existing dbt marts to PORT (N0.3).
GATE/AC: `nfl_data_inventory.md` refreshed + ground-truthed on live nflverse releases + Odds API (real sample pulls); the by-position advanced coverage mapped (free-via-NGS/PFR vs gap); the source→lake-table plan locked; cost stated ($0 new). ⇒ N0.2 (scaffold) + N0.3 (model port) start from a locked source set. Operator handoff + `git add`.
```
```
▶ N0.2 — LAKEHOUSE SCAFFOLD + FRESH nflverse BACKFILL (brownfield re-pull)   [Data/Infra · 🧭 OPUS · Phase 0 · after N0.1]  ✅ DONE 2026-07-17 — `football/nfl/ingest/` scaffold built (32-source registry: 30 typed nflverse + 2 Odds API; s3io copied from NCAAF + 2 NFL hardenings [null-col→string void-cure, empty-slice skip]; typed-vs-JSON write routing); `sports_dbt` `stg_nfl_*` staging (6 models) + `nfl_delta()` macro. LOCAL Delta smoke green (9 sources incl. wide `pbp`/participation + live odds; dbt build 6 models+15 tests PASS over the tree). Fast gate + dbt compile green. ✅ **BOX BACKFILL COMPLETE — full 1999–2025 on S3, ALL 32 sources, 0 errors** (`stats_player_week` 476k, `pbp` 1.28M, NGS 2016+, PFR 2018+, injuries 2009+; box runtime gate: preflight + backfill + `dbt build nfl.staging.*` 21/21). 3rd landmine cured: cross-season type-drift (VARCHAR≤2015/INT2016+ → registry `str_cols` VARCHAR-pin; needed a DROP+rebuild of `rosters`/`weekly_rosters` per the INC-19 rule).
🎯 GOAL: stand up the `nfl/` ingest → S3/Delta pipeline REUSING the proven NCAAF pattern, and re-pull FRESH nflverse (don't migrate stale Snowflake rows).
🚩 **N0.1 GOTCHAS (inherit — don't re-hit):** (1) use **`stats_player`** (145 cols, current thru 2025), **NOT legacy `player_stats`** (53 cols, caps at 2024); (2) ⚠️ **nflverse column names DIFFER BETWEEN tables** (`players` uses `rookie_season`/`draft_pick`; `rosters` uses `rookie_year`/`draft_number`) → **`DESCRIBE` every asset before mapping, don't assume**; (3) backfill windows per §7: full advanced stack 2016–2025, team/box/roster to 1999; CLV window 2020+.
Read: `../../sport_data_platform.md` §2 + the NCAAF `../ncaaf/ingest/` (the reference — copy `s3io.py`/`sources.py`/`handler.py`/`backfill.py`/Dockerfile) + `nfl_data_inventory.md` (N0.1) + ⚠️ the CLAUDE.md/landmine box gotchas.
DO: build `football/nfl/ingest/` per the NCAAF pattern — `sources.py` = the nflverse-release + Odds-API registry (source → nflverse `read_parquet` URL / fetch fn → season col → table); land raw → `s3://credence-sports-lakehouse/nfl/raw/<source>/season=YYYY/…` as **Delta**; a first `dbt-duckdb` staging layer in `sports_dbt` (`stg_nfl_*`). **Backfill 2015–present ON THE BOX** (in-region multipart, instance-role write — the NCAAF-P0.2 lesson: laptop can't do the large PUTs; laptop IAM is RO). Box-safe: `s3io.storage_options()` (AKID cure), DuckDB region us-east-2 + box-aware memory. Serving target = DynamoDB→S3 (NOT Railway). ⚠️ NGS coverage starts ~2016; PBP deep — scope the backfill windows per N0.1.
GATE/AC: a live ingest of ≥1 nflverse source + Odds API NFL → S3 Delta (a real box run — runtime gate); `stg_nfl_*` staging reads it; the backfill runner does the multi-season pull; repo layout matches `sport_data_platform.md §2`. Operator handoff (box run-order) + `git add`.
```
```
▶ N0.3   ✅ **DONE 2026-07-18 (merged + prod-deployed) — the brownfield dbt IP is PORTED.** 9 new `stg_nfl_*` (typed→plain renames) + **14 marts, all sport-tagged `'nfl'` + point-in-time/leakage-safe** (`dim_player`, `dim_player_role` [Type-2 SCD], `team_week_calendar`, `week_clock_bounds`, `fct_player_week`, the 3 NGS `sat_*` + `sat_snap_counts_weekly`, `mart_opportunity/efficiency_player_week`, `mart_player_season`, `mart_projections_preseason` [fantasy], `dim_nfl_betting` [betting]). Box runtime gate: **PASS=82 (29 models + 53 tests) 69.6s**; validated vs fresh sample (Mahomes 2024 wk1 28/291/1 15.14 PPR, real betting lines, clean SCD-2). 2 port divergences (improvements): `mart_projections_preseason`+`dim_nfl_betting` were hardcoded 2024/25 → made DYNAMIC; `dakota`/qb_efficiency_index absent from `stats_player_week` → carried NULL (the one metric lost, no other dropped). ⭐ **cleared the NCAAF-P0.2 infra flags for the shared stack: dbt-duckdb is NOW on the box image + `sports_dbt_ci.yml` (offline parse+compile) exists → NCAAF-P1.1 EXTENDS this CI, doesn't stand it up.** 2 baked-image-drift landmines (documented): dbt-duckdb needed adding in 3 places (pyproject/lock + box Dockerfile pip list + `up -d --build`); the CI compile needed a secrets-free `--target ci` profile. ⏭️ **UNBLOCKS N0.4 (Odds/injuries) + NFL Phase 1** (the ported `mart_projections_preseason` + `dim_nfl_betting` are the fantasy/betting head-starts). (Orig prompt ↓.)
▶ N0.3 — PORT THE EXISTING dbt MODELS (the IP) → `sports_dbt` over the lake   [Data/dbt · 🧭 OPUS · Phase 0 · the brownfield IP port]  ✅ **UNBLOCKED — N0.2 DONE 2026-07 (lake LIVE: all 32 sources, 1999–2025, 0 errors; 6 `stg_nfl_*` staging models built over S3).**
🎯 GOAL: port the WORKING model logic from the old `jaffle_shop/` Snowflake dbt project to `sports_dbt` (dbt-duckdb over the S3/Delta lake) — the model SQL is the IP; only the source/target adapter changes.
🚩 **N0.2 INHERITED FLAGS (build ON the staging, don't re-hit the ingest landmines):** (1) the lake is **TYPED Delta** (nflverse ships typed release Parquet → columns preserved; staging = plain renames, NOT `json_extract` — the divergence from NCAAF's JSON); build marts over the existing `stg_nfl_*` (schedules/player_week/snap_counts/injuries/players/odds). (2) ⚠️ **`jersey_number`/`draft_number` + `rosters`/`weekly_rosters` are VARCHAR-PINNED** (nflverse types them VARCHAR ≤2015 w/ dirty values like `'79D'`, INTEGER 2016+ → pinned VARCHAR for a stable Delta type) → `try_cast` at the use-site if a mart needs them numeric (the INC-23 pattern). (3) `players` lands at `season=0` (dimension, not season-partitioned); `odds_nfl` is a CURRENT snapshot only (NOT historical closing lines — that's N0.4). (4) ⚠️ **INC-19 rule reconfirmed on the box: a stored Delta type CANNOT change on a re-run → a type change needs an operator DROP + rebuild** (bit `rosters`/`weekly_rosters` in N0.2). (5) use the `nfl_delta()` macro (`sports_dbt/macros/nfl_lake.sql`) to read the lake.
Read: `~/Documents/machine_learning/football/jaffle_shop/` (the existing models — the IP to port) + `nfl_data_inventory.md` §3/§4 (the staging views + refined marts) + MLB `scd2_convention.md` (mirror for the Type-2 SCDs) + the NCAAF-P1.1 dims/facts approach.
DO: port to `sports_dbt` (FBS-style filters N/A — NFL is one league): the **staging** `stg_nfl_*` (1:1 over raw) + the **refined** marts — `dim_player`, **`dim_player_role` (Type-2 SCD)**, `team_week_calendar`, **`fct_player_week`** (the core weekly fact), the **NGS satellites** (`sat_*_ngs_weekly`), `sat_snap_counts_weekly`, `mart_opportunity_player_week`, `mart_efficiency_player_week`, `mart_player_season`, **`mart_projections_preseason`** (the fantasy head-start), **`dim_nfl_betting`** (the betting head-start). Point-in-time / leakage-safe (as-of-week — never fold post-game into a pregame row); sport-tagged (the multi-sport serving/entitlement decision); wire the dbt build into a box Dagster job (dbt-duckdb on the box) + stand up the NFL CI gate (shares the `sports_dbt` CI stood up by NCAAF-P1.1 — extend it). ⚠️ re-validate each ported model against a fresh sample (the old logic was exploratory, not production).
GATE/AC: the staging + refined marts build in `sports_dbt` over the S3/Delta lake, point-in-time-correct + sport-tagged; `fct_player_week` + `mart_projections_preseason` + `dim_nfl_betting` reproduce (validated vs a fresh sample); the dbt build wired into a box job + covered by CI; a real box build (runtime gate). Operator handoff + `git add`.
```
```
▶ N0.4   ✅ **DONE + box-verified 2026-07 — 🏁 NFL PHASE 0 COMPLETE (N0.1–N0.4 all done).** Landed to `nfl/raw/`: closing GAME lines `odds_nfl_historical` (2020–2024, 1,697 snaps, 25 books incl. Bovada, 1,408 games, 100% leakage-safe); closing PLAYER PROPS `odds_nfl_props_historical` (2023–2024, 570 games, 10 books, 10 markets, ~950–1,200 lines/game, true closing ~8–10min pre-kickoff, 0 leakage); injuries 2009–2025 (`report_status='Out'` = the leakage-safe pre-kickoff inactive signal); live feeds on-demand. Credits ~88k spent, ~4.98M left. **⚠️ KEY CONSTRAINTS for Phase 1:** (1) **props have a HARD vendor floor of 2023** (Odds API began props ~2023-05; 2020–22 = 422) → **NFL prop CLV/backtest window = 2023–24 (2 seasons), game-lines 2020+**; (2) Phase-1 CLV marts should STILL enforce `snapshot_ts < commence_time` (belt-and-suspenders — game-lines carry a few harmless over-capture dups); (3) standalone rush-TD/reception-TD props don't exist (folded into `player_anytime_td`) → 10 of 12 markets. Engineering safety: `on_demand` gating excludes the 3 paid per-event odds sources from default backfill (no accidental credit burn); `odds_backfill.py --dry-run` free credit estimate. Fast gate 1444✅ (no dbt touched — the staging over these raws is a Phase-1 story). ⏭️ **UNBLOCKS NFL PHASE 1** (honest surfaces by 9/9). (Orig prompt ↓.)
▶ N0.4 — NET-NEW MARKET + STATUS DATA: Odds API (odds/props/scores) + injuries/inactives   [Data · 🧭 OPUS · Phase 0]
🎯 GOAL: land the data the old stack LACKS — the betting markets + the injury/inactive status that moves NFL lines. This is the net-new betting data (player data was just re-pulled in N0.2/N0.3).
DO: (1) **Odds API NFL** on the N0.2 ingest pattern → `nfl/raw/odds*/` Delta — game lines (h2h/spread/total) + the DEEP prop markets + scores; CAPTURE CLOSING lines with commence-time (leakage-safe close, for CLV — the NFL scoreboard). ⚠️ like NCAAF-P0.6, confirm the Odds-API NFL **historical** floor + credits before a big historical pull (current-vs-historical distinction — don't mis-tag a live feed as closing lines). (2) **injuries / inactives** — nflverse injuries + game-day inactives → `nfl/raw/injuries/` (NFL lines move sharply on these = the CLV/props signal). Point-in-time (an inactive is known pre-kickoff; a mid-week injury report is as-of its date — leakage-safe).
GATE/AC: NFL odds (lines + props + scores, closing snapshot leakage-safe) + injuries/inactives landed to the lake, point-in-time; historical-odds floor + credits documented; feeds Phase-1 CLV/props/parlay surfaces. Operator handoff + `git add`.
```
```
═══════════ NFL PHASE 1 — 3 MODEL VERTICALS + a data-mart FOUNDATION (operator-scoped 2026-07; deadline 9/9, AFTER NCAAF 8/29) ═══════════
🎯 **STRUCTURE (operator):** three model epics — **N1.1 game-line (moneyline/total/spread) · N1.2 props · N1.3 fantasy/projections** — on a shared **N1.0 data-mart foundation** (build the marts BEFORE the models — the "don't become a mess" discipline).
🎲 **BAYESIAN INFERENCE = a core program selling point (operator 2026-07) — bake it in, don't bolt it on:** consistent with MLB's sequential-Bayes team/bullpen posteriors + EB priors + the NCAAF-P1.2 partial-pooling mixed-effects → **every model epic PRE-REGISTERS a Bayesian/hierarchical candidate in its §0.5 bake-off** (partial pooling for small-sample players/teams/rookies, informative priors, **POSTERIOR PREDICTIVE distributions — not point estimates**), and the PRODUCT surfaces the posterior UNCERTAINTY (credible intervals / the full distribution — the honest "here's our estimate AND how sure we are," ties E9.43 conviction + the calibration). Bayesian shrinkage is structurally the RIGHT tool for the small-sample corners (rookies where the prior/feeder dominates, thin-usage players, tiny NFL game samples) → likely competitive, but it earns its place on the metric like any candidate. The honest-uncertainty framing holds regardless of which model wins. 🔒 **HONEST FRAME throughout — TEST OPEN-MINDEDLY, do NOT assume the null (operator pushback 2026-07):** the MLB "market efficient" finding is an informative PRIOR (NFL is heavily bet), NOT a foregone conclusion for NFL — **run the SAME §0.5 bake-off + PBO<0.2/DSR≥0.95/FDR + forward-CLV gate and let the DATA decide.** The deflation gate is NOT biased toward null — it rejects MIRAGES while a REAL edge survives it → so keeping it IS the open mind (it's how a genuine NFL inefficiency would prove itself). **If a candidate clears the gate + a positive forward-CLV window → that's a REAL edge and we ship it (unlike MLB, this isn't pre-decided); a deflation-honest null is ALSO a valid deliverable.** ⭐ **actively LOOK where NFL is most plausibly SOFTER** (the college-thesis analog): thin prop markets on lesser players, backup-QB / high-injury games, news-driven in-season line movement, smaller books — not the heavily-arbitraged main game line. Calibrated probabilities are PRODUCT value EITHER WAY (power the pick/parlay/CLV/fantasy surfaces regardless of edge). ⚠️ Props CLV window = 2023–24 (vendor floor, N0.4); game-lines 2020+.
```
```
▶ N1.0 — DATA-MART FOUNDATION (readiness pre-plan — BUILD BEFORE the model epics)   [Data/dbt · 🧭 OPUS · Phase 1 · the "marts before models" gate]   (operator-scoped 2026-07)
✅✅ **DONE (2026-07-22) — the full team-game + CLV mart layer is BUILT & VERIFIED GREEN on the real lake** (local dbt-duckdb over S3; box runtime gate = the operator's one-time `sports_nfl_dbt_build_job` run, see handoff). **Gap table:** `quant_sports_intel_models/football/nfl/nfl_mart_inventory.md` (N0.3 = player-week only → team-game + CLV were the two holes; both filled). **Shipped (`sports_dbt/models/nfl/`):** 5 staging (`stg_nfl_pbp` 1.28M plays w/ EPA·success·wp·garbage-flag, `stg_nfl_team_week` box, `stg_nfl_historical_odds`, `stg_nfl_props_historical`, `stg_nfl_team_geo` 32-team lat/long+name crosswalk) + 7 marts (`dim_nfl_game` spine w/ travel[Haversine]·dome·weather·rest, `fct_nfl_team_game` 2-rows/game pbp EPA off/def, `rollup_nfl_team_week_asof` [pregame-safe], `rollup_nfl_team_week_opponent_adjusted` [2-pass, **what N1.1 consumes**], `rollup_nfl_team_season`, `mart_nfl_clv_game_lines`, `mart_nfl_clv_props`) + 3 HALT gates (as-of/opponent-adj kickoff-date leakage + CLV `snapshot_ts<commence`). **Build green:** 20 staging + 21 marts + 129 tests, 0 fail; 3 leakage gates PASS on real data. **All 5 readiness lessons cleared:** (1) odds↔schedule join match-counted 1424 events, 0 unmapped names, 0 null game_id; (2) leakage gates DATE/kickoff-based + HALT + PASS; (3) NFL `week` VERIFIED monotone (no postseason reset — used raw `week` as as-of anchor); (4) CD path-filter `sports_dbt/**` confirmed live in `orchestration_cd.yml`; (5) parity spot-check 2024 KC-BAL opener (KC 27-20, def_epa mirrors opp off_epa, travel 1538km, Bovada -145 @ 30min pre-kickoff) + 2024 adj-net-EPA top-5 = BAL/PHI/BUF/DET/MIN + week-1 rows honest-NULL. **Coverage (EXPECTED):** team-game 2020–2025, CLV game-lines 2020–2024, props 2023–2024 (N0.4 vendor floors). ⇒ N1.1/N1.2/N1.3 start on a ready mart layer.
🟢🟢 **PM READINESS PASS (2026-07-21) — CLEARED TO KICK OFF; 5 cross-cutting lessons from the NCAAF/MLB sessions that post-date this prompt (fold them in):**
 **(1) 🔑 VERIFY EVERY JOIN KEY + ROW COVERAGE ON THE REAL LAKE (the P1.2b dead-bridge lesson — a documented key can be wrong + ship a silently-EMPTY mart, CI-green).** N1.0's riskiest joins are cross-source: the **CLV/odds mart = `odds_nfl_historical`/`_props_historical` (keyed commence_time + teams/event) ⋈ the team-game / player-week marts (keyed game_id / team-week)** — DIFFERENT number spaces. Match-count each join on the real build before trusting it; a wrong key silently drops rows. Expect **game-lines 2020+, props 2023–24** (the N0.4 vendor floors) — coverage below that is EXPECTED, not a bug.
 **(2) ✅ THE LEAKAGE GATE MUST BE DATE-BASED + PROVEN-TO-FAIL + HALT-tier (the P1.1/P1.2/P1.3 standard — "leakage spot-check" below is too weak).** Every point-in-time mart needs a gate that uses each game's own KICKOFF as the boundary (not a `week < W` filter), wired HALT into the box job, AND **verified to actually FAIL on a tampered/back-dated row** so its green means something. This is the layer every N1.1/N1.2/N1.3 model trains on — a silent leak here contaminates all three verticals.
 **(3) 🗓️ CHECK THE NFL WEEK-ORDERING for a postseason trap BEFORE using `week` for any as-of window (the P1.1 CFBD postseason-week=1 lesson).** CFBD reset `week` to 1 for the postseason → week-based CV/embargo silently leaked. NFL is *probably* safe (nflverse `week` runs 1–18 reg + 19–22 playoffs, monotone) BUT **VERIFY on the real lake** — confirm playoff weeks are monotone (or that `season_type` disambiguates) and use **game_date/kickoff as the ultimate ordering anchor**, never a raw week that might reset. Do NOT assume.
 **(4) 🚚 CONFIRM THE CD PATH-FILTER INCLUDES `sports_dbt/**` (NCAAF-P1.1 found it did NOT since N0.3 → NFL was EXPOSED).** N1.0 ADDS marts to `sports_dbt` — the exact path that wasn't triggering CD → a model-only change would deploy STALE behind a green run. Confirm the fix (added in P1.1) is live so N1.0's new marts actually deploy; a dbt selector matching nothing exits 0 → keep the non-empty-selector CI assertion.
 **(5) ✅ VERIFY MARTS ON REAL DATA — a fresh-sample parity spot-check + a per-family coverage report** (the N0.3 Mahomes-2024 pattern + the P1.3 coverage report), not just a green build. Venue IS available (NFL schedules carry stadium/roof/surface → dome/travel ARE buildable — don't inherit the "no venue" assumption; the NCAAF "no venue" gap was itself wrong). NULL = unknown, keep NULL. ⭐ the opponent-adjusted team-efficiency mart you build here is what a later N1.1 strength model consumes — `hierarchical.py` (P1.2's sport-agnostic solver) is reusable there, so build the efficiency inputs clean.
🎯 WHY (operator pre-plan): N0.3 ported PLAYER-WEEK marts (`fct_player_week`, `mart_opportunity/efficiency_player_week`, `mart_player_season`, `mart_projections_preseason`, `dim_nfl_betting`) — great for props/fantasy — but the GAME-LINE vertical (N1.1) needs **TEAM-GAME** marts that DON'T exist yet (the ported jaffle IP is player-focused). Build the missing marts so all 3 verticals are supported, ONCE, cleanly.
DO: (1) **AUDIT** the existing `sports_dbt/models/nfl/` marts vs what each vertical needs → a gap table. (2) **BUILD the missing TEAM-GAME marts** (for N1.1): `fct_team_game` (team-game box + result), opponent-adjusted team efficiency (offense/defense EPA/success/explosiveness — derive from `pbp`), pace, situational (home/rest/travel/dome/weather-if-available), point-in-time as-of-kickoff (leakage-safe). (3) **BUILD the CLV/ODDS marts** over N0.4's closing lines (`odds_nfl_historical`/`_props_historical`): a leakage-safe closing-line mart **enforcing `snapshot_ts < commence_time`** (belt-and-suspenders, per N0.4) — the market benchmark + CLV scoreboard for all 3 verticals. (4) confirm the player-week marts (N0.3) cover props/fantasy needs (mostly yes). Sport-tagged; extend the `sports_nfl_dbt_build_job` + the `sports_dbt_ci.yml` gate.
GATE/AC: the gap table + the missing team-game + CLV/odds marts built in `sports_dbt` over the lake, point-in-time (leakage spot-check) + `snapshot_ts<commence` enforced; the 3 verticals' mart needs all met; box build green (runtime gate). ⇒ N1.1/N1.2/N1.3 start on a ready mart layer. Operator handoff + `git add`.
```
```
▶ N1.1 — GAME-LINE MODELS EPIC: moneyline · total points · spread   [Model · 🧭 OPUS · Phase 1 · §0.5 discipline · needs N1.0 team-game marts]   (operator-scoped 2026-07)
🎲 **MONTE-CARLO / SIMULATION GUIDANCE (PM 2026-07-21 — the small-sample question, answered explicitly):** ⚠️ **MC does NOT manufacture information from small samples** — simulating 10k games from a noisy 17-game strength estimate just gives a distribution around a noisy mean. The small-sample problem is a PARAMETER-uncertainty problem → its cure is the **Bayesian partial pooling already mandated** (the hierarchical team-strength posterior; `hierarchical.py` reusable), NOT outcome simulation. ✅ **WHERE MC GENUINELY EARNS ITS PLACE = POSTERIOR-PREDICTIVE SIMULATION (make this the DEFAULT distributional mechanism):** sample team strength from its POSTERIOR (wide, because only ~17 games) → sample a joint (margin, total) [or home/away points] outcome given that strength → repeat → derive ML=P(margin>0)/spread=P(margin>line)/total=P(total>line) off the simulated distribution. This makes the predictive intervals HONESTLY WIDE (accounts for game-variance AND team-strength uncertainty) and **directly fixes the P1.2-class "sd is ~1.5× too tight" trap** — a point-estimate-plus-parametric approach would understate uncertainty at 17 games. It's CHEAP MC (two nested samples), not a heavy engine. 🔬 **BOTTOM-UP DRIVE/POSSESSION SIMULATION = a §0.5 BAKE-OFF CANDIDATE, NOT the default — carry a STRONG prior AGAINST it:** MLB's E13.2 (the bottom-up PA-level Singlearity-style simulator) came back a CLEAN NULL vs the simpler parametric convolution — the expensive sim didn't beat it. So pre-register a drive-level sim as ONE candidate if you want (it can capture game-script/clock correlations a parametric form misses), but do NOT lead with it; make it earn its complexity on the metric. 🏈 **FOOTBALL-DISCRETENESS TIE-IN (E2.1-r hygiene):** scoring is lumpy/discrete (TD 6–8, FG 3) + correlated, NOT smooth-Normal → the outcome form is a semi-simulation of scoring events OR a carefully-chosen discrete/mixture parametric, and the calibration gate is **randomized-PIT flatness (max-decile-dev), `calib_80 ≥ 0.80` as a FLOOR** — never interval-coverage-as-target (it rewards under-dispersion on discrete counts + is EXACTLY the E2.1-r inverted-metric trap). ⇒ **the bake-off's distributional-form axis = {posterior-predictive parametric [default] · posterior-predictive semi-sim of scoring events · full drive-level sim [E13.2-null prior]}, all posterior-predictive so uncertainty is honest.**
🗓️ **WEEK-1 / COLD-START CAPABILITY + FUTURES HOOK (PM 2026-07-21):** ✅ **Week-1 predictions ARE producible with zero in-season games** — the strength model falls back to its PRE-SEASON prior (last-season carryover + the ported `mart_projections_preseason` + the P1A rookie feeder + ⚠️ **the OFFSEASON-CONTINUITY signal — the NFL analog of NCAAF's roster-continuity/portal; N1.0/N1.1 MUST wire it as a first-class Week-1 input** because NFL offseason turnover [FA/draft/trades/scheme] makes last-season a WEAK prior alone). The posterior-predictive MC makes the Week-1 interval honestly WIDE. 🔒 **REQUIRED AC — VALIDATE THE EARLY-SEASON REGIME SEPARATELY (the MLB E13.7 cold-start analog):** Week 1–3 is a DIFFERENT feature regime (priors-heavy, in-season features NULL) than mid-season — a season-averaged calibration HIDES Week-1 quality. Add an AC: eval Week 1–3 calibration on its own, with CV folds that predict Week 1 from **prior-season + pre-season data ONLY** (no peeking at the current season). Surface the higher Week-1 uncertainty honestly (it's the correct answer, not a weakness). 🏆 **FUTURES ENABLEMENT (Bovada's NFL board is RICH — operator 2026-07-21): Super Bowl winner · division winners (8) · conference champ (AFC/NFC to win the SB) · which DIVISION produces the SB winner · FIRST-TIME winner (novelty) · individual PLAYOFF-GAME winners.** ⭐ A downstream SEASON-SIMULATION (the NCAAF `P1.5` analog — its own N-story on top of N1.1) produces almost ALL of these from ONE engine: draw each team's strength ONCE per simulated season from its posterior → play the whole schedule + playoff bracket → count frequencies. **Division/conference/SB/which-division = direct sim frequencies; FIRST-TIME-winner = sum P(win SB) over never-won teams (a derived aggregate); PLAYOFF-GAME winners = the N1.1 game model applied DIRECTLY to the bracket matchups the sim generates** (not a season aggregate). So the season-sim + N1.1 game model together cover the entire board. **⇒ REQUIREMENT ON N1.1: structure the game model so the sim can consume it — expose the strength POSTERIORS + a per-matchup predictive-sample fn; the sim draws strength once/season/team (NOT re-drawn per game — that's what makes futures honest).** Encode the NFL playoff structure (7 seeds/conf, the current 4-division alignment). Futures = HIGH-hold, brand/public-shaped → `best_alpha=0` hard (de-vig, deflation gate, favorite-longshot bias as a to-TEST soft spot); calibrated title/division odds are product value regardless.
🎯 WHY: honest, CALIBRATED game-line probabilities to power the pick surfaces (win-prob lean, E9.24-analog), the parlay-calculator legs (E10.1 analog), and the model-vs-market CLV scoreboard — AND an honest edge TEST (don't assume the null). Calibrated probs are product value regardless; a gate-clearing survivor is a real edge to ship.
DO: ⭐ **DESIGN LEAN (mirror MLB E2): model the JOINT scoring distribution ONCE (margin + total, or home/away points) → DERIVE all 3 markets** — moneyline = P(margin>0), spread = P(margin>line), total = P(total>line). Features from the N1.0 team-game marts (opponent-adjusted efficiency, pace, situational, injuries `report_status='Out'`, QB availability). §0.5: pre-register ≥3 model classes **(one MUST be Bayesian/hierarchical — a partial-pooled team-strength posterior, the NFL analog of NCAAF-P1.2, yielding a posterior-predictive scoring distribution with honest uncertainty)**, Optuna, feature-ablation, purged/embargoed CV (by week), PBO<0.2/DSR≥0.95/FDR counting every config. Calibrated (PIT/reliability per market — the honest number). ⚠️ **tiny samples (~17 games/team) → out-predicting the line is HARDER than MLB → a clean null is the expected, valid deliverable; ship the calibrated probabilities for product regardless.**
GATE/AC: the joint-scoring bake-off → calibrated ML/spread/total probabilities (PIT-flat per market), §0.5-deflated, honest (`best_alpha=0` unless it clears the gate + forward-CLV); feeds the pick/parlay/CLV surfaces. Operator handoff + `git add`.
```
```
▶ N1.2 — PROPS MODELS EPIC: player-prop projections + pricing   [Model · 🧭 OPUS · Phase 1 · §0.5 · the DEEP NFL market · shares player-perf modeling with N1.3]   (operator-scoped 2026-07)
🎯 WHY: player props are the DEEPEST NFL market (N0.4: 10 markets, ~950–1,200 lines/game, 10 books incl. Bovada) — passing/rushing/receiving yards + TDs + receptions + `player_anytime_td`. A real E5-analog props surface: honest calibrated per-prop distributions → P(over/under) at the line + the CLV scoreboard.
DO: per-prop distributional projection (the MLB E2/E5 pattern) off the N0.3 player-week marts + NGS/PFR advanced (all FREE for NFL) + opportunity/usage; §0.5 bake-off per market, Optuna, ablation, purged CV, PBO<0.2/DSR≥0.95/FDR; PIT-calibrated per prop (reliability/ECE). ⭐ **the shared player-performance core is a HIERARCHICAL BAYESIAN model (pre-registered candidate): partial-pool across players within position, informative priors (a thin-usage player / rookie shrinks toward the position/role prior — the feeder prior for rookies), emit a POSTERIOR PREDICTIVE distribution → the props P(over/under) + the fantasy points (N1.3) both read from it, with honest credible intervals.** (Compete it against the non-Bayesian candidates on the metric — but it's the structurally-right tool for the small-sample corners.) ⚠️ **CLV/backtest window = 2023–24 ONLY (the vendor floor, N0.4) → thin; forward-CLV-honest.** Honest: calibrated projections are PRODUCT value even at `best_alpha=0` (E5.4 precedent — the K-prop null still shipped the projection surface). Shares the player-performance modeling core with N1.3 (build the per-player-week projection once, consume for both props pricing + fantasy points).
GATE/AC: per-prop calibrated P(over/under) at the line (PIT/ECE per market), §0.5-deflated, honest framing (`best_alpha=0` unless it clears the gate + forward-CLV on the 2023–24 window); the shared player-week projection reusable by N1.3. Operator handoff + `git add`.
```
```
▶ N1.3 — FANTASY MODELS + PROJECTIONS EPIC (related to N1.2 — both = player performance)   [Model · 🧭 OPUS · Phase 1 · the LARGEST fantasy market + Dynasty · fed by the NCAAF-P1A rookie feeder]   (operator-scoped 2026-07)
🎯 WHY: NFL fantasy is the largest fantasy market. Season + weekly fantasy-point projections + Dynasty (rookies fed by the NCAAF→NFL translation, P1A). Builds on the N0.3 `mart_projections_preseason` head-start + shares the player-performance core with N1.2 (props).
⏰ **NICE-TO-HAVE: 2026 SEASON projections by 8/22 (operator's draft) — a FAST-PATH exists that does NOT need the full N1.2/N1.3 modeling + does NOT delay NCAAF:** N0.3 already ported `mart_projections_preseason` + made it DYNAMIC (populates on current 2026 data) → a first-pass 2026 season projection for VETERANS may already be servable from that mart with light validation/ranking. **ROOKIES need NCAAF-P1A (the feeder) — which is a NCAAF story anyway** (so building P1A serves BOTH the NCAAF value AND the fantasy rookie leg; do NOT delay NCAAF for this). ⇒ 8/22 first-pass = `mart_projections_preseason` vets (fast) + P1A rookies (when P1A lands); the full N1.3 modeling refines it after.
DO: player fantasy-point projections (weekly + season; standard + PPR — the N0.3 PPR calc is validated) from the shared player-week projection (N1.2 core) → opportunity × efficiency × role (`dim_player_role`); Dynasty/rookie board keyed on the **NCAAF-P1A college→NFL rookie projections** (the feeder — where the market is otherwise priors-only, the real differentiator); honest uncertainty; leakage-safe. ⚠️ **DEPENDENCY: the rookie leg needs NCAAF-P1A** (the feeder) — season-vets can ship without it, rookies wait on P1A. §0.5 where a predictive model is selected.
GATE/AC: weekly + season fantasy projections (std + PPR) + a Dynasty/rookie board (rookies from P1A when ready); validated vs a holdout season; honest uncertainty; feeds the fantasy surface. Operator handoff + `git add`.
```
```
▶ NFL Phase-1 HONEST SURFACES (app layer, consume the models) + Phase 2 — SPEC LATER: the parlay CALCULATOR (E10.1 analog) + per-book/CLV transparency (E3) are the app surfaces that CONSUME N1.1/N1.2 (calculator legs = the calibrated probs; CLV = the N1.0 odds marts). Phase 2 = MODEST gated edge (sharp-anchor E4 / props E5 / CLV E3 under the deflation gate) — NFL efficient, expect MLB-like nulls. Written from the model-epic handoffs.
```

---
_Created 2026-07-17. Story IDs = `N<phase>.<story>` (NFL). NFL = product/fantasy/feeder (efficient market); the edge weight stays on NCAAF. Brownfield port onto the NCAAF-proven `sports_dbt`/`credence-sports-lakehouse`/Dagster-EC2 stack. Data facts re-verified live in N0.1._
