# NFL Fantasy / Dynasty — Roadmap (the NFL fantasy vertical)

**Status:** v0.1 — created 2026-07-22 (operator: NFL fantasy has its OWN requirements → its own folder; the COMMONALITY with betting is the week-to-week player projection). Split out of the NFL betting catalog (was `N1.3`).
**Parents:** `../nfl_roadmap.md` (the NFL BETTING sibling under the same `nfl/` sport umbrella) · `../nfl_story_prompts.md` (N-series betting; **builds the SHARED player-week projection this vertical consumes**) · `../../../baseball/fantasy/` (the MLB fantasy sibling — same product line, different sport) · `../../../gtm_strategy.md` (Dynasty = the GTM lead).
**Sibling docs (this folder):** `nfl_fantasy_story_prompts.md` (the NF-series prompts).

---

## ⏭️ CURRENT EXECUTION ORDER — remaining NF stories (operator-set 2026-08-01)
The organizing goal is the fantasy + multi-sport LAUNCH (monetization via E9.45 fantasy-paid; the operator's 8/22 draft = the natural GTM window). Runs as TWO concurrent lanes (one Opus-FF session, one Sonnet session), so the sequence interleaves rather than being one strict line.

**Opus-FF lane (one at a time):**
1. ~~**NF-C0** — platform league import.~~ ✅ **CODE-COMPLETE 2026-08-01.** **SLEEPER SHIPS LIVE-VERIFIED** (public read-only API, zero credentials; the adapter was run end-to-end against a real 12-team superflex dynasty league and its config round-trips byte-identical through `LeagueConfig`). **YAHOO IS BUILT AND BLOCKED ON YAHOO** — see the two findings below. **ESPN = earned NO-GO** (`docs/nf_c0_espn_access_probe.md`). Import saves into the SAME `fantasy_leagues` map NF-C0b writes, as the SAME `LeagueConfig`; provenance rides the storage envelope so the config object is unchanged.
   - ⚠️ **THE STORY'S YAHOO PREMISE WAS STALE AND THE PROBE CAUGHT IT.** The story assumed "~5 console clicks + paste two secrets." Yahoo now gates Fantasy API access behind an **application REVIEW** (`sports.yahoo.com/developer/access/`: submit → review → "we'll reach out with next steps"), and the old `developer.yahoo.com/fantasysports/guide/` **308-redirects** to `sports.yahoo.com/developer`. ⇒ Yahoo import is **code-complete but NOT date-committable** — approval latency is Yahoo's and unbounded. Operator guide: `docs/nf_c0_yahoo_oauth_setup.md` (click-by-click, exact redirect URI, the 3 SSM keys, the IAM grant).
   - ⛔ **ESPN NO-GO, earned on the RED LINE not on fragility** (48% MAU, so the reason matters). The only path to a PRIVATE ESPN league is replaying the user's `espn_s2`/`SWID` **session cookies** — not read-scoped, not fantasy-scoped, not individually revocable, no consent screen, months-long life ⇒ **functionally password-equivalent**, which violates the red line in substance while satisfying it in letter. ESPN publishes no developer program and no OAuth. Independent corroboration: `www.espn.com/robots.txt` carries `User-agent: anthropic-ai / Disallow: /`, so no further automated probing was possible — meaning any ESPN adapter would have shipped on ASSUMPTION, which this story's own discipline forbids. **Re-check next off-season (2027-02).**
   - ✅ **SLEEPER IMPORT CONFIRMED WORKING BY THE OPERATOR 2026-08-01** on a real league (12-team half-PPR). Three follow-ons SPUN OFF from what that first real import exposed — all in `nfl_fantasy_story_prompts.md`:
     - ~~**NF-C0c**~~ (Sonnet, small) — Sleeper player-file ingest + id bridge. ✅ **DONE 2026-08-01.** Rosters/draft picks now render real names — a `gsis_id`-direct bridge (name+position crosswalk fallback for the no-gsis_id skill-position cohort only) published as a slim daily S3 artifact, read narrow+memoized by the backend. Unblocks roster-aware waivers (NF-C5), "my team" highlighting, and any live-draft sync. Full detail in `nfl_fantasy_story_prompts.md`.
     - **NF-C0d** (Sonnet, small) — admin telemetry on CAPTURED terms. The coverage verdict is shown to the user and then discarded, so we cannot see WHICH gaps are common; the first real league had **20 captured terms** and we have no way to tell if that is typical. Aggregate-only (the setting, never the user).
     - **NF-C0e** (Opus, §0.5) — project the commonly-captured terms. ⭐ **The DST yards-allowed tiers are the clear first target**: highest |weight| on the list (+6…−6) and the DIRECT structural analog of NF1.6's shipped points-allowed tiers (expected-games-per-bucket ⇒ a league's tier table applies EXACTLY, not approximately). ⚠️ **Guarded by the NF-D11 degenerate-ceiling rule: a term graduates from captured to applied ONLY if its projection beats an all-league-average baseline on held-out data** — a no-skill projection still moves the board, just on noise while wearing an "applied" label, which is strictly WORSE than an honest "captured".
   - 🧪 **THE FIRST REAL IMPORT EARNED ITS KEEP AS A TEST CASE** — it caught a mapping bug two fixture-based test leagues could not: Sleeper supports BOTH a coarse `fgm_50p` and a fine `fgm_50_59`/`fgm_60p` pair, and only the coarse one was mapped, so a league paying 5 for a 50-59 and 6 for a 60+ had both rules silently dropped to captured. It also produced the story's FIRST genuine `exact=False` fold (those two values really differ, so collapsing them onto our single 50+ projection bucket IS an approximation — and the panel says so). **Generalisable: a second REAL payload finds what a second fixture cannot, because fixtures inherit the author's assumptions.**
   - ⭐ **THE FLOOR IS WHY A NO-GO COSTS NOTHING.** NF-C0b already ships, so refusing an uncompliant path costs a user CONVENIENCE, never ACCESS. That is what makes "we will not hold your ESPN login" a decision with no hostage — and it is the general shape: **an import layer built on top of a working manual floor can afford to be principled.**
2. **E9.46** — home-page revamp for the multi-sport + fantasy launch (the landing surface; ready BY launch, after the product it showcases is real).
3. ~~**NF1.5b**~~ — ✅ **DONE 2026-08-01: the served 2026 board is now the market-aware refined board on every surface** (re-grade PASSED; data live on prod). One residual: merge dev→main so the MarketLeanNote caveat is live. ⚠️ the ship CORRECTED my readiness pass (the "ordering-only ⇒ inherits everything" lock was false of the code — the derived board had drifted on universe/interval/rookies; cured) AND corrected the claim (beats ADP ON AVERAGE, RB a wash, ADP-specific — see NF3.2).
4. **NF-C2.1** — mock draft simulator. Explicitly a post-8/22 fast-follow; last.

**Sonnet lane (interleaved with other Sonnet work — E5.0/E11.25/etc.):**
1. **NF-C0b** — manual league-settings editor. FIRST: it's READY, it's the customization FLOOR (E8.2 Path-A analog — no user ever blocked regardless of platform) and it unblocks the real beta tester on an un-pullable league. Highest value-per-effort of the set.
2. **NF3.1** — fantasy player page (kills the dead-end board rows; browse-surface completion).
3. ~~**NF-D13** — audit the NF-D3 competitor scorecard for the aggregate-vs-member defect.~~ ✅ **DONE 2026-08-01 — CLEAN NULL, no fix needed.** Traced `benchmark_scorecard.py` line by line against E7.11's baseball-side defect (a source can't "disagree" with a consensus it alone constitutes). NF-D3's scorer is STRUCTURALLY a different, safer shape than E7.11's: it never builds an N-source consensus column at all — every competitor system (ADP/ECR/ESPN/Sleeper/FF-file) is graded against us **independently, pairwise** (one inner-join `m` per system, so both sides are always present by construction — no possible "1-source row"), and the disagreement z-scores (`_disagreement`) are computed over the IDENTICAL merged population for both sides (no mismatched percentile denominators). "Us" is never folded into or compared against a consensus it's a member of. All three sub-defects (1-source rows / self-inclusive consensus / denominator mismatch) require an aggregate-of-members step that simply doesn't exist here. Pinned with a regression test (`test_scorecard_systems_score_independently_of_each_other`) + an inline audit note in `benchmark_scorecard.py::_disagreement` so a FUTURE change that folds multiple systems into one blended "consensus" entrant knows to re-apply the E7.11 discipline first. Numbers UNCHANGED (no fix applied) — the honest fade claim stands: ADP fade ρ 0.478 vs 0.247, per `ablation_results/nf_d3_benchmark_scorecard.md`. Unblocks ONE of NF3.2's two gates.
4. ~~**NF3.2**~~ — ✅ **DONE + PUBLISHED 2026-08-02** (commit 148899d6): the public `/fantasy/track-record` receipts page is LIVE (2019-2025, 2026 locked at the payload layer). Implemented the entitlement season-split itself (2nd public router + NF3.1 all-or-nothing→season-scoped) + added MFL as a 2nd ADP source (FFC has no 2025 archive). Honest framing held (RB a wash, one season a loss, ECR/Sleeper beat us overall). ⏭️ deferred: NF-D17 (is the +0.022 headline understated vs a matched-population ~+0.144? operator "not now"). 🚨 new infra landmine (public route needs the API-GW Cognito authorizer disabled per-route) → CLAUDE.md + aws_resources.md.
5. **E9.47** — launch blog post (the announcement; once the product surfaces + the home page are live). Last.

**Collapsed critical path:** NF-C0b → NF-C0 + NF3.1 → ~~NF-D13~~ + ~~NF1.5b~~ (both done) → NF3.2 (unblocked; waits only on the NF1.5b merge) → E9.46 → E9.47 (NF-C2.1 after).
⚠️ **ONE THING TO VERIFY BEFORE LOCKING:** is the **NF3 base browse surface** live to subscribers yet? NF3.1/NF3.2 are drill-downs that assume it; MVP-3's draft tool is still admin-only, so if NF3-base isn't shipped it precedes NF3.1.

## 0. Structure + the shared core (read first)
🗂️ **GROUP BY SPORT, VERTICAL BY FOLDER (operator 2026-07-22).** NFL's verticals under `football/nfl/`: **betting** = `nfl_story_prompts.md` (N-series: N0.x data, N1.0 marts, N1.1 game-line, N1.2 props, futures) · **fantasy/dynasty = THIS folder** (`NF`-series — parallel to MLB's `F`-series). The college→NFL rookie feeder = NCAAF-**P1A** (done).
🔗 **THE COMMONALITY = the WEEK-TO-WEEK PLAYER PROJECTION.** Betting-props (N1.2) and fantasy (NF) diverge downstream (props → P(over/under)/CLV; fantasy → fantasy points/dynasty board), but both consume ONE shared per-player-week posterior-predictive projection. ⚠️ **REPRIORITIZED (operator 2026-07-22 — FANTASY over betting, draft-season is hot): the shared player-week core is now a FOUNDATION that NF1 (fantasy) DRIVES/BUILDS, and N1.2 (props) CONSUMES later** — the reverse of the original plan. So building NF1 delivers the shared projection basis for BOTH; the NFL betting surfaces (N1.1 game-line, N1.2 props pricing) sequence AFTER the fantasy MVP. Build once — NF1 owns the core, N1.2 reuses it.
⚖️ **EDGE-INDEPENDENT (like the MLB fantasy vertical):** this is a PROJECTION product — no PBO/DSR/forward-CLV gate (that's the betting posture). The gate here is projection CALIBRATION vs realized fantasy outcomes + honest uncertainty.

## 1. Strategic frame
- 🥇 **REDRAFT IS THE PRIORITY (operator 2026-07-22): redraft is the MAJORITY of fantasy-football leagues; dynasty is far less prevalent.** So NF1 (seasonal/weekly redraft projections) + the redraft TOOLS lead; **Dynasty (NF2) is the underserved *differentiator* but SECONDARY by league prevalence** — build it, but after the redraft product.
- **NFL fantasy is the LARGEST fantasy market.** The betting product's efficient-market caveat does NOT apply; calibrated projections are pure product value. **Honest-uncertainty framing** throughout (credible intervals, not false-precise points).
- 🎯 **THE PRODUCT WE'RE COMPETING WITH = "The Fantasy Footballers"** (operator benchmark) — rankings + start/sit + trade + draft tools. ⇒ the bar is a full TOOLS suite on top of the projections, not just projections.
- 🎨 **DRAFT-UI BENCHMARK = PFF's Mock Draft Simulator + Live Draft Assistant (operator 2026-07-25).** Two distinct competitive bars: **FF = the PROJECTIONS/rankings bar** (NF-D3 scorecard proves we beat it); **PFF = the DRAFT-UX bar** — a fast, polished mock simulator (CPU opponents, deep league config), a live draft assistant (sync your real ESPN/Sleeper/Yahoo draft, pick-by-pick), a custom big-board builder. ⇒ MVP-3/NF-C2 (+ NF-C0 for live-sync + follow-on NF-C2.x for the sim/big-board) must beat PFF on draft-UI QUALITY. ⭐ our edge = OUR projections + VOR-driven "why this pick" transparency + honest uncertainty, vs PFF's ADP/rankings. Detail in `nfl_fantasy_story_prompts.md` (NF-C2 UX BENCHMARK).
- 🧱 **THE KEYSTONE ARCHITECTURE — a LEAGUE-CONFIG / SCORING layer (NF-C1):** every tool (draft optimizer, trade calc, FA rankings, start/sit) must be **CUSTOMIZABLE BY LEAGUE** — so NF1's projections are RAW stat lines, and a league-config layer (scoring rules + roster/starter slots + league size) converts them into league-specific fantasy points / values / VOR. Build the config layer ONCE; every tool reads it. ⭐ **This engine is SHARED with the MLB fantasy vertical (`baseball/fantasy/`) — build the league-config + tool ENGINES once, instantiate per sport** (the `hierarchical.py`-style reuse; the projection INPUTS differ, the optimizer/trade/config LOGIC doesn't).
- 🔌 **PLATFORM INTEGRATION IS THE HARD PREREQUISITE (NF-C0) — you can't customize by league without INGESTING the user's league** (rosters + settings + live draft state). **Football platforms, by 2025 MAU (Sensor Tower Aug-2025): ESPN 48% · Sleeper 33% (fastest-growing, dynasty-heavy) · Yahoo 18%** — those THREE cover ~99% of redraft; then **CBS Sports** (mid) + **MyFantasyLeague (MFL)** & **Fantrax** (the serious-DYNASTY platforms) + **NFL.com Fantasy** (smaller). ⭐ **API reality drives the build order (verify current status per platform): Sleeper (public read-only API, well-documented — easiest × #2 reach) + Yahoo (official OAuth API) are the clean high-value STARTS; ESPN is the must-have but its API is UNOFFICIAL/cookie-based/fragile; MFL is API-friendly (the dynasty long-tail); CBS/Fantrax have APIs; NFL.com is limited.** ⇒ integration order ≈ **Sleeper + Yahoo → ESPN → CBS/MFL/Fantrax.**
- ⏰ **GTM TIMING — draft season is NOW (Aug–early Sept; the operator's own draft 8/22):** a working draft optimizer + league import for the top platforms during draft season is a PRIME GTM push — a user imports their real ESPN/Sleeper/Yahoo league and drafts with our tool. ⇒ the near-term MVP = NF-C0 (Sleeper+Yahoo+ESPN import) + NF-C1 (config) + NF-C2 (draft optimizer) + NF-FASTPATH projections, shippable for the draft window.

## 2. Phased plan (NF-series; the shared core in §0 is the input)
- **NF1 — WEEKLY + SEASON FANTASY-POINT PROJECTIONS** (the core fantasy model): from the shared N1.2 player-week projection → fantasy points (std + PPR — the validated N0.3 PPR calc) via opportunity × efficiency × role (`dim_player_role`). §0.5 where a model is selected; posterior-predictive; honest uncertainty; validated vs a holdout season. ✅ **CODE-COMPLETE + SMOKE 2026-07-26** — the JOINT LEARNED re-weighting of the FULL NF-D feature set (`nf1_model.py` + `run_nf1.py`), MARKET-BLIND (operator call — no ADP/ECR; the market stays the NF-D3 benchmark) + a FULL matchup-aware weekly leg (DVP × Vegas env × home, Gamma predictive). §0.5 bake-off (4 learners, Optuna, walk-forward CV, oracle-checked) + calibrated season interval + a flagged-approx weekly interval. Awaiting the operator's full laptop bake-off/grade (report `fantasy/ablation_results/nf1_model_report.md`). **NF1.1 ✅ DONE 2026-07-27 (NULL — MVP-1 stands): per-position independent models + top-tier selection established the MARKET-BLIND CEILING (two structurally-different passes, NF1 + NF1.1, both beat MVP-1 on holdout but lost on the consensus universe; QB gap = the market signal).** **NF1.3 ✅✅ DONE 2026-07-27 — PRODUCT WIN at QB/RB (dual-board): the operator-sanctioned MARKET-AWARE variant (`nf1_3_model.py`/`run_nf1_3.py`, NF1.1 harness + ADP/ECR position-conditional). Full bake-off + NF-D3 grade RAN: the BEST GENERATION EVER on the product metric — FIRST board to beat ADP (+0.015 pooled vs MVP-1 −0.06), best-ever vs ECR (−0.02), QB vs ADP −0.15→+0.032 / RB −0.10→+0.006. Per-position lean as hypothesized (QB/RB market-led, WR market-informed, TE blend). TRADE-OFF: fade edge 0.543→0.478 + top-tier ρ not deflation-robust (PBO 0.23–0.69) → it's "tracks the market + adds signal," NOT an independent edge. ⇒ DUAL-BOARD: serve the market-aware board for the QB/RB user product (honest lean labels), keep market-blind MVP-1 as the fade-claim baseline. Deep-system ceiling (Sleeper/ESPN) unbroken. Serving = operator's product call.**
- **NF2 — DYNASTY / ROOKIE BOARD (⚠️ SECONDARY by league prevalence — redraft is the majority; build after the redraft product + tools):** rank players/prospects for a multi-year hold; ⭐ the rookie leg consumes NCAAF-P1A as a **RESIDUAL ON DRAFT SLOT** (rank on draft position + where `projected_nfl_z` disagrees; skill positions RB/TE/QB/WR only; ≈0 for DL/LB/DB; recalibrate the parameter uncertainty). The underserved differentiator.

**THE TOOLS LAYER (the Fantasy-Footballers-competitive suite; league-customizable; the ENGINE is SHARED with `baseball/fantasy/` — build once, instantiate per sport):**
- **NF-C1 — ⭐ LEAGUE CONFIGURATION + SCORING ENGINE (the KEYSTONE — build FIRST of the tools):** a league-settings schema (scoring rules per stat, roster/starter slots, league size, PPR variant, superflex…) + a scorer that converts NF1's RAW stat-line projections → **league-specific fantasy points + VOR/value + positional scarcity**. Every tool below reads it.
- **NF-C2 — DRAFT OPTIMIZER (⭐ LIVE-draft-aware; SNAKE/redraft):** given the league config + the current DRAFT STATE (who's gone, my roster, my slot) → the value-maximizing pick (VOR + positional-need + tier-break aware), **usable DURING a live draft** (real-time state input). The flagship draft tool.
- **NF-C5 — AUCTION DRAFT SUPPORT (NEW 2026-07-25; parallel to NF-C2 — snake is primary, auction is the OPTION):** auction VALUES (VOR → $ given the league budget) + a budget-aware live optimizer — max-bid (inflation-adjusted value ∩ affordability, never strand a slot) + nomination strategy. ⭐ **the core dynamic (fixed budget + live price INFLATION) has no snake analog → its OWN optimizer.** SPORT-AGNOSTIC in `fantasy_engine` → ⭐ **MLB roto-auction reuses it (auction is MORE prevalent in fantasy baseball)**. Fast-follow to MVP-3 unless the operator's 8/22 league is auction.
- **NF-C3 — TRADE CALCULATOR (redraft ROS-value + dynasty multi-year):** value a proposed trade for MY league config — redraft = ROS value; dynasty = multi-year (consumes NF2/keeper). Honest "who wins + by how much + uncertainty."
- **NF-C4 — FREE-AGENT / WAIVER RANKINGS (customizable by league ROSTERS):** rank available players by league-adjusted ROS value RELATIVE to a given roster's needs (roster-aware, not a generic list). The weekly in-season churn tool.
- **NF3 — FANTASY APP SURFACES:** the projections + ALL the tools (draft optimizer, trade calc, FA rankings, start/sit) + the dynasty board in `frontend/` (Next.js; app-target guard). Honest-uncertainty display.
- ⏰ **NF-FASTPATH (near-term, does NOT need the full NF1 model): 2026 SEASON projections by 8/22 (operator's draft).** `mart_projections_preseason` (N0.3, dynamic) already gives a first-pass 2026 VETERAN projection with light validation/ranking; rookies attach when P1A is available. Ship for 8/22; NF1 refines after. Do NOT delay NCAAF for this (P1A is a NCAAF story serving both).

## 2b. ⚡ DRAFT-SEASON MVP PATH (operator 2026-07-22 — a shippable slice for the Aug–early-Sept draft window)
🎯 **THE MVP = a user drafts with our tool: pick their league FORMAT → see our 2026 projections converted to their scoring → mark players taken during their draft → get best-available / best-VOR pick recommendations.** De-risking rule: **platform AUTO-IMPORT (the fragile API work) is LAST** — manual format entry + manual pick-marking is a legit v1, so the tool ships even if integrations slip. Live platform sync is v2.
- **MVP-1 = NF-FASTPATH (2026 projections) — ✅ DONE 2026-07-23.** Landed `mart_nfl_fantasy_season_projection` (S3 Delta `nfl/fantasy/derived/season_projections/`, 576 players): a RAW stat-line projection (vets = per-game line × expected-games role fix; rookies = draft-slot curve × P1A residual) + honest 80% intervals. Face-valid + 93% coverage + holdout Spearman 0.75–0.78. The output schema is the MVP-2 input contract (see the NF-FASTPATH story block). The content foundation everything ranks. **⚠️ HONEST GAP (2026-07-24 close): loses WITHIN-TIER ordering to The Fantasy Footballers (RB 0.45 vs 0.61, WR 0.10 vs 0.29) — the offseason usage/context we don't yet ingest; within-tier ordering is what wins drafts → the NF-D cluster below is the runway.**
- **⭐ NF DATA FOUNDATION (NEW 2026-07-24, from the MVP-1 asks — the runway to out-project the consensus, all edge-independent):** **NF-D1 ✅ DONE 2026-07-25** = the repeatable annual 2026 roll-forward (nflverse rosters/schedules/depth_charts/injuries/draft/combine, all free/no-key; Dagster weekly Mar–Aug, STOPPED/operator-gated). ⭐ **landed real 2026 depth → MVP-1's 2026 board now uses REAL 2026 role (794 players) vs the 2025 carryover** (re-run the season projection to pick it up). Fixed 2 bugs of the "max season PRESENT ≠ max season PLAYED" class (depth-chart cold-start + auto-detect on unplayed rows) → now a shared `sport_data_platform.md` landmine (WILL recur in NF-D2 + NCAAF/NCAAB rollforward). · **NF-D2** = projection-quality usage/context sources, run as ablated SLICES (⭐ each kept only if it lifts held-out within-position ρ): **slice 1 snap-usage role ✅ SHIPPED (RB/WR +0.009)** · **slice 2 NGS/PFR advanced ⛔ NULL (efficiency re-encodes past production — dropped)** · **slice 3 team-change/depth-jump opportunity ✅ SHIPPED (skill +0.006–0.008, movers +0.03)** · **slice 4 Vegas team environment (QB, leakage-safe Week-1 lines) ✅ SHIPPED (QB +0.012; no new ingest)**; remaining ranked sources = injuries/combine + ADP · **NF-D4 ✅ DONE 2026-07-26** = ⭐ FORWARD-VEGAS TEAM ENVIRONMENT — grow slice-4's QB env tilt. ⚠️ **Probe: the Odds API has NO NFL win-total futures market** (only super_bowl_winner outrights) → forward win total came from a one-time STATIC public backfill (`win_total_source.py`, covers.com, 2020–2026). Win-total ALONE was WORSE than Week-1 (team quality, not offense — the +0.046 ceiling is offense-specific); the **win-total × Week-1 z-BLEND SHIPPED** (6-season-mean QB ρ 0.684 vs 0.682, wins 4/6, stabilises the noisy single Week-1 line). Extending beyond QB added no lift → QB-scoped. No new capture (static source, graceful fallback); the going-forward OPENING-LINE capture is the deferred future lever (serves 2026+, can't validate on history) · **NF-D5** ✅ DONE 2026-07-26 (schedule RUNNING on box) — ⭐ SLEEPER INJURY FEED — ingest the FREE Sleeper players feed (`injury_status`/`injury_body_part`/`gsis_id`) → `nfl/raw/sleeper_injuries` Delta (WARN-tier), COALESCE'd Sleeper-over-nflverse in `load_forward_roster_status` so offseason surgeries/PUP surface on the draft board NOW (no backtest — a coverage/correctness add on the slice-5 availability flag) · 🏁 **NF-D2 ✅✅ DONE 2026-07-26 — closed at its shipped slices (#6 ADP shipped OFF as a blend → delivered as the `nfl/fantasy/benchmarks/adp_benchmark` asset for NF-D3 + an optional QB/RB prior. ⚠️ **NF-D3's full multi-system backtest (2026-07-26) CORRECTED the ADP-only read: we LOSE overall within-tier ordering to EVERY consensus (ADP/ECR/ESPN −0.06, Rotowire −0.14); we win ONLY on high-conviction fades (0.48 vs 0.25) + are competitive at WR → the only honest claim is narrow, NOT "we beat consensus"). NOT reopened; the successor chain is NF-D2 (done) → NF-D4 + NF-D5 (self-contained) → NF1, the JOINT learned re-weighting home for the full NF-D feature set (no separate NF-D6)** · **NF-D3** = competitor-projection benchmarks (FantasyPros ECR/ADP + PFF/ESPN → a `benchmarks/` lake asset + a standing us-vs-consensus scorecard + NF1/MVP-2 calibration target). Prompts in `nfl_fantasy_story_prompts.md`.
- **⭐ NF-D6 ✅ DONE 2026-07-27 (operator REDEFINED NF-D6 2026-07-27 = the forward DEFENSE projection; the earlier "no separate NF-D6" referred to the NF1 consolidation, a different thing) + NF-D7 ✅ DONE 2026-07-27:** **NF-D7** (xFP + TD regression) — construct built + premise VALIDATED (expected-TD out-predicts actual-TD forward) but the projection blend + learned-feature ablations are clean NULLs → delivered as leakage-safe FEATURES via `xfp_source.load_xfp_features` (shipped OFF, the recency line already mean-reverts TDs). **NF-D6** (forward pass-D/rush-D strength — the SOS baseline NF1.2 is BLOCKED without) — shipped `defense_source.load_forward_defense(season)` + landed `nfl/fantasy/defense/forward_defense_strength` Delta: opponent-adjusted mixed-model strength (the shared `hierarchical.fit`, EB-shrunk, honest posterior sd) SHIPS (ties raw on walk-forward, is the principled schedule-confound-free SOS quantity); the two roster-churn LEVERS are honest NULLs — the churn POINT-shrink adds no walk-forward lift (OFF) AND the operator's "bigger churn ⇒ wider uncertainty" is refuted (ρ(churn,|error|)=−0.04, defense is volatile regardless of continuity) → forward uncertainty is instead a calibrated season-to-season VOLATILITY floor (0.69 1-sd coverage); churn/returning-shares ship as diagnostic features. ⇒ **NF1.2's SOS can now join `load_forward_defense`; NF-betting N1 + a future DST projection inherit it too.**
- **⭐ NF1.4 ✅ DONE 2026-07-27 (rookie-prior refinement — NULL model, one shipped interval fix):** the story's premise was that MVP-1's rookie slot curve "runs too hot"; **the measurement refutes that as a LEVEL claim** — over draft classes 2019–25 the incumbent is COLD on the draftable rookie tier at EVERY position (`tier_bias` QB −32 / RB −58 / WR −49 / TE −44 PPR) and its point projection for the #1-overall rookie QB is nearly unbiased (+8.9 PPR vs a ~201 realized mean). A §0.5 bake-off of **134 pre-registered configs × 4 positions** (P1A `projected_nfl_z` backbone + combine/athletic + a derived breakout-age block + recruiting pedigree; 6 learner forms; shrink λ swept; walk-forward by DRAFT CLASS) repoints **NOTHING** — QB had zero do-no-ordering-harm-eligible candidates, RB beat the incumbent on tier MAE (80.4→71.8) but PBO 0.343, WR/TE fail BH-FDR. The `where games > 0` SURVIVORSHIP filter is a genuine flaw (15% of drafted skill rookies never play, 35% at QB, inflating the survivor-only QB mean 67→104) but an arm removing it — the incumbent's exact form, zero new DOF — made held-out accuracy WORSE at every position, because the board was already too cold. **The dogfooding symptom ("a rookie QB floated to #1") is therefore a RANK effect produced by VARIANCE around an unbiased point estimate** (the #1 QB projects QB11–15 and finishes ≈QB19.5; rookie QB outcomes are hugely dispersed and veteran QB projections pack densely at that level) ⇒ an UNCERTAINTY problem. ⭐ **SHIPPED:** the rookie 80% band, which claimed 80% and delivered **0.680 (0.444 at QB)** under MVP-1's `fp × cv` parameter width, recalibrated to an EMPIRICAL per-position tercile band fit on the FULL drafted population → **0.790 (QB 0.741 / RB 0.831 / WR 0.812 / TE 0.720)** with `point_projection_max_abs_change = 0.0` (pinned by a test). Plus `season_projection.rookie_board_face_validity()` — an ADVISORY gate (no rookie at #1/in the overall top-10; no rookie above the Q90 of realized rookie seasons over the full drafted population) wired into the MVP-1 report; CORRECTED after a first cut whose reference class (Q90 of ALL drafted rookies) was cleared by the REALIZED top rookie in 25/28 cohort-positions and therefore fired 7/7 — the real reference is the per-class BEST rookie, against which the incumbent breaches 0/28 vs reality's 9/28 and the 2026 board passes; a third independent confirmation the prior runs COLD. Files: `nf1_4_rookie.py`, `run_nf1_4.py`, 29 tests; report `ablation_results/nf1_4_rookie.{md,json}`. ⚠️ open follow-up for NF1.5/NF-C1: `fantasy_engine/scoring.py` rescores the league interval from one `_sd` and RE-SYMMETRISES the (strongly asymmetric) rookie band — the width improvement lands, the shape does not. ✅ **CLOSED by NF1.7 (2026-07-29)** — `SportProfile.base_p10_column/base_p90_column` now carry each SIDE of the band through the rescore independently, so the asymmetry survives (verified across formats; veterans stay symmetric on the CV fallback).
- **⭐ NF1.7 ✅ DONE 2026-07-29 (PER-PLAYER rookie 80% bands — the §0.5 coverage-vs-sharpness landmine, fixed):** NF3 surfacing found NF1.4's shipped rookie band was **CLASS-LEVEL**: 81 rookies on the served 2026 board carried **12** distinct intervals (3 per position) against **647** for 703 veterans, so every top-bucket rookie QB carried an identical 26.5–277.0 while their point projections spanned 25.1→268.3 — a 268-pt projection pinned in the top 3% of "its own" band. ⚠️ **AND COVERAGE COULD NEVER HAVE CAUGHT IT**, which is the durable lesson: NF1.4 selected that band on coverage (0.680→0.790) and the band it picked is genuinely well-covered, because **coverage is a property of the POPULATION, not of the player** — a position-CONSTANT band hits its nominal rate while carrying zero per-player information. NF1.7 therefore selects on the **Winkler/Gneiting interval score** (the τ=0.10+0.90 pinball sum — PROPER for the quantile pair, so it pays for width AND for missing) with coverage kept strictly as an eligibility **FLOOR**, per the E2.1-r rule applied to intervals. **SHIPPED = `qreg α0.01` — a linear QUANTILE REGRESSION on log(point), log(draft slot), the P1A residual sd and position, i.e. the DIRECT-LEARNED FOIL beat all three non-parametric families** (per-position ratio quantiles, per-position k-NN, and pooled position-normalised k-NN). 44 configs × 7 held-out draft classes (2019–25): interval score **181.16 vs the incumbent's 202.14 (−10.4%)** at coverage 0.808 (floor 0.80), **PBO 0.029 / spread 27% = LOW-PBO + WIDE-spread ⇒ a genuinely separated winner**; distinct-band fraction **0.184→0.989**, points in the extreme 5% tail of their own band **30/553→0**, live board **12→80** distinct rookie bands, `audit_interval_quality()` GREEN. Rookie POINT projection byte-identical (pinned). ⭐ **THE WINNER IS *WIDER* THAN WHAT IT REPLACES** (127.7 vs 124.4 mean PPR, ≈2× a veteran's): the win came from putting width where it belongs per player, not from shrinking it — sharpness was the SELECTION metric, honesty the CONSTRAINT, enforced by a **zero-width degenerate that must lose** (a naive sharpness metric would crown it) beside a **max-width degenerate that must also lose** (a coverage target would). 🧨 **METHOD LESSONS (guarded):** (a) a band must condition on the **SERVED** point — re-deriving it was wrong by 3.3 PPR (the served point is the target MINUS the fumbles term), so `rookie_point_projection` DELEGATES to `project_rookies`; (b) ⭐ **an oracle that fails to fit makes the oracle check VACUOUSLY TRUE** — both oracles silently returned None on an ~80-row held-out class and the check passed on nothing (now raises); (c) the oracle floor is **same-family AND same-sample** — a peeking misspecified oracle is beatable (215.5 vs 218.0) and a peeking k-NN's capacity depends on n (214.9 vs 262.2), so the unit test uses a permutation instead; (d) a "widen-only" knob written two-sided SHARPENED half the field and cost coverage 0.808→0.773. ✅ **CLOSED by NF1.8 (2026-07-29): QB coverage 0.739 → 0.815 under a PER-POSITION floor** (a Mondrian per-position conformal layer on a √-scale quantile regression), at a cost of +1.3% interval score. The gap was inherited, not caused — and the fix is a per-position CALIBRATION, not a per-position FIT (the latter was pre-registered beside it and made QB worse). Report `ablation_results/nf1_7_rookie_intervals.{md,json}`; harness `run_rookie_interval_ablation.py`; 22 tests.
- **⭐ NF1.8 ✅ DONE 2026-07-29 (rookie intervals under a PER-POSITION coverage floor — the honest NF1.7 follow-up):** NF1.7's coverage floor was pre-registered POOLED, and pooled duly cleared 0.80 — but coverage **REDISTRIBUTED** rather than improving uniformly (QB 0.741, RB 0.777, TE 0.870, WR 0.826). ⚠️ **That is the same class of blind spot as NF1.4's, one level up: pooled coverage is a POPULATION property that one position can pay for on another's behalf**, and rookie QB — ~10 drafted a class, ~35% never take an NFL snap — was the position paying. NF1.8 re-selects under a PER-POSITION floor while **still selecting on the interval score** (coverage stays a FLOOR, never a target). **SHIPPED = `qreg_sqrt+cqr[pos,add] · sdgain 0`** — NF1.7's quantile regression on the √ scale plus a **MONDRIAN (group-conditional) CONFORMALIZED-quantile-regression** calibration layer; 80 configs × 7 held-out draft classes (553 rookie-seasons). **Every position now clears the full nominal 0.80** (Tier 1; the pre-registered Tier-2 QB relaxation was NOT needed): QB **0.741→0.815**, RB **0.777→0.804**, TE 0.870→**0.900**, WR 0.826→**0.835**, for **+1.3% interval score** (180.99→183.41) — still 9.1% better than the NF1.4 class-level incumbent (201.76) at a distinct-band fraction 0.89 vs 0.18. Rookie POINT projection **byte-identical** (verified end-to-end on the 2026 class: max |Δ| = 0 on `proj_fp_ppr` and every projected stat column); `audit_interval_quality()` GREEN. ⭐ **THE FOIL IS WHAT MAKES THE CLAIM ATTRIBUTABLE:** the pre-registered POOLED conformal variant is a numerical **NO-OP** (183.451→183.451, QB 0.778→0.778) because the base band's AGGREGATE out-of-fold coverage is already ≈ nominal — only reading conformity **WITHIN a position** exposes the QB shortfall; on the served 2026 class the fitted inflation is **+0.82 PPR at QB and 0.0 at RB/TE/WR**. ⚠️ The other pre-registered per-position mechanism, a separate quantile REGRESSION per position, **LOST** and made QB worse (0.716) — **per-position CALIBRATION works where per-position FITTING does not** (separate slopes overfit at QB's in-fold n). 🧨 **METHOD LESSONS (guarded, and reusable well beyond this story):** (a) **a per-group floor cannot be a mean of per-class means** — NF1.7's convention dropped a position thin in a class (<3 rows), so the floor would protect a different population than it names ⇒ NF1.8 pools over ROWS, reporting both conventions; (b) **`max_width` SATISFIES every per-position floor and still loses** — the proof the floor remained a CONSTRAINT rather than becoming a target, and the reason the floor is NOT tightened above nominal 'for safety' (each notch moves the eligible set toward the degenerate); (c) ⭐ **PBO ALONE WOULD HAVE CONDEMNED A TIE** — PBO(eligible) 0.514 beside a 27% whole-field spread reads as textbook overfitting, but the field mixes ~15 near-clones (within 0.3%) with known-bad nulls 27% away, so the whole-field spread is the wrong discriminator; the **FLIP DISTRIBUTION** (12/35 in-sample halves to the shipped arm, 11/35 to `knn_norm k80`, 0.94% apart) shows a **two-arm tie ACROSS FAMILIES** ⇒ the NULL on *which* arm, corroborated by Bailey's performance **DEGRADATION of 2.0%** and a 0.11% top-quartile spread ⇒ **report the flip distribution + the degradation beside PBO, never PBO and a whole-field spread alone**; the tie is broken on the program's own **DEFECT metric** (the runner-up reverts 4.9% of rookies to the class-level band) and never on coverage headroom, which `max_width` wins; (d) **a check that runs on nothing passes on nothing — again**: the first per-group-scalar test filtered on `lo > 0`, which selects ZERO rookie rows (q10 floors at 0 for nearly everybody), so a deliberate per-player mutation PASSED — cured by measuring the un-floored HI side, asserting non-vacuity, and a constructed-model test pinning both bounds; (e) the NF1.7 reference arm is **PINNED as a literal** — deriving it from the production constants NF1.8 changes would compare NF1.8 against ITSELF and print 0.00 for every 'vs NF1.7' figure. 🚨 **DISCLOSED: the floor is met with a THIN margin** — in ROOKIE-SEASONS the slack is QB **1**, RB **0**, TE 10, WR 7; QB's whole fix is 6 more covered seasons out of 81; at n=81 a hard nominal floor rejects a perfectly-calibrated arm ~50% of the time (exact binomial, and that rate barely moves with n — n buys detection of a smaller shortfall, not a lower false-reject rate); NF1.7's 0.741 and RB's 0.777 were both **not significantly below nominal**, so the floor is partly noise-selecting — hence the deflation and the pre-registered Tier-2 fallback. The conformal arms calibrate IN-FOLD and so cannot see a NEXT-class gap, which is positive evidence the QB gap is class-to-class variation rather than fittable miscalibration. **RB is now the tightest position — a future class that moves RB is the one to watch.** Report `ablation_results/nf1_8_rookie_perposition_floor.{md,json}`; harness `run_rookie_perposition_ablation.py`; 23 tests (every invariant mutation-verified to FIRE); changelog 1 item wk 2026-07-27. ✅ **SHIPPED + LIVE 2026-07-29** — operator re-ran the projection and published 16 files to `s3://credence-prod-s3-api-cache/fantasy/nfl/2026/` (dry-run reviewed first; the NF-D12 banner fired). Served board: rookie mean band 125.4 → **130.8** PPR, rookie QB 145.6 → **152.1**, **80 of 81 distinct rookie bands**, no `[ALERT] interval quality` line (audit GREEN), NF1.4 rookie face-validity **pass**, ADP coverage 100% across 8 samples. ⭐ **VETERAN bands UNCHANGED — 0 of 703** (p10/p90/tier), so the change is surgically rookie-only, and the served point is identical to 2 decimal places for all 784 players. Frontend deploy still needed for the changelog item.
⚙️ **VERIFICATION NOTE (found during the operator's re-export, 2026-07-29) — DO NOT ASSERT BYTE-EQUALITY ON A POINT PROJECTION ACROSS TWO SEPARATE RUNS.** The handoff said the pre/post `proj_fp_ppr` diff "must be 0.0"; the operator measured **5.97e-13** and it looked like a defect. It is not, and the cause is worth recording because the same false alarm will fire on any future re-export or promotion check: **`np.polyfit` (LAPACK lstsq) returns ULP-different coefficients when the SAME rows arrive in a different ORDER** (measured: 3.55e-15 on the rookie slot curve's log-log coefficients), and a DuckDB read without a total `ORDER BY` is not order-stable across processes. Through `exp(a)·slot^b` and the stat-line rescale that becomes ~1e-13 PPR. **Two runs of byte-identical code differ by 8.5e-14, so the residue is process-level float noise, not a code change.** The band-config question was settled the right way instead — IN ONE PROCESS on the real data, NF1.7's config vs NF1.8's gives a point diff of **exactly 0.0** (and the in-repo guard `test_the_point_projection_is_IDENTICAL_across_every_band_configuration` pins that with `check_exact=True`). ⇒ **For a cross-run check, assert `< 1e-9` PPR, or compare the ROUNDED served values** (here: the served point is identical to 2 dp for all 784 players). ⭐ The check that actually proves NF1.8 was surgical is a different one and should be the one run in future: **VETERAN bands must be UNCHANGED — 0 of 703 p10/p90/tier changed**, while 81 of 81 rookie bands moved.

- **⭐ NF-D8 ✅ DONE 2026-07-27 (contract/financial features):** shipped `contract_source.load_contract_features(season)` + landed `nfl/fantasy/contracts/{player_contract_features,team_cap_aggregates}` Delta (2017–2026). **Source pivot from the story's OTC/Spotrac ask:** both probed LIVE — overthecap.com's `robots.txt` explicitly disallows the Anthropic-AI crawler and Spotrac hard-blocks automated fetches — so instead of scraping either site this reads **nflverse's own `historical_contracts.parquet` release** (CC-BY-4.0; the SAME upstream vendor already trusted for pbp/rosters/snap_counts), which itself fetches + redistributes the OTC data and carries `gsis_id` directly (91.9% all-time coverage) — a DIRECT join to our marts' `player_id`, no fuzzy name crosswalk needed. Per-player features (cap hit / base salary / guaranteed $ / `guaranteed_ratio` / the cited non-linear `log(apy)×guaranteed_ratio`) merge with per-team aggregates (O-line cap total/share off C/LG/RG/LT/RT — the concrete H-OLINE proxy; a Herfindahl skill-cap CONCENTRATION index) onto one per-player row. Leakage-safe (`year_signed ≤ season`, most-recent-signing-wins on extension overlap). **Coverage: 97–99% skill-position (QB/RB/WR/TE) join match-rate 2022–2025** (fixing a first-pass measurement bug — `fct_player_week`'s season grain carries stale never-played historical-roster rows that must be filtered to `played_flag`, the same posture NF-D7's `_games_per_season` uses). 13 fast-gate tests, report `ablation_results/nf_d8_contract_features.md`. ⇒ **NF1.2's H-CONTRACT candidate + NF1.5's capstone sweep can now join `load_contract_features`.**
- **⭐ NF-D11 ✅ DONE 2026-07-29 (PROJECTION-UNIVERSE fix + a §0.5-selected availability prior):** the base-season INNER-JOIN anchor could not tell "retired" from "injured all year," so it silently DELETED every player who missed the whole base season — including actively-drafted ones (Brandon Aiyuk, Tank Dell, Jonathon Brooks, MarShawn Lloyd for 2026; also the root cause of operator defect #7). Fixed as a decided FALLBACK, not a deleted rule: a base-season-absent player is anchored on his MOST-RECENT PLAYED season **only when a projection-season depth-chart/roster row proves he is still in the league** (retired players — Mixon, Carr, Cooper, Elliott — still excluded), then discounted by a **return-from-absence availability prior**. Board **716 → 777**; every returner ships `confidence = low` with a 0-floored honest band. ⭐ **The rescue is STRICTLY ADDITIVE (max |Δ| across all 716 incumbents = 4.5e-13)** — earned by computing the in-fold priors over BASE-ANCHORED rows only and standardising the QB env tilt on the base-anchored field (otherwise the new rows silently swapped 36/716 adjacent ranks). **§0.5 bake-off: 21 configs × 9 held-out seasons, 431 historical returners → `ratio @ 1.0`** (a multiplicative haircut on the player's own expected games, ≈0.4) beats the flat/tier/missed levels AND the direct-learned foil: returner **CRPS 18.36 → 13.52**, 80% coverage **0.637 → 0.915**, board ρ unmoved, **PBO 0.0** over 126 CSCV splits with a 35.8% spread. 🚨 **A LIVE METRIC INVERSION was caught and rejected: MAE (the obvious metric) is minimised at the median on a cohort where 43% play ZERO games, so the DEGENERATE "project nothing" arm BEATS the best candidate on it (15.47 vs 18.96)** ⇒ selection moved to CRPS, with an ORACLE floor and a DEGENERATE ceiling scored every run + a population-validity re-score on the draftable high tier. 🔎 Also shipped the **STANDING ADP-vs-universe coverage diagnostic** (`projection_coverage.py`, all 8 ADP samples on every board build, `alias_candidate` vs `true_absence` kept separate) — the free audit that found this; it now reports 0 alias misses and exactly ONE remaining absence (Carson Beck = NF-D12). ⚠️ The board FADES the market on returners by design (Dell WR95 vs ADP ~157) — an open fade with ADP rendered beside it, never a hidden claim. Report `ablation_results/nf_d11_absence_prior.md`.
- **⭐ NF-D14 🟡 DONE-AS-A-RECORDED-NULL 2026-07-31 (rookie-QB AVAILABILITY prior — the honest lever for the QB interval gap, measured and DECLINED):** NF1.8 cleared the rookie-QB 0.80 floor with **1 covered rookie-season of slack out of 81** and diagnosed the residual as **class-to-class VARIANCE, not fittable miscalibration**. NF-D14 tested the one lever that is not another recalibration: **availability at the source** (35% of drafted rookie QBs never take a snap). Two legs, and they answer differently — which is the whole result. ⭐ **LEG 1: the availability signal is LARGE AND REAL.** 49 pre-registered configs × 7 held-out draft classes (553 rookie-seasons), selected on the **DISCRETE CRPS** of a predictive PMF over games ∈ {0..17} (proper on the integer support; a Normal CRPS would misprice a 31%-at-zero atom under a hard 17 ceiling). Winner **`tier_empirical[depth] · blend 1`** — a per-(position × week-1-depth-rank) empirical distribution, EB-shrunk — cuts CRPS **3.227 → 2.455 (−23.9%)** and **3.375 → 2.325 at QB (−31.1%)**, beats its own permutation (3.520) decisively, **PBO 0.0** over 35 splits with the flip mass on 4 arms inside 1.8% (a tie among near-clones, all real), and **every position survives BH-FDR at q=0.10** (QB p=0.019). ⭐ **LEG 2 — THE STORY'S GATE — IS A CLEAN NULL.** Fed into the NF1.8 band through BOTH pre-registered channels (a quantile-regression FEATURE and a strictly widen-only WIDENER) × BOTH pre-registered drivers (the predictive **sd** of games; the naive **bust-risk** 1−p_play), under NF1.8's own folds/reducer/floors/anchors: **no arm both clears every per-position floor AND beats the shipped band's interval score.** The selection rule (argmin IS80 among floor-clearing arms) picks **the shipped NF1.8 band**. ⇒ **the rookie-QB interval variance is IRREDUCIBLE AT THIS N**, now confirmed by a second independent route; the band STANDS, the floor does **NOT** move (E2.1-r; NF1.8 §1), and the question is RETIRED rather than left for a third recalibration attempt. Standing re-validation re-run: **✅ ALL FLOORS MET**, rookie QB 0.8148 / 1 row of slack — byte-identical to NF1.8, because the plumbing lands **default-OFF** (pinned by `test_the_availability_channel_is_inert_by_default` on BOTH the dataclass field and the load-bearing fitter kwarg). Rookie POINT projection byte-identical, max drift **0.000000**. 🧨 **METHOD LESSONS (guarded, and reusable):** (a) ⭐ **MAE INVERTS WHEN THE CONDITIONAL MEDIAN IS AT ZERO — NOT merely when the zero ATOM is fat.** The story pre-registered the NF-D11 inversion as certain; it **did not fire**. NF-D11's returners had 43% zeros *and a median of 1 game* → inverted. Rookie QBs have 31% zeros but a **median of 4 games** → the nihilist arm loses MAE outright (5.96 vs 3.79). ⇒ *"zero-heavy" is not the test — score the degenerate and READ IT*, which is exactly why it is in the field every run rather than reasoned about. (b) ⭐ **THE WINNER LEGITIMATELY BEATS THE PEEKING ORACLE (2.455 vs 2.977) — the NF1.9 (f) CAPACITY effect, reproduced**: the oracle fits an ~79-row held-out class, the winner ~500 training rows, so the floor is only well-posed **at matched n** (oracle 2.977 ≤ `matched_n_candidate` 3.168 ✅). A harness that read the raw comparison would have condemned a valid selection as a metric inversion. (c) **A DEFLATION STATISTIC COMPUTED OVER A FIELD THAT CONTAINS ITS OWN NULLS MEASURES THE NULLS** — the NF1.8 spread lesson, one statistic over: whole-field **DSR 0.069** vs contender-set **0.945**, because `deflated_sharpe`'s expected-max-SR term scales with trial-Sharpe DISPERSION. Both reported, the pre-registered whole-field gate binds — and *even the generous reading misses the pre-registered 0.95*, so nothing turns on the choice. (d) ⭐ **THE PER-POSITION FLOOR CAUGHT A LIVE SHARPNESS-BY-UNDER-COVERAGE TRADE.** The sharpest arm in the field (`+avail FEATURE+WIDEN [pshort] gain 0.35`, IS80 174.5 = **−4.9%** vs shipped, and it lifts QB to 0.840) is **INELIGIBLE by ONE covered rookie-season at RB** (118 of the 119 the floor requires). Declined, and the refusal is stated in ROWS: the documented Tier-2 fallback relaxes only QB and only when Tier 1 admits nothing, so it would **still reject** this arm; admitting it would be reverse-engineering the floor from the answer. The FEATURE channel is where it comes from — a two-sided knob on an uncertainty covariate is free to NARROW (the NF1.7 (d) hazard on the channel the widen-only clamp does not cover). ⚠️ **DISCLOSED: the depth-chart proxy is FRESHER than the served signal** — the historical feature is the *week-1* chart (post-final-cuts) while the live board reads an August `stg_nfl_depth_charts_current` snapshot, which can only FLATTER leg 1; capital-only arms are pre-registered beside every depth arm (they also beat the null). ⭐ **The availability POINT channel is measured, reported and DECLINED ON THE EVIDENCE — and the evidence came back SPLIT, not in the direction the story assumed.** The expected-games ratio is not a haircut (it exceeds 1 for high-capital rookies), so it IMPROVES held-out MAE at RB/TE/WR by 1.2–3.5 PPR (partially correcting NF1.4's documented COLD bias) while **BREAKING QB — the one position the story exists to fix — MAE 50.15 → 58.56 (+16.8%), bias −10.7 (cold) → +11.4 (hot)**, because the slot curve already prices draft-capital-driven playing time once and the ratio prices it twice. Declined: a change that helps three positions a little and damages the fourth badly is not a projection improvement. 🔎 **OPEN LEAD, recorded not pursued:** the RB/TE/WR gain is real but is a NON-QB POINT change, outside this story's pre-registered scope — it needs its own §0.5 gate on the point (proper score + do-no-ordering-harm + per-position deflation), not this story's interval gate. ✅ **CLOSED by NF-D15 (2026-08-01) — as a RECORDED NULL, and it corrected this story's own reading of the mechanism:** the lift reproduces but clears neither DSR nor BH-FDR at 7 classes, and NF-D15's matched constant foil shows the gain is **PER-PLAYER, not the cold-bias LEVEL correction named above** (the constant that delivers exactly that correction fails to beat the incumbent at RB and TE). Model `rookie_availability.py`; harness `run_nf_d14_availability.py`; report `ablation_results/nf_d14_rookie_availability.{md,json}`; 28 tests (the two ship-safety invariants mutation-verified to FIRE). **No app change, no re-export, no changelog** — nothing user-facing moved.
- **⭐ NF-D15 🟡 REAL-BUT-UNDERPOWERED (*not* absent) — DONE 2026-08-01, ⚖️ PM-RATIFIED same day (the AVAILABILITY-SCALED rookie POINT for RB/TE/WR — NF-D14's open lead, given its OWN gate and CLOSED):** ⚖️ **PM RULINGS.** (1) **RATIFY-BUT-RE-LABEL.** The pre-registered per-position framing GOVERNS and nothing ships, but the record reads **"blocked by the three-test BH MULTIPLICITY correction at n=7 (pooled single-hypothesis p=0.081) — NOT absence of effect"**: the lift reproduces, PBO is clean, and the matched foil attributes it. A bare "null" would invite a future reader to conclude the effect isn't there and would SUPPRESS THE CORRECT RE-RUN. The DSR level was verified NON-load-bearing ⇒ **the FRAMING is the sole binding constraint.** (2) **RB/TE re-run SCHEDULED on the annual re-validation cadence — TE at 10 classes (2028), RB at 11 (2029); 🚨 any revival re-measures on the served August depth snapshot FIRST (the current lift is an UPPER BOUND). WR DROPPED PERMANENTLY as a GENUINE ABSENCE** (29 classes, and its own best arm is the constant foil) — a different kind of finding from RB/TE's underpowered null, and recorded as such. (3) **SPIN OFF `NF-D16`** — a pre-registered rookie-point LEVEL recalibration — **as the higher-priority next pull** (low-risk, moves no ranks, testable NOW vs data-gated for years). (4) **ADD a forward cross-reference to NF-D14's `_LIMITATIONS`; do not rewrite its reasoning** — ⏭️ handed off as a trivial code follow-up. ───── **SESSION RECORD:** NF-D14 measured, outside its pre-registered scope, that scaling the rookie point by its availability prior's expected-games ratio improves RB/TE/WR MAE by 1.2–3.5 PPR, and declined it (a POINT change under an INTERVAL gate, and it BREAKS QB). NF-D15 is the honest home: the SAME fitted prior (`tier_empirical[depth] · blend 1`, read out of NF-D14's artifact and asserted by `require_reused_prior` — REUSED, never refitted), 33 arms × 7 held-out draft classes, selected on **NF1.4's own `tier_mae`** (inherited, not chosen — grading a change to an already-selected product on a new metric is metric-shopping), under a **do-no-ordering-harm** constraint (NF1.4's `ORDERING_DO_NO_HARM`, checked PER POSITION) and **per-position** deflation. ⭐ **THE LIFT REPRODUCES AND THE GATE STILL SAYS NO:** RB tier MAE 80.43 → 68.44, TE 53.92 → 47.33, universe MAE 42.05 → 38.60; PBO 0.029/0.057 (clean); but **DSR 0.022/0.008 (gate ≥0.95) and BH-FDR survives at NO position** (p = 0.074/0.059/0.190 against BH cutoffs 0.033/0.067/0.100). Nothing ships; the rookie point STANDS, the interval is untouched. 🧨 **METHOD LESSONS (guarded, reusable):** (a) ⭐ **THE MATCHED FOIL REFUTED THE PARENT STORY'S STATED MECHANISM.** NF-D14 explained the lift as "the ratio partially corrects NF1.4's documented COLD bias" — a LEVEL story, which a per-position CONSTANT delivers with zero per-player information and zero ordering risk. Registering that constant at the identical base and identical λ (built by averaging the partner's OWN post-shrink scale, so the pair has a byte-identical per-position mean and differs in NOTHING but per-player variation) shows it **fails to beat the incumbent at all** — RB 87.33 vs 80.43, TE 55.49 vs 53.92 — while the availability arm beats it by **18.9 PPR (p=0.034, 5/7 classes)** and **8.2 PPR (p=0.022, 6/7)**. ⇒ the effect is PER-PLAYER, not a recalibration. **The bias corroborates from the other side**: the winner moves the pooled bias −20.87 → −22.44, i.e. FURTHER from zero, while MAE improves — a pattern a level story cannot produce. *A leaderboard rank could not have separated these two readings (NF-D10).* (a′) ⚠️ **BUT DO NOT COLLAPSE THAT INTO "THERE IS NO LEVEL EFFECT" — both things are true and they are SEPARATE.** The matched foil refutes the level story *as the explanation of the AVAILABILITY arm's lift*; independently, a per-position CONSTANT recalibration (`mean[residual]`) beats the incumbent at ALL THREE scaled positions (pooled 1.0301 vs 1.0738, 6/7 classes, **p=0.0052**, per-class sd 0.031 — a FIFTH of the availability arm's). ⇒ NF-D14 was **wrong about what makes the availability arm win, and right that there is something cold to correct**. 🔎 **RECORDED AS A POST-HOC LEAD, NOT A RESULT** — best of 33 arms chosen after seeing them, undeflated, never pre-registered; it needs its OWN story (a rookie-point LEVEL recalibration) with a clean gate, and reading it as shippable here would be the E2.1-r inversion. ⭐ *The low-risk half of this study (zero ordering harm by construction) carries the strongest evidence in it — worth knowing before scheduling the expensive half.* (b) ⭐ **A NULL MUST BE SHOWN NOT TO REST ON A GATE THE AUTHOR CHOSE — computed, not argued.** NF-D15 pre-registered NF-D14's stricter DSR ≥ 0.95 over NF1.4's own ≥ 0.0; the ship decision is RE-RUN at 0.0 **and with the DSR removed entirely**, and nothing ships under any reading because **BH-FDR blocks all three positions independently**. (If they HAD disagreed the pre-registered gate would still govern — the sensitivity is a disclosure, never a licence to re-read, which is the E2.1-r inversion facing outward.) ⚠️ **AND THE SAME DUTY APPLIES TO THE FRAMING, WHERE IT IS PARTLY LOAD-BEARING:** splitting the question into three per-position searches costs a multiplicity penalty a single POOLED test would not — pooled as ONE test the best availability arm reads **p 0.081** against the three-test BH cutoff of 0.033 it actually faced. The pre-registered per-position framing GOVERNS (it is the right frame for a per-position product, chosen in advance), but **it is the multiplicity correction, not the effect size, doing the blocking** — so the honest label is UNDERPOWERED, and a reader is owed that number rather than a shrug. (c) ⭐ **STATE THE MARGIN IN DRAFT CLASSES, NOT IN p-VALUE DECIMALS** (NF1.8's "in ROWS" convention, one unit over): 'p 0.074 vs a 0.067 cutoff' reads like a hair's breadth; it is SEVEN classes of a quantity whose per-class spread (sd 19.1) EXCEEDS its mean (12.0). Holding effect and spread fixed, RB needs **11** classes and TE **10** to clear BH unconditionally, WR **29**. ⇒ **RB/TE are UNDERPOWERED, NOT ABSENT — re-run when 2026–2028 land; WR is absent** (and its own winner is the constant foil). (d) **Where an availability arm wins at all, the winner is the DIRECT-LEARNED FOIL — not `ratio_pos`, which is NF-D14 §6's own construction** ⇒ the prescribed multiplicative haircut is not even the right functional form, a second independent reason §6's arm should not have shipped on its MAE alone. (e) **A shared deflation helper can put an INTERVAL story's column name on a POINT metric** — NF1.8's `deflate` labels its column `IS80`; rendered verbatim that would have printed an interval score's name over a tier MAE. ⛔ **QB WAS NEVER RE-OPENED:** the exclusion lives in ONE function (`apply_position_scale`) so no arm — candidate, degenerate or ORACLE — can route around it, and `qb_untouched` is PROVEN on emitted projections at max drift **0.000000000** across all 33 arms × 7 classes. **Wiring proof:** the harness rebuilds NF-D14 §6's scale in new code and REPRODUCES its published table exactly (±0.05, all four positions incl. the QB row it is forbidden to act on). ⚠️ **The selected arm is DEPTH-DRIVEN, so the week-1-vs-August provenance caveat is LIVE: the measured lift is an UPPER BOUND on what the served signal could deliver, and any revival must re-measure on the served August snapshot FIRST.** Board churn had it shipped: RB mean 21.4 PPR, 11 of 42 tier displacements. Model `rookie_point_scaling.py`; harness `run_nf_d15_point_scaling.py`; report `ablation_results/nf_d15_rookie_point_scaling.{md,json}`; 57 tests (the QB exclusion + the matched-foil mean-identity mutation-verified to FIRE — the first draft's symmetric fixture passed against a MEDIAN foil, i.e. pinned the shape of the check and none of its content). **No app change, no re-export, no changelog** — nothing user-facing moved.
- **⭐⭐ NF-D16 ✅⏸️ MODEL RATIFIED / PUBLISH HELD 2026-08-01 (rookie-point LEVEL RECALIBRATION, RB/TE/WR — NF1.4's DOCUMENTED cold bias, finally corrected; the FIRST NF-fantasy MODEL win after five deflated nulls):** NF1.4 measured a cold rookie prior (`tier_bias` RB −58 / WR −49 / TE −44 PPR) and never corrected it; NF-D15's matched foil then proved the AVAILABILITY lift is per-player and NOT that level correction, while separately observing a per-position constant beat the incumbent — a best-of-33 post-hoc lead. NF-D16 asks that question CLEANLY. 4 pre-registered correction FORMS (per-position multiplicative constant · additive offset · per-position × draft-tier constant · a direct-learned per-position OLS foil) × a shrink grid whose zero end IS the incumbent EXACTLY (an OUTPUT-space blend, because NF-D15's geometric shrink is undefined for an additive or affine arm), + the incumbent NULL, 17 arms × 7 held-out draft classes, on **NF1.4's own `tier_mae`, inherited not chosen**. ⭐ **SHIPS: `ols_slope · λ 1`, pooled tier MAE 1.0738 → 0.9407, positive in ALL 7 classes, PBO 0.029, whole-field DSR 0.996, p=0.0033 vs α=0.10** (RB 80.43→72.30, TE 53.92→42.29, WR 68.84→65.33). **The bias signature confirms the mechanism**: pooled bias −20.87 → **−5.41**, toward zero — the exact test NF-D15's per-player arm FAILED (it moved further, to −22.44). **FRAMING PRE-REGISTERED AS POOLED, IN WRITING, BEFORE THE RUN** (a level effect is a-priori common across positions; the ship unit is the whole per-position vector) and it did NOT decide the answer — the disclosed per-position reading has RB and TE surviving the 3-test BH-FDR independently. 🧨 **LESSONS:** (a) ⭐ **a peeking ceiling must be per FORM, not one for the field** — this story's first cut floored richer families (`mult_tier`/`ols_slope`, which CONTAIN the constant) on the CONSTANT ceiling and would have vetoed a real result as a metric inversion; the four per-form ceilings now order exactly as capacity predicts (ols 0.778 < tier 0.835 < const 0.909 < offset 0.911); (b) ⭐ **a permutation anchor is near-vacuous against a LEVEL hypothesis** — a level is a MARGINAL statistic, so a within-position permutation preserves it EXACTLY for the additive form (max |Δ| 7.1e-15); both permutations registered, the within one declared an expected TIE in advance; (c) ⭐ **'zero ordering harm by construction' was partly WRONG** — an affine is only CONDITIONALLY monotone (it inverts a board on a negative slope), so the measured zero is a property of this run's fitted slopes; only `mult_tier` genuinely reorders and it is INELIGIBLE at every λ, which is what keeps the constraint non-vacuous; (d) ⭐ **the low-risk framing is false ACROSS the board** — rookies move against VETERANS, and NF1.4's face-validity gate is TWO-SIDED (the cold incumbent breaches 0/28, REALITY breaches 9/28, so zero is the SYMPTOM not the target); the candidate breaches 0/28 ⇒ not over-corrected (veto-only check, added post-hoc and disclosed); (e) ⭐ **the ceiling gap's two pre-registered readings were BOTH answered** — pre-ship (A) ESTIMABLE (`skill_vs_null` 0.306, 45% of headroom captured), and re-running the whole bake-off against the SHIPPED point returns the incumbent as its own winner with headroom collapsed 0.2954 → 0.0152 and `skill_vs_null` −0.017 ⇒ (B) CLASS-VARIABLE: the effect is EXTRACTED and the lead is CLOSED. ⛔ QB excluded, scope inherited from NF-D15 BY IMPORT, drift 0.000000000 in the harness and on the served path. ⭐ **SERVED CHANGE VERIFIED TO BE THE CHANGE MEASURED:** applied to the FINAL SCORED point (not `fp_target` — that would ship an ungraded quantity, the E7.9 skew class), carrying the whole stat line by one scalar so displayed points still equal the scored line exactly; **OPT-IN via a new `recal_hist` arg** so no existing caller (or NF1.4's pinned point-invariance test) changes silently. Residual served-vs-graded Δ 0.032 PPR / 1 rank, both the pre-existing fumble-rounding quantisation (the lone 'swap' is two WRs at 37.788 vs 37.796). ⭐ **INTERVAL FLOORS RE-CONFIRMED after the centre moved: ✅ ALL MET and coverage IMPROVED** (rookie pooled 0.8354 → 0.8391, TE 0.90 → 0.92; RB's zero-slack floor held). Carried: NF-D14 `_LIMITATIONS` cross-reference, the NF-D15 RB/TE re-run POINTER (not a gate) in `run_interval_revalidation`, and the **ADP cache-poisoning fix** (an FFC error payload was cached permanently → a season silently served ECR-only). `rookie_point_recalibration.py`, `run_nf_d16_point_recalibration.py`, 45 tests; report `ablation_results/nf_d16_rookie_point_recalibration.{md,json}` (+ `*_post_ship`). ⏸️ **PUBLISH HELD — ⚖️ PM ruling (option C): validate the tripped clause before letting it decide.** On the rebuilt 2026 board the recalibration trips NF1.4's ADVISORY `rookie_board_face_validity` **PLACEMENT** clause (a rookie RB at overall rank **6** vs a clause admitting none inside the top 10); the LEVEL clause passes with ~40 PPR of margin and `top1_is_rookie` is False. ⭐ **NF-D17 — the pre-registered re-specification — is DONE and REFUTES the premise that the unvalidated half must be mis-calibrated:** the rank-space mirror of NF1.4's LEVEL fix (cap = Q10 of the realized per-class BEST rookie's overall rank, reference `(15,13,13,52,10,7,29)`) gives **8.8 vs the incumbent's 10** — one board slot — and reality breaches 'no rookie in the top 10' **2/7 (29%)**, indistinguishable from the corrected LEVEL clause's **9/28 (32%)**. ⇒ **rank 6 breaches an honestly-calibrated bar = a GENUINE VETO, and the verdict is THRESHOLD-INVARIANT** (it breaches at every quantile Q05–Q25 AND against the observed minimum 7 — outside the reference's entire support, so no threshold choice could have produced it). Serving is flipped **OFF** (one line, `recal_hist` not passed; the recalibration is OPT-IN so the point is byte-identical to the pre-NF-D16 incumbent), the lake artifact was re-run non-recalibrated, and the prod api-cache was never touched (DRY-RUN only). ⚠️ **STRUCTURAL FINDING, recorded so it is not mis-cited: the correction REORDERS ROOKIES ACROSS POSITIONS** — rookie #1 flipped QB→RB (Love 33 → 6) because RB/TE/WR are lifted and QB, by pre-registration, is not. **NF-D16's 'moves no ranks' claim was always WITHIN-POSITION**, never a claim about the merged board — which is exactly how a level change reaches a placement gate. ⏭️ Any 'attenuate at the top' idea is a NEW pre-registered hypothesis, never a λ re-pick (λ=1 was selected from a pre-registered grid; the slot-varying `mult_tier` was already ineligible on do-no-ordering-harm). **User-facing: a `frontend/data/changelog.json` line is staged but NOT live — it ships with the eventual publish, not now.**
- **MVP-2 = NF-C1-lite (config/scoring + VOR) ✅✅ DONE 2026-07-24.** Shipped the ⭐ SPORT-AGNOSTIC engine `quant_sports_intel_models/fantasy_engine/` (`league_config`/`scoring`/`vor`; a `SportProfile` plugs a sport in → **MLB F-C1 reuses it verbatim**) + NFL presets + `mart_nfl_fantasy_league_board` (10,024 rows = 7 presets × 2 sizes × 716 players). ⭐ **A LEAGUE = `(format, size)` two keys — league SIZE is a normalized grain dimension (VOR replacement scales with size).** Face-validity proven (full-PPR lifts pass-catchers; superflex lifts QBs #26→#8). ⚠️ inherits MVP-1's within-tier gap (NF-D2 fixes upstream); K/DST unprojected; intervals are a CV rescale (recalibrate before pricing). ⚠️ OPERATIONAL: the S3 write is `replaceWhere`-by-season → the WHOLE preset×size set must land in one run (adding a size = re-run the full set).
  - 🧩 **OPEN PRODUCT QUESTION (PM, trivial eng — a priority call, not a cost):** which extra league sizes/formats to ship as standing presets? 14-team was deferred; 8-team, 2-QB (vs superflex), IDP, TE-premium-as-default are candidates. Each = add to defaults + re-run.
- **MVP-3 = NF-C2 (draft board + optimizer) — ✅✅ DONE + LIVE (2026-07-24 ship + 2026-07-25 v1.3 final; ADMIN-ONLY to start):** an all-client-side live snake draft optimizer — VOR board + best-available + pick recommendation (VOR + roster-need + tier + bye-stack aware) + a live snake tracker, instant per-pick recompute. Engine `fantasy_engine/draft.py` (sport-agnostic, reused by MLB F-C2) + `export_draft_board_json.py` + TS mirror `frontend/lib/draft-optimizer.ts` + `frontend/app/fantasy/draft/` (AdminGuard). 4 feedback rounds folded in (K/DST, capacity-based surplus penalty, roster-fit guard, bye-stack penalty); Taysom-Hill multi-position dup root-fixed. **The usable draft product for the 8/22 draft.** ⭐⭐ **STRATEGIC SIGNAL: dogfooding MVP-3 proved the remaining gaps are UPSTREAM projection quality (off QB ranks → NF1; rookie over-value/aging/injuries → NF-D2; rookie team_id → NF-D1), deliberately NOT hacked in the tool (honest-analytics + §0.5). ⇒ the draft tool is now a QA HARNESS that makes NF-D2 the CLEAR highest-value next NFL-fantasy pull.** 🔁 refresh recipe: `run_league_board.py --from-lake --s3` → `export_draft_board_json.py --from-lake` (AWS_DEFAULT_REGION=us-east-2). ⚠️ engine LOCK-STEP: `draft.py` (Python) + `draft-optimizer.ts` (TS) change together. 🩹 load-bearing stopgap: rookie team_id backfilled export-side until NF-D1 durably wires it.
- **MVP-4 = NF-C0 (platform auto-import) — the FAST-FOLLOW / enhancement:** auto-pull league settings + rosters from **Sleeper (easiest public API) + Yahoo (OAuth)** → replaces manual config; then ESPN; then live draft-state sync. Slips safely — the manual MVP already works.
⇒ **build order for the open session: MVP-1 → MVP-2 → MVP-3, with MVP-4 as the enhancement.** A minimal draft tool = MVP-1+2+3; auto-import is the polish.
🎯 **DECISION (operator 2026-07-24) — GO WIDE NOW + a PARALLEL battle-testing track:** the primary push is wide (MVP-2 → MVP-3 draft tool for the draft-season GTM — a working tool on decent projections beats perfect projections with no tool). **BUT the model must be battle-tested, because a defensible "we beat System X's 2025 projections" is a top-tier GTM claim.** ⇒ run **NF-D3 (benchmark scorecard) + NF-D2 (the quality lever that actually closes the FF within-tier gap)** as a PARALLEL deep track — NF-D1's 2026 data is cheap/time-boxed, slot it in regardless. ⚠️ **THE CLAIM IS NOT FREE: MVP-1 does NOT beat FF today (loses RB/WR ordering; wins TE + MAE) → the broad "we beat X" claim is EARNED only after NF-D2 closes the gap AND NF-D3 proves it out-of-sample, multi-system, multi-season, apples-to-apples, no hindsight (see NF-D3's honesty bar). Scope every public claim to what's provably true — on an honest-analytics brand a cherry-picked claim backfires.** Fantasy is the ONE surface where a performance claim is on-brand (vs betting's `best_alpha=0`).

## 3. Sequencing / dependencies
- **NF1** needs the N1.2 shared player-week projection core (so N1.2 lands first, or NF1 builds the shared core if it runs first — coordinate).
- **NF2 rookie leg** needs NCAAF-P1A (✅ DONE). Veterans ship without it.
- **NF-FASTPATH** is runnable NOW-ish (needs only the N0.3 `mart_projections_preseason` mart) — the 8/22-timely deliverable.
- Reuse `hierarchical.py` + the feeder-MLE discipline + posterior-predictive + the E2.1-r discrete-metric hygiene (fantasy points off discrete-count props) — all proven elsewhere.

### 🧭 Model-refinement track — SEQUENCE (PM 2026-07-27; ✅ ARC COMPLETE 2026-07-28) — the market-blind SEASON-LEVEL feature space is EXHAUSTED + now CLOSED as a product investment (DECISIVELY proven 6× — NF1/NF1.1/NF1.2/NF1.4 + NF1.5's FULL-POWER blind sweep at 2.25× data / 6 model classes). NF1.5 shipped the REFINED-DUAL-BOARD (served) + a product win (beats ADP at all 4 positions, disagreement win-rate 0.507 > coin-flip 1st time). ⇒ the projection FRONTIER now moves to the WEEKLY / in-season model (the strategic successor). Future "add feature X to the SEASON model" pitches: DECLINE by default.
Rule: only ONE board-repointing Opus pass in flight at a time (two winners = a merge/selection collision to reconcile); the NF-D* DATA stories are cheap Sonnet + parallel-safe → run them ALONGSIDE, not in the queue.
1. **NF1.3 — market-aware (position-conditional ADP/ECR)** — ✅✅ DONE 2026-07-27: a PRODUCT WIN at QB/RB, first board to beat ADP (see §2) — but BUILT-NOT-SERVED. 🔒 SERVING HELD for NF1.5 (operator 2026-07-27): the SERVED board stays market-blind MVP-1 for now; NF1.5 revisits which board serves (market-aware-refined vs blind vs dual). The honest QB/RB BAR that NF1.2/1.4 face is the market-aware RESULT (NF1.3 proved market-aware wins QB/RB), even though it isn't the live board yet.
2. **NF-D8 — contract / financial features — ✅ DONE 2026-07-27.** Lands `contract_source.load_contract_features` (see §2 above) so NF1.2's H-CONTRACT candidate exists. (NF-D6 defense ✅ + NF-D7 xFP ✅ + NF-D8 contract ✅ all landed.)
3. **NF1.2 — veteran refinements** (SOS + system/pass-rate + correlation + O-line + xFP + opportunity-stability + spillover + contract) — ✅✅ DONE 2026-07-27, **VERDICT NULL**: all 7 families pre-registered + ablated (add-one/drop-one attribution arms + the NF1.1-baseline arm, ~140 configs/position deflated); every position beats the raw MVP-1 null on top-tier ρ but NONE passes PBO<0.2/DSR≥0.95/FDR → **MVP-1 stands as the market-blind fade board** (NF1.3 dual-board keeps QB/RB). Attribution: H-CONTRACT is the only positive isolated add (RB +0.021; NEGATIVE at WR — position-conditional, not universal); **TE is the near-miss** (only PBO pass at 0.143; the environment cluster sos/system/spill/qbcorr/opp ALL positive add-one; DSR 0.24 kills it — re-testable with more target seasons); WR — the honest win condition — cleanly fails (extended set a net drag, −0.019 vs the NF1.1-base arm). YPRR + catchable-rate probed UNAVAILABLE (PFF-gated). Harness kept for NF1.5's library: `nf1_2_model.py`/`run_nf1_2.py` (`add_refinement_features` attaches on the FULL pre-filter universe — the survivorship-order contract); report `ablation_results/nf1_2_refinements.{md,json}`.
4. **NF1.4 — rookie-model refinement** — ✅✅ DONE 2026-07-27, **MODEL VERDICT NULL (slot curve stands) + the HOT-CURVE PREMISE REFUTED**; ONE fix shipped (the rookie 80% interval). 134 configs × 4 positions, walk-forward by draft class 2019–25: no position repoints (QB had ZERO do-no-harm-eligible candidates; RB beat on tier MAE but PBO 0.343; WR/TE fail FDR). **The incumbent rookie prior is COLD, not hot** — `tier_bias` negative at every position (QB −32 … RB −58 PPR) and the #1-overall rookie QB's point projection is nearly unbiased over 7 classes (+8.9 PPR vs ~201 realized). The `where games > 0` SURVIVORSHIP filter is real (15% of drafted skill rookies never play, 35% at QB → the survivor-only QB mean inflates 67→104) but removing it made held-out accuracy WORSE everywhere (the board was already too cold). **The dogfooding symptom is a RANK effect from VARIANCE, not a level error** (the #1 QB projects QB11–15, finishes ≈QB19.5 — rookie QB outcomes are hugely dispersed and veteran QBs pack densely at that level) ⇒ it is an UNCERTAINTY problem. ⭐ **SHIPPED: the calibrated rookie band** — MVP-1's `fp × cv` parameter band covered **0.680 of its nominal 0.80 (0.444 at QB)**; the empirical tercile band (fit on the FULL drafted population so a late pick's p10 can honestly be 0) covers **0.790 (QB 0.741)** with the point projection byte-identical. Plus `rookie_board_face_validity()` (advisory gate, wired into the MVP-1 report — reference class CORRECTED to the per-class BEST rookie after the first cut fired 7/7 = uninformative; incumbent now breaches 0/28 vs reality's 9/28 and the 2026 board passes). ⚠️ follow-up: `fantasy_engine/scoring.py` re-symmetrises the league interval from a single `_sd`, so the band's WIDTH lands but its asymmetry does not (deliberately out of NF1.4's scope). ✅ **CLOSED by NF1.7 (2026-07-29)**, along with the class-level band itself. Report `ablation_results/nf1_4_rookie.{md,json}`.
5. **NF1.5 — CAPSTONE feature-combination bake-off** — LAST. ✅ UNBLOCKED (NF1.2 ✅ + NF1.3 ✅ + NF1.4 ✅). ⭐ REFRAMED post-NF1.2: since the market-BLIND space is EXHAUSTED, the blind combination sweep here is now a CEILING-PROOF (a clean null is the LIKELY + valuable outcome — it PROVES MVP-1/NF1.3 as the library ceiling, doesn't assume it); the **MARKET-AWARE refinements (blend-vs-learned + dispersion-weighted market, from NF1.3) are the ONLY credible remaining SEASON-LEVEL lift → the PRIMARY hypothesis.** Reuse NF1.2's `add_refinement_features`/`RefinementInputs` + the pre-filter attach contract. NF1.5 ALSO owns the deferred serving decision (which board serves) + the interval-calibration verify. ✅✅✅ **FINAL 2026-07-28 (expanded re-bake-off: base-2006 pool 2.25× data, 6 classes incl. PosTwoPart/PosRank): STAGE 1 = ALL FOUR positions beat the NF1.3 incumbent (QB learned+dispersion blend, RB flat blend, WR/TE learned blends; +0.005…+0.014); PRODUCT = best ever — vs ADP +0.026 pooled, WR +0.044, and the ADP-disagreement win-rate ABOVE coin-flip for the first time (0.507); vs ECR −0.011; calibration ✅ κ=2.075 calib_80=0.804. STAGE 2 at full power (16 scored seasons, 451–631 cfg/pos) = STILL NULL → the market-blind ceiling is DECISIVELY proven; nearest-miss recorded honestly: WR mlp×base (DSR 0.964 ✓, PBO 0.219 misses by 0.019 — auto-retest when 2026 accrues). SERVING = REFINED-DUAL-BOARD** (market-blind MVP-1 stays the fade baseline). Harness: `nf1_5_model.py`/`run_nf1_5.py` (staged, placebo'd, incumbent-barred, archives superseded stages; 30 tests). ⏭️ operator: re-run `--mode build --s3 --base-from 2006` (served board still carries the v1 selections).
⚠️ Every pass gates on the TOP-TIER product metric (the NF1.1 selection-metric lesson) under full deflation (PBO<0.2 / DSR≥0.95 / FDR + placebo where a search is wide); a survivor repoints the served board, a null keeps the incumbent (both valid). DL is a LOW-prior candidate (a small MLP earns ONE deflated seat in NF1.5; heavier DL — LSTM/GNN/DRL/multimodal — parked, see the "NF — Deep-learning research" backlog card). Weekly-model + sentiment/weather features are a SEPARATE future layer, not this track.

## 4. Maintenance
Update when an NF-story ships. Shares the `sports_dbt` NFL marts + the honest-analytics posture with the NFL betting catalog; does NOT share the betting edge/CLV machinery.
