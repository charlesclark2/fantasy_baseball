"""PFF feasibility spike (NF-W9-0) — authed client, facet crawler, entity resolution.

RESEARCH ONLY. `best_alpha = 0`; nothing here is on a serving path.

⛔ RAW STATS ONLY. See `guards.py`: PFF's own projections / grades / rankings are a
COMPETITOR'S MODEL OUTPUT, and consuming them would launder it into ours. The guard is
enforced in CODE (an endpoint refusal + a column strip that reports what it stripped),
not by convention, because a convention is exactly what gets forgotten under deadline.
"""
