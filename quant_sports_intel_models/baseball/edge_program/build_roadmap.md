# Baseball Build Roadmap — Two Tracks (now → MLB All-Star break)

**Status:** living · **Window:** now → the **MLB All-Star break (~mid-July 2026)** · **Scope: baseball only.**
**Last synced:** 2026-06-21 (resuming after ~1.5 days off) — **INC-6 ✅ RESOLVED** (zero-height bio row; fixed via NULL-coerce + bio tests→warn + run-gates/test-non-blocking build split + dbt-runner GitHub auto-deploy; Sunday build GREEN). **🎉 ALL INCIDENTS RESOLVED (INC-1 through INC-6) — incident board CLEAR.** (Residual trivial Parlay sweep = E11.6b, a normal low-pri backlog item, not an incident.) **Session A: E13.0 ✅ done (HOLD) → E13.1 next** (market-anchored residual). **Session B: E11.6 + E11.6b ✅ done (Parlay fully decommissioned) → E11.1-W1 next** (primary Snowflake lever). _(E11.7 pipeline-resilience scope narrowed — daily path done by INC-6, residual = sensor/cron paths + the documented HALT/WARN/ALERT contract + full severity sweep.)_
**MODEL TRACK (Session A):** **E1.9 ✅ + E13.0 ✅** — v6 = lean + de-leaked, no offline edge (expected de-leak fingerprint); `home_win/pre` PROMOTE deflated on pass-2 to a thin/ambiguous signal → **HOLD-for-deploy** (challenger registered, deploy withheld; not worth bespoke forward infra). **E13.1 🔴 KILLED 2026-06-21** (market-anchored residual fails CLV both tiers; H2H *edge* dead from a 4th angle). **🔒 BUT the H2H PREDICTION is a non-negotiable product feature** — objective = accuracy/CALIBRATION (Brier/ECE), NOT edge → **E13.6 (calibration/prediction-quality) is a standing model-track priority.** **The *edge* search pivots off H2H to TOTALS + props:** E13.3 (sub-models, cheap closure) → pull **E2.2/E2.3** forward (totals distribution = real edge hope) → E13.2/E13.4 totals-scoped; E13.5 deprioritized. Live betting-edge hope = **totals (E2) + props (E5)**; H2H = best honest calibrated prediction, presented without a beat-the-market claim.
**COST-OPT (Session B):** E11.3/E11.0/E11.4/E11.5/E11.2-T2/E11.0c all ✅ shipped → **NEXT = E11.1-W1** (heavy Statcast marts → dbt-duckdb/S3, the PRIMARY Snowflake credit lever, runs on fusion). Then **E11.7** (pipeline failure-semantics audit, closes INC-3/5/6 class), E11.0b (residual Dagster meter), E11.6 (Parlay cleanup). **6/22 audit** = first real Snowflake measurement.
**APP TRACK (Track 2):** beta onboarding LIVE; **next card = E9.1** (+EV breakeven line, bundle E9.14/E9.22). _(Full history below.)_

**Earlier (2026-06-18):** **🚨 E2.1b OVERTURNED THE E1 HEADLINE:** the program's #1/#2 feature (`bp_eb_xwoba`) is a **within-game leak** → "bullpen dominates / invest in bullpen" is RETRACTED; real signal is modest (coverage/uncertainty); **E1.7 de-leak ✅ SHIPPED + VALIDATED 2026-06-18** (serving-null fixed 37/37; MDA collapse confirmed all 3 targets; parity 1.0; slim contracts re-derived INTERIM — **`elo_diff`/`pythagorean` revealed once the leak was gone**; retrains gated post-E1.8). **E1.8 full leakage sweep is now the model-track critical path** (gates the final contract re-derive; if the #1 feature leaked, others may — may explain the broad H2H+Totals collapse). **New E1.9 = the deferred retrain upgraded to a v6 clean-slate model bake-off → Optuna** (best learner wins on the clean matrix, not the incumbent XGBoost/NGBoost defaults). **E2.0 + E2.0b ✅ SHIPPED** (238k derivative closes + live team/alt-totals cron); **🔴 probe killed forward-F5: The Odds API offers NO F5 lines at all** → **E2.0c** done → 🅿️ SportsGameOdds chosen but deferred post-revenue (validate free first). **💰 COST-OPT ACTIVATED 2026-06-18: "minimum Snowflake until profitable"** — E11.2-T2 (state-aware builds) + E11.1-W1 (Statcast marts → dbt-duckdb/S3) now lead Session B; 6/22 audit measures savings; AWS confirmed negligible (~$2-3/mo). **E1 COMPLETE** (gate set) but finding #3 retracted; **E4 closed** (CLV-too-small) but **H2H model stays live through beta**; E3.0/E3.0b ✅; **E2.1 ✅ gate-pass** (swap bullpen channel to leakage-safe before serving); slim re-promote **gated on E6.7 + re-derive post-de-leak**; E3.3 parked. App track: **E9.13 + E9.15 + E9.10 + E9.5 + E9.4 ✅ SHIPPED 2026-06-18 + E9.20 ✅ SHIPPED 2026-06-18 → beta onboarding LIVE (users provisioned)** (E9.10 added live `/users/profile` → unblocks E9.17; E9.5 reset validated w/ real email; E9.4 branded invites + bounce/complaint handling; **E9.20 P0 fixed — narrative side-attribution; chip was right, away-pick narratives were flipped**; changelog page shows full Mon–Sun range); Track 2 **reconciled** with the guide's E9 backlog (added E9.3/E9.9/E9.10; parked E9.7/E9.8/E10.1 flagged). **🟢 SES PRODUCTION GRANTED 2026-06-18** (50k/day, out of sandbox) → unblocks E9.4/E9.5 + E9.9's email path; Resend Path B retired; E9.5/E9.4 bumped up as beta-launch enablers. **Changelog convention noted: weeks group Monday→Monday** (§0.2); new **E9.18** (changelog accordion) added. **New E9.19 (MFA, Cognito TOTP) — P1 security, HARD GATE before E9.8 Stripe / paying customers.**
**Purpose:** the persisted, ordered execution backlog (the Trello source-of-truth) for the two parallel tracks. Distinct from the edge guide **§7** (dependency/lane logic) and `../../multi_sport_roadmap.md` (the cross-sport, fall-targeting strategy). Full story specs live in `edge_program_implementation_guide.md`; standalone prompts in `story_prompts.md`.

**Scope rule for this window:** **strictly baseball.** Multi-sport (NFL/NCAAF/NCAAB) and the Fantasy/Dynasty vertical are **parked** until after the break (see *Out of scope* below). The two tracks below are independent and run in parallel; the **model track is primary** (the platform's quality core), the **app track** ships beta-facing value on top.

---

## 📊 DASHBOARD — what to tackle next  *(added 2026-06-21 · the quick-nav layer; full detail in the tables further below)*

### 🎯 Pending — top items per track (priority order)

**▶️ MODEL PAUSE LIFTED (operator 2026-06-23) — focus the KEY DATA-QUALITY items + situational awareness BEFORE revisiting MLB totals/H2H (still critically important).** Sequence (operator 2026-06-23): **(1) E13.8 ✅ DONE 2026-06-23** market-accuracy benchmark (year×book H2H+totals — "what are we targeting?"; report + widened census analysis shipped; H2H ceiling ~0.245 Brier ≈ no headroom, totals price = coin-flip so any value is in the NUMBER which is already near the variance floor); **(2) E13.7 ✅ DONE 2026-06-23** cold-start fallback (recent-window archetype/Stuff+/platoon NULL 15%→0% via leak-clean league baseline + `is_cold_start` flag; ⚠️ a retrain is needed before the new convention serves live); **THEN (3) ▶️ NOW ACTIVE — revisit totals + H2H:** the **E13.4 lift-tests are UNBLOCKED** (run on the trustworthy non-cold-start subset; stratify on `is_cold_start`), aimed at the DERIVATIVE markets per the E13.8 bar (main total = efficient); + **E2.3** distribution; + H2H calibration-parity. Cost work is now mostly measure-gated to 6/25. **✅ E13.11 SHIPPED 2026-06-23 — the de-leaked v6 is LIVE (all 3 targets × both tiers); the bullpen leak is GONE FROM PROD, explanations now led by elo_diff/pythagorean; E13.6 calibrator re-fit (T→1.64); v5 = one-step rollback. (was: DEPLOY the de-leaked v6 regardless of lift, reverses the E1.9/E13.0 HOLD.) Production still serves leaky v5 → SHAP explanations are bullpen-leak-dominated → misleads beta users on our methodology (integrity issue). Bundle the deploy with E13.7's cold-start convention + any E13.4 winners that land in time; gate on parity + the E13.6 calibrator re-fit, NOT edge. This is the near-term model-track priority + the retrain that resolves the E13.7 serving dependency.** 💡 Idea crowdsource (2026-06-23) added: **E13.10** zone-matchup signal (flagship novel axis) + **E13.2** revived as the PA-simulator-for-derivatives + cheap E13.4 candidates — see those rows.

**🧪 Model — Session A (un-paused; data-quality first)**

- **🔑 EDGE-SEARCH REFRAME (operator 2026-06-22): audit feature/signal COVERAGE before re-modeling.** **✅ E13.4 audit done 2026-06-22 — and it CORRECTED its own premise (the valuable part):** the recency/momentum axis is NOT missing — it's heavily covered via **Statcast** (`mart_team_rolling_offense/pitching` 7/14/30d form; E2.1 per-side = 308 feats w/ trend deltas). The FanGraphs windows are unused but **redundant** with it. **So the models already had momentum and still came up thin → tightens "no edge."** Genuine non-redundant gaps are NARROW: **⭐ times-through-order penalty (#1, raw col feeds no feature)**, bullpen-fatigue×game-length interaction, FG wRC+ (likely redundant), park×batted-ball (low). **⏭️ NEXT: operator runs the pre-registered incremental-lift tests (per-side run-means first, then H2H).**
1. **E13.4** — audit/dossier ✅; the candidate lift-tests are the remaining edge work (TTO penalty leads). Ship only leak-clean+shrinkage winners (purged CV + PBO/DSR) into the E2.1 means + H2H.
2. **E2.3** ✅ **PRODUCT-COMPLETE 2026-06-24** — convolution → predictive distributions: total + team-totals CALIBRATED (calib_80 0.838/0.863/0.847, PIT-flat); run-diff = documented accepted near-miss (the by-design dropped home/away dependence, NOT dispersion — not chased, contradicts E2.2; run-diff isn't a served surface). Leak-guard was already resolved by E1.7. Served `totals_distribution_v1.json` (per-side r 4.03/3.57 + P05–P95 grid + p_over). **Calibration ≠ edge** (main total efficient per E13.8; edge question gated at E2.6). **▶️ NEXT in the chain = E2.5** (signal registration + leakage-safe backfill) → E2.6 (derivative pricing + gates; emits the E2.7 UX prompt). _(detail: Track 1 table row 8.)_
3. **E13.2** — bottom-up game simulator (AFTER the coverage audit — a better engine on a coverage-gapped mart just reproduces thin results; revisit once the audit ships real signal)
4. **E13.5** — generic shadow-serve + forward-scoring harness (deprioritized; revive for a totals/props challenger)
5. **E2.0c** — F5 alt data source (🅿️ parked post-revenue; validate free tier first)
   - *Standing: **E13.6** H2H calibration re-fits after E12/30.3 or a served v6 (registry guard). **E13.3** meta-model re-fit gated on a served de-leaked v6 champion. **E2.2 ✅ done** (copula unnecessary, ρ≈0).*

**🛠️ Cost-opt / Infra — Session B**  ·  **✅🎯 COST SPRINT VERDICT 2026-06-24 — IT WORKED (the target is being met):** the E11.2-T2 follow-up re-measure is CONCLUSIVE — **Snowflake COMPUTE_WH ~56.96 → ~23.2 cr/7d = −59% (clean post-fix ≈ −50% ongoing, ~8.14→~6.4 cr/day); untagged CREDITS −62.9% (56.5→20.93 cr); untagged% 99.4→90.3%.** All 4 initiatives delivered/visible: **T2** = the primary driver (incremental feature-rebuild skipping live), **E11.9** = delivered (untagged WORKLOAD now tagged across all prod jobs; the residual 90.3% untagged is BENIGN dbt-fusion internal bookkeeping — test asserts/COMMITs/SHOW/SYSTEM calls on implicit connections, un-taggable = a connector limit, NOT a regression), **E11.10/E11.11** tagged+visible, **W1 decommission LANDED 6/22** (Snowflake pitch CTAS removed). Plus the INC-13 view-skip guard shipped+deployed (state-aware selector always unions `config.materialized:view`; 2 regression tests). MCP queries already tagged (0.06 cr, negligible). **⏭️ 7/1 final re-check** (the 6/24→7/1 window = 100% post-fix → the precise number; invocation_id fragments age out ~6/29). **⇒ "minimum Snowflake until profitable" is being delivered; this closes the 6/25 cost-re-measure + E11.0b re-measure legs.** _(orig target ↓)_ **🎯 TARGET (operator 2026-06-22): Snowflake at/near MINIMUM by ~July 1** — lead with E11.9 (the bill is rebuild-frequency, NOT migration) → W1/W2/W3 move all BATCH/non-serving marts off Snowflake → **serving path (E11.1-Wsv) stays on Snowflake & migrates LATER, carefully (NOT rushed into the window).** **MS.0 multi-sport odds backfill ✅ DONE 2026-06-23** (all 3 sports × 2020–2025 in S3; 3.77M credits still in hand; scores follow-on = MS.1, free sources). **🧹✅ DONE (E13.11 session, 2026-06-23): prediction-BACKFILL cost-opt — `--is-backfill` ranges now pull the whole window in ONE Snowflake read → local parquet, and buffer all dates into ONE transaction (single DELETE + chunked executemany + single commit) → warehouse spins up ONCE, not ~N times.** (Live single-date + per-date writes untouched; `executemany`-once chosen over write_pandas to keep the proven 80-col casts + dodge the DATE/TIMESTAMP_NTZ corruption footgun.) 9 tests; 498 pass.

1. ✅ **E11.9 deployed** (tagging fix + redundant-rebuild removed + ADD-COLUMN-per-write killed) · ✅ **E11.9-T2 shipped** (feature_pregame → incremental, parity-validated, predictions unchanged) → **⏳ THE BILL RE-MEASURE is now the key checkpoint: `WAREHOUSE_METERING_HISTORY` COMPUTE_WH + rebuild query-count vs 57.57 cr/7d / 3,233-q baseline, after a few days of incremental cycles.** The two biggest Snowflake levers are in; measure the delta.
2. **✅ E11.15 OBJECTIVE STANDS — but the Railway substrate was SUPERSEDED by INC-16 (AWS), reconciled 2026-06-28.** Sequence: E11.15 self-hosted OSS Dagster on Railway (6-25) → Railway T&S-restricted (6-26, INC-16) → SAME OSS Dagster re-homed to **AWS EC2** (INC-16 P1–P6) + Railway CANCELLED. So "self-hosted OSS = sole orchestrator, Dagster+ idle-rollback" has held continuously since cutover; only the HOST changed (Railway→AWS). 💵 Cost now BEATS the ~$50/mo target (Railway fully gone → ~$15–35/mo AWS). ⏳ **E11.15's Phase 4 (bank the saving = DECOMMISSION Dagster+) is the SAME pending gate as INC-16 Phase-J** — still open, after the weekly-schedule soak. The Railway-specific details below are HISTORICAL. _(orig ↓:)_ **✅✅ E11.15 SHIPPED + CUT OVER 2026-06-25 (Opus) — SELF-HOSTED DAGSTER (OSS) IS THE SOLE LIVE ORCHESTRATOR; Dagster+ idle/rollback-only.** 4 Railway services (codeloc gRPC / daemon / webserver / Postgres); full OSS (not Hybrid); dbt stays out-of-process on the dbt-runner; `DefaultRunLauncher` + `QueuedRunCoordinator` (max 5, group-limit 1) to keep RAM/$ lean; `run_monitoring` + 4h max-runtime. **Both serving paths validated end-to-end on self-host** (morning daily_ingestion compute_elo→dbt_daily_build→predict_today→narratives; intraday lineup_monitor). **Expected ~$325→~$50/mo (~85%).** Files in `services/dagster/` (+ runbook README). 🐞 Deploy bugs fixed (in the runbook): dagster-postgres missing dep; Railway argv `$PORT` not shell-expanded → hardcode :3000; `dagster._core.*` paths; bind `[::]` for dual-stack. **🔑 CI/CD: a Dagster pipeline change isn't live until `dagster-codeloc` REDEPLOYS off main (gRPC serves the baked image). ⚠️ STEADY-STATE DISCIPLINE: do NOT merge to main during an active serving run (a codeloc redeploy interrupts in-flight ops).** **⏭️ PHASE 4 (banks the saving — NOT done): after a clean multi-day self-host window (watch the next few ~12:00 UTC daily runs), DECOMMISSION Dagster+ + confirm the first $0 invoice. KEEP the Dagster+ account as rollback until then — do NOT delete yet.** **▶️ NEXT cost session (Opus) = E11.1-W2** (W1d now proven → reuse the external-table pattern). _(orig escalation ↓)_ **🚨🚨 DAGSTER COST — WORSE THAN IT LOOKED + RE-PLATFORM APPROVED 2026-06-25 → E11.15 (prompt READY) = the PRIMARY fix.** **⚠️ The "$139" is ONLY the June 18–30 partial cycle (~13 days) → TRUE MONTHLY run-rate ≈ $320–350/mo (and likely GROWING as the season ramps).** $59.64 MTD; this blew through E11.0b's "~$50–100 → close" threshold by 3–4×. **⏱️ FREE INTERIM (operator, no session): toggle OFF the dead `odds_snapshot_job` + `pregame_snapshot_job` schedules in the Dagster+ UI — ~16% of the meter, zero effort, stops it during the migration window.** **🔀 E11.14 DEFERRED (operator 2026-06-25): the next Sonnet session continues the E11.1-W LAKEHOUSE waves (→ W2) instead of E11.14** — E11.15 handles the structural Dagster $; E11.14's interim trim is marginal + would collide with E11.15's `sensor_jobs.py` edits, whereas W2 (Railway-cron, independent) doesn't. E11.14 becomes a post-E11.15 LOAD-hygiene follow-on (apply the cadence-widen + change-gate to the self-hosted instance once it's live). **Operator decision: (a) SELF-HOST Dagster (OSS) on Railway → swap metered Dagster+ Serverless run-minutes for FLAT Railway compute (largely eliminates the bill) = E11.15 (phased: parallel→validate→cutover→decommission; 🔒 touches LIVE serving orchestration); (b) UPGRADE Railway Hobby→Pro (>$20/mo crossed + the self-host needs the headroom) = ⏭️ OPERATOR billing action, confirm Pro active before E11.15 Phase 1.** **E11.14** (change-gate the serving rebuild + cadence/odds-cron trims) is RE-PRIORITIZED to a LOAD-hygiene follow-on — on flat Railway compute it's no longer the $ lever (do it after/alongside E11.15 to relieve CPU contention). _(E11.0b's paused scope folds in here.)_ _(orig E11.0b ↓)_ **STEP-0 DONE 2026-06-23, PAUSED for the 6/25 re-measure (expected CLOSE).** Confirmed by reading the job graphs: **~95% of the Dagster meter = the serving chains** (`lineup_monitor`/`statcast_catchup`, whose dbt gates predict in-graph → can't fire-and-forget; that's E11.1-Wsv, post-July-1). The cron-direct slice (`odds_current`/`odds_clv`) = ~6 min/day (<5%). T2 too fresh to measure (committed 6/22). Baseline in `docs/e11_0b_step0_baseline.md`; 6/25 reminder armed. **⇒ the residual Dagster lever is ALSO Wsv** (like Snowflake's serving cost). **⚠️ NEW VARIABLES for the 6/25 re-measure: E11.10 (✅ 6/23) cut write_serving_store ~8min→35.7s on BOTH serving chains, AND E11.11 (✅ 6/23) zeroed the per-fire Cortex/narrative + umpire work on no-change cycles → the 6/25 Dagster number (AND the Snowflake Cortex line) should drop materially from these alone (separate from T2/Wsv); attribute BOTH before concluding.** Refined frame: serving chains can't be MIGRATED off Dagster pre-Wsv, but their per-fire WORK can be trimmed (E11.10 did). At 6/25: if Dagster ~$50–100 → close; else migrate only the odds crons.
3. ✅ **E11.1-W1 / W1d FULLY SHIPPED 2026-06-25** — external tables live, `mart_pitch_*` repointed, HALT tiering + ordering done, CI green, dev→main unblocked. **🔍 OPEN DQ (hand to PM): `feature_pregame_lineup_features` has duplicate `(game_pk, side)` rows (SCD-2 race in `feature_pregame_lineup_state`; now warn-only); assess training-data impact before W2.** Operator: commit 5 files → merge → deploy Dagster → re-run `run_w1_lakehouse.py` (boolean-coalesce fix). Next: E11.1-W2.
4. ✅ **E11.8 done** — feed-freshness monitoring completeness (found 2 real bugs: archetype not in daily job; SLA breach was silent `SkipReason`). Matrix + 10 sensors.
5. **✅ E11.1-W2 + E11.16 SHIPPED 2026-06-26 (Opus, merged) — 8 pitch-derived batch marts → DuckDB/S3 (proven W1 pattern; Snowflake = thin views over `lakehouse_ext` external tables).** Smart call: kept the W2 build INSIDE the self-hosted Dagster `run_w1_lakehouse_op` (NOT a new Railway cron — the "cron" guidance was a Dagster+ billing concern that E11.15 eliminated). **🔴 The durable value = validation caught 3 LATENT PROD BUGS W1d had shipped silently:** (1) **`woba`-zero (SERVING): `mart_pitch_play_event`'s DuckDB branch dropped `woba_value/denom/xwoba` but the external-table DDL still declared them → Snowflake read NULL → `mart_pitcher_batter_history` all-zero woba/xwoba across 1.23M rows SINCE W1d → ZEROED the H2H matchup features.** Fixed (repoint to `stg_batter_pitches`; verified real woba restored). ⚠️ FOLLOW-UP: drop the dangling woba cols from the DDL + **grep ALL other readers of those cols — they were getting NULL too (blast radius may be wider than the matchup feature).** _(Likely-minor served-pick impact — matchup feats tested inert per E13.10 — but confirm + the window was W1d→W2.)_ (2) **2018 17% data loss (W1 prod bug): S3 `stg_batter_pitches` had 599k/722k 2018 pitches → W1's prod `mart_pitch_*` were ~17% short on 2018 since W1d.** Operator re-ingested 2018; W1 repaired (training-integrity, 2018 historical → low serving impact). (3) `game_date` VARCHAR→date cast. + a re-ingest dupe (old year-file → 2018 doubled; `aws s3 rm`). **`parity_check_w2.py` now HARD-PRE-FLIGHTS stale-year-file + natural-key dupes → this corruption class fails loud in W3.** **E11.16 = LOCAL `dbt_state.sh` deferral fix** (CI was already correct; the 404 was local-only fusion downloading a dbt-Platform manifest). ⚠️ **SECURITY: `~/.dbt/dbt_cloud.yml` holds a LIVE dbt Cloud token in plaintext (not in repo) → operator: rotate + `chmod 600`.** ⏭️ Cutover finish (next self-hosted daily cycle): confirm the 8 build as views, W2 builds before `dbt_daily_build`, refresh covers W2. **✅ E11.1-W3pre DONE/LIVE/VALIDATED 2026-06-28 (Opus)** — the 4 odds/CLV staging models (`stg_oddsapi_odds/events`, `stg_derivative_odds`, `stg_statsapi_games`) now serve as VIEWs over S3 external tables; full `daily_ingestion_job` ran clean on the box; `mart_odds_outcomes`/`mart_game_odds_bridge`/CLV marts all validated fresh; woba W1d bug CLOSED (8,218 non-null); **dbt Cloud token ROTATED** (E11.16 security item closed). Solved mid-cutover: NULL-ts spine sentinel, DuckDB OOM (spill+threads=2+dedup-first), `deploy.sh` false-rollback (→120s readiness poll), **P5 CD gap fixed (`orchestration_cd.yml` now rebuilds on `dbt/**`+`scripts/**`)**. **✅ E11.1-W3-main DONE/MERGED/CUTOVER LIVE 2026-06-28 (Opus)** — ⚠️ scope PIVOT (the "safe/eval marts first" set was mechanically BLOCKED — reads serving-coupled parents not yet in S3 → deferred to Wsv): migrated the **11 genuinely-unblocked PITCH-derived marts** (archetype/handedness/tto/base-state/bullpen/reliever splits) instead; verified healthy; reusable DuckDB-compat lessons (`::decimal(scale)` not `::number`, `dateadd`→`interval`, `game_date::date` before window ops, deliberate scale-0 `woba` zeroing). 📊 **INVENTORY: S3-lakehouse = 26 marts** (W1 7+W2 8+W3 11+substrate); **Snowflake-only residual = 35** — the "all-batch-off-Snowflake finish" was NOT achieved; residual by blocker (each a FUTURE wave): odds/CLV eval (4) + odds/serving (~9) → **Wsv**; `odds_snapshots_historical` (2021–25) S3-export precursor → unblocks `mart_closing_line_value`+`mart_odds_line_movement`; FanGraphs facts → 3 marts; posteriors/cluster tables → 5; seeds (`ref_teams`/`dim_team_name_lookup`)+`mart_game_results` chain → ~10; raw savant (`catcher_framing_raw`/`oaa_team_season_raw`/`stg_batter_sprint_speed`) → 3. 📌 AC tail: confirm the Snowflake credit drop (11 fewer CTAS/day) next billing window. 🚀 **ACCELERATION DECISION (operator 2026-06-28): DROP the July-1 calendar gate — migrate EVERYTHING off Snowflake ASAP, end-state = ONLY the Cortex narrative (`generate_pick_narratives.py`) on Snowflake.** Rationale: the "serving moves last / POST-July-1" caution is obsolete — (a) P2 put DynamoDB between users and Snowflake, so marts are now read at BATCH time by `write_serving_store`/the morning build, NOT user-request time → same low-risk class as W2/W3; (b) INC-16 + P5 CD auto-rollback + P6 alerting + the proven value-preserving parity pattern make fast safe. **New gate = PER-MART READ-PATH (batch vs request-time), NOT a date.** ⚠️ **W4 first task: AUDIT whether ANY Snowflake read still happens at USER-REQUEST time post-P2** (backend routers / Lambda) — likely none → then the whole 35-mart residual is migratable now. Keep the hardened per-mart parity gate (it caught 3 latent serving bugs W1d shipped silently). Sequence the residual as rapid waves NOW: precursors (S3-export each external source: `odds_snapshots_historical`, FanGraphs facts, posteriors/cluster tables, raw savant, seeds+`mart_game_results`) → their marts → then the feature/serving READERS off the Snowflake views to direct S3 (kills residual scan credits = the old "Wsv"). **✅ W4 DONE 2026-06-28 (6 marts: FanGraphs ×3 + park-granular + woba-vs-cluster + catcher-framing; + FG precursor subtree + 2 builders on DuckDB; parity GREEN).** ⭐ **W4 AUDIT (program-defining): NO mart is read at request time — every request-time Snowflake read is FALLBACK-tier (DynamoDB→S3→Snowflake) and clusters in the serving/odds/results subtree = W5/W6/W7 → W4 batch-safe, W5/W6/W7 keep the careful tier (repoint/remove the Snowflake fallback on migration; P2b already did `/picks/ev`).** Inventory after W4 ≈ 32 S3-lakehouse / 29 residual. ⚠️ builder dual-write: `cluster_pitchers`/`fit_granular_park_priors` `--s3` is additive — Snowflake builds stay until W5/W7 migrate the feature-layer + archetype consumers (partial cost win until then). **✅ W5 DONE 2026-06-28 (15 marts: seeds + the `mart_game_results`/`mart_game_spine` team/game chain + the 4 W4-deferred; parity GREEN, ready-to-merge; FIRST incremental→view conversions).** Inventory now ≈ **47 S3-lakehouse / ~14 residual.** Careful-tier result: only `mart_team_pythagorean_rolling` is request-read (game-detail Snowflake fallback behind the cache) — left in place (serves the S3 view post-cutover). **✅ W5b DONE 2026-06-28** (the only tolerance-class wave: archetype Bayesian chain `cluster_batters`/`compute_archetype_posteriors`/`mart_batter_archetype_vs_pitcher_cluster` → S3; seeded cutover VALUE-EXACT 1,125/1,125 Δ=0; merged). **✅ W6 DONE / DEPLOYED 2026-06-29** (the most serving-coupled tier: 11 odds/CLV+serving marts + 2 staging flattens + 2 Group-C marts → S3, parity GREEN 15/15; ⭐ intraday refresh = option (b): `mart_odds_outcomes` split `_history`(daily)/`_current`(intraday O(today)) so served prices stay fresh; 3 cutover fixes incl. wiring `monthly_schedule` re-export to prevent an INC-17-P2-class lineup-null post-cutover). **Only the READERS + Cortex remain on Snowflake.** Inventory now ≈ **63 S3-lakehouse.** 🔭 PM to confirm next cycle: first `--w6` daily build green + lineups non-null; intraday `_current` fresh; `/performance` CLV today; EV/Line-Shopping/game-detail fresh + cache-miss 200 (INC-18 grant held). **✅ W7a DONE / DEPLOYED 2026-06-29** (the cluster credit-drop = banked): native `cluster_batters`/`cluster_pitchers` builds now read by NOBODY — 4 matchup consumers + `compute_archetype_posteriors` got `--s3` modes, 4 dbt feature models repointed to `lakehouse_clusters` external tables; `W7A_LAKEHOUSE_S3=1` on box; parity Δ=0. INC-18 resolved as KEEP+FLAG (the `stg_*` request-path reads serve pre-game/live data with NO clean post-game mart equivalent → kept behind the band-aid + last-resort, flagged as a W7b serving-mart backlog). 🐞 4 latent `--s3`-write bugs caught by actually running them (worst: a full-season-replace wiped the rolling posterior history → matchup feature went dead mid-session → fixed to upsert; **lesson: activating a dormant `--s3` WRITE path needs a real run, parity only validates reads**). ⏰ **CAREFUL-TIER WATCH — tomorrow's first scheduled `daily_ingestion_job`** (matchup ops `s3=True`+succeed; `signal_available`~100%; feature archetype/cluster cols non-null; posteriors parquet stays full history); rollback = `W7A_LAKEHOUSE_S3=0` + recreate dagster. ⏭️ W7a Phase 5 (seasonal cluster builds `--s3` only + optional DROP native) + Phase 6 (measure credit delta). **➡️ NEXT = W7b** (the prediction-path core: `feature_pregame_*` DuckDB branches + `predict_today`/`write_serving_store` reader repoint [incl. E9.37's `_LINE_MOVEMENT_SERIES_BATCH`] + request-path last-resort→direct-S3 + `mart_player_profile_identity`/`feature_pregame_injury_status` + the multi-day PARALLEL-RUN). ⚠️ **HONESTY REFRAME (from the W7a handoff): "Cortex-only" is NOT after W7b** — a LONG TAIL remains (W8+): the other sub-model signal generators (bullpen/run_env/defense/env_state/starter/offense), the EB posterior builders (`compute_starter`/`bullpen`/`lineup_posteriors`, `fit_park_priors`), `compute_elo`, the **raw ingestion scripts** (write the lakehouse substrate → S3-native = a big change), and the monitoring sensors. W7a/W7b bank the MART + cluster + feature-reader credits; the tail is more waves. **Plan the end-state honestly: W7b is the prediction-path finish, NOT the program finish.**
✅ **E11.1 RESIDUAL AUDIT DONE 2026-06-29** (`E11_1_snowflake_residual_audit.md`) — authoritative tail map. **Honest count: ~6 substantive waves (W8–W13) + W14 (Cortex→Claude) to ZERO-Snowflake.** Counts: 76 `lakehouse_ext` external tables ✅; 74/128 dbt models S3-tagged; **19/21 `feature_pregame_*` STILL native**; ~20 raw-ingestion writers still Snowflake; 1 Cortex script. **W8–W14 PLAN:** W8 feature-layer dbt→S3 (HIGH/HIGH, serving-critical) · W9 the **7** sub-model signal generators→S3 (HIGH) · W10 stateful builders (sequential posteriors + `compute_elo` — accumulate semantics, HIGH risk) · ⭐**W11 raw-ingestion→S3-native (the LONG POLE — retires the raw schemas + the export bridges; VERY HIGH; independent/per-source/parallelizable; template = `ingest_statcast_to_s3.py`)** · W12 monitoring sensors→S3 (10 sensors, MED) · W13 serving-state (`daily_model_predictions`/`prediction_log`/state tables) + `settle_user_bets` + the `pipeline.resources` lazy-init cleanup · W14 swap Cortex `COMPLETE()`→Anthropic API (LOW; the precondition for FULL decommission). **🔧 AUDIT CORRECTIONS to the W7a tail:** the EB posterior builders are now **dbt models** (`eb_*`), NOT the unwired Python `compute_*_posteriors.py` (→ W8 dbt problem); **7** generators not 6 (+`starter_ip`); NEW items W7a missed = sequential posteriors (player/team), the SCD-2 state writers, the serving-state tables, `settle_user_bets`, the import-coupling, the ~180 offline training scripts (one low-pri class — frees up once `data_loader` gets an S3 mode). ✅ **PM DECISIONS (operator 2026-06-29) — these SHRINK + DE-RISK the remainder:** **(1) PRAGMATIC Cortex-only — KEEP a minimal Snowflake** (`daily_model_predictions` as Cortex's I/O surface + optionally the small stateful model-state tables). **W14 (Cortex→Claude) is DROPPED** — deliberately stay on the platform (option to scale back UP if profitable). **(2) Serving layer = DynamoDB (primary) + S3 (cache backup)** — corrects the audit's stale "Railway PG" (Railway CANCELLED); a new RDS/PG is NOT introduced. **⇒ REFRAME of the remaining program:** because a minimal Snowflake STAYS anyway, the **hardest/riskiest stateful-accumulate tables (Elo `team_elo_history`, the sequential posteriors, `daily_model_predictions`) can simply REMAIN on that minimal Snowflake** rather than risk the accumulate migration (the W7a posterior-wipe class) → **W10 + W13 become OPTIONAL/cost-driven, not mandatory; W14 dropped.** **MANDATORY remainder = W8 (feature layer→S3 ✅ SPEC'D — casting-discipline baked in for the INC-19 type-drift) + W9 (the 7 signal generators→S3) + W12 (sensors→S3)** — gets the serving/feature/signal/sensor path fully off Snowflake. **OPTIONAL/cost-driven = W10 (stateful builders), W11 (raw-ingestion→S3-native, the long pole that retires the raw schemas + warehouse storage), W13 (serving-state).** 🏛️ **CRISP 3-TIER DATA-HOME PRINCIPLE (operator 2026-06-29):** **DynamoDB = ONLY data the APPLICATION reads** (the serving cache: picks/performance/bankroll/etc.) + S3 cache backup · **S3 lakehouse (DuckDB) = analytical/build data** (marts, features, signals) · **minimal Snowflake = Cortex I/O (`daily_model_predictions`) + the stateful MODEL-STATE tables** (Elo `team_elo_history`, sequential posteriors). ⇒ **model-state tables do NOT move to DynamoDB — they STAY on minimal Snowflake** (the application never reads them; they're builder state). 💰 **COST:** W7a/b banked a real partial cut; remaining big lever is **W11** (raw schemas/storage) — now a cost-ROI decision, not a mandate. **Sequencing after W7b: W8 + W9 + W12 to finish the serving/feature/signal/sensor path off Snowflake; W11 opportunistically for raw-cost; W10/W13 stay on minimal Snowflake (NOT mandatory).** · 📌 track: `mart_odds_events` stale since events feed stalled 06-04 (pre-existing, non-W3). **✅ W7 SPLIT → W7a DONE/CODE-COMPLETE 2026-06-29 (Opus); W7b deferred** (recap `E11_1_W7a_session_recap.md`). **W7a banked the credit drop + resolved INC-18, NOT yet cut over** (operator gate `W7A_LAKEHOUSE_S3` + the W7 cluster external tables): (a) ⭐ the 4 matchup-signal consumers got an `--s3` read mode (writes stay Snowflake) + the 6 native-`source('statsapi',…_clusters)` refs in the 4 feature models repointed to a NEW `lakehouse_clusters` source over `lakehouse_ext.{batter,pitcher}_clusters` (new `generate_w7_external_tables.py`; refresh `W7_TABLES`; daily ops gated `W7A_LAKEHOUSE_S3` default-OFF) ⇒ once flipped + `cluster_*.py --s3`-only, the native cluster builds drop = the real credit drop. (b) INC-18/request-path RESOLVED **keep+flag** — every `stg_*` request read is pre-game/live/box-score with NO clean post-game mart equiv (`mart_game_results`=Final-only) → KEEP behind the committed band-aid (post-hook + FUTURE GRANTS) + KEEP the last-resort; `stg_statsapi_games`/`probable_pitchers`/`lineups_wide` flagged as the W7b serving-mart backlog. (c) `mart_player_profile_identity` + `feature_pregame_injury_status` **DEFERRED to W7b** (feature_* serving model, from-scratch mini-wave — precursor `stg_statsapi_player_injury_status` not in S3). Gates: `dbtf compile` 1771/1771✅, fast pytest 690✅. ⚠️ **Cortex-only is NOT one wave away** — after W7b's prediction-path core there's a LONG TAIL still on Snowflake (the OTHER signal generators, EB posterior builders, `compute_elo`, raw ingestion, monitoring sensors). **➡️ NEXT = W7b** = the dedicated prediction-path session (`feature_pregame_*` DuckDB branches + `predict_today`/`write_serving_store` reader repoint + last-resort→direct-S3 + multi-day parallel-run + cutover). **✅ W7b-1 CODE-COMPLETE / GATED-OFF / PARALLEL-RUN STARTED 2026-06-29 (Opus, open PR) — NOT cut over** (recap `E11_1_W7b_HANDOFF.md`). ⚠️ **W7b SPLIT:** W7b-1 = the READ-PATH mirror (exports the Snowflake-built feature OUTPUT → S3 daily + points `predict_today`/`write_serving_store`/backend-last-resort reads at S3; the feature BUILD stays on Snowflake → a daily Snowflake→S3 mirror dependency remains); **W7b-2 = the feature-BUILD DuckDB conversion = 🟰 W8** (same wave — the ~7k-line `feature_pregame_*` tree; the risky blind-translation; deferred; retires the mirror). 🐞 **2 prod bugs caught on the FIRST real parallel run** (both fixed/verified): (A) **boto3 AKID** — the 7 S3 exporters passed `aws_access_key_id=os.environ.get(...)` UNSET on the instance-role EC2 → `AuthorizationHeaderMalformed`; fixed ALL 7 to fall back to the credential chain (incl. HALT-tier `export_statcast_to_s3`) — now a program convention (boto3 S3 writers must use the instance-role fallback); (B) **wrong tier** — the mirror was HALT inside `predict_today_morning` but is parity-only during the parallel window → `_run_mirror()` HALT only once `W7B_LAKEHOUSE_S3=1`, else ALERT-continue (+ split `--w6` HALT / `--w7b-only` mirror-tier). CI: fast pytest 718✅, `dbtf compile` 1771✅. ✅ **MERGED TO MAIN + FULL DEPLOY 2026-06-29** — EC2 now runs the fixed exporters → daily host mirror LIVE (prior green parity = a manual local run). Remaining: confirm next morning op's `export_features_to_s3` succeeds (no AKID) on a HOST-written mirror → run `parity_check_w7b.py` on it → THEN the multi-day parallel window counts. 🚦 CUTOVER GATE = consecutive green parity days incl. (a) a real-picks day + (b) a ≥0.70-coverage slate; then `W7B_LAKEHOUSE_S3=1` + backend redeploy; ⛔ don't drop `lakehouse_ext` views (on-SF build still reads them). ⚠️ intraday/post-lineup serving stays on Snowflake until W7b-2/W8 (the mirror is daily-cadence). ➕ NEW tail item: `admin.py`/`finances.py`/`pipeline.py` routers still `execute_query` Snowflake at REQUEST time (W7b cleared only picks/performance/bets — mostly sanctioned state/billing reads but ticket it).
6. **E5.0 / E5.1** — prop ingestion + backfill (after the cost-opt wave)
7. **E11.13** ✅ **SHIPPED 2026-06-25 (E5.2 session, fast-follow).** Profiling was the whole story: **5 Monte-Carlo calibration tests = 251s of 268s (94%)**; the other 611 run in ~17s. Surgical fix (not a rewrite): `pytest-xdist -n auto` + a registered `slow` marker (`--strict-markers`) on those 5 (the E2.3 totals + E5.2 K PIT/coverage guards — NOT shrunk; tolerances are tight, shrinking would risk flaky CI); `addopts="--durations=10 --strict-markers"`, dropped `-v`. **CI split: Unit Tests (fast gate) `-m "not slow" -n auto` = 268s→15.4s (17×) + Unit Tests (slow) `-m slow -n auto` = 96.6s — both REQUIRED for merge (PM 2026-06-25).** Coverage UNCHANGED (no test removed; only 5 `@slow` decorators + config + CI split; IO already mocked; the /tmp op-diet state files already use per-test `tmp_path` → xdist-safe). Full suite green ×3, no flakes. CLAUDE.md updated (fast-gate command + the **>5s ⇒ `@pytest.mark.slow`** rule). xdist added to MAIN deps (not dev — else `uv sync --no-dev-packages` drops it). Handoff `E11_13_HANDOFF.md`. **⏭️ Operator: (1) `git add` the E11.13 list (separate from E5.2's); (2) add the slow-tests check to BRANCH PROTECTION (so "both required" actually gates).** _(orig spec ↓)_ Python test-suite speedup + structure (DX, fast-follow).
8. **🌙 OPEN FOLLOW-UPS:** **(a) E11.16 ✅ SHIPPED 2026-06-26 (W2 session) — but it was the LOCAL `--state`/deferral fix (`scripts/dbt_state.sh`); the CI partial-build-null class is STILL band-aided per-column with `coalesce` (has_odds, now `team_features` games_last_7d/14d/tz_changed) → a more durable CI fix (so deferred-upstream lag doesn't null today's-scheduled rows) is still owed; the runtime-`dbtf build`-in-CI item also remains.** (b) **🔴 INC-14-root SCD-2 writer fix STILL PENDING (prompt READY: "INC-14 (root)") — NOT folded into W2 (the live SCD-2 writer couldn't be validated; consumer QUALIFY protects serving); the exact SQL was handed off → apply + test.** (c) **🔒 SECURITY: rotate the dbt Cloud token in `~/.dbt/dbt_cloud.yml` + `chmod 600` (plaintext, live; not in repo).** (d) **`mart_pitch_play_event` external-table DDL: drop the dangling `woba_value/denom/xwoba` cols + audit ALL readers (they read NULL since W1d).**
9. **🗂️ FULL-MIGRATION SCOPE = SCRIPTS TOO, not just dbt marts (operator 2026-06-26).** Each lakehouse wave must migrate the PYTHON SCRIPTS that read/write Snowflake alongside the marts they feed — the migration is NOT done when the dbt marts move. **END-STATE: the ONLY thing touching Snowflake is `generate_pick_narratives.py` (indirectly — `SNOWFLAKE.CORTEX.COMPLETE` for the pick-reasoning LLM); everything else → S3/DuckDB.** Inventory (~40 scripts; grep `snowflake.connector|write_pandas`):
   - **✅ KEEP (the exception):** `generate_pick_narratives.py` (Cortex LLM). _(future option: swap to a non-Snowflake LLM to close Snowflake ENTIRELY — flag, not now.)_
   - **INGESTION writers → migrate with their wave's marts:** `savant_ingestion` (→ already has `ingest_statcast_to_s3`), `odds_api_ingestion` + `derivative_odds_backfill` + `backfill_historical_odds_snapshots` + `pregame_snapshot` (→ W3 odds wave), `ingest_statsapi`, `ingest_umpires`/`_scorecards`/`_historical`/`backfill_umpire_assignments`, `ingest_weather`/`backfill_observed_weather`, `ingest_oaa`/`sprint_speed`/`catcher_framing`/`player_profiles`/`transactions`, `ingest_actionnetwork_betting`, `compute_elo`, `score_playing_time`, `scd2_writer` (⚠️ INC-14 writer).
   - **🔒 SERVING (Wsv — LAST, deliberate, post-July-1):** `predict_today` (reads features, writes `daily_model_predictions`), `write_serving_store`, `write_api_cache`, `lineup_monitor`, `backfill_prediction_log`.
   - **READ/monitor/util:** `check_data_freshness`, `check_prediction_coverage`, `compute_model_health`, `settle_user_bets`, `validate_*` + **the shared `scripts/utils/snowflake_loader.py` = the CHOKEPOINT** (provide an S3/DuckDB equivalent → many scripts flip at once). + pipeline ops/sensors (`daily_ingestion_ops`, `intraday_ops`, `end_of_day_ops`, odds sensors).
   - **❌ DEAD (decommission, don't migrate):** `parlay_api_ingestion` (Parlay already decommissioned). (c) `bullpen_fatigue` test = 0% historical coverage → needs a `--full-refresh` of `feature_pregame_game_features` (Low; Sunday-build only). (d) `generate_pick_narratives` timeout = was a mid-run codeloc redeploy → add an explicit WARN-tier timeout if it recurs (Low). (e) DEV_BETTING stale `mart_pitch_*` base tables → optional cleanup, no prod impact (Low).
   - *✅ E11.7 done (failure-semantics contract + 12 regression guards).*
   - *6/22 audit DONE → BOTH bills up/flat, same root cause: the `feature_pregame` rebuild frequency drives Snowflake AND Dagster. Re-measure after E11.9+E11.0b, not after W1.*

**📱 App — Track 2** — **⏸️ PARKED (operator 2026-06-23): app stories on HOLD until the MODEL + COST initiatives are in a better spot.** Exception = a genuine P0 trust/serving bug (like E9.25 was). Nothing below is dropped. *(Recently-shipped detail — E9.25, E9.27, E9.29, E9.1, E9.14 — relocated to the "✅ Recently shipped — App (archive)" section below.)*

**In flight / residual:**
- **E9.26** 🟡 — performance-metric HONESTY. Model-skill strip SHIPPED 2026-06-23 (now conditions on the model's directional pick; Win% h2h 54.1% / totals 50.8%; CLV flat; tooltips honest). **Residual (parked): fund-level `/performance/summary` + `/by-model` + `write_api_cache._CLV_SUMMARY_SQL` still use the home/over convention → align to bet-direction; the E9.25 win-semantics reconciliation folds in here.** Umbrella for performance-metric honesty across all surfaces.
- **E9.28** ✅ SHIPPED 2026-06-24 (Sonnet, prod) — bulk permanent-cache invalidation for champion promotions. New `POST /admin/cache/invalidate-permanent` (admin-guarded, idempotent) calls `invalidate_permanent_picks()` on BOTH stores — PG (`DELETE … is_permanent=TRUE AND cache_key LIKE 'picks/game/%'`) + S3 (targeted `api-cache/permanent/picks/game/` prefix only, NOT the whole permanent prefix); returns `{s3_objects_deleted, pg_rows_deleted}`; lazy regen on next load. 7 unit tests; CI 566 pass. **Runbook updated: new Step 6b (purge permanent caches) right after the redeploy** — the v6-style manual surgery (manual S3 purge + 33-row PG delete) is now one curl call. No open items. *(detail → archive below.)*

**Pending (priority order; SPEC'D = full prompt in `story_prompts.md`):**
1. **E9.16** ✅ **SHIPPED 2026-06-27** — Bet Log paginated 25/page (newest-first; summary tiles stay full-season-filtered, not per-page; controls only when >25; resets on filter change). The E9.18 guard caught a Tuesday week-key on first try → forced the Monday merge (recurring bug now un-shippable).
2. **E9.31 / E9.31b** ✅ **FULLY SHIPPED 2026-06-28** — ⭐ **matchup heatmap viz + daily overlay generation**. E9.31 (render): Track A live since INC-16-P2 (2026-06-26); IAM + frontend wired. **E9.31b (generation)**: CODE-COMPLETE 2026-06-28 — `scripts/generate_zone_overlays_today.py` (lakehouse DuckDB + Snowflake lineup/starter query → S3 serving prefix) + `build_zone_matchup_overlay_op` (WARN-tier, fans out from `predict_today_morning`) wired into `daily_ingestion_job`. **Operator ships via P5 CD + runs the one-time 14-day backfill (`--days-back 14`).** Track B (overlap scalar) CLOSED — null both targets (5th no-edge).
3. **E9.17** ✅ **FULLY DONE 2026-06-29 (both phases) — bankroll line CLOSED.** Phase 1 (06-27): per-book balances + deposit/withdrawal + honest net-of-cashflow growth % (distinct from ROI) + rename/delete. Phase 2 (06-29): "Bankroll by Sportsbook" per-book breakdown on Performance + opt-in per-book "Reset baseline" (confirm-gated, idempotent, history-PRESERVING). Parallel-safe (no serving/W7a-path touch); 13 bankroll tests, fast gate 695. No open items; no further phases specced.
4. **E9.3** 🟡 DEFERRED — live scores: re-sourced to the FREE MLB StatsAPI (was Odds-API `/scores` = credit burn) + **deferred until the live in-game StatsAPI ingestion exists** (reuse `services/schedule_capture`); a new real-time data path, build deliberately. Drops below the others.
5. **E9.9** ✅ SPEC'D — push notifications (**P1** backend; SNS/Lambda/Web Push; SES-unblocked; completes E9.10's notif toggle).
6. **E9.18** ✅ **SHIPPED 2026-06-27** — changelog accordion (Radix, per-week collapsible) + **the DURABLE FIX for the recurring Monday-week-bucket bug**: render auto-snaps any `week` to its Monday + merges split blocks, AND `test_changelog_guard.py` (fast gate) FAILS the build on a non-Monday `week` key or duplicate Monday → bad writes can't merge. **The CLAUDE.md week-bucket "reminder" is now backstopped by code** (auto-correct + hard gate) — no more operator fixes. · **E9.21** — PostHog in Admin (P3).
   - *🆕 Newly captured 2026-06-25: **E9.33** perf-page model time-series (honest: quality-over-time, NOT a profit curve) — ⚠️ HOLD (touches `performance.py` = W7a); **E9.34** Admin live-model-version fix (P3/low — need prompt); **E9.35** quantify the pick-detail feature-importances (need prompt; serving-add → coordinate W7b); **E9.36** ✅ SPEC'D — team-page clickable lineup/SP→player + SP last-3-starts; **E9.37** ✅ **CODE-COMPLETE 2026-06-29** — per-market line-movement-over-time on pick detail (3 increments: multi-book 7-book selector + h2h de-vig + totals line+Over%; Snowflake-free backfill; ⚠️ pending operator run-order; W7b must repoint the new `_LINE_MOVEMENT_SERIES_BATCH`; past non-permanent games show no chart by design).* **E9.36** ✅ **DONE 2026-06-29** — clickable lineup batters + SP → player page (new tab) + SP last-3-starts on PICK-DETAIL (prompt mis-placed them on the team page → moved; lesson: pin the exact page in app prompts). 🐞 **found+fixed a real prod bug:** the RECURRING `monthly_schedule` divergence (3rd sighting) left yesterday's games stuck in `Preview` → empty lineups; band-aided via a 30-min intraday re-export (`SCHEDULE_LAKEHOUSE_INTRADAY=1`). 🔧 **DURABLE FIX OWED (backlog → the residual-audit's raw-ingestion-S3-native item): flip `ingest_statsapi.py` to write `monthly_schedule` directly to S3** (it's deferred-3× now). **✅ E9.39 DONE 2026-06-29** (Admin sweep: EC2-Dagster repoint + Snowflake 10%-cloud-svc credit fix [201.70→184.28cr] + AWS Cost-Explorer line-items; folded in **E9.34** ✅; ✅ operator set `DAGIT_BASIC_AUTH_*` on the Lambda → Dagster panel live, no tail). 🔑 **surfaced a DATA DRIFT: v6 SERVING since ~06-23 but `model_registry` has no v6 (still v5 `is_current`)** — the E13.11 swap skipped `record_promotion` → **▶️ E13.11-reconcile (✅ SPEC'D): record v6 via the runbook [real metadata, NOT hand-fabricated] across all 3 targets + make `record_promotion` non-skippable.** **🟢 STILL SPEC'D: E9.21 (PostHog Admin), E9.38 (backfilled-not-projected label)** · ✅ residual audit DONE — all frontend/own-page, avoid the W7a routers + W7b serving files. Heavy model bake-offs HOLD until W7b lands (feature layer stabilizes on S3).
   - *Parked: **E9.23** (→ E2.3); GTM seq **E9.19 → E9.7 → E9.8**; **E10.1** parlay calculator (pull-in candidate).*
   - *❌ **E9.12 DEPRECATED (operator 2026-06-25)** — daily card is now redundant: the Dashboard already shows today's picks + conviction + a `qualified_bet` filter, and E9.11's "Line Shopping" tab already shows the value plays + best price + Pinnacle anchor. Its "what bets to make" framing is also gated on the bet-permission gate (Epic 19/PBO-DSR) that hasn't cleared (`best_alpha=0`). **Revisit only if/when that gate clears** (a true "bets to make" card becomes honest then) — and even then as a consolidation of existing surfaces + the narrative "why," not new capability.*

### ✅ Completed (rollup — full detail in the tables below)

- **Model (Session A):** E1 audit · E2.1 · E2.1b · E1.7 · E1.8 · E12 · E1.9 · E13.0 (HOLD) · **E13.6** (H2H recalibrated → honest ~0.43–0.57 band; recurs per re-fit guard) · **E13.3** (sub-models tapped out — bullpen Layer-3 promote was leak-inflated → reject; only run_env+offense survive). **Killed:** E3.1 · E4 · E13.1 → *H2H straight-bet edge + sub-model/ensemble edge both exhausted.*
- **Data / market (Session B):** E2.0 · E2.0b · E3.0 · E3.0b.
- **Cost-opt / Infra:** E11.3 · E11.0 · E11.4 · E11.5 · E11.2-T2 · E11.0c · E11.6 · E11.6b.
- **App (Track 2):** E9.15 · E9.13 · E9.10 · E9.5 · E9.4 · E9.20 · E9.2 → **beta onboarding LIVE** · **E9.25** (yesterday-badge P0) · **E9.27** (book-comparison leakage P0) · **E9.29** (public-betting + DH bridge fix) · **E9.1** (breakeven chip + +EV pills) · **E9.14** (Fanatics + Caesars fix) · **E9.28** (bulk permanent-cache invalidation + promotion-runbook Step 6b) · **E9.24** (win-prob lean framing + hover legend + per-book selector + CLV tooltip) · **E9.11** (best-price line-shopping + EV-tracker "Line Shopping" tab) · **E9.32** (Book Comparison hold/vig indicator + LOW-VIG badge) · **E9.30** (public editorial About page, v2). *(full detail → "✅ Recently shipped — App (archive)" below.)*
- **Incidents:** INC-1 … INC-6 ✅ **all resolved — board clear.**

### ✅ Recently shipped — App (archive)  *(full detail relocated from the Pending dashboard 2026-06-24; newest first)*

- **E9.30** ✅ SHIPPED 2026-06-26 (v2, prod/Vercel) — **public editorial "About Us" page** at `/about` (no auth; linked from nav + footers). REDESIGN after v1 (prose-wall, Penumbra-first, flat) was rejected: editorial/principles-led, NO product UI. Structure: eyebrow → POV hero ("An honest maybe beats a confident guess") → serif origin pull-quote → numbered principles 01–04 (Show the uncertainty / Close what fails / **No false certainty** / Show the work) → one-sentence "who it's for" (FanGraphs-reader mindset contrast) → footer (Penumbra byline + CTAs). **🔑 #3 worded to NOT contradict the live pick-of-the-day** (framing — conviction always shown, no win-rate streaks — not "we don't show picks"). ~70% shorter than v1; honest framing held (best_alpha=0). Files: `app/about/page.tsx` (new), `components/nav.tsx`, 3 static-page footers, `changelog.json` (bucketed to the `2026-06-22` Monday ✅).
- **E9.32** ✅ SHIPPED 2026-06-25 (prod, `936df9b`) — **Book Comparison hold/vig indicator.** Pure client-side display math on `frontend/app/picks/[game_pk]/page.tsx` (no backend/serving change): helpers `computeH2HHold`/`computeTotalsHold` = `1/dec_A + 1/dec_B − 1` from the A0.4.32 decimal odds (American fallback) + a **"LOW VIG" badge** (cyan) on the lowest-hold book in the selector (Pinnacle typically — reinforces the sharp anchor) + a **cross-book hold summary** (per-book hold % for Moneyline + Totals independently, ★ on the tightest per market). Honest footer: "★ Tightest = least book margin = better price to shop. Not a bet recommendation." ✅ changelog bucketed correctly to the `2026-06-22` Monday block (the recurring bug did NOT recur).

- **E9.11** ✅ SHIPPED 2026-06-25 (prod) — **best price / line-shopping for the model's value plays.** Backend: 4 new best-price fields on `BookOddsComparison` (`best_h2h_home/away`, `best_totals_over/under` = max American odds per side across the non-Pinnacle US books) + a new `GET /picks/line-shopping` endpoint (`models/picks.py` `LineshoppingPlay`/`LineshoppingResponse`; reads PG `picks/line-shopping/{date}`, empty on miss). Serving: `write_serving_store.py` `_compute_book_odds_payloads` adds best-price selection (Pinnacle excluded as `is_sharp_reference` but kept as the fair-value anchor) + new `_compute_line_shopping_payload` (positive-edge plays only, sorted by edge desc, Pinnacle anchor + game meta) → PG. Frontend: green **BEST** badge on the best-priced book in the game-detail book selector (reuses E9.24's `selectedBook`); **EV-tracker rewritten with tabs — "EV Plays" + "Line Shopping"** (the latter = full desktop table + mobile cards, 10 cols, live play-count badge; replaced the undiscoverable Collapsible). 14 tests (`test_best_price_e9_11.py`). 🔒 Honest framing enforced throughout (`best_alpha=0` in the models + UI subtitle/tooltip/footer: "best price = highest American odds across [US books]; Pinnacle = fair-value anchor only; no demonstrated edge"). **⇒ unblocks E9.12 (daily card).** Changelog 2026-06-25.
- **E9.24** ✅ SHIPPED 2026-06-25 (prod) — **honest win-prob "lean" framing (the E13.6 disclosure layer).** Shared `components/probability-bar.tsx`: the prob bar now has a hover/tap **legend** naming each marker (white tick = model estimate, orange/gray tick = market-implied, green band = 80% credible interval, gray center = 50% coin-flip ref) + the attributed-team numbers ("54% (51–57%) — STL · slight lean" via `leanStrength()`); game-detail bar keyboard-focusable + aria-label + high-variance disclaimer. **BONUS this session:** (1) a **per-book SELECTOR pill row** on the game-detail win-prob bar (Pinnacle/BetMGM/Caesars/FanDuel/DraftKings/Fanatics/Bovada, filtered to books with a line; clicking switches the market reference tick + tooltip; synced to the existing `selectedBook` that drives Book Comparison) — **partially advances E9.11's per-book surface**; (2) a **CLV-bar hover tooltip** (P(CLV+), 80% CI, 50% baseline; honest copy incl. "Totals CLV model v0 has no demonstrated signal, AUC 0.445 — transparency only"); (3) **"Bovada" → "Market" relabel** on dashboard/homepage. **⚠️ DATA-SEMANTICS CATCH (worth a follow-up): `bovada_devig_prob` in the serving store is actually `home_win_prob_consensus` (an all-book average, NOT Bovada's line) — the UI label was corrected to "Market," but the SERVING FIELD NAME is still a misnomer → audit other consumers of `bovada_devig_prob` that may wrongly assume it's Bovada-specific.** Files: `probability-bar.tsx`, `picks/[game_pk]/page.tsx`, `dashboard/page.tsx`, `page.tsx`, `changelog.json` (3 entries 2026-06-25).
- **E9.28** ✅ SHIPPED 2026-06-24 (Sonnet, prod) — **bulk permanent-cache invalidation for champion promotions** (the v6 deploy's manual cache surgery, codified). New `POST /admin/cache/invalidate-permanent` (admin-guarded via `get_admin_user`, idempotent) → `invalidate_permanent_picks()` on BOTH stores: PG (`services/pg.py` — `DELETE … is_permanent=TRUE AND cache_key LIKE 'picks/game/%'`, returns row count, rollback→0 on error) + S3 (`services/s3_cache.py` — paginate+delete the TARGETED `api-cache/permanent/picks/game/` prefix only, not the whole permanent prefix; returns object count); `routers/admin.py` returns `{status, s3_objects_deleted, pg_rows_deleted, message}`. Stale Final-game blobs clear from both stores, regen lazily on next load; safe to re-run. 7 unit tests (`test_invalidate_permanent_cache.py` — graceful-zero, counts, rollback, targeted-prefix assertion); CI 566 pass. **Runbook: new Step 6b (`docs/model_promotion_runbook.md`), immediately after Step 6 (redeploy)** — curl + expected JSON + idempotency/scope notes. Closes the E13.11 promotion-runbook item #3. No open items.
- **E9.14** ✅ SHIPPED 2026-06-24 — Fanatics live in the Book Comparison (21,218 rows / 52 games; full treatment — breakeven chip + counted in the +EV pills; the E9.27 guard + E9.1 logic auto-integrated cleanly). ⭐ BONUS BUG FIX: Caesars was SILENTLY BROKEN — the filter used `'caesars'` but `mart_odds_outcomes` keys it `'williamhill_us'` (the dbt canonical macro never materialized a column) → Caesars dropped from the comparison entirely; fixed (filter `williamhill_us` + Python canonical-map back to `caesars`; 20,174 rows live). Changelog merged to one 2026-06-22 week block (7 items). ❌ E9.22 (odds-freshness) = CANCELLED (operator 2026-06-24) — already solved off-stream after the Parlay-API decommission.
- **E9.1** ✅ SHIPPED 2026-06-24 — model-relative **breakeven chip** (per book/market: the American price where the model's edge = 0; green=still +EV, red=line crossed it; Pinnacle as the no-vig sharp reference). `prob_to_american()` util + `breakeven_american_*` API fields + serving-store writer + frontend. **Bonus:** cross-book **+EV summary pills** ("BetMGM +EV · Rockies ML" / "2 of 4 books +EV" / "No book +EV"; Pinnacle excluded since not US-bettable) + UI defaults (Book Comparison opens, Bovada collapses). **Bug fix:** pills mis-gated on `pick_side` (null when layer4 abstains) → compute `effectiveH2H/TotalsPickSide` (fall back to edge direction). 🔒 Honest framing held (best_alpha=0; "+EV per MODEL", a transparency tool). **⚠️ light QA worth doing: confirm the no-demonstrated-edge caveat reads as prominently as the green "+EV" visual** (a beta user could misread a green pill as "good bet").
- **E9.27** ✅ SHIPPED 2026-06-23 (operator-noticed, prod) — **P0 LEAKAGE fixed: Book Comparison no longer shows post-game-start odds.** `_BOOK_ODDS_BATCH` bridge CTE now LEFT JOINs `stg_statsapi_games` for `game_start_ts` (= `gameDate::timestamp_tz`, PRECISE first-pitch UTC); `all_odds` filters `o.ingestion_ts < game_start_ts` (fail-open NULL guard). **Sweep CLEAN:** `_BOVADA_BATCH` already guarded; picks/EV/history read frozen `daily_model_predictions`; line-movement + public-betting have mart guards — `_BOOK_ODDS_BATCH` was the only hole. Regression test `test_book_odds_leakage_guard.py`. CI 489 pass. Changelog 2026-06-23 (fix).
- **E9.29** ✅ FIXED 2026-06-23 (dbt) — **Market Action / Public Betting absent for today's slate.** Root cause: `stg_actionnetwork_public_betting_snapshots` resolved `game_pk` via an INNER JOIN to `mart_game_results` (completed games only) → a PRE-GAME feature gated on POST-GAME results → today's games dropped every rebuild. Fix: repointed to `feature_pregame_game_features` (canonical pregame spine); dropped redundant `game_type='R'`. Verified today 30/30 AN rows resolve (was 0); `dbtf compile` 41/41 no cycle. **🔧 RELATED DH FIX ✅ 2026-06-24 (mart_game_odds_bridge): the CHC/NYM DH on 6/23 had BOTH games `has_odds=false` because `odds_events_deduped` collapsed the two DH events to one → G2 served NO odds/picks. Fixed: slot-aware routing (`ROW_NUMBER() … = game_slot` joined to `game_number`).** ⚠️ Residual (deferred, low-impact): public-betting same-day same-matchup DH still miss 1–2 games/day (ambiguous (date,home,away) join).
- **E9.25** ✅ SHIPPED 2026-06-23 (prod-confirmed) — **P0 TRUST BUG fixed: front-page "Yesterday" badge no longer always shows a WIN.** Backend `_FEATURED_YESTERDAY_SERVING_SQL` rewritten to mirror the TODAY selection exactly (same conviction-flag qualifier + dedup + `game_datetime ASC LIMIT 1`; cherry-pick removed) + computes the REAL bet result from `actual_outcome`×`pick_side` → emits `status: win/loss/pending`. Frontend `app/page.tsx` badge branches: win→emerald✓, loss→red✗, pending→gray clock. **⚠️ FOLLOW-ON (→ E9.26): the performance-summary payload still defines "wins" as CLV-positive (`write_serving_store.py` ~L442) → reconcile the two surfaces.**

---

## Track 1 — Model build  *(2 model sessions: A = validation/totals · B = market/infra)*
Ordered; lane in brackets. **E1's go-live gate is now CLEARED (E1.1–E1.6 ✅ + PBO/DSR + purged-CV on record)** — E2–E5 can promote to live once each clears its gate (PBO<0.2 + DSR>0 + forward-CLV).
> **🗄️ DATA-FIRST PRINCIPLE (operator 2026-06-18): get the data into the best possible state BEFORE any retrain / the v6 bake-off.** Order on the model track: leakage remediation (E1.7 ✅ + E1.8 ✅ + the Stuff+ de-leak) **+ serving parity (E12)** → **then** E1.9 (bake-off + retrain). Rationale: if the serve path is skewed, the bake-off's CV doesn't predict *live* skill, so it'd crown a model leaning on features that are null at serve. Fix the data/serve first so v6 is selected for what's actually servable.
> **💰 ACTIVE PRINCIPLE (operator 2026-06-18): keep Snowflake spend at the MINIMUM until profitable.** Snowflake is ~60% of burn and growing 25% MoM pre-revenue, so **cost-opt is now active work, not parked** — the E11 wave (rows 2–3) leads Session B ahead of E5.0/E5.1. Migrate jobs onto the **same lean lakehouse (S3 + dbt-duckdb on Railway) the new sports use**; new/changed transforms default off-Snowflake where feasible (§6); the **live serving path stays on Snowflake and moves last.**

| # | Story | Title | Lane | Note |
|---|-------|-------|------|------|
| — | **E2.1** ✅ | Per-side count-distribution model | A·market-blind | **🔴 REVISIT → E2.1-r (prompt READY 2026-06-24, `story_prompts.md`): built SINGLE-architecture (LightGBM-Poisson mean + NegBin r, only-vs-Poisson, no Optuna/learner-foils) — violates §0.5; LOAD-BEARING (feeds E2.3/E2.4/E2.5/E2.6) + the E2.3 dispersion/run-diff near-miss traces here → E2.1-r runs the per-side bake-off + Optuna, picked on the CONVOLVED-total calib. ⚠️ UPSTREAM of E2.5/E2.6 — ideally run first. See Decisions & kills.** ✅ **GATE PASS 2026-06-18** — NegBin beats Poisson (5/5 folds, +0.093 NLL), overdispersion recovered. ⚠️ **Before serving, swap its bullpen channel to the leakage-safe aggregate** (E2.1b found the incumbent `bp_eb_xwoba` is a within-game leak — not computable live). Artifact `totals_perside_v1.pkl` (not promoted). **🔧 CORRECTION (E2.2, 2026-06-22): E2.1's "dispersion `r` non-stationary 33→8" was a TRAIN-FIT estimation artifact** (r_train falls as the train set grows; r_oos is STATIONARY ≈3.7). So do NOT condition `r` on time/period; E2.3 calibrates one stable held-out `r≈3.7`. Also: E2.1 fit `r` on optimistic train-fit means → under-dispersed (the ~24% totals variance deficit). **🎯 INTEGRATION TARGET for E13.4 winners** (per-side mean barely separates games — E2.2 §4.5, between-game mean-var 0.894 — so a new signal moves a number HERE first). **🔧 E13.4 correction (2026-06-22): E2.1 is already the RICHEST contract in the repo (308 feats) and ALREADY carries the Statcast recency/form axis** (rolling team scoring/prevention, velo/csw drift) — so generic "momentum" is NOT the gap here. The specific E13.4 candidate to feed E2.1 is the **times-through-order penalty** (`pitcher_times_thru_order`, raw-but-unused) + the **bullpen-fatigue × expected-game-length** interaction — test those into the per-side means first. |
| — | **E2.1b** ✅ | Bullpen model deepening (`bullpen_v3`) | A·market-blind | ✅ **COMPLETE 2026-06-18 — gate FAIL by design = the finding.** Deepening can't beat the incumbent because the incumbent (`bp_eb_xwoba`) is a **within-game leak** (weights each reliever's EB by `outs_in_game`). Proven 3 ways (leak-signature NLL, source, MDA collapse #1/#2→noise). **Deliverable = the leak discovery + a leakage-safe replacement + the de-leak card (E1.7).** `bullpen_v3` NOT promoted (no lift). See `E2_1b_HANDOFF.md`. |
| — | **E1.7** ⭐ ✅ | **De-leak the production bullpen feature** (Tier-0 correctness) | A·correctness | ✅ **SHIPPED + VALIDATED 2026-06-18** (steps A–D; retrains=E gated post-E1.8). SQL de-leak (Path A, equal-weight), both leaks fixed; **serving-null FIXED 37/37 scheduled games (was 0); 96.9–98.9% historical coverage 2016–26**; parity ✅ (Jaccard 1.0, corr 0.962); **MDA collapse confirmed on ALL 3 targets** (bp_eb_xwoba value ~0% retained; `coverage_pct` rises to #1/#2). **Slim contracts re-derived (INTERIM):** total_runs 14→21, home_win 31→21, run_diff 19→15 — **`elo_diff` + `pythagorean_win_exp_diff` now enter both H2H contracts** (real team-strength signal the leak had masked). ⚠️ Offline metrics *drop* = correct; gate on live/forward. |
| — | **E1.8** ⭐ ✅ | **Full feature-surface leakage sweep** | A·correctness | ✅ **COMPLETE & MERGED 2026-06-18 (CI green)** (`ablation_results/feature_leakage_audit.md`; Stuff+ de-leak applied + regression guards shipped). **VERDICT: the bullpen leak was the STANDOUT, not the tip of an iceberg** — construction surface is essentially leak-free post-E1.7. Exactly **2 residual leaks, both LOW-magnitude**: 🟥 **Stuff+/arsenal = LEAKY-season-to-date** (current-season join, no `< game_date`; embeds game-G-and-later pitches) — hits 2 totals-slim-contract features (`home_starter_stuff_plus`, `away_starter_avg_fastball_velo`) → de-leak before E1.9; 🟨 catcher framing = current-season-blended (noise, no contract). All else AS-OF-SAFE (bullpen E1.7 fix confirmed; sequential posteriors safe via `prior_mu`/strict-`<`; H2H + run_diff slim contracts FULLY CLEAN). **Gap attribution: leaks ≠ the cause** — bullpen (fixed) was the largest named construction slice, Stuff+/catcher minor; the bulk of 0.42→0.001 is **serving skew (30.3)**, not construction leakage. **✅ CONTRACT-RE-DERIVATION GATE CLOSED 2026-06-19** (the last open E1.8 item; was NOT done despite the sweep being merged). **Trap caught:** neither importance JSON had *both* de-leaks at once (`bullpen_v3` was Stuff+-leaky; `stuffplus_deleaked` had the bullpen leak back at #1) → prior contracts were hand-derived off a stale leaky ranking. **Fix = a reproducible tool, no hand-pruning** (`betting_ml/scripts/derive_clustered_contract.py` codifying the E1.3 non-noise-cluster rule + a leakage guard that refuses any source not on the FULLY-de-leaked matrix `bullpen=v3 + stuff_plus=deleaked`; reproduces the old E1.7 contract byte-for-byte; `test_derive_clustered_contract.py` 3 guards incl. a contract↔MDA drift test; suite 381 green). **Re-derived on the clean both-de-leak matrix:** `total_runs` 21→**13 FINAL** (both leaky Stuff+ feats — `home_starter_stuff_plus`, `away_starter_avg_fastball_velo` — dropped to noise once de-leaked, confirming the season-to-date peek); `home_win` 21→**19 FINAL** (NOT a no-op: 8 dropped/6 added at the signal/noise boundary — vindicates re-deriving vs trusting the stale prune); `run_diff` ⏳ **MDA running → derive on landing.** **E1.8 fully closes when run_diff lands** + operator `git add`s run_diff/home_win contracts + the 2 new MDA `.json`/`.md` artifacts. Sequential dbt regression guards (§7.3) shipped. |
| — | **E12** ⭐ ✅ | **Serving parity / point-in-time serving completeness** (from master 30.3) | A·model·correctness | ✅ **STRUCTURAL AC MET 2026-06-19** (forward-validation longitudinal, ~2–3 wk). **🔑 REFRAME: there's NO structural train/serve misalignment** — training + serve read the same `feature_pregame_game_features`; only the `has_full_data` filter differs. The 0.42→0.001 collapse = pure **point-in-time value-completeness** (row sparse AM / dense post-backfill) → **the 0.42 benchmark is OPTIMISTIC (a dense re-read).** Fix already built (Story 33.0 **pre_lineup tier**: home_win 211→156 cols, drops lineup-gated NULL families; 30.3 post_lineup re-score; 30.13 freshness gate). E12 added the **tier-aware parity harness + standing guard (21 tests)**; live-sparse run 2026-06-19 → **all 3 targets PASS, 0 strong-tier degraded.** E1.9 now unblocked (serving confirmed sound) **but must be TIER-AWARE** — see its row. |
| — | **E1.9** ⭐ ✅ | **v6 clean-slate champion rebuild** (bake-off → Optuna) | A·model | ✅ **COMPLETE 2026-06-21 — 1 PROMOTE-candidate, 5 HOLD, NO champion swap (all v6 registered as challengers).** Honest verdict: v6's value is **leanness (13–36 vs 154–374 feats) + de-leak correctness, NOT an offline edge** — and that's *expected*, not a failure: de-leaking removes fake offline signal, so v6 tying/slightly-trailing leaked-v5 offline is the correct fingerprint (the real test is live retention, which the offline gate can't show). **🟢 `home_win/pre` = the program's FIRST-ever offline PROMOTE** (glm/36 vs the 33.0 morning baseline, sig CI [−0.0055,−0.0021]) — stays a challenger until a 2nd hysteresis pass + forward/serving-parity (→ E13.0). The tie/HOLD across the dense models **confirms the straight H2H/totals point-models are signal-thin** (E1's "more feats ≠ edge" restated). **→ Model track now pivots to E13** (find orthogonal signals + alt architectures) — see the E13 block. v6 artifacts deliberately NOT persisted (nothing serves them; registry stanzas are the durable record). **Original build note (for reference):** built on the leak-clean data, tier-aware. **⚠️ MUST BE TIER-AWARE (E12 reframe):** serving is two-tier — rebuild BOTH the **pre_lineup** (morning, reduced contract) AND **post_lineup/champion** (dense) variants, and **evaluate each on its tier-appropriate matrix** — do NOT evaluate the morning model on dense re-reads (that's the optimistic-0.42 trap). The honest morning-skill target is *lower* than 0.42 (0.42 is the dense post_lineup ceiling; the actionable edge lives in the post_lineup re-score). **🟢 DATA PREP NOW DONE UPSTREAM (E1.8 closure, 2026-06-19):** Stuff+ de-leak applied (dbt repoint, committed `eb00a5d`) AND the slim contracts re-derived reproducibly on the both-de-leak matrix via `derive_clustered_contract.py` — `total_runs` **13 FINAL**, `home_win` **19 FINAL**, `run_diff` ⏳ landing. So E1.9 **inherits FINAL clean contracts and does NOT re-derive them** — it starts at the bake-off. **Batch order:** **bake-off** (XGBoost/LightGBM/CatBoost/NGBoost/GLM/stack vs market+no-skill floors, purged CV, **per tier**) → Optuna (PBO/DSR-guarded) → promotion-gate v6 vs v5; keep v5 until v6 clears live/forward. **🔑 TIER-MATCHED GATES (PM decision 2026-06-19):** the **post_lineup (dense) gates compare v6 vs the dense v5 champion** (apples-to-apples ✓). The **pre_lineup (morning) gates must compare v6-morning vs the 33.0 MORNING baseline, BOTH scored on the morning/pre_lineup matrix — NOT vs dense v5** (a morning-v6 "loss" to dense-v5 is the optimistic-0.42 trap, meaningless). Bar = "beat the model we currently serve in the morning," not "match the dense ceiling." Run the 3 post-tier gates as soon as ready; hold the 3 morning gates until re-pointed to the morning baseline. **E1.7 deferred retrains fold in here.** **⚠️ Gate before starting: run_diff contract must have landed** (E1.8's last item). Promoting v6 shifts live picks → changelog. |
| — | **E2.0** ✅ | Derivative-odds backfill (F5/team/alt closes) | B·data | ✅ **SHIPPED 2026-06-18.** `mart_derivative_closes` = **238,421 closes / 5,896 games, 2023-05-03→2026-06-06, 24 books, zero leakage violations**; team-totals + alt-totals + F5 (h2h_h1/totals_h1); idempotent (sentinel rows). EVAL/CLV-only ✓. Unblocks the E2.6 derivative gate. ⚠️ **Two gaps → E2.0b:** (1) ~~**F5 historical is sparse — only ~65 games, all 2023**~~ **→ CORRECTED 2026-06-23: that was the WRONG KEY (`h2h_h1`/`totals_h1`). The real baseball F5 keys are `h2h_1st_5_innings`/`totals_1st_5_innings` → RE-BACKFILL F5 history with the correct keys (folded into the prop windfall) — F5 history IS available, the 65-game result was the wrong market key, not true sparsity;** (2) latest game 2026-06-06 → **ongoing games need a live capture cadence.** |
| — | **E2.0b** ✅ | Live derivative-odds capture (forward cadence) | B·data | ✅ **SHIPPED 2026-06-18** — `services/derivative_capture/` Railway cron (`*/30`, probe-derived cadence) capturing **team_totals + alt_totals** live → `derivative_odds_raw`. **🔴→✅ CORRECTED 2026-06-23: the "F5 offered by ZERO books" finding was a WRONG-KEY probe.** We queried `h2h_h1`/`totals_h1` (the generic "1st Half" family — baseball books don't populate it); the correct BASEBALL keys are **`h2h_1st_5_innings`/`totals_1st_5_innings`** (+ `1st_1`/`1st_3`/`1st_7_innings` + alternates), confirmed on the Odds API doc → **F5 IS available from The Odds API.** Re-probe the correct keys (folded into the prop windfall) + add them to this live derivative cron. ~~F5 unavailable from this source~~. **✅ OPEN ACTION RESOLVED (2026-06-24 E2.0b-fix): code default in `derivative_odds_backfill.py` updated to `h2h_1st_5_innings`/`totals_1st_5_innings`/`totals_1st_1_innings` (NRFI); stale comments in `railway.toml`/`entrypoint.sh` fixed. Live probe 2026-06-24 confirmed 16 books (FanDuel/BetMGM/Pinnacle/Bovada/DraftKings/Caesars/etc.) return F5 on correct keys. SERVICE NOT YET DEPLOYED — operator action: create + deploy `services/derivative_capture/` in Railway (see handoff). `DERIVATIVE_CAPTURE_MARKETS` var on `odds-api-data-feed` is DEAD config (wrong service) — operator to remove it.** team_totals = 4 books, alt_totals = 5 books, ~30-min cadence. **✅ NOT affected by the Parlay-API decommission (INC-2): E2.0b runs on The Odds API (`oddsapi`), not Parlay — the E11.5 session's "E2.0b moot" claim was wrong.** ⚠️ Deploy status to re-confirm in the INC-2 audit (E11.5 session offhand-noted "pending deploy"; roadmap had it shipped). |
| — | **E2.0c** | Alternative derivative-odds data source (F5) | B·data·research | **✅ MOOT — RESOLVED 2026-06-24 (E2.0b-fix live probe CONFIRMED it): F5 IS fully available live from The Odds API — 16 books (FanDuel/BetMGM/Pinnacle/Bovada/DraftKings/Caesars/…) returned F5 on the correct `*_1st_5_innings` keys; the cron is fixed + deployed.** The SportsGameOdds purchase rationale is GONE ($299/mo avoided; keep the $119 Odds API line). Only revisit if F5 BOOK COVERAGE proves too thin (a coverage question, not availability) or the outstanding SportsGameOdds/OddsJam inquiries add books beyond our 16 at <$400/mo. _(was: 🟢 LIKELY MOOT, operator 2026-06-23.)_ The E2.0/E2.0b "no F5 / ZERO books" finding was a **WRONG-MARKET-KEY probe**: we queried `h2h_h1`/`totals_h1` (the generic "1st Half" family, which baseball books don't populate), but the correct BASEBALL keys are **`h2h_1st_5_innings` / `totals_1st_5_innings`** (+ `spreads_1st_5_innings`, `alternate_*`, and `1st_1_innings`=NRFI / `1st_3_innings` / `1st_7_innings`) — confirmed on the Odds API betting-markets doc. **⇒ F5 (and F1/F3/F7 inning derivatives) come from the incumbent we already pay for → DON'T buy SportsGameOdds for F5 ($299/mo avoided); keep the $119 Odds API line.** Re-probe the correct keys (folded into the prop windfall). Only revisit SportsGameOdds if F5 BOOK COVERAGE on The Odds API proves too thin (Pinnacle/Fanatics on F5) — a coverage question, not an availability one. _(orig ↓.)_ ✅ **RESEARCH DONE → 🅿️ DEFERRED (operator 2026-06-18).** Chosen source = **SportsGameOdds Pro** ($299/mo, ~$179 annual; F5 via "Partials/1h" + history + 82 books incl. Pinnacle/Fanatics) — **buy the PAID plan AFTER (a) ~100 paying users / revenue AND (b) the cost-opt items (E11) free the budget.** 🆓 **But validate FREE first (anytime, $0):** use the free Amateur tier to confirm MLB `1h` = F5 + capture the JSON + build the ETL shim — de-risks the purchase, not gated by the milestones. F5 PARKED (not killed). At purchase: evaluate **replace-The-Odds-API vs additive**. OddsJam inquiry still out. Survey: `docs/e2_0c_f5_source_survey.md`. |
| — | **E11.3** ✅ | Query/job cost tagging (Snowflake `QUERY_TAG` + Dagster op tags) | B·infra | ✅ **COMPLETE 2026-06-19.** `on-run-start` hook in `dbt_project.yml` sets `QUERY_TAG={job}\|{env}\|{invocation_id}` for all dbt runs; `_run_dbt()` injects `DBT_JOB_NAME=context.job_name` via subprocess env; `_run_script()` injects `DAGSTER_JOB_NAME`; `get_snowflake_connection()` (22+ scripts) + `_sf_connect()` (write_serving_store) auto-tag; inline Snowflake ops (check_games_today, odds_current_rebuild_sensor) use `session_parameters`. Report: `uv run python scripts/ops/snowflake_cost_by_job.py [--days 7]`. |
| — | **E11.0** ✅ | Dockerized dbt runner on Railway/EC2 (the execution substrate) | B·infra | ✅ **SHIPPED + DEPLOYED 2026-06-19.** `services/dbt_runner/` FastAPI (POST /run → bg thread → dbtf; GET /status; bearer auth; 409 on concurrent) **live at `dbt-runner-production-8899.up.railway.app`** (Railway project `nurturing-embrace`/prod; Snowflake creds + `DBT_RUNNER_AUTH_TOKEN` set). **`DBT_RUNNER_URL` set in Dagster+ → remote dispatch ACTIVE** (feature-flagged, in-process fallback if unset). `_manifest_dict()` strips dbt-fusion `operation.*` nodes (@dbt_assets crash fix). 363 tests pass. **✅ LIVE-VALIDATED 2026-06-19** — `odds_current_dbt_rebuild` triggered in Dagster+ and **dispatched to the dbt-runner container (remote execution confirmed).** Substrate is proven, not just deployed. **⚠️ BILLING GAP (operator 2026-06-19): when a *Dagster op* triggers the runner it POSTs then POLLS `/status` until done → the op stays metered for the full dbt wall-clock** (that run = 36s Dagster, ~same as in-process). So E11.0 moved dbt *compute* off the worker + gave us the substrate, **but a Dagster-op trigger does NOT cut run-minutes.** **The win comes from triggering the runner from a Railway CRON (flat-cost, no Dagster meter) — which is exactly E11.4's model — or async fire-and-forget for dbt that must stay Dagster-orchestrated → E11.0b.** *(E11.3 tagging should still attach on the remote path — confirm after the ACCOUNT_USAGE lag.)* |
| — | **E11.4** ⭐ ✅ | Decompose intraday polling jobs (python→crons, dbt→E11.0) | B·infra | ✅ **SHIPPED + DEPLOYED + LIVE-VALIDATED 2026-06-19.** Two high-freq Dagster polling jobs (**~27% of Dagster run-minutes**) moved onto **off-meter Railway crons**; dbt routes to E11.0 via `trigger_dbt.py`/`_run_dbt_remote`. **Delivered & deployed:** (1) `services/schedule_capture/` (`*/30` cron, **14:00-03:30 UTC** window) + `trigger_dbt.py` (graceful no-op if `DBT_RUNNER_URL` unset) — replaces `intraday_schedule_job` (~20% Dagster, 48+ fires/day eliminated). (2) `services/weather_capture/` (hourly cron, **10:00-02:00 UTC** window) — replaces `intraday_weather_job` (~7% Dagster, 19+ fires/day eliminated). (3) `lineup_monitor_job` slimmed: `lineup_ingest_schedule` (Railway cron handles it) + `lineup_odds_snapshot` (dead Parlay API, decommissioned 2026-06-16) removed → `lineup_ingest_umpires` is now chain head (no `start=`). (4) `all_intraday_schedules` pruned to `[odds_clv_rebuild_schedule]`; the 4 decommissioned `ScheduleDefinition`s **retained (not deleted)** for manual UI fallback. **Deploy:** Railway first (capture uninterrupted), Dagster redeployed second → the 4 schedule names gone from UI ✅, `lineup_monitor_job` slim in UI ✅, active-window guards confirmed firing in runtime logs ✅. **🐞 Caught at deploy:** weather guard initially `<= 13` (would've started 14:00 UTC = 10 AM ET, **too late for T-3/T-1 forecasts on morning games**) → fixed to `<= 9` (starts 10:00 UTC = 6 AM ET) before Railway redeploy. **CI:** 15 new tests `betting_ml/tests/test_e11_4_cron_services.py` (4 classes: decommissioned schedules / lineup slim / umpires sig / cron-service files), green. **6/22 audit measures the Dagster run-minute drop vs baseline** (odds_snapshot 20.6% + schedule 20% + weather 7% ≈ 47.6% of MTD run-min now off-meter going forward, plus lineup slim). |
| — | **E11.5** ⭐ ✅ | dbt engine decision spike (fusion vs core for the cost-opt lakehouse lane) | B·infra·spike | ✅ **DONE 2026-06-19 — VERDICT: STAY ON dbt-fusion, single engine (no dual-engine, no second binary).** The T2 blocker was **NOT a fusion limitation — it was a YAML format issue:** moving `loaded_at_field`/`freshness` into a `config:`-nested block makes fusion accept them (no more `dbt1060`). Fusion also **empirically drives the dbt-duckdb adapter**, so W1 needs no second binary. **Operator hypothesis "latest dbt-core runs on Fusion" = FALSE for 1.11.x** (dbt-core 1.11 = Python venv pkg; dbt-fusion = standalone 222MB Rust binary; separate codebases) — **but dbt Core v2.0-alpha (released 2026-06-01) IS the unified Rust foundation → monitor, not adopt yet.** Doc: `docs/e11_5_dbt_engine_decision.md`. 9 empirical tests; `dbtf parse`/`compile` clean (125 models/1629 tests). Unblocks W1 + reactivates T2. |
| — | **E11.2-T2** ⭐ ✅ | Daily state-aware dbt builds (`source_status:fresher+`) | B·infra | ✅ **LIVE — DEPLOYED 2026-06-19** (Railway dbt-runner redeployed by operator; the E11.5 YAML fix). `server.py` un-bypassed → runs `dbtf source freshness` (non-fatal) then `dbtf build --select source_status:fresher+ --state /tmp/dbt-state/`; first run = cache-miss full build that uploads manifest+sources.json to S3; **second+ runs rebuild only fresher-source descendants** = the credit saver. **⏳ VERIFY at 6/22 audit:** (a) the 2nd run actually skipped unchanged subtrees (check the runner logs / S3 state cache hit), and (b) the Snowflake-credit delta vs the 6/18 baseline — **this is the first real Snowflake savings in the wave.** ⚠️ The 4 `parlayapi` freshness blocks are **warn-only forever** (Parlay decommissioned — see INC-2 / E11.6). |
| 4 | **E11.1-W1** ⭐✅ | Migrate heavy Statcast marts → dbt-duckdb/S3 (lakehouse Wave 1) | B·infra·**Opus-only** | **✅ FULLY DONE 2026-06-25 (Opus finished W1d): all 7 `mart_pitch_*` are now Snowflake VIEWs over `LAKEHOUSE_EXT.*` external tables (S3 parquet) — NO Snowflake CTAS builds remain.** The S3 build chain (`ingest_statcast_to_s3`→`run_w1_lakehouse`→`refresh_external_tables`) is HALT-tier + ordered before `dbt_daily_build`, backstopped by `check_data_freshness` + the 30.13 serve-time gate. The prior Sonnet band-aids were CLEANED: 14 `not_null` tests (commit `f463bd2a`) promoted warn→**error**, all verified 0-null in prod first. **⚠️ `scripts/parity_check_w1.py` is now TAUTOLOGICAL** (the Snowflake views are over the SAME S3 parquet the script compares) → a green there is no longer independent validation; don't cite it as evidence. _(architecture/value, NOT a credit cut — marts are tagged-tiny.)_ _(prior partial-state writeup ↓:)_ **🔶 W1d PARTIAL — DECOMMISSION CORE UNVERIFIED / LIKELY NOT DONE (PM 2026-06-25; the session's "fully shipped" is OVERCLAIMED).** What W1d ACTUALLY did: `pitch_sk` parity fix (MD5-upper-64 to match Snowflake) + boolean `coalesce(...,false)` hardening on 3 mart_pitch_* + test-severity downgrades + the S3-build HALT re-tier + `w1_parity_schedule` off + a CI unblock. **What it did NOT show (the story's CORE): creating the Snowflake EXTERNAL TABLES, repointing `ref('mart_pitch_*')`, and DROPPING the Snowflake `mart_pitch_*` builds** — and the operator-remaining still says "re-run `run_w1_lakehouse` to rebuild S3" (= the marts are STILL building in parallel). ⇒ **the decommission is likely incomplete; re-verify + finish on OPUS** (`E11.1-W1d` prompt; folded into the live E11.15 Opus session 2026-06-25 via addendum). **🐞 WORSE: Sonnet's W1d changes also introduced a WRONG-DATA-TYPE error that BREAKS `dbt_daily_build` at RUNTIME** (HALT-tier — gates the feature store + predictions) — and it PASSED CI because `dbtf compile` is COMPILE-ONLY (type mismatch only surfaces on an actual run). Opus is cleaning it up. ⚠️ CONFIRM whether the broken changes were merged to `main` (recap listed merge as a to-do → likely still on `dev` → prod safe on old main) or merged (→ daily build was failing until fixed). **🛠️ PROCESS GAP exposed: CI compile-only missed a runtime build break → consider a runtime `dbtf build --select state:modified+` check (not just compile) in the dbt-Build CI.** The parity prep that DID land (pitch_sk, coalesce) was partly the SOURCE of the type error — not the clean "not wasted" I first credited. **🧭 ROUTING (operator 2026-06-25): ALL E11.1-W* lakehouse stories run on OPUS, not Sonnet (Sonnet mishandled W1d).** CI green (pytest 635 + dbtf compile 1771); merge unblocked. _(low stakes — marts are tagged-tiny, no cost/serving risk from the partial state; they just keep building on Snowflake.)_ 🔍 **OPEN DQ ITEM → now 🔴 INC-14 (see incident register + the INC-14 prompt): `feature_pregame_lineup_state` duplicate `is_current=true` rows → duplicate lineup-features → potential inflated training weight; the unique test was softened error→warn → investigate + fix + re-promote.** 🔍 **OPEN DQ ITEM (surfaced 2026-06-25, hand to PM): `feature_pregame_lineup_features` has duplicate `(game_pk, side)` rows — `unique_combination_of_columns` test is now WARN (not error). Likely root cause: `feature_pregame_lineup_state` has multiple `is_current = true` rows for the same game+side (SCD-2 transition bug or race condition). Diagnose: `SELECT game_pk, home_away, COUNT(*) FROM feature_pregame_lineup_state WHERE is_current = true GROUP BY 1,2 HAVING COUNT(*) > 1`. If confirmed, the duplicates propagate through the feature store to model training — assess training-data impact before W2.** _(Prior history ↓:)_ **🔄 CODE-COMPLETE 2026-06-22 — in PARALLEL-VALIDATION window (parity check fires 2026-06-25 20:00 UTC). ✅ PARITY GREEN + DECOMMISSION UNBLOCKED 2026-06-24 (pulled forward by E13.2 Phase 0): the redesigned `parity_check_w1.py` (completed-seasons + total + PK = HARD, in-flight = WARN) PASSES — 2015–2025 row-exact, all 7 `mart_pitch_*` pass. The earlier INC-10 slip risk is moot (W1 ran clean + the Phase-0 verify deduped a stale-export overlap + caught INC-13). **⏭️ Operator: remove `mart_pitch_*` from the Snowflake dbt schedules + turn off `w1_parity_schedule` per the runbook → W1 decommission can proceed (the 6/25 gate is already green).** Built: `scripts/ingest_statcast_to_s3.py` (Savant→S3 Parquet, bypasses Snowflake; idempotent SHA-256 pitch_sk, 14-day lookback) + `run_w1_lakehouse.py` (DuckDB reads `stg_batter_pitches` from S3 → writes all **7 `mart_pitch_*` models** to S3) + `parity_check_w1.py` (S3-vs-Snowflake row count ≤0.1% + PK uniqueness) wired as a **hard-fail Dagster job** (`w1_parity_job`, one-shot 6/25 schedule). The 2 ingest/build ops are **soft-fail dead-end tail** after `backfill_prediction_log` → the critical prediction path completes first. Fixes: region us-east-1→**us-east-2**, `union_by_name=true` (multi-year NULL/DOUBLE glob). CI 424✅; defs load 15 jobs/9 schedules. **🔑 ARCHITECTURE DECISION (PM 2026-06-22): use SNOWFLAKE EXTERNAL TABLES over the S3 parquets** — keeps existing `ref('mart_pitch_*')` in the (Snowflake, serving-coupled) feature models resolving with NO downstream code change, captures the cost win (the heavy TRANSFORM moves to DuckDB + storage→S3; downstream reads become cheap scans), and respects "serving moves LAST." Direct-S3 refs are the *eventual* end-state when the feature/serving lane itself migrates (a later wave), **NOT now** (would force migrating serving-coupled models out of scope for W1). **→ After parity PASSES 6/25: create external tables → repoint → decommission the Snowflake `mart_pitch_*` builds.** **🚨 RE-SCOPED as a COST lever (6/22 audit): these marts are tagged-and-TINY (~0.02–0.04 cr/run) → W1 will NOT meaningfully cut Snowflake credits** (the real spend is the 96.7% UNTAGGED — see E11.9). So **finish W1 for its LAKEHOUSE/architecture value (cheap to complete, value-preserving), but it is NOT the credit cut — E11.9 is.** Don't prioritize the next lakehouse wave for cost until E11.9 lands. ⚠️ Railway Hobby fine on COMPUTE; watch the 5 GB volume. |
| 7 | **E11.1-W2** 🆕 | Lakehouse Wave 2 — next batch-mart tier → dbt-duckdb/S3 | B·infra | **NEW (2026-06-22) — toward the July-1 "almost everything off Snowflake (except serving)" target.** After W1 (Tier-3 Statcast pitch marts), migrate the **next heaviest NON-serving batch marts** (from `baseball_data_mart_inventory.md` by tier + serving-coupling: rolling team/bullpen/starter aggregates, FanGraphs marts, EB-posterior intermediates — anything batch + NOT on the live `predict_today` read path). **Reuse the W1 infra** (`run_w1_lakehouse.py`/`parity_check_w1.py` patterns): DuckDB build → S3 → **parity-check vs Snowflake (≤0.1% + PK)** → external table → decommission. **⚠️ Put the build ops on a RAILWAY CRON, not Dagster ops** (E11.0b: W1's Dagster ops added run-minutes). Value-preserving; serving stays. |
| 8 | **E11.1-W3** 🆕 | Lakehouse Wave 3 — remaining batch/non-serving marts → S3 | B·infra | **NEW (2026-06-22).** The remaining non-serving batch transforms not in W1/W2 → DuckDB/S3, same parity-gated pattern + Railway-cron builds. **Target: by ~July 1, ALL batch/non-serving marts off Snowflake.** Serving path explicitly excluded (→ E11.1-Wsv). |
| — | **E11.1-Wsv** 🆕🔒 | Serving-path migration off Snowflake (feature store build + `predict_today` reads) — **POST-JULY-1, careful** | B·infra·serving | **NEW (2026-06-22) — the HARD, LAST mile; do NOT rush into the July-1 window.** Migrate the `feature_pregame_*` feature-store BUILD + `predict_today`'s feature READS off Snowflake to DuckDB/S3. (Note: the app already reads Railway PG first — Snowflake is the last-resort serve; so the risk is *predictions not generating*, not the app going down.) **Mandatory discipline:** PARALLEL-RUN both paths + an EXTENDED parity window (predictions byte-identical for N consecutive days) BEFORE any cutover; then decommission the Snowflake serve. **This is the live product — an outage here hits beta users. Deliberate, not deadline-driven.** |
| 2 | **E11.0b** ⬆️ | Stop Dagster ops blocking-polling the dbt-runner (async / cron-direct) | B·infra | **🚨 RE-ELEVATED 2026-06-22 — Dagster is UP not down ($36.20 since 6/18 ≈ $240/mo).** A **Dagster op** that triggers the runner + polls `/status` stays **metered for the full dbt wall-clock** (E11.0 billing gap). E11.4 removed the light polling schedules but NOT this — and with `feature_pregame` rebuilding ~266×/day (E11.9 finding), the blocking-poll metering is now a **big chunk** of the Dagster bill, not a minor residual. Fix: **cron-direct where there's no downstream Dagster dep** (like E11.4), else **async fire-and-forget + sensor/callback continuation** for `odds_current_dbt_rebuild` / the `daily_ingestion` chain / `odds_snapshot/clv_dbt_rebuild`. **🔗 Pairs with E11.9 #1** (cutting the rebuild FREQUENCY cuts both the metered wall-clock AND Snowflake). **⚠️ ALSO (W1, 6/22): the `ingest_statcast_to_s3_op`/`run_w1_lakehouse_op` dead-end tail runs the DuckDB build INSIDE Dagster → new run-minutes; move it to a Railway CRON (E11.4 pattern), not a Dagster op.** ↔ E11.0c already made the 409-wait compute-free; the residual is the in-run `/status` poll during the actual dbt run. **🔧 SCOPE CLARIFIED (E11.0b session, 2026-06-23): NOT a quick fix — the real fix is MIGRATING `lineup_monitor` / `statcast_catchup` / `odds_*_rebuild` OFF Dagster onto Railway crons** (the included-compute pattern; multi-service, needs live-runner validation — not shipped blind). **Fold the one-shot W1 parity job into the same migration story.** So E11.0b = a proper job-migration effort, sequenced behind E11.9-T2 (the bigger, shared rebuild-frequency win). **🔒 STEP-0 DONE → MEASURE-ONLY, NO BUILD (PM 2026-06-23):** baseline recorded in `docs/e11_0b_step0_baseline.md`. Key findings: (1) T2 shipped 6/22 (<1 day old) → post-T2 bill unmeasurable yet; (2) `lineup_monitor`/`statcast_catchup`/`daily_ingestion` are **chained SERVING graphs** (dbt gates `predict`/`write_serving_store` in-graph) → can't cron-migrate without **E11.1-Wsv** (post-July-1) — OUT of E11.0b scope; (3) the only cron-direct-eligible jobs (`odds_current_rebuild` + `odds_clv_rebuild`) are **~6 min/day combined = <5% of the metered minutes** (serving chains are ~95%). **⇒ Re-measure ~6/25–26; then likely CLOSE or scope to the single worst non-serving cron job. W1 DuckDB ops → fold into W1/W2, not here.** |
| — | **E11.0c** ✅ | Port runner delegation to `sensor_ops` + consolidate the 3 `_run_dbt` copies (fix INC-3) | B·infra | ✅ **DONE 2026-06-19 (resolves INC-3).** Consolidated to `pipeline/ops/_dbt_exec.py`; sensor_ops now delegates to the runner (statcast_catchup/lineup_monitor/pregame_snapshot off the in-process path). Plus the 409-contention fix: on a single-tenant-runner 409, the op raises `RetryRequested(40×30s)` → Dagster **releases the compute slot during backoff** (no billed idle). 19/19 tests green. See INC-3 in the incidents block for detail + the single-tenant-chokepoint note. |
| — | **E11.7** ✅ | Pipeline failure-semantics audit + resilience hardening | B·infra·reliability | ✅ **DONE 2026-06-22 — closes the INC-3/5/6 class.** 3-tier contract (HALT/WARN/ALERT-loud) documented in **CLAUDE.md** + guide §0.1 with the **canonical op→tier map** (15+ entries). dbt severity sweep: 3 non-serving models → `warn` (`fct_fangraphs_hitting_analytics`, `stg_derivative_odds`, `stg_parlayapi_line_movement`); serving-critical confirmed already `error`. **Full ops/sensor/cron audit: all ALREADY compliant from the prior incident fixes — no code changes needed** (the point-fixes had it right). **12 AST-based regression guards** (`test_e11_7_failure_contract.py`) make INC-3/5/6 patterns un-reintroducible (run-before-test, never `build` on critical path, no silent `except`-swallow, INC-5 URL-unset→stderr). CI: 436 pass, `dbtf compile` 1762. Committed. _(orig spec ↓)_ **NEW (2026-06-21) — P2, closes the INC-3/5/6 class. SCOPE NARROWED by INC-6:** INC-6 already applied the run-gates/test-non-blocking split to the DAILY path. **Residual:** (1) **document the ONE contract** (HALT = serving-critical only · WARN-but-continue = data-quality/bio/soft-deps · ALERT-loud-on-silent-skip = missing env/URL) into CLAUDE.md/§0.1; (2) extend the pattern to the **sensor/cron paths** (not just daily); (3) **error→warn severity sweep across ALL dbt models** (INC-6 did just the 2 bio tests) — serving-critical stay `error`, peripheral → `warn`; (4) regression guards (assert critical-path op uses `run` not `build`; flag silent `print()`-only skip paths). Prevents whack-a-mole. |
| 1 | **E11.9** ✅-deployed | Fix the broken dbt tagging + cut the real Snowflake cost drivers | B·infra·cost | **✅ DEPLOYED 2026-06-23 (A/B/C) — ⏳ re-measure pending the ACCOUNT_USAGE lag (vs the 57.57 cr/7d baseline + confirm untagged % collapses).** **📏 BEFORE baseline (the AC number): COMPUTE_WH = 57.57 cr/7d** (50.70 compute + 6.87 cloud); ALTER-ADD-COLUMN 8,160/7d; feature_pregame rebuild 3,233/7d. **Shipped (code, pytest 440✅):** (A) **redundant feature-store rebuild removed** — `lineup_dbt_clv_rebuild` was rebuilding the WHOLE feature subtree a 2nd time per lineup trigger for 2 CLV marts that don't consume it → narrowed to `mart_closing_line_value mart_prediction_clv`; (B) **ADD-COLUMN-per-write killed** — 49 unconditional ALTERs → `_migrate_prediction_columns()` (read INFORMATION_SCHEMA once; steady-state 0 DDL); (C) **dbt-fusion QUERY_TAG fixed** — connection-level `query_tag` in `profiles.yml` (validated: `dbtf debug` shows the tag; fusion accepts it). **✅ Corrections to the audit:** (D) COMPUTE_WH **already** `auto_suspend=60s` (no change); (E) Parlay inserts **already self-stopped 6/21** (the 3,570 was the pre-cutover tail) — just confirm stays 0. **⏭️ OPERATOR: commit the 5 files → DEPLOY to dbt-runner(Railway)+Dagster (REQUIRED for any savings) → re-measure vs 57.57 cr/7d after the ACCOUNT_USAGE lag.** **🔭 The BIGGEST lever is still pending → E11.9-T2** (convert `feature_pregame_game_features(_raw)` full-CTAS → incremental scoped to today's game_pks; deferred — ML-correctness-critical, needs `dbtf build` validation). _(orig audit detail ↓)_ **NEW (6/22 audit + QUERY_HISTORY drill-down, 2026-06-22) — THE Snowflake cost lever (the prior wave optimized the wrong 3%).** The "untagged 96.7%" = the **normal `DBT_RW`/`COMPUTE_WH` dbt+pipeline workload that isn't carrying QUERY_TAG** (queries show `dbt_version: 2.0.0` = dbt-fusion → **E11.3's `on-run-start` tag hook doesn't propagate to dbt-fusion per-model queries**). NOT a hidden poll. **Punch list (by query text):** **(A) FIX TAGGING** on the dbt-fusion path so audits are honest. **(B) ⭐ THE #1 DRIVER: `feature_pregame` CTAS ran 1,861×/7d (~266/day, ~2.0 cr cloud-svc alone)** — the heavy feature mart is fully rebuilt on (seemingly) every intraday tick → make the INTRADAY rebuild incremental/scoped/less-frequent (extend the E11.2-T2 `source_status:fresher+` logic to the intraday path, not just daily). **(C) `ALTER TABLE … ADD COLUMN IF NOT EXISTS` ran 8,850×** on `daily_model_predictions` (idempotent DDL firing on EVERY write) → run it ONCE, not per-write. **(D) Dead Parlay inserts** (`parlayapi.*` ~3,570×) — pre-6/16, self-resolve post-decommission; confirm they stop. **(E) App backend (`CREDENCE_API_RO`) hits Snowflake** on the picks/narrative path — small now, watch as users grow + consider serving from PG. **(F) Aggressive auto-suspend on COMPUTE_WH.** ⚠️ Credit numbers in QUERY_HISTORY are cloud-services-only; use `WAREHOUSE_METERING_HISTORY` for true warehouse credits — but the COUNTS make the targets unambiguous. **Measure credits before/after; THIS is "minimum Snowflake until profitable," not W1.** |
| — | **E11.9-T2** ⭐ ✅ | Convert `feature_pregame_game_features(_raw)` full-CTAS → incremental | B·infra·cost | ✅ **SHIPPED + VALIDATED 2026-06-23 (the biggest Snowflake lever; predictions provably unchanged).** Both models `table`→`incremental` (unique_key `game_pk`, `delete+insert`, `on_schema_change=sync_all_columns`) + an `is_incremental()` spine date-window filter (`pregame_incremental_lookback_days=7`, CLI-tunable). **No-stale guarantee is STRUCTURAL** — pure date window (not source-status conditional) → every run unconditionally re-materializes the whole window, so an in-window game can't be stale; the scheduled→completed `is_scheduled` flip is covered by N=7; >7-day retro corrections caught by the retained weekly `--full-refresh`. **Validation ALL GREEN:** byte-identical `HASH_AGG(*)` (all ~700 cols, order-independent) full-build vs incremental over the 7-day window MATCH (past-settled 88 + today/future 43 games, both tables); 25,977 rows = distinct game_pks (no drop/dupe); predict_today 0 absent/0 all-null, coverage 1.00; **E12 serving-parity PASS both tiers (pre_lineup + champion, 0 degraded)**. Evidence `/tmp/e11_9_t2_parity.md`. **⏳ NON-BLOCKING: the cost AC** — measure `WAREHOUSE_METERING_HISTORY` COMPUTE_WH + rebuild query-count vs the 57.57 cr/7d / 3,233-query baseline after a few days of incremental cycles. _(Pre-existing, NOT from T2: best_alpha=0 guard, 2 contract feats NaN, 1 serving-guard abstain.)_ | The feature mart is full-CTAS rebuilt ~3,233×/7d; E11.9-(A) removed the *redundant* 2nd rebuild but the mart is still a full `create or replace` every intraday tick. Convert to `incremental` scoped to **today's `game_pk`s** (rebuild only the slate, not all history). **⚠️ ML-CORRECTNESS-CRITICAL** — deferred from E11.9 on purpose: needs operator `dbtf build` validation that incremental output == full-refresh output (no stale rows, as-of-safe) before it ships. Pair the validation with the E12 serving-parity harness. |
| — | **E11.8** ✅ | Feed-freshness monitoring completeness ("what else has no alert?") | B·infra·reliability | ✅ **DONE 2026-06-22 — closes the INC-7/8 "no-alert" class + found 2 real bugs.** Canonical **feed→monitor→severity matrix** (`docs/e11_8_feed_monitor_matrix.md`, 20+ feeds + a scheduling audit). **Bugs caught/fixed:** (1) **archetype posteriors op was wired ONLY into `statcast_catchup_job` (which SKIPS when Statcast is early) — never the unconditional `daily_ingestion_job`** → went stale silently; now in BOTH jobs (the structural INC-8 close). (2) **`statcast_freshness_sensor` SLA breach fired `SkipReason` (= success in Dagster, silent) → changed to `raise` (pages)** — the monitor-the-monitors / INC-5 lesson. **Coverage added:** new HARD `schedule_freshness_alert_sensor` (schedule/`stg_statsapi_games` was WARN-only) + WARN on `derivative_odds_raw` + `eb_park_factors_raw`. 11 regression tests; CI 451 pass. _(Note: E11.8 makes archetype FRESH; the rookie/call-up NULL fill is separate = E13.7.)_ |
| 1 | **E11.10** ✅ | Scope `write_serving_store_op` per-fire work in the sensor jobs (stop the daily teams/players reload) | B·infra·cost | **✅ DONE 2026-06-23 — ~8 MIN → 35.7 SEC per fire.** New `write_serving_store_intraday_op` passes only `["--picks","--game-detail","--book-odds"]` (the 3 intraday-volatile sections); swapped into BOTH `lineup_monitor_job` AND `statcast_catchup_job` (the session caught that statcast_catchup ran the full path too — bonus trim). Daily `daily_ingestion_job` unchanged (still full `run_all` → owns teams/players/history/performance once/day). HALT-tier failure semantics preserved (only the write SCOPE changed). CI `uv run pytest` 461 pass/1 skip. No changelog (same data, correct cadence). **🔗 Materially cuts the ~95% serving-chain Dagster meter (both chains fire ~every 10 min intraday) → feeds INTO the 6/25 E11.0b re-measure (Dagster should drop from this alone, independent of T2/Wsv).** **CONCEPTUAL UPDATE: the serving chains can't be MIGRATED off Dagster until Wsv, but their per-fire WORK can be trimmed pre-Wsv — E11.10 is the first such win.** | ✅ 8min→35.7s measured; both sensor jobs; serve parity intact; CI green. _(orig spec ↓)_ **DO-NOW Dagster serving-chain trim (no Wsv needed).**
| 1 | **E11.11** ✅ | Serving-chain op DIET — cut per-fire redundant work in `lineup_monitor_job` | B·infra·cost | **✅ DONE 2026-06-23 — deployed + production-confirmed (fire 1: 7 Cortex calls on empty state; fire 2: 0 Cortex calls, 3 narratives restored from cache).** **(1) Narrative pick-delta guard** (`generate_pick_narratives.py --pick-delta-guard`): per-game fingerprint `side:round(prob,2)` vs a day-keyed state file (`/tmp/narrative_pick_state_{date}.json`); changed pick → Cortex regen; unchanged-but-NULLed (re-score blanked it) → fast UPDATE restore from cache (never serves a blank); unchanged+populated → no-op; nothing-to-do → exits before opening a Snowflake connection. **⇒ 0 Cortex (paid LLM) calls on no-pick-change cycles** (was N/game/fire). **(2) Umpire once-captured guard** (`ingest_umpires.py --skip-if-exists`): skips the MLB-API delete/insert if today's assignment is already present → ≤1 ingest/day. **30.13 self-heal PRESERVED + unit-tested** (a pitcher scratch that flips the pick → new fingerprint → Cortex regen + serve within one cycle). Both ops stay WARN-tier soft-fail (guards change per-fire WORK only). CI 480 pass/1 skip (+19 new tests `test_e11_11_op_diet.py`). No changelog. **⚠️ Minor latent: state file in `/tmp` → a mid-day container restart re-incurs ONE full narrative batch (safe fallback, just cost); fine unless restarts are frequent (then move state to PG/S3).** 🔗 stacks with E11.10 → both feed the 6/25 E11.0b Dagster re-measure. | ✅ 0 Cortex on no-change cycles + umpire ≤1×/day; 30.13 intact (tested); CI green. _(orig spec ↓)_ **the E11.10 follow-on; cost-first priority.** `lineup_monitor_job` fires ~every 10 min intraday; audit its ops for work redone every fire that didn't change. **⭐ #1 candidate `generate_pick_narratives_op`: regenerates Cortex LLM narratives EVERY fire even when no pick changed → hits BOTH bills (Dagster wall-clock + paid Snowflake Cortex credits); a pick only moves on a real lineup/odds change → most cycles regenerate identical text for nothing.** Fix: pick-DELTA guard — only (re)generate for games whose pick (side/prob) changed since the last generated narrative; skip entirely if no delta (it's already SOFT-fail, so safe to skip). **#2 `lineup_ingest_umpires`: re-ingests the HP-umpire assignment every fire, but it's posted ONCE (afternoon) and never changes → once-captured guard (skip if today's assignment already ingested).** Leave the dbt rebuilds (staging/feature/clv) — already scoped (clv narrowed, feature incremental) + load-bearing for the 30.13 self-heal. `statcast_catchup_job` fires once/day → low priority, note only. **🔒 HARD RULE: every skip is CHANGE-gated, NEVER timer-gated — a real intraday lineup change must still trigger a full re-score + fresh narrative + serve within one cycle (preserve the 30.13 self-heal invariant; do NOT reorder; lineup_predict stays last).** HALT-tier (serving-critical). Measure per-op wall-clock + Cortex calls before/after. 🔗 extends E11.10; trims the serving-chain meter pre-Wsv. | measured per-fire wall-clock + Cortex-call drop; 30.13 self-heal intact (a forced lineup change still re-scores+re-narrates+serves in one cycle); CI green. | `write_serving_store_op` runs `write_serving_store.py` with NO args → `run_all` = every section. **`lineup_monitor_job` fires repeatedly intraday as lineups post, and each fire rebuilds `--teams` (static in-season) + `--players` (expensive season-stat blobs) + `--history`/`--performance` — only `--picks`/`--game-detail`/`--book-odds` actually change with a lineup → ~8 MIN/run wasted.** **FIX: give the lineup_monitor invocation the intraday-VOLATILE set only (`--picks --game-detail --book-odds`); keep the DAILY `daily_ingestion_job` op as the full `run_all` (owns `--teams`/`--players`/`--history`/`--performance`).** Mechanically: parameterize the op (Dagster config / op factory) or add an intraday variant — do NOT touch the daily full run. **⚠️ HALT-tier (gates Railway PG/S3 serve) — VERIFY no intraday-critical serve path needs teams/players sub-daily** (lineup display reads the game-detail/picks payloads, not the profile blobs → profile pages can lag to the daily run); if a fresh-callup profile gap matters, make `--players` INCREMENTAL (changed players only) rather than dropping it — but confirm it's even on the intraday path first (likely not). **🔗 Trims the ~95% serving-chain Dagster meter (E11.0b) WITHOUT the Wsv migration.** Measure op wall-clock before (~8 min) / after × the lineup_monitor fire frequency. Tier stays HALT; only the per-fire write scope changes. | measured run-time drop on the lineup_monitor `write_serving_store_op`; serve parity intact (no intraday-critical blob missing); CI green. |
| 2 | **E11.12** ✅ | Feed-monitoring completeness RE-SWEEP — close the staleness class (INC-11/12 residual) | B·infra·reliability | **✅ DONE 2026-06-23 — the staleness class now has a named detector per incident.** **(A) Event-date antipattern sweep:** audited every `ts_col` → fixed 3 sparse-feed monitors (`batter_pitches.game_date`, `mart_player_archetype_posteriors.as_of_date` → `game_day_only:true`; confirmed `eb_bullpen…fit_date` + others are real ingest heartbeats). **(B) Found 5 feeds with CALLERS but NO MONITOR → added them** (sprint_speed, catcher_framing, OAA, player_profiles, matchup_cell_posteriors; all WARN/non-blocking). **(C) INC-11 class closed:** enumerated ALL `scripts/` + `betting_ml/scripts/` → ~40 Dagster-wired, 2 Railway crons, 18 intentional one-offs (each documented) → an auditable record so "is this feed dead?" no longer needs a full re-sweep. `docs/e11_8_feed_monitor_matrix.md` fully rewritten (ts_col-type column + caller enumeration + one-off table). **✅ "what would have caught each": INC-8 = caller matrix (op not in the unconditional job = red flag); INC-11 = script enumeration; INC-12 = event-date antipattern sweep.** CI 498 pass; monitors auto-activate next daily run (no wiring change). **⚠️ tiny residual: `monthly_schedule` has no ingest-heartbeat column → can only detect a missed MONTHLY load, not a mid-month failure (add an ingest_ts someday; low-pri).** _(orig spec ↓)_ **NEW (operator 2026-06-23) — Sonnet session.** 3 silent staleness incidents in a week (INC-8 dead-caller op, INC-11 dead-caller SCRIPT, INC-12 sparse-monitor gap) → the monitoring keeps being a step behind; E11.8's "completeness" sweep only enumerated Dagster OPS, so it missed by-hand scripts (INC-11). **(A) ANTIPATTERN SWEEP:** audit every monitor in `check_data_freshness.py` for an EVENT-date `ts_col` (e.g. `effective_date`, `*_date` = when the event happened, not when we ingested) that goes stale on quiet days even when the feed runs → switch to `ingestion_ts` OR a rows-in-last-N-days cadence check where the feed has legitimately empty days (INC-12 did this for `player_transactions` — find the rest). **(B) CALLER/MONITOR ENUMERATION:** list EVERY ingestion/compute feed — Dagster ops (`pipeline/ops/`) AND standalone scripts (`scripts/`) — and confirm each has (1) a scheduled caller, (2) a monitor with a sane threshold; flag any script with NO caller (INC-11 class) → wire or mark intentional-one-off; any feed with no monitor → add one. Refresh `docs/e11_8_feed_monitor_matrix.md` to the complete feed→caller→monitor→ts_col-type→threshold matrix. (The permanent-blob display-staleness class is separate — fixed by INC-12's `ORDER BY`; just catalog display-derived statuses in the matrix.) | complete matrix; every feed has a caller + monitor; no event-date `ts_col` on a sparse feed (fixed/cadence-aware); CI green. |
| — | **E11.6** ✅ | Decommission dead Parlay-API ingestion + prune the `parlayapi` dbt branch | B·infra·cleanup | ✅ **DONE 2026-06-21 (resolves INC-2).** 3 Parlay ops removed (daily head → `ingest_action_network`); `pregame_odds_snapshot`→no-op stub; 3 odds marts cleaned (parlay union/CTEs gone, **historical rows kept as cold archive**, line-movement now uses real `commence_time`); 4 Parlay freshness configs removed. Value-preserving (Snowflake parity ✅); pytest 405/1, `dbtf compile` 1761/1761. **Residual ops-cleanup = E11.6b** (remove 17 dormant `odds_snapshot_schedules` + the no-op `pregame_snapshot_job`; PM-approved, low-priority). |
| — | **E11.6b** ✅ | Final Parlay sweep: remove dormant schedules + no-op job | B·infra·cleanup | ✅ **DONE 2026-06-21 — Parlay decommission fully closed (INC-2 → E11.6 → E11.6b).** Pure code cleanup across 7 files: removed the 17 `odds_snapshot_schedules` + `_ODDS_SNAPSHOT_CRONS`, `odds_snapshot_job` + its 3 ops, `pregame_snapshot_job`/`pregame_odds_snapshot`/`pregame_dbt_clv_rebuild` ops, the `pregame_snapshot_sensor` (file deleted). `stg_parlayapi_*` kept (cold-archive). pytest 405/1 ✅; no dbt changes. |
| — | **E2.2** ✅ | Dependence structure (copula) | A·market-blind | ✅ **DONE 2026-06-22 — honest finding (NOT-MET gate = the finding, like E2.1b).** Operator run (11,659 eval games 2021–25): **residual ρ = −0.0035** (Kendall −0.0046, naive raw +0.0002) → **home/away runs are essentially INDEPENDENT; the Gaussian copula is UNNECESSARY** (global; no material park/temp/dome bucket ρ). Corr AC ✅; **var(total) AC ❌ — but identically with/without coupling** (copula 15.15 ≈ independent 15.18 vs empirical 19.99, **~24% shortfall**) ⇒ the Story-29.1 variance deficiency is in the **MARGINALS, not the dependence.** The added **dispersion diagnostic** pins it: E2.1 fits the NegBin `r` on optimistic TRAIN-fit means → biased high (under-dispersed); an **OOS-calibrated `r` (~4 vs train ~6–15) reproduces var(total) → closes the gap.** Harness `betting_ml/utils/copula.py` + `fit_copula.py` + 14 tests (`test_copula.py`); ρ fit on the RESIDUAL dependence (distributional-transform→normal-scores, discrete-marginal-correct), not raw pairs (double-count). **➡️ E2.3 directives: drop the copula (convolve independently) + calibrate per-side dispersion on held-out residuals.** `copula_v1.json` (ρ≈0), `ablation_results/e2_2_copula_fit.json` + `e2_2_copula_decision.md`. |
| 8 | **E2.3** ✅ | Convolution → predictive distributions | A·market-blind | **✅ PRODUCT-COMPLETE 2026-06-24 (operator gate + per-side re-run, 11,662 eval games 2021–25): total + team-totals CALIBRATED; run-diff = documented accepted near-miss.** `betting_ml/utils/totals_distribution.py` + `scripts/totals_generative/fit_totals_distribution.py` + 17 tests. Convolves the two E2.1 marginals INDEPENDENTLY (ρ=0, reuses `copula.py::sample_gaussian_copula_negbin` — no copula built). **Dispersion thesis VALIDATED:** leakage-safe expanding-window held-out `r` stable per-side (`r_home`=4.03/CV 0.008, `r_away`=3.57/CV 0.023; fixes E2.1's under-dispersed 8.5). **Total ✅ calib_80 0.838 + PIT-flat 0.0068; team-totals ✅ (home 0.863/away 0.847)** → core totals deliverable MET + serving-ready (`totals_distribution_v1.json`: per-side r + P05–P95 grid + p_over). **run_diff = ACCEPTED near-miss** on PIT-flatness (0.0303 vs 0.025; coverage 0.839 + center 0.503 fine). **Cause settled, 2 lines:** (1) synthetic — correct model → flat run_diff 0.0067 (miss is real, dispersion asym alone caps ~0.015); (2) per-side re-run MOVED home/away dispersion (r_home 4.03 > r_away 3.57) but left run_diff UNCHANGED (0.0301→0.0303) ⇒ the miss is NOT dispersion but the tiny home/away dependence the independent convolution omits BY DESIGN (E2.2 ρ≈0: negligible for the *total* which passes, but the *difference* is uniquely sensitive). **NOT chased:** the only fix = re-add a copula, contradicting E2.2 (don't force a coupling the data lacks), and run_diff is NOT a served surface (shipped H2H uses E13.6). Honest finding (like E2.1b/E2.2 "NOT-MET = finding"), not a blocker → **proceed to E2.5.** **🔒 Leak-guard RESOLVED: bullpen swap ALREADY shipped by E1.7** (`bp_eb_xwoba` ← de-leaked `eb_bullpen_team_posteriors`, `appearance_date < game_date`, 96.5–98.9% populated). Outputs: `totals_distribution_v1.json` + `ablation_results/e2_3_convolution_calibration.{json,md}`. **⚠️ CALIBRATION ≠ EDGE (E2.2 §6): the per-side mean model barely separates games (between-game mean-var only 0.894 → consistent with E1 "totals point-models signal-thin").** So a well-calibrated total may still NOT beat the main line; **the EDGE question is NOT settled here — it's gated at E2.6** (main-line un-pause needs NLL<2.8893 AND Brier<0.248; the realistic value path is the DERIVATIVE markets — team/alt totals where the book prices lazily — each vs its own close + PBO<0.2/DSR>0). E2.3 ships the honest distribution + the UX; it does not assume an edge. **🔭 E13.8 BAR (2026-06-23) — the empirical target for the totals re-eval: the MAIN over/under PRICE is a coin flip at EVERY book (Brier ~0.250 = no-skill, over-rate 0.49–0.51) and the NUMBER itself is near the variance floor (line MAE ~3.4–3.7 / RMSE ~4.5). ⇒ the main-line E2.6 gate is empirically confirmed near-unbeatable; this does NOT touch E2.3's product value (calibrated distribution + UX) and the edge hope stays the DERIVATIVE/alt markets, not the main total.** |
| 10 | **E5.0** | Prop market ingestion (live) | B·data | Props elevated; softest market. **Runs after the E11 cost-opt wave** (Session B). |
| — | **🎯 E5 SYNTHESIS (operator 2026-06-24)** | The K-props bet + components | A·model | **✅ E5.3 DONE 2026-06-25 (de-vig + per-book edge table built) → only E5.4 (the hard gate) remains.** `prop_edge.py` (24 tests: two-way de-vig + hold + the half-line-vs-integer PUSH convention + EV-with-refund + name-bridge keys) + `edge_devig_props.py` (scores the E5.2 bundle off the cached frame — no Snowflake; S3-first DuckDB read of the K lines). **🔑 The E5.2-flagged NAME→ID BRIDGE solved 42%→94.6%** (7,351/7,774 keys) via 3 stacked techniques: ref_players normalize (accent/Jr-Sr/team-tag fold) + (last-name, first-initial) fallback for legal-vs-common names (+278) + a **±1-day UTC window** (S3 commence_time UTC vs local game_date — the biggest resolver, +3,780); unresolved all FLAGGED (302 scratch/IL, 112 non-modelled-starter, 9 ambiguous), not dropped. Output: 63,765 (pitcher×date×book×line) rows, 14 books, Pinnacle anchors 81.3% (NOT thin). **🟡 HONEST RESULT — the leading indicator into E5.4 (expect null): two-sided `edge_over` ≈ 0 (mean −0.013) → the model neither over- nor under-shoots the K market; blind-over EV ≈ −8.7%/$1 NET OF VIG (median hold ~6.9%) → betting these prices unselected just pays the vig.** The favourable-side EV>0 fraction is LINE-SELECTION-BIASED + unproven (flagged) — that's exactly what E5.4 deflates (PBO/DSR + forward CLV on the SELECTED subset). Edge table → `e5_3_prop_edge_table.parquet` (the E5.4 input; contract in the summary json). §0.5 cost-hygiene held; fast gate 635✅. **▶️ NEXT (final prop step) = E5.4** — the hard gate decides ship-vs-null; the E5.3 unbiased read says null is the likely (and acceptable) outcome, but the gate is the decider. _(E5.2 detail ↓)_ **✅✅ E5.2 DONE + COMMITTED 2026-06-25 — bake-off-selected, calibration-gated, served.** Bake-off (9-config grid × model-class × rate-construction × feature toggles; CRPS primary + coverage@80 + PIT-KS + ECE under purged CV; **single PBO over the whole grid = 0.0**) → **the LEARNED count models BEAT the hand-built structural compound** (lgbm CRPS 1.227/ECE 0.007, glm 1.236/0.010, vs best compound 1.270/0.035). **Served = `poisson_glm_k`** (best PIT-KS + near-best CRPS + interpretable; matches the PM call) — bundle `strikeout_glm_v1.pkl` (gitignored, regenerable). **AC MET on 26,062 starts: calib_80 0.8104 ✅, mean at-the-line ECE 0.0202 ✅** (per-line 0.013–0.029), pitcher_outs calib_80 0.9016, market-leak guard ✅, PIT max-decile 0.0505 (diagnostic, not gated — 80% coverage is the gate). **Findings:** recency HELPS only when a MODEL weights it (glm wins on raw recency feats) — as a hand-rolled EB-shrunk rate it HURTS (+0.034, EB already handles small-n); **lineup-log5 matchup HURTS (+0.0203 — corroborates E13.2 "matchup ≈ identity")**; catcher-framing inert (+0.0016). Cost hygiene per §0.5 (one Snowflake pull → cached parquet, all candidates/folds read it). Cleaned a naming footgun (removed a stale compound `…_v1.json` under the canonical served-params name → would've misled E5.3). best_alpha=0 (calibration≠edge) → **▶️ NEXT = E5.3** (de-vig vs the K line; **needs the `ref_players` name→pitcher_id bridge** to join the 7,774 S3 closing lines to predictions) → **E5.4 the HARD gate** (a clean null is acceptable). 36 tests. Handoff `E5_2_HANDOFF.md`. _(prior CODE-COMPLETE detail ↓)_ **✅ E5.2 CODE-COMPLETE 2026-06-24 (pending operator gate run):** market-blind K-prop pricer built — `betting_ml/utils/prop_pricing.py` (pure, 29 tests) models **K = K-RATE × BATTERS-FACED**: p_k = log5(EB-shrunk pitcher K [season→career→league], opposing-lineup `avg_k_pct_30d`, league) + a TEMPERED catcher-framing logit nudge (γ=0.04; NO platoon/TTO per the E13.2 temper — leans on framing + EB small-sample shrink + market laziness, NOT a matchup model); BF = `starter_ip_v1` outs-NegBin + reach-NegBin; **K|BF ~ Beta-Binomial(s)**, `s` the leakage-safe concentration lever (K analogue of E2.3's NegBin r). Reuses E2.3 `totals_distribution` diagnostics + E1.1 purged CV + `market_blind` guard. Also prices `pitcher_outs` (analytic NegBin) + `batter_total_bases`/`hits` (PA multinomial). Orchestration `betting_ml/scripts/prop_pricing/fit_prop_pricing.py` (operator-run >1-min Snowflake → calib_80/PIT/per-line ECE + served params + `ablation_results/e5_2_prop_pricing_calibration.{json,md}`). E5.1 K data already in S3. **First gate run (2026-06-25, 26,320 starts): lead calib_80 0.859≥0.80 ✅, ECE ~0.035 ✅; PIT a documented near-miss (0.033 vs 0.025 after the λ=0.85 spread-recalibration) — cf. E2.3 run_diff 0.0303.** best_alpha=0 (calibration≠edge) → E5.3 de-vig → **E5.4 HARD gate**. Handoff `E5_2_HANDOFF.md`. **🔬 METHODOLOGY UPGRADE 2026-06-25 (operator pushed on 2 gaps): (1) one model type → `bakeoff_strikeouts.py` MODEL-CLASS bake-off (compound-flat / compound-recency / LightGBM-Poisson-on-K / Poisson-GLM; purged-CV CRPS+coverage+PIT-KS+ECE, every candidate λ-recalibrated=fair, winner=min-CRPS-among-well-calibrated, PBO-guarded); (2) "train-on-prev-season ignores in-season stuff change" → forward-CV stays (leak-honest) but the FLAT season+career K-rate → `build_predictors(rate_mode=…)` RECENCY construction (k_pct_7d/30d/csw_3start/velo-trend — already-existing features — EB-shrunk to career→league); bake-off ABLATES rate×framing×lineup. 35 tests. Operator: STEP1 bakeoff→pick winner, STEP2 set `_RATE_MODE_DEFAULT`+gate run.** _(orig synthesis ↓)_ **The "what's worth pursuing" review collapses into ONE coherent next-market bet: pitcher STRIKEOUT props (E5.2 lead).** A softer, lower-limit player-prop market (books price off recent-avg + reputation = Miller-Davidow lazy pricing) + plays to our EB/NegBin stack + a genuinely DIFFERENT market from the exhausted game-level ones. **E5.2 enriched** = K = K-rate (NegBin + EB career/league priors + platoon + ⭐catcher-framing, the underweighted factor we have data for) × batters-faced (a SURVIVAL/hazard workload model = the K-opportunity denominator) + F5 scoping; edge-gated by the existing **E5.4** (calib_80 + PBO/DSR per market + forward CLV net of vig — "props overfit, this gate is the crux"). **Components/follow-ons scoped:** **E5.7** 🔮 NLP injury/role signal (props enrichment — the novel info-timing signal; gated on E5.2 promise, heavier); **E10.2** 🔮 uncertainty-aware (posterior) bet sizing (closed-form Kelly-shrunk-by-CI, fixes the Jensen overbet; gated on a PROVEN edge; NOT RL). **Dedup:** E5.6 zone-matchup = **E13.10** (canonical). **Filed (right-tool-wrong-time, NOT stories):** Kalman/SPUE (cost-opt note), bandits (≥2 proven markets first), causal/DAGs (hygiene), line-timing (needs paid tick data). Calibration is a SOLVED STRENGTH, not a gap. Prompts in `story_prompts.md`. **⚠️ K props = a HYPOTHESIS held to E5.4 — we've found efficiency everywhere; the bull case is it's an untested softer market.** |
| 11 | **E5.1** 🟢 | Historical prop backfill | B·data | **✅ SUBSTANTIALLY DONE per the 2026-06-24 S3 AUDIT — PLAYER PROPS complete (2023–25 + 2026 current, incl. `pitcher_strikeouts`) + TIER-1 derivatives complete (h2h_1st_5/totals_1st_5/NRFI) → E5.2 & E13.13 data READY. Double-write resolved; ~563K cr (renews 5M 7/1). Deferred to post-7/1 (idempotent from the 8/11 checkpoint): Tier-1 alts + Tier 2/3 + multi-sport. See the credit-burn note's AUDIT RESULT for the full inventory.** _(orig ↓)_ **⏰ ELEVATED 2026-06-23 → WINDFALL GRAB before 7/17; SCRIPT WRITTEN 2026-06-23 (`scripts/backfill_multisport_props_to_s3.py --sport baseball_mlb`); probe pending operator run.** E5.1 was temporarily marked BLOCKED after the initial probe returned `INVALID_MARKET` on the main historical odds endpoint — **that was the wrong endpoint.** Historical player props require the two-step events endpoint: (1) `GET /v4/historical/sports/{sport}/events` → event IDs (1 cr/date), (2) `GET /v4/historical/sports/{sport}/events/{eventId}/odds?markets=pitcher_strikeouts,...` → props per game (10 × markets × regions cr/event). Docs confirm historical prop availability since 2023-05-03T05:30:00Z — exactly the spec floor. Cost estimate: ~10 cr/event × 5 markets × 2 regions × ~10,800 MLB games = ~1–2M cr (probe will measure actual cost). **`scripts/backfill_mlb_props_to_s3.py` DEPRECATED** (used wrong endpoint). Use `backfill_multisport_props_to_s3.py`. S3 layout: `mlb/props/market={market_key}/season={season}/date={game_date}/data.parquet`; seasons 2023–2026. |
| — | **E6.7 → Slim-contract re-derivation** | prune re-derivation (core ✅ done in E1.8 closure) | A | **✅ CORE SATISFIED 2026-06-19 (E1.8 closure).** The slim contracts were originally chosen *with* the leaky `bp_eb_xwoba` ranked #1/#2, so the prune had to be re-derived on clean data — **done reproducibly via `derive_clustered_contract.py`** (E1.3 non-noise-cluster rule + leakage guard, no hand-pruning) on the both-de-leak matrix: `total_runs` 13, `home_win` 19 FINAL, `run_diff` landing. This **closes E6.7's core ask** (re-derive the prune on leak-clean data, reproducibly). **Residual (optional, NOT blocking E1.9):** the deeper validation E6.7 also scoped — **per-game SHAP stability + slice parity + the explicit PCA-vs-keep-raw call** — was *not* run (the tool used clustered-MDA, which already handles redundancy). Fold these into E1.9's per-tier model validation **if** the bake-off shows contract instability; otherwise this row is effectively closed. |

**🐞 Incidents — INC-1…INC-15 ✅ RESOLVED · 🟢 INC-16 RECOVERY COMPLETE (soaking to close) · ✅ INC-17 FULLY RESOLVED (P1 diagnose = serving gap + P2 dual-source dbt fix [gate passed 0.176] + P3 monitoring hardening [post_lineup coverage check + spread-threshold fix] — chain closed: the next such gap fires at the INPUT) · ✅ INC-18 RESOLVED 2026-06-29 (prod `/picks/ev` 503 — `CREATE OR REPLACE` strips Snowflake GRANTs) · ✅ INC-19 RESOLVED 2026-06-29 (recurring dbt incremental TYPE-DRIFT, 4th occurrence — the **W5 `mart_bullpen_effectiveness`** migration flipped 13 bullpen cols Snowflake-NUMBER→DuckDB-FLOAT → the downstream incremental `feature_pregame_game_features_raw` couldn't change type in place → `dbt_umpire_feature_rebuild` HALT. FIX: operator DROP'd the 2 incremental tables → rebuilt fresh [fusion `--full-refresh` unreliable]; DURABLE: **pinned the 13 cols to explicit types at the `mart_bullpen_effectiveness` source** [value-preserving, compile-green] — ships standalone with W7b's batch, no `feature_pregame_*` collision. ⭐ **RECOMMENDED CLASS-FIX (greenlit → see story): a dbt TYPE-CONTRACT CI test that FAILS THE BUILD on a column-type drift BEFORE it HALTs prod** — land it BEFORE/with W8 [the feature-layer wave = the most type-drifts]; same class as INC-15/W1d/INC-16-P0.)**
- **✅ INC-18 RESOLVED 2026-06-29 — prod `/picks/ev` 503 (4 AM, cache-miss → Snowflake fallback → `STG_STATSAPI_GAMES does not exist or not authorized`).** ROOT: **every Snowflake `CREATE OR REPLACE VIEW/TABLE` SILENTLY DROPS existing GRANTs** — the INC-17 P3 `state:modified+` build recreated `stg_statsapi_games` (an upstream ancestor) → stripped `CREDENCE_API_RO`'s SELECT → the request-path fallback failed. FIX: operator ran the 6 GRANTs incl. **`GRANT SELECT ON FUTURE TABLES/VIEWS IN SCHEMA baseball_data.betting TO ROLE CREDENCE_API_RO`** + a durable `+post-hook` re-grant on the staging block in `dbt/dbt_project.yml` (pending commit). 503 resolved (200). ⚠️ **PROGRAM-WIDE HAZARD:** every W-wave does `CREATE OR REPLACE` → any request-read object loses its grant on recreate → folded a GRANT-PRESERVATION rule into the decommission program + a direct W6 addendum (the odds marts `mart_odds_outcomes`/`mart_game_odds_bridge` are request-read). 🧹 **ROOT FIX → 🔀 FOLDED INTO W7 (2026-06-29):** the root anti-pattern (STAGING on the request path — `picks.py`/`bets.py` should query MARTS/serving-layer, not `stg_*`) is now done AS PART of W7's reader migration (same routers, same goal = request path zero-Snowflake), not a separate session. Band-aid (post-hook + FUTURE GRANTS) stays as defense-in-depth. _(INC-13: per the 2026-06-24 report the DDL + full-2026 re-ingest are DONE; parity passes; recurrence-fixed. Residual = the 2018 415-game gap = a separate low-pri operator backfill decision. ⭐ `miss_distance` confirmed = the bat-to-ball WHIFF-SEVERITY metric, 2026-ONLY data — see the E13.2b/E5.2 constraint.)_
- **🟢 INC-17 FULLY RESOLVED 2026-06-28 (P1 diagnosis ✅ + P2 serving fix ✅ — gate PASSED, backfilled, serving current).** ✅ **GATE: `home_win` corr 0.031→0.176 (5.7×, PASS) after the operator rebuild + rescore-backfill (06-23→06-27 post_lineup) + serving-store push** — matches P1's prediction. ✅ **W-SERIES CLEARED — pre-existing dbt bug, NOT a lakehouse regression.** Serving is current NOW (dbt rebuilt directly vs Snowflake); the 3 model commits' FULL deploy is DEFERRED until W5 completes (operator). 📌 **2nd follow-on:** the model-health SPREAD sub-check (0.0299<0.030) still "fails" = the **E13.6 temperature calibrator (T=6.30), a KNOWN characteristic NOT a regression** → make the spread threshold calibrator-aware so P6 doesn't cry-wolf on it. _(detail ↓)_ P1 DIAGNOSIS = SERVING GAP (NOT model decay): gate failed `home_win` (corr 0.013) + `run_differential` (−0.021) post_lineup; decisive rescore → live corr ~0.03 → rescored 0.12–0.15 (5×, PASS). **P2 ROOT CAUSE FOUND & FIXED:** `feature_pitcher_cluster_matchups`, `feature_pitcher_batter_h2h_matchups`, and `feature_batter_archetype_matchups` all pull lineups ONLY from `stg_statsapi_lineups_wide` (historical 2015–2025). For 2026 games the confirmed lineup lives in the SCD-2 `feature_pregame_lineup_state` source — which was NOT included. Result: all `slot_*_player_id` were NULL for every 2026 game → cluster/archetype/H2H matchup aggregates all NULL → imputed to constants → model sees a flat feature vector. `avg_eb_woba` (from `feature_pregame_lineup_features`) was correctly dual-sourced and populated. **Fix: added the same dual-source lineup CTE** (SCD-2 UNION historical staging, `NOT IN` guard) to all 3 models, identical to the already-working `feature_pregame_lineup_features.sql` pattern. Python fast gate **675 passed**. **▶️ Operator deploy (see INC17_P2_session_recap.md):** (1) `dbtf build --select feature_pitcher_cluster_matchups feature_pitcher_batter_h2h_matchups feature_batter_archetype_matchups`; (2) `dbtf build --select feature_pregame_game_features_raw feature_pregame_game_features`; (3) rescore 2026-06-23..today as post_lineup backfill; (4) run gate: `uv run python scripts/ops/model_health_metrics.py --since 2026-06-23 --prediction-type post_lineup --schema betting_ml` → expect home_win corr ≥ 0.05. ⚠️ Pre-existing `dbtf compile` failure in `mart_team_rolling_offense/pitching` (E11.1-W5 Jinja `{% else %}` parse error in dbtf-fusion); unrelated to this fix — flag to operator. ⛔ DO NOT RETRAIN v6. 📌 OPEN FOLLOW-ON: harden the A2.5 coverage metric to include lineup-gated feature families so the next null-serving gap triggers an alert (currently BLIND to cluster/archetype/H2H null by design).
- **🟢 INC-16 — RECOVERY COMPLETE (P0–P6 all ✅ LIVE); SOAKING to formal close (2026-06-28).** Railway workspace was T&S-restricted (irreversible) → fully re-hosted on AWS; serving Postgres → **DynamoDB**. ✅ P0–P5 + **P6 observability EXECUTED LIVE 2026-06-28 (6-layer email alerting, emails CONFIRMED received)**; ✅ Railway CANCELLED. 🔥 P6 found the legacy alert stack had gone SILENT at cutover (sensors raised into Dagster+'s now-OFF email-on-failure) → re-armed under self-hosted OSS. **2 GATES TO FORMAL CLOSE (operational, not build):** (1) clean multi-day soak incl. ONE run of each WEEKLY schedule — esp. `weekly_ml`/`weekly_meta_model` = the 4 GB CPU/mem risk (P6 now alerts so this soaks safely); (2) **Phase-J: decommission Dagster+ Cloud** (the idle free rollback until then). ✅ **INC-16-P2b DONE 2026-06-28** — DynamoDB cache backfilled (S3-copy 6/15→6/27 + Snowflake regen 3/28→6/14, 77 dates); past picks load. 2 root causes: an IAM gap (`baseball-access-user` lacked DynamoDB perms → `write_serving_store` SILENTLY S3-only — a P6 hardening candidate) + a `/picks/ev` historical bypass that read Snowflake at REQUEST time (fixed → ⭐ tell W4: one request-time read found+fixed). ⏳ pending operator merge of `picks.py` to deploy the fix. ✅ **INC-16-P6a DONE 2026-06-28** (on `dev`→main via CD): healthcheck false-positive fixed via deploy-lock + 3-strike debounce (real outage still pages in ~15 min); + the P2b addendum (`write_serving_store` now alerts on DynamoDB write/connect failures instead of silent S3-only). ✅ both smoke tests VERIFIED (operator 2026-06-28: real outage paged; forced DynamoDB-fail fired SNS) — no open items. 🔵 **P7 (pinning/upgrades) + P8 (dev box) SPUN OUT as STANDALONE non-critical hardening — NOT required to close INC-16** (P7's urgent slice = flaresolverr pin already done in P5; remainder = ongoing maintenance posture). 🔒 Post-beta: dagit SG is `80`/`443` open-to-world with NO IP-allowlist (Caddy basic-auth = sole gate) → harden with Tailscale/Cloudflare Access. Full runbook = `INC16_AWS_REHOST_RECOVERY.md`; prompts P0–P8 in `story_prompts.md`. **✅ P0 DONE** (pre + post-lineup picks served off-Railway to S3; DAILY MANUAL cycle until cutover — gotchas: `DATABASE_URL=''` to skip the dead PG, dbt FLOAT type-drift self-heal, W3pre stash-before/pop-after). **✅ P1 DONE 2026-06-26** (commit `702a8a5` on `dev`): EC2 t4g.medium `i-07594af1679f81c38` / EIP `100.57.225.242`, 6-container Docker Compose in `services/dagster/aws/` (Dagster + dbt-runner + flaresolverr, flaresolverr verified solving Cloudflare from the shared egress), FanGraphs ops WARN-tier; **box runs off `dev`, all schedules STOPPED — Dagster+ Cloud still owns live (no double-serve)**; box in `infrastructure/aws_resources.md`. **⚠️ ODDS CORRECTION:** no live runtime odds pull — predict_today + write_serving_store read CAPTURED odds from `mart_odds_outcomes` → displayed PRICES are stale-as-of-last-capture (pick SIDE is fine); raises P3 urgency. **✅ P2 DONE 2026-06-26** (serving cache → DynamoDB): table `credence-prod-serving-cache` (single-table structured PK/SK), new `app/backend/services/serving_cache.py` reader (DynamoDB→S3), 6 routers repointed + `pg.py` deleted, `user_portfolios`→`portfolio` map on the DynamoDB users table, `daily_picks` retired, `write_serving_store` writes DynamoDB+S3, IAM grants on the EC2 instance-profile + Lambda role, serving `DATABASE_URL` dropped from box + Lambda, **E9.31 unparked + heatmap renders live**. Key gotcha solved: a Compose `env_file` can't carry PEM newlines → `pipeline/resources/__init__.py` (+ writer + dbt-runner entrypoint) now normalize `\n`-escaped/base64 keys (this had silently broken ALL container Snowflake access). **✅ P3 CODE-COMPLETE 2026-06-26 — approach EC2 host-cron** (operator decision; Lambda/EventBridge deferred until data sources / frequency grow): the 4 capture crons (odds/schedule/derivative/weather) re-homed as run-once images under the `capture` profile in `docker-compose.yml`, fired by host crontab `services/dagster/aws/capture.crontab`. Same PEM-key bug fixed in all 4 capture entrypoints. Served-price freshness: P3 restores RAW odds; DISPLAYED prices refresh via the odds-mart rebuild driven by `odds_current_rebuild_sensor` (enabled at P4) or the optional interim cron chain. Operator deploy: build images → smoke-test → install crontab → run odds backfill for the live gap. ✅ **P3 VERIFIED LIVE + committed to `dev`** (odds fresh in `mart_odds_outcomes` 20:26 UTC, full chain proven to DynamoDB, derivative 15/15). 🔑 **derivative_capture was NEVER scheduled on Railway** (8-day gap predates the outage) → P3 ENABLED it for the first time. 🔁 **2 deferred backfills (non-blocking):** (a) odds intraday gap 11:02→20:05 UTC — run TOMORROW once 06-26 is past (`odds_api_ingestion.py historical-odds --start-date 2026-06-26 --end-date 2026-06-26`, MAIN key; closing lines captured live, only daytime line-movement holed); (b) derivative 8-day gap (EVAL/CLV-only, non-serving) when E5 resumes. **✅ P4 CUTOVER EXECUTED LIVE 2026-06-27 (box on `main`, schedules+sensors ON, Dagster+ OFF→no double-serve, Railway CANCELLED, HTTPS dagit + SSM live, SSH retired; now SOAKING). 🔥 Phase-G flushed 4 env/IAM gaps (`ARTIFACTS_FROM_S3`/`CACHE_BUCKET`/`USER_BETS_TABLE` + IAM `credence-s3-api-cache-rw`/`credence-dynamo-user-bets-settle`) — all "box env ≠ Railway env, instance-role ≠ static keys". ⭐ KB: BAKED-IMAGE DRIFT (a `git pull` doesn't update a running container → must `up -d --build`) → folded into P5. SG = open-:80/:443, no IP-allowlist, Caddy basic-auth sole gate (post-beta harden).** Artifacts** = dev→main cutover (rebuild capture-profile images on pull; pre-cutover checklist: PEM key loads, IMDS hop-limit=2, region, Lambda grants persist; ⚠️ **captures are HOST-CRON not Dagster schedules — VERIFIED in code: NO registered Dagster schedule duplicates them** [`intraday_schedule_capture_*`/`intraday_weather_*` omitted from `all_intraday_schedules`; registered = 7 weekly/daily + `odds_clv_rebuild_daily`, none a capture; nothing boots RUNNING]; re-comment `capture.crontab` line 42 + enable `odds_current_rebuild_sensor` in the same window; enable dagit schedules while turning Dagster+ OFF in the same window) + **dagit HTTPS built: `caddy` service + `Caddyfile`** (Let's Encrypt + basic-auth; operator-confirmed auth = **Caddy basic-auth + SG allowlist**; webserver re-bound `127.0.0.1:3000`, public `:3000` dropped at cutover) + **SSM** to retire SSH (attach `AmazonSSMManagedInstanceCore`) + DNS A `dagster.credencesports.com`→EIP (Route 53) — full operator runbook `services/dagster/aws/README.md` §P4 + `aws_resources.md` record + cancel Railway + bank the ~$275/mo Dagster+ saving; **✅ P5 EXECUTED LIVE 2026-06-27 = orchestration CI/CD in GitHub** (all 3 CI checks GREEN on PR; CD proven end-to-end OIDC→SSM→deploy.sh on the box; auto-on-merge confirmed; operator did OIDC role + repo vars/secret + branch protection): `.github/workflows/orchestration_ci.yml` (Dagster defs-validate + `compose config` + lean-image builds + `scripts/ci/check_env_parity.py` + `scripts/ci/check_deploy_wiring.py`) + `orchestration_cd.yml` (OIDC→SSM RunCommand → `services/dagster/aws/deploy.sh`); deploy.sh = **`git pull --ff-only` FIRST** → env-parity-nonempty (vs `env.required`) → snapshot :rollback → graceful drain → `up -d --build` + `--profile capture build` (kills the baked-image drift) → crontab-reinstall-if-changed → verify (defs/daemon/dbt-runner-health/PEM/IMDS) → AUTO-ROLLBACK on failure. flaresolverr pinned off `:latest` (P7-t1 folded in); cloud-init/provision now bootstrap cronie/SSM/IMDS-hop-2/P4-IAM so a fresh box is fully wired. 🔥 **LIVE-DEPLOY GOTCHAS:** (1) CI runner has NO box `.env`/dbt artifacts → defs-validate needs 4 `SNOWFLAKE_*` placeholders (read at import, never connect) + `dbtf parse` to build `dbt/target/manifest.json` offline; `compose config -q` needs `cp .env.example .env` first. (2) env-parity REORDERED to run AFTER the pull (was pre-pull → validated the stale manifest → key changes never took effect via CD). (3) BOOTSTRAP: deploy.sh can't deploy itself — a new deploy.sh / change to its own pull-order needs ONE manual box `git pull` (SSM) first; pull-first self-heals thereafter. (4) `OPENWEATHERMAP_API_KEY` dropped from required (optional weather fallback); operator set `SERVING_CACHE_TABLE` on box .env. Kills the manual "codeloc redeploy off main" drift that caused INC-15; true blue/green deferred to a post-beta SLA. **✅ P6 EXECUTED LIVE 2026-06-28 = observability + email alerting (deployed via P5 CD; EMAILS CONFIRMED received — direct channel test + end-to-end healthcheck with flaresolverr genuinely stopped both delivered)**: unified SNS topic→one email sub (provision `services/observability/provision-observability.sh`). Built — (1) OFF-box dead-man Lambda + EventBridge (08:30 ET) reading a NEW daily heartbeat `write_serving_store` writes to DynamoDB (`pk=ops,sk=heartbeat#daily`); (2) CloudWatch agent (mem/swap/disk) + alarms (status-checks, mem>85%, swap>50%, disk>85%, CPU>90%/30m SUSTAINED, CPU-credits-per-mode); (3) host-cron `healthcheck.sh` (core containers up+reachable, 1h cooldown); (4) OSS `run_failure_alert_sensor`→SNS (LOUD for HALT jobs); (5) the 5 raise-to-alert sensors (odds/schedule/statcast freshness + clv + model_health) now call `send_alert` directly + `check_data_freshness` routed via crontab; (6) `deploy.sh` rollback pages. Shared notifier `pipeline/utils/alerting.py` (soft-fail+rate-limited) + shell `notify.sh`. `ALERT_SNS_TOPIC_ARN` added to env.required. CI GREEN (fast 680, defs-validate, guards). 🔑 KEY INSIGHT: post-P4 the ENTIRE legacy alert stack was SILENT — every sensor raised into Dagster+'s now-OFF email-on-failure. Box runs live; alerting goes live once operator provisions+confirms the email sub. **Cutover-window add-ons (P4): dagit at `https://dagster.credencesports.com` (Caddy/HTTPS + auth) + SSM shell to retire SSH.** **P7 = dependency/upgrade & patch mgmt** (self-hosting = we own versions): 🛠️ **PIN all images — kill `:latest` (e.g. `flaresolverr:latest`) — DO-NOW, fold into P5's reproducible box** (P5's auto-`--build` could pull a breaking upstream image to the live box otherwise); Dagster daemon/web/codeloc/lib must bump together + `instance migrate` (→ `UPGRADING.md`); Renovate/Dependabot PRs through the P5 CI gate; ~monthly patch cadence + advisory subs. **P8 = 🔭 DEV/staging box for PR pipeline integration testing** (down-the-road, NOT beta-blocking; extends P5): runs the affected pipeline end-to-end on real-ish ISOLATED dev data (catches runtime/data-shape breaks compile misses — the INC-15 class) → one box that also doubles as P7's upgrade-test box + P5's blue/green standby; ephemeral-on-PR to keep cost ~$0; hard data isolation (dev Snowflake schema/S3 prefix/DynamoDB, never prod serving). ✅ **E11.1-W3pre DONE/LIVE 2026-06-28** (staging tier on S3; see the W-series row above) → W3-main green-lit.
- **✅ INC-15 RESOLVED 2026-06-25 (operator-confirmed: `dbt_daily_build` GREEN on `main` again → serving restored before tomorrow's slate).** Residuals: (a) the W1 decommission + band-aid-test audit continue in the Opus/E11.15 session (W1d still 🔶 until the external-table swap lands + tests re-promoted); (b) ⚠️ confirm the green is RUNTIME (an actual build ran, not just compile) + ideally a `predict_today` smoke before the AM; (c) the CI PROCESS GAP stands (compile-only let the runtime break through → add a runtime `dbtf build --select state:modified+` to dbt-Build CI). _(orig P0 ↓)_ **🚨🔴 INC-15 P0 — `dbt_daily_build` BROKEN ON `main` (wrong data types from the Sonnet W1d changes; merged 2026-06-25).** HALT-tier → the daily build gates the feature store + `predict_today` → **production picks are down until fixed.** Compile-clean (CI is compile-only) but fails at RUNTIME on data types. **⏱️ DEADLINE: green + verified before tomorrow's ~7am daily build / slate** (today's games complete → safe overnight window, hard end AM). **Opus (E11.15 session) is on it.** 🧭 PRIORITY ORDER: (1) RESTORE the build to green FIRST — **recommend REVERT the breaking W1d dbt changes on `main` to known-good** (instant restore; safest under deadline) OR fix-forward IF verified with a REAL `dbtf build`; (2) VERIFY a clean end-to-end cycle (`dbtf build --select state:modified+` actually RUN + a `predict_today` smoke + serving-store write) — NOT compile; (3) ONLY THEN resume the decommission/E11.15 (on a branch, real-build-verified). **The decommission must NOT layer on a broken build.** Process fix (later): add a runtime `dbtf build` to CI (compile-only let this through).
- **✅ INC-14 RESOLVED 2026-06-25 (Opus) — consumer dedup shipped + stale rows purged.** Root surfaced as a LIVE failure: postponed game 823613 produced dual `is_current=true` lineup rows → duplicate rows failed `generate_offense_signals_op`. Fix: `QUALIFY ROW_NUMBER() OVER (PARTITION BY game_pk, home_away ORDER BY valid_from DESC)=1` in `feature_pregame_lineup_features.sql`; the `(game_pk, side)` uniqueness test promoted warn→**error**; operator DELETEd 16 stale dup rows from `offense_v2_signals`. **⚠️ DURABLE ROOT FIX STILL OWED (new follow-up story, Medium): the SCD-2 WRITER should set `valid_to`/`is_current=false` on the OLD official_date when a game is rescheduled — the consumer QUALIFY neutralizes it but the bug lives in the writer.** _(orig ↓:)_ **🔴 INC-14 OPEN — duplicate `is_current=true` rows in `feature_pregame_lineup_state` → duplicate `(game_pk, home_away)` rows in `feature_pregame_lineup_features` → potential INFLATED TRAINING-SAMPLE WEIGHT (surfaced by the E11.1-W1d session, 2026-06-25; prompt READY).** **Hypothesis:** the SCD-2 transition in the lineup ingest path fails to flip old `is_current=true`→`false` before writing a new lineup (race / missing update) → the `lineups` CTE (`feature_pregame_lineup_features.sql`, `WHERE is_current=true`) fans the dupes through. **Detection was incidental** — the `unique_combination_of_columns(game_pk, side)` guard (`feature/schema.yml:367`) only entered CI scope now via `state:modified+`, and was downgraded **error→warn** to unblock the W1d merge → **the build no longer blocks on it + the guard is now SOFT, so dupes persist silently.** ⚠️ Could have been true for a while → may have contaminated the v6 / E2.x / E5.x training matrices (inflated weight on affected games). **NEXT = investigate (prompt INC-14 in `story_prompts.md`):** run the diagnostic (`…WHERE is_current=true GROUP BY game_pk,home_away HAVING COUNT(*)>1`) → quantify #games + time window → assess training-data reach → FIX the SCD-2 transition (exactly one current row per game×side; atomic flip; dedup guard) → RE-PROMOTE the test to `error`. **Doesn't hard-gate W2** (different marts) but is higher-value than W2 for model-data integrity → prioritize. Files: `feature/schema.yml:367`, `feature_pregame_lineup_features.sql` (lineups CTE), `feature_pregame_lineup_state` (the SCD-2 source).
- **INC-13 ✅ FIX SHIPPED 2026-06-24 — `savant_ingestion.py` now has a 14-day lookback (was: no lookback, start from MAX+1). Root cause: two patterns both caused by the missing lookback: (A) UTC-boundary: night games finish near midnight ET → Savant not published yet; (B) postponed/makeup games stored under original official_date → ingest had already passed that date. Gap-sizing: 2026 has 2 REAL missing games (825099 CWS@ARI + 824912 SF@ATL); 2018 has a 415-game historical gap (Aug 1–Sep 9, separate partial-backfill root cause); other seasons 0–2 strays. Fix prevents recurrence. Addendum: `miss_distance` (new Savant bat-tracking whiff-severity field, swings-only) captured in schema + stg; loud stderr banner on future unrecognized columns. ⏭️ Operator: (1) run DDL `ALTER TABLE baseball_data.savant.batter_pitches ADD COLUMN MISS_DISTANCE TEXT`; (2) run 2026 backfills (see story_prompts INC-13 for commands); (3) 2018 backfill is a separate operator decision (low serving priority).**
- **INC-12 ✅ RESOLVED 2026-06-23 — and it was NOT a feed problem: the dbt chain was correct the whole time (player_transactions / feature_pregame_injury_status / mart_player_profile_identity all had Civale ACTIVE, `is_on_il=false`).** 🔑 ROOT CAUSE = a SERVING-READ non-determinism bug: `api_cache` accumulates multiple `is_permanent=True` rows (one per write date; the `ON CONFLICT` only updates the same-day row), and `pg.get_cache()` did `WHERE is_permanent=TRUE LIMIT 1` with **NO `ORDER BY`** → PostgreSQL returned an arbitrary heap-order row → for Civale, a PRE-activation blob carrying both `is_on_il=true` AND the 5/25-frozen game log. **The coupling hypothesis was RIGHT** (one stale blob → both symptoms). **FIX (generic, closes the whole class): `ORDER BY updated_at DESC` on the permanent-blob read** (`app/backend/services/pg.py:80`) → most-recently-written row always wins. Deployed (Lambda) + `--players` re-write + Civale page verified (no IL badge, current log). **Secondary fix (the sparse-feed hardening I wanted): the `player_transactions` monitor used `MAX(effective_date)` (roster-EVENT date — goes stale on quiet days even when the feed runs) → switched to `ingestion_ts`/36h** (`check_data_freshness.py`), so "quiet day" vs "broken feed" is now distinguishable. Doc: `e11_8_feed_monitor_matrix.md` gained a "Display-derived status coverage" section cataloging all `is_permanent` statuses with this risk. CI 470 pass. Files: `pg.py`, `check_data_freshness.py`, `e11_8_feed_monitor_matrix.md` (committed). **⏭️ TWO residual reliability follow-ons (lower-urgency, app-parked): (A) sweep the OTHER freshness monitors for the same event-date-vs-`ingestion_ts` antipattern (only player_transactions was fixed); (B) the full DEAD-CALLER completeness enumeration (every by-hand script → has a scheduled caller + monitor — the INC-11 class, NOT what bit us here) is still worth doing standalone. INC-12 closed 2 of the 3 staleness classes (permanent-blob read + sparse-monitor); the dead-caller enumeration is the 3rd.** _(orig OPEN writeup ↓.)_ **player IL-status + game-log STALENESS; likely WIDER than one player.** Repro: **Aaron Civale shows "on the IL" in the app but is starting tonight for the Athletics (FG: last pitched 6/18); his app game log shows nothing past 5/25.** ⚠️ NOT root-caused yet (needs live Snowflake queries). **Ruled out the dead-caller pattern:** `ingest_transactions.py` IS wired into `daily_ingestion_job` (L75) + `player_transactions` IS monitored (`effective_date`, 168h). **So it's not a dead feed → more likely:** (a) IL-status DISPLAY logic deriving status from a stale/last-known transaction without processing the activation; (b) a **sparse-feed monitor gap** — `player_transactions` has legitimately empty days, so the 7-day threshold won't alert a partial/silent capture failure; (c) the StatsAPI ingest silently not capturing activations; (d) the player game-log/"last pitched" feed (separate source, not yet pinned) dead ~5/25. **🔗 COUPLING HYPOTHESIS: if Civale is wrongly flagged IL, downstream logic may EXCLUDE his recent appearances → one stale-status root cause produces BOTH symptoms.** **⏭️ INVESTIGATION (needs Snowflake): MAX(effective_date) of player_transactions + is Civale's IL activation present? where does the app's "on the IL" derive (table/mart/serving field)? where does the game-log/last-pitched come from + its freshness? systemic (count affected players) or isolated? Then fix the feed/logic + backfill + a sparse-feed-aware monitor.** **🚨 3rd staleness in a week (INC-8 op, INC-11 script, INC-12 — and the FIRST on a MONITORED feed) → ELEVATES the dead-feed completeness re-sweep (below) from optional to queued: enumerate ALL feeds incl. by-hand scripts + DISPLAY-derived statuses, and harden SPARSE-feed staleness detection (expected-cadence, not just hours-since-max).**
- **INC-11 ✅ RESOLVED 2026-06-23 (found by E13.4) — a SECOND silent feed death: `compute_archetype_posteriors.py` had NO scheduled caller, dead since 2026-05-31** → the archetype×cluster MATCHUP block (a heavy `home_win` input) was frozen ~3 weeks. Fixed: backfilled + wired into the daily job + **6 new freshness monitors** in `check_data_freshness.py` (the blind spot that hid it). **⚠️ RECONCILE vs INC-8: confirm this is DISTINCT from INC-8** — INC-8/E11.8 fixed `update_archetype_posteriors_op` → `mart_player_archetype_posteriors` (player-archetype posteriors); this is `compute_archetype_posteriors.py` → archetype×cluster matchups (a different feed). If distinct (likely), **E11.8's "monitoring completeness" sweep was itself INCOMPLETE — it missed this feed** (meta-lesson: the no-alert sweep needs to enumerate ALL by-hand scripts, not just Dagster ops). Documented structural caveat persists: archetype-matchup nulls rise to ~20%+ mid-season (rookies/call-ups) → graceful-degradation follow-on (nearest-centroid cluster) overlaps E13.7's proper-fix / E7.
- **INC-10 ✅ RESOLVED 2026-06-23 — `run_w1_lakehouse_op` was failing EVERY daily run, silently (WARN-tier, non-paging); TWO root causes fixed.** **Duration: wired into `daily_ingestion_job` 2026-06-22 → failed every run since → W1 had NEVER successfully written its S3 Parquets in the deployed job; confirmed running clean 2026-06-23.** **🔧 Root cause 1 (primary): missing dep** — `duckdb>=1.1.0` in `pyproject.toml` (since 2026-05-27) but never in the Dockerfile's hand-curated `pip install` list → `ModuleNotFoundError`. Fixed: added `duckdb>=1.1.0` to the Dockerfile + a build-time smoke-import guard (`RUN python -c "import duckdb"`) so a missing dep now FAILS THE IMAGE BUILD instead of silently reaching prod. **🔧 Root cause 2 (secondary, surfaced after the dep fix): unresolved Jinja** — `scripts/run_w1_lakehouse.py` uses a hand-rolled regex to extract runnable SQL from the dbt model files (no compile step); its comment wrongly assumed the marts' `{{ config() }}` sat inside a `{% if target.name=='duckdb' %}` wrapper, but it's top-level → the parser left `{{ config(...) }}` intact → DuckDB `ParserException`. Fixed: strip `{{ config() }}` directly (`re.DOTALL`) + added an **unresolved-Jinja guard** that raises a clear `ValueError` if any `{{`/`{%` survives all substitutions (future model additions fail fast, descriptive). **Files: `Dockerfile`, `scripts/run_w1_lakehouse.py`.** **⚠️ 6/25 PARITY: W1 has only ~1–2 clean cycles before 6/25 → the W1-parity LEG LIKELY SLIPS** until enough clean S3 data accrues; the check MUST assert freshness (loud fail on stale/absent), not silent pass. **(orig OPEN writeup ↓, now satisfied.)** _(orig:)_ `run_w1_lakehouse_op` failing EVERY daily run, silently (WARN-tier, so non-paging). `ModuleNotFoundError: No module named 'duckdb'` in `scripts/run_w1_lakehouse.py:122`. **Root cause: the Dockerfile installs a HAND-CURATED explicit `pip install` list (Dockerfile L30), NOT pyproject deps — `duckdb>=1.1.0` is in `pyproject.toml` but was never added to that explicit list**, so it's absent from the deployed Dagster image. **NOT an E11.7 violation** — the op correctly `try/except`→`context.log.warning` (WARN tier, "S3 track; Snowflake primary"), so it's non-fatal *by design*; the gap is that a WARN-tier op failing on every run is invisible (no page). **Serving impact: NONE** (W1 is a dead-end S3 branch; Snowflake `mart_pitch_*` is primary; nothing downstream depends on it pre-decommission). **⚠️ REAL IMPACT — threatens the 6/25 W1 PARITY CHECK: W1 has likely NEVER successfully written its S3 Parquets in the deployed job, so the parity check would have stale/absent data to compare → the W1-parity leg of the 6/25 triple-checkpoint (and the W1→decommission→W2/W3 critical path) slips until W1 runs clean for a few cycles.** (The other 6/25 legs — E11.0b re-measure + E11.9/T2 bill verification — are independent and proceed.) **FIX (2 parts):** (1) add `duckdb>=1.1.0` to the Dockerfile explicit install list + a smoke-import guard (mirror L70 `python -c "import pymc…"`) so a pyproject-vs-Dockerfile drift can't silently drop a dep again; rebuild + redeploy (Railway MCP). (2) The 6/25 W1-parity check must ASSERT W1 freshness (fail loudly if the S3 Parquets are stale/absent), not just compare values. **Class lesson: any dep added to pyproject for script/in-process use won't reach the deployment unless ALSO added to the Dockerfile list — they drift.** ⏭️ Operator: confirm how long it's been failing (when duckdb landed in pyproject / W1 was wired vs last image build).
- **INC-9 → ✅ RESOLVED 2026-06-23 (SECURITY). Keys rotated.** A Railway `list_variables` call leaked the live Snowflake RSA private key + AWS access/secret keys into a session transcript → **operator ROTATED all three same-day** (new Snowflake key pair + new AWS access/secret) and updated the consumers. Old keys now invalid. **📎 Process note (carry-forward): avoid `list_variables` that echoes secrets into transcripts — set vars write-only.**
- **INC-7 → ✅ RESOLVED 2026-06-22 (P0 — user-facing outage). Odds feed silent death + app collapsed to 4/13 games.** The Odds API **"events" endpoint stopped updating June 4 and ran broken 18 days**, poisoning the game↔odds linkage (→ app showed 4 of 13 games). **Undetected because NO odds feed was monitored.** Compounded by a **dev/prod footgun**: manual predict re-runs silently wrote to the DEV db while the app reads PROD (so fixes "didn't work"). **Fixes:** repointed the odds linkage onto the live healthy feed (100% of today's + historical games resolve); **hardened the serving layer so one degraded prediction can never blank the whole slate** (graceful fallback to best complete data); documented the dev/prod write target; **+ new hard-alert odds freshness monitor.** ⏭️ Follow-on: the broader "what else has no alert?" pass = **E11.8**.
- **INC-8 → ✅ RESOLVED 2026-06-22 (P1 — silent model-input staleness, found by the post-INC-7 sweep). Player-archetype data 22 days stale (since May 31).** Root cause: the archetype computation **was never wired into any schedule** — only ever run by hand. Matters for the MODEL (heavy prediction input). **Fixes:** backfilled the 3-week gap; **wired the archetype job into the daily pipeline**; added a freshness monitor (would've caught it day 2, not day 18). **Monitoring expanded 7→13 feeds.** ⏭️ Operator: deploy the pipeline change + run the test suite. **⚠️ Structural limitation characterized (NOT a bug, but it threatens edge eval — see E13.4 caveat): archetype is ~20–25% "unknown" and RISING through the season** (pitchers matched to LAST season; rookies/call-ups have no prior record) — **worst in exactly the recent games we'd use to judge edge.** Proposed fix = an unknown-pitcher cold-start fallback prior (→ **E13.7**, the bridge to MiLB).
- **INC-6 → ✅ RESOLVED 2026-06-21.** Sunday `dbt_daily_build` red-failed → predictions blocked. Root cause: a malformed StatsAPI bio row (**zero height**) hit `stg_statsapi_player_profiles` during the weekly full-refresh; two `accepted_range` tests (`height_inches`/`weight_lbs`) were default-ERROR severity → one bad row exit-1'd the whole `build` and killed the prediction pipeline. **3 durable fixes shipped:** (1) **SQL coerce** out-of-range bio → NULL in the model (`CASE WHEN … BETWEEN`; range test skips NULLs, downstream imputes); (2) **severity downgrade** both bio tests `error`→`warn` (a bad bio row can never block a build again); (3) **build-path split** in `daily_ingestion_ops.py` — build days now run `dbtf run` FIRST (gates the pipeline) then `dbtf test` in a NON-BLOCKING `try/except` (tests warn, never fail the op). **🎁 Bonus CI/CD fix:** the dbt-runner had no GitHub wiring (needed a manual `railway up` after every dbt change — the gap that complicated this deploy) → new `.github/workflows/deploy_dbt_runner.yml` auto-rebuilds+redeploys on any `main` push touching `dbt/**` or `services/dbt_runner/**`. Verified Sunday build GREEN; pytest 405/405 ✅, `dbtf compile` 1761/1761 ✅. **↔ Advanced E11.7:** fix #3 already applied the run-gates/test-non-blocking pattern to the DAILY path — E11.7's residual is the broader audit (sensor/cron paths + the documented HALT/WARN/ALERT contract + the error→warn severity sweep across ALL models). `dbt_daily_build` runs `dbtf run` on weekdays but `dbtf build --full-refresh` (run + FULL test suite) on Sundays; today's Sunday build hit two **error-severity** `dbt_utils.accepted_range` tests on `stg_statsapi_player_profiles` (`height_inches` ∉ 60–84, `weight_lbs` ∉ 130–375 — a placeholder/zero or bad StatsAPI bio on a new player). Because tests gate a `build` and `dbt_daily_build` is upstream of sub-model signals + predict, **one bad bio row exit-1'd the whole Sunday rebuild + blocked predictions** — though bio only feeds clustering features that impute downstream (never serving-critical). **Immediate unblock:** re-trigger the runner with `run --full-refresh --target baseball_betting_and_fantasy` (models, no test gate). **Durable fix (E-fix prompt INC-6):** (1) coerce out-of-range/zero height & weight → NULL in `stg_statsapi_player_profiles.sql` (range tests ignore NULLs; downstream imputes); (2) **stop peripheral data-quality tests gating the serving pipeline** — downgrade bio tests to `severity: warn` AND split the Sunday path so models always rebuild + tests run NON-BLOCKING (`run --full-refresh` then separate `test`/`--store-failures`). **📎 Same lesson as the catchup-job + INC-5: a data-quality check should ALERT, not HALT predictions.**
- **INC-5 → ✅ RESOLVED 2026-06-19 ~17:00 UTC.** `schedule_capture` Step 2 (the dbt staging rebuild via `trigger_dbt.py` → E11.0 runner) had been **silently no-op'ing on every cron fire since deploy** — `DBT_RUNNER_URL` was set to an **empty string** on the service, and the script's missing-URL guard exited 0 with only a quiet `print()`, so the job looked successful while `stg_statsapi_lineups`/`_wide`/`stg_statsapi_probable_pitchers` stayed stale until the overnight build. **Fix:** set `DBT_RUNNER_URL=https://dbt-runner-production-8899.up.railway.app` on schedule-capture (via Railway MCP, auto-redeployed) + **hardened `trigger_dbt.py`** — the silent skip is now a loud `WARNING:` to stderr (flush) so a misconfig shows in log tails. **Verified:** 17:00 UTC trigger rebuilt all 3 staging models (4/4 success); intraday refresh fully operational. `trigger_dbt.py` ✅ committed/pushed 2026-06-19. **📎 Lesson (shared with INC-3): dbt steps must fail/skip LOUDLY — a silent no-op hid a half-dead E11.4 cron for days.**
- **INC-4 → ✅ RESOLVED 2026-06-19 ~16:52 UTC.** `schedule_capture` (E11.4 cron) crashed every fire on `Invalid account identifier … no dots or slashes` → intraday statsapi schedule refresh was down. **Root cause: a TRAILING SPACE in `SNOWFLAKE_ACCOUNT`** (`IHUPICS-DP59975 `) on BOTH schedule-capture AND weather-capture (same paste) — the connector rejects spaces with the *same* "dots or slashes" message, which masked the diagnosis (the hidden-char hypothesis was right). **Fix:** corrected the Railway env to `IHUPICS-DP59975` (no space) on both services → auto-redeployed SUCCESS in ~35s; manual trigger verified Snowflake connect + 401 games fetched/inserted + clean run. **Hardening shipped + ✅ COMMITTED/PUSHED 2026-06-19:** account-normalization (strip whitespace/scheme/path/`.snowflakecomputing.com` + loud warn) added to `get_snowflake_connection()` in ALL FOUR capture scripts — `ingest_statsapi.py`, `ingest_weather.py`, `derivative_odds_backfill.py`, `odds_api_ingestion.py` — so this paste class can't recur. ⚠️ Surfaced INC-5.
- **INC-3 → ✅ RESOLVED 2026-06-19 (E11.0c).** Original symptom: `sensor_ops._run_dbt` was a third, un-migrated copy that shelled `dbtf` locally (no `DBT_RUNNER_URL` check), so `statcast_catchup` ran the heavy `stg_batter_pitches+` subtree in-process on metered Dagster + hung ~1 hr. **Fixed:** consolidated to a shared `pipeline/ops/_dbt_exec.py` and sensor_ops now **delegates to the runner** (confirmed — it hits the runner, gets 409s, polls to completion). **Bonus fix discovered en route:** the dbt-runner is **single-tenant (HTTP 409 on concurrent `/run`)** and the client never backed off — `_run_dbt_remote` called `raise_for_status()` so a 409 became an HTTPError that Dagster's `RetryPolicy` burned through → job failed. Now on 409 the op raises **`RetryRequested(max_retries=40, seconds_to_wait=30)`** → Dagster **releases the compute slot during the wait** (no billed idle; iteration-1's sleep-loop had held the slot ~13 min billed) and re-executes when the runner frees up (20-min budget). 19/19 tests green (incl. the `test_dbt_runner.py` assert that E1.9 flagged — fixed here, not E1.9). Files: `_dbt_exec.py`, `test_dbt_runner.py`. **📎 Note — single-tenant runner is now a shared chokepoint:** statcast_catchup + daily_ingestion + odds rebuilds + schedule_capture all route to ONE runner; 409-serialization is handled gracefully but if heavy dbt runs pile up this is a future capacity item (queue or 2nd instance) — fine at beta scale. **↔ E11.0b overlap:** the 409-wait is now compute-free (E11.0b philosophy), but the in-run `/status` blocking-poll still meters during the actual dbt run — that residual is still E11.0b's job.

- **INC-2 → ✅ FULLY RESOLVED 2026-06-21 (E11.6 shipped) — last open incident; board now CLEAR.** Decommissioned dead Parlay from the pipeline + dbt: removed the 3 Parlay ingestion ops (daily job head rewired to `ingest_action_network`), gutted `pregame_odds_snapshot`→no-op stub + removed dead `lineup_odds_snapshot`; cleaned the 3 odds marts (`mart_odds_outcomes` parlay union removed, **historical Parlay rows kept as cold archive**; `mart_game_odds_bridge` parlay CTEs gone, `parlay_api_event_id` preserved as `NULL` for downstream schema compat; `mart_odds_line_movement` dropped the placeholder-time workaround CTEs, now uses real Odds-API `commence_time`); removed the 4 Parlay freshness alert configs (`sources.yml` source block retained so `stg_parlayapi_*` still compile for cold-archive). **Value-preserving — Snowflake parity ✅** (`parlay_api_event_id=0` all bridge rows, no parlay in line-movement, cold archive intact). CI: pytest 405/1, `dbtf compile` 1761/1761 ✅. **⏭️ Operator follow-on cleanups (PM-approved, low-priority, non-blocking) → `E11.6b`:** (1) **remove** the 17 dormant `odds_snapshot_schedules` in `intraday_schedules.py` (Parlay is permanent-off, the "manual failover" is dead — APPROVED); (2) **keep** `stg_parlayapi_*` staging (cold archive — agreed); (3) **remove** the now-no-op `pregame_snapshot_job` from `sensor_jobs.py`/`jobs/__init__.py` (APPROVED, when convenient). _(The `deploy_dbt_runner.yml` auto-deploy workflow also confirmed shipped here — already credited under INC-6.)_
- **INC-2 (original audit note, 2026-06-19): NOT a live-data-integrity incident.** Parlay API is permanently off (operator-confirmed). **Lineage audit verdict: the live serving path was ALREADY repointed off Parlay → The Odds API on 2026-06-16 (Story 12.3.7/A2.18)**, *before* the permanent-decommission call. Per `intraday_schedules.py`: The Odds API (Railway cron → `mlb_odds_raw` → `odds_current_rebuild_sensor`/`odds_clv_rebuild_daily`) replaced Parlay as the live source and validated **healthier** coverage (28 games/37 books incl Bovada+Pinnacle vs Parlay 18/15); `mart_odds_outcomes` **dedup prefers `oddsapi`**, so the frozen `stg_parlayapi_*` rows are inert. Live `feature_pregame_edge_market`/`feature_pregame_odds_features` get 2026 odds from The Odds API → **no stale-odds risk to live picks.** **Also corrected:** the E11.5 session's "E2.0b moot" was WRONG — E2.0b runs on The Odds API (`oddsapi`), unaffected. **→ Converted to a cleanup story (E11.6, Track 1 Session B):** (1) remove the still-present `parlay_api_ingestion.py` calls in `daily_ingestion_ops.py` (L232/237/242) + `sensor_ops.py` (L248–273) — with Parlay dead they fail/hang each run (bounded by the 600s ceiling: noisy, non-blocking); (2) prune the dead `stg_parlayapi_*` branch from `mart_odds_outcomes`/`mart_game_odds_bridge`/`mart_odds_line_movement`; (3) drop the 4 warn-only `parlayapi` freshness blocks + the `parlayapi` source once tables are retired. Low priority; do AFTER the E11.5/T2 deploy + W1.
- **INC-1 (P1) — weekly `train_bayesian_meta_model_op` failed (h2h + totals): ✅ RESOLVED 2026-06-18.** Root cause: the Dagster **runtime image was missing `pymc` + `h5py`** (both in `pyproject.toml`/dev env but never in the Dockerfile; E9.2 had added arviz/h5netcdf for the serve-side but missed the training deps). **Fix:** added `pymc` + `h5py` to the Dockerfile pip block + a **build-time Bayesian-dep import smoke-check `RUN`** (the prevention we flagged — fails the image build loudly if a dep is absent). Operator rebuilt + re-ran → both markets retrain green; `uv run pytest` 325 ✓. **📎 Lesson:** the first smoke check gave a **false green** — `h5netcdf` imports fine without `h5py` (lazy backend load), so the trace serialization still failed; **smoke checks must import the actual backend deps (`h5py`), not just the facade package.**

**Sessions:** Session A = per-side totals + bullpen + the slim re-promote; Session B = data ingestion (E2.0, E5.0/E5.1). **E5 pricing (E5.2+)** is the props edge test — starts once ingestion lands.

**Deprioritized by the E1 audit:** **E6.1/E6.3 generic feature work** — E1.3 delivered the redundancy finding (the slim contracts), and the audit concluded **more features ≠ edge.** ~~The one feature-investment direction that survives is bullpen~~ — **CORRECTED 2026-06-18 (E2.1b):** the bullpen "signal" was largely a **leak** (`bp_eb_xwoba` peeks at `outs_in_game`); the only real pre-game bullpen signal is **data-depth (`coverage_pct`/`uncertainty`), and it's modest.** There is **no "deepen the bullpen model" investment** to chase — the actionable item is the **de-leak correctness fix (E1.7)**, not more bullpen modeling.

**Realistic by the break:** **the production de-leak (E1.7)** landed + champions re-evaluated honestly; **totals distribution calibrated (E2.1–E2.3)** + derivative backfill (E2.0) staged; **prop data ingested (E5.0/E5.1)**. The betting-edge hope rests on **totals-derivatives + props** — H2H straight-bet edge is closed, and the apparent bullpen signal turned out to be a leak (see *Decisions*).

## Decisions & kills (on record)
- **🔬 MODELING BAKE-OFF DISCIPLINE — STANDING STANDARD (operator 2026-06-24, after the E5.2 lapse). Codified: guide §0.5 + CLAUDE.md conventions.** Any story that BUILDS or SELECTS a predictive model runs a **bake-off, not a single architecture**: pre-register ≥3 candidate model classes (a prescribed structure is ONE candidate, always with a direct-learned foil), Optuna-tune each, feature-ablate, pick on the metric under purged/embargoed CV PER TIER, guard the search with PBO<0.2/DSR>0. **A single architecture missing its gate is NOT a trustworthy null** — that's earned only after the whole candidate set, tuned + ablated, fails (with the mechanism named). Exemplar = **E1.9 v6** (6 learner classes → Optuna → gate; glm/ngboost WON, weren't assumed). Exceptions: pure registration/backfill (E2.5), cheap sub-model re-eval (E13.3), explicit harness/eval (E13.4). Prompts injected: **E5.2, E2.4, E2.6, E13.9**. **✅ VINDICATED 2026-06-25 by E5.2:** the mandated bake-off (9 configs, PBO=0.0) found the LEARNED `poisson_glm_k` beats the originally-shipped hand-built compound on CRPS (1.236 vs 1.270) AND ECE (0.010 vs 0.035) — i.e. the single-architecture first pass would have served a measurably worse-calibrated model. The standard caught a real quality gap, not a hypothetical one.
  - **⚠️ REVISIT AUDIT (completed model stories vs the new standard, 2026-06-24):**
    - **🔴 E2.1 (per-side count model) — REVISIT (HIGH).** Built as a SINGLE architecture: `LightGBM Poisson mean + MLE NegBin r`, compared only vs a Poisson baseline — no learner bake-off, no Optuna, no learned-count foils (XGBoost/CatBoost/NGBoost-native-count, GLM). **It's LOAD-BEARING** (every totals model convolves it: E2.3/E2.4/E2.6), and E2.3's run-diff PIT near-miss + the ~24% dispersion gap trace to the per-side MARGINAL quality → a proper per-side mean bake-off + Optuna could tighten calibration materially. **Highest-value revisit on the board → written up as E2.1-r (prompt READY in `story_prompts.md`, 2026-06-24): ≥5-candidate bake-off (LightGBM/XGBoost/CatBoost/NGBoost-native/GLM × 2-step-vs-joint dispersion) → Optuna → picked on the CONVOLVED-total calib, PBO/DSR-guarded.** ⚠️ E2.1-r is UPSTREAM of E2.5/E2.6 — ideally run BEFORE them, else those may need a re-run if the marginals change.
    - **🟡 E13.2 Phase 1 (PA-outcome model) — REVISIT (CONDITIONAL, MED-LOW).** Null was declared on essentially ONE learner (LightGBM multiclass), no Optuna/CatBoost/NGBoost. BUT the null mechanism (86% batter×pitcher IDENTITY priors → log5 captures by construction; per-class ECE 0.0026) is **architecture-robust** — a different GBM is unlikely to beat log5 if the signal is identity. So: already PARKED; **IF reopened (E13.10 zone-matchup trigger), add the learner bake-off + Optuna before any FINAL null** — but don't expect it to overturn.
    - **🟢 E1.9 v6 — COMPLIANT (no revisit):** full 6-learner bake-off + Optuna + per-tier purged CV + PBO/DSR. The exemplar.
    - **🟢 E13.6 (calibration) — COMPLIANT-enough:** TemperatureCalibrator was a REASONED Temperature-vs-Platt choice (contamination → robust 1-param), documented — a deliberate candidate comparison, not a blind single-pick. No revisit.
    - **🟢 E3.1 (line-movement regression / "predict-the-move") — NO REVISIT (operator-flagged 2026-06-25; market-bound null, not architecture-bound).** Yes, the first pass was a single architecture, BUT — unlike E2.1, whose limiter is MODEL quality — E3.1's limiter is **MARKET EFFICIENCY**, which no bake-off can overcome. The "no cashable CLV/H2H edge" verdict is the program's most-corroborated null, from 4 independent angles: **E4.3** (the deeper version of the same CLV idea — found a REAL monotone CLV gradient but ~0.5–0.9pp vs ~4% vig = structurally SUB-VIG), **E1.9** (full 6-learner bake-off + Optuna → thin), **E13.1** (distinct market-anchored-residual architecture → strictly worse), and **E13.8** (empirical benchmark: H2H closes ~0.245 Brier, no headroom — the price is efficient). A bake-off would hit E13.8's efficiency wall + E4.3's sub-vig ceiling = mechanism-robust (like E13.2 but empirically pinned). Reopening also contradicts the deliberate pivot to derivatives/props. **If the appetite to retest "find CLV edge with optimal features" is ever real, the DISCIPLINED SUCCESSOR is E13.9 (selective best-bet — pre-registered, PBO/DSR, abstention, aimed at the SOFT markets), NOT an E3.1 redo.** (Same applies to E4/E13.1 — all market-bound, all corroborated; no revisit.)
- **E1 ✅ COMPLETE (2026-06-18)** — full audit arc built + validated (E1.1–E1.6); PBO/DSR + purged-CV on record → **the go-live gate is set.** Findings: **(1)** ~~no leakage in any champion~~ → **PARTIALLY OVERTURNED (see E2.1b): a within-game leak *was* present and purged CV missed it** (it guards temporal/cross-fold leakage, not a feature peeking at its own row); **(2) massively over-parameterized** — ~370 feats → **14/31/19** with no loss → re-promote on the slim contracts (**but re-derive post-de-leak — finding #3 contaminated the ranking**); **(3)** ~~bullpen EB dominates every target → clearest signal-investment direction~~ → **❌ RETRACTED 2026-06-18: that #1/#2 rank was leak-inflated** (`bp_eb_xwoba` weights by `outs_in_game`); de-leaked, it collapses to noise (0% retained). The real bullpen signal is **data-depth (coverage/uncertainty), modest**; **(4) history extension (E1.6) is a wash** → don't extend history. **Revised conclusion: the edge is the E2 per-side-totals *architecture* (honest distribution/derivatives), NOT a bullpen signal; the most valuable action E1 surfaced is the de-leak (E1.7).**
- **E2.1b ✅ COMPLETE — "gate FAIL by design" (2026-06-18)** — bullpen deepening cannot beat the incumbent because the incumbent is a **within-game leak**, proven 3 ways (leak-signature: de-leaked equal-weight 2.4582 ≈ v3 2.4571, both lose to leaky-static 2.4303 by an identical ~0.027; source `eb_bullpen_team_posteriors.sql:33`; MDA collapse #1/#2 → noise, 0% retained). **Blast radius: the leaky column is in the live home_win/run_diff/total_runs training matrices → a named mechanism for the offline→live skill collapse (corr 0.42 → 0.001), previously filed under "serving skew."** `bullpen_v3` not promoted (no real lift). Experiment B (per-reliever×handedness EB) explicitly **not** pursued (zero headroom). → **action: E1.7 de-leak.** Full detail: `E2_1b_HANDOFF.md`.
- **E1.7 ✅ SHIPPED + VALIDATED (2026-06-18)** — production de-leak (Path A pure-dbt, equal-weight). **Two leaks fixed:** the `outs_in_game` weight **and** the appeared-in-game roster (which made `bp_eb_xwoba` **null for the scheduled slate at serve time** — the serving-null half of the 0.42→0.001 collapse, now mechanistically named). **Serving-null fixed: 37/37 scheduled games populate (was 0); 96.9–98.9% historical coverage.** Parity ✅ (Jaccard 1.0, corr 0.962). **MDA collapse confirmed on ALL 3 targets** — the leak inflated bullpen on every head, not just totals; `coverage_pct` (data depth) is the real, modest signal that rises to #1/#2. **🟢 Encouraging reveal:** de-leaking the H2H contracts surfaced **`elo_diff` + `pythagorean_win_exp_diff`** as real team-strength signals the leaky bullpen rank had been *masking* — i.e. removing the leak didn't just subtract, it uncovered sensible fundamentals. Slim contracts re-derived **INTERIM** (re-derive after E1.8); retrains (step E) gated post-E1.8. Detail: `E1_7_HANDOFF.md`.
- **E1.8 ✅ COMPLETE (2026-06-18) — leakage sweep verdict: the bullpen leak was the STANDOUT, not an iceberg.** Swept every signal-bearing + slim-contract feature × 3 targets (construction audit + serving-parity + leak-signature A/B, traced to dbt SQL). Post-E1.7 the construction surface is **essentially leak-free** — exactly **2 residual leaks:** 🟥 FanGraphs **Stuff+/arsenal = leaky-season-to-date** (no `< game_date` guard; importance collapsed ~88% de-leaked but tiny accuracy impact; it sat in the totals slim contract → de-leaked, held for E1.9) + 🟨 catcher framing (noise, no contract, staged). Everything else as-of-safe (bullpen E1.7 fix confirmed live; sequential posteriors safe via consumer-enforced `prior_mu`/strict-`<`; H2H + run_diff slim contracts **fully clean**). **🎯 Gap attribution (the headline): the leaks do NOT explain the 0.42→0.001 collapse — the bulk is point-in-time SERVING SKEW** (lineup-dependent strong-tier null at morning serve), already on the **Epic 30.3** track. **So construction leakage is now handled (offline numbers + contracts trustworthy); the remaining live-performance lever is serving skew (30.3), not more leak-hunting.** Shipped regression guards: `assert_no_leakage_sequential_posteriors.sql` + `test_seq_asof_guard.py` (prevent recurrence). `ablation_results/feature_leakage_audit.md`.
- **E12 ✅ STRUCTURAL AC MET (2026-06-19) — serving is sound; the collapse was a benchmark artifact, not a misalignment.** Training + serve read the same table; only `has_full_data` differs → the 0.42→0.001 gap is **point-in-time value-completeness** (sparse AM / dense post-backfill), so **0.42 was an optimistic dense re-read.** The fix pre-existed (33.0 pre_lineup tier drops lineup-gated NULL families; 30.3 post_lineup re-score; 30.13 freshness gate). E12 added the **tier-aware parity harness + 21-test standing guard**; live-sparse run → all 3 targets PASS, 0 strong-tier degraded. **Implications:** E1.9 must be **tier-aware** (rebuild/evaluate pre_lineup vs post_lineup separately; don't grade the morning model on dense re-reads); the honest morning target is below 0.42. **Latent risk (only observable live via the standing harness):** a strong-tier feature *inside* the pre_lineup contract arriving all-null some morning = a new serving bug. Remaining: longitudinal live-skill forward-validation (~2–3 wk).
- **E3.0 ✅** built + live-validated (2026-06-17) — `feature_pregame_edge_market`.
- **E3.0b ✅** built — bookmaker drift model quantified the decay (soft books' distance to Pinnacle ~halved 2021→2025). **Commit the artifacts** (`feature_edge_book_market_era_quality`, `feature_edge_sharp_anchor_backtest`) — reusable analysis regardless of E4's death.
- **E3.1 🔴** no-edge (predict-the-move).
- **E4 🔴 CLOSED (2026-06-18)** — "bet soft toward Pinnacle": a **real, monotone CLV gradient (the program's first non-null signal)** but **too small to beat vig** (~0.5–0.9 prob-points CLV vs the ~4% soft vig) → **not cashable**; pooled ROI negative, per-season noise, totals same. Killed on **ROI net of vig** (the cashability gate); PBO/DSR not run — it can't rescue a negative (reserved for *apparent positives*). **The CLV signal still feeds the honest transparency/fair-value app surfaces (E9.11/E9.12) — just not as "+EV bets."**
- **E13.1 🔴 KILLED (2026-06-21) — market-anchored residual model fails CLV, both tiers, 8,161 games / 3 purged seasons.** Anchoring to the line + learning the residual is **strictly worse than the market-blind point model** (which is itself the best CLV config at a tiny ~0.2pp — thinner than E4's dead ~0.5–0.9pp); DSR excess-over-drift = 0 (doesn't beat always-bet-home). **This is the 4th independent confirmation H2H straight-bet edge is dead (after E3.1, E4, E1.9).** Implication: stop hunting H2H edge; the remaining model-track edge hope narrows to **totals-distribution (E2) + props (E5)** (softer markets), and the H2H model stays live for product value (projections + CLV/fair-value), not "+EV bets." Full detail in the E13 block.
- **E3.3 latency-arbitrage 🅿️ PARKED** — the only un-killed E4-adjacent thread, but a **sub-second ops/infra bet incompatible with the manual-advisory model** (a human can't win a seconds race vs a soft book's repricing) and not analyzable on 30-min snapshots. The lead-time/freshness ops work still helps CLV/transparency — not as a cashable edge.

**Strategic read:** H2H straight-bet edge is closed on **both** heads (E3.1 + E4), and now the program's apparent **#1 model signal (bullpen EB) turned out to be a within-game leak** (E2.1b) — a sobering but valuable correction that also **names the mechanism for the offline→live collapse** and makes the **de-leak (E1.7) the highest-value model-track item right now** (it may explain why honest live skill has been ~0). **Update (E1.7, 2026-06-18):** de-leaking didn't only subtract — it **revealed real team-strength signal** (`elo_diff`, `pythagorean_win_exp_diff`) that the leaky bullpen rank had masked in the H2H contracts. That's a modestly hopeful sign: the honest H2H models may carry genuine (if small) fundamentals once the leak is gone — **but this is unproven until the post-E1.8 retrain + a forward/serving-parity check** (offline importance ≠ live edge; H2H straight-bet edge is still closed on the cashability gate). **Update (E1.8 → E12, 2026-06-18/19) — the live gap, diagnosed AND structurally handled:** E1.8 found leakage isn't the main driver; the bulk of 0.42→0.001 is point-in-time value-completeness at serve. **E12 then resolved it structurally: there's NO train/serve misalignment** — the fix (33.0 pre_lineup tier + 30.3 post_lineup re-score) was already built, and a tier-aware harness + standing guard now PROVE the served matrix is complete per tier (all 3 targets PASS on a live-sparse day). **Two carry-forwards:** (1) **the 0.42 benchmark is optimistic** (dense re-read) — the honest morning target is lower; the actionable edge is the post_lineup re-score; (2) **E1.9 must rebuild + evaluate per tier** so it doesn't re-introduce the optimistic trap. Remaining E12 work = longitudinal live-skill forward-validation (~2–3 wk). Remaining betting-edge hope = **totals-derivatives (E2) + props (E5)**, valued as an honest distribution/derivative *architecture* rather than any single dominant feature. Durable product value = **transparency/CLV surfaces + the fantasy vertical** (which never needed an edge). Reinforces "subscriptions, not betting profit, fund the early days" (`../gtm_strategy.md` §0).

> **🔒 H2H PREDICTION IS A NON-NEGOTIABLE PRODUCT FEATURE (operator 2026-06-21).** Surfacing a calibrated win-probability for every game is **a hard product requirement — there is no viable product without it.** Two separate objectives, do not conflate: **(A) the PREDICTION** = the honest win-prob we surface, optimized for **accuracy + CALIBRATION (Brier / log-loss / ECE / reliability), NOT CLV** — alive, central, permanent; **(B) the EDGE** = beating the close / cashability (CLV/ROI) — *that* is what's dead (E3.1/E4/E1.9/E13.1), and we simply don't claim it. A well-calibrated predictor that can't beat an efficient market is still an excellent product feature; the two aren't in tension. **Honest ceiling:** MLB single-game H2H is inherently high-variance (best team ~60%, worst ~40%) → any calibrated estimate lands near the market's ~55–58%; the product win is a **trustworthy, well-calibrated, explained** estimate + the "why" (SHAP) + honest variance framing, not superior accuracy or a beat-the-book claim. **v6 (de-leaked + calibrated) IS that honest prediction.** So: the H2H model stays a first-class served model; the model-track H2H goal is **prediction quality (esp. calibration), not edge** — see E13.6.

---

## 🔬 E13 — Liquid-Market Model Re-Evaluation  *(Session A · model R&D · commissioned 2026-06-21)*
**Why now:** E1.9 confirmed the straight H2H/totals point-models are signal-thin on the matrices/architectures we've tried. The operator's call: people pay for these liquid markets, so we owe a *real* edge search before accepting the thin verdict. **Honest base rate:** MLB ML/totals are among the most efficient markets — durable edge is rare, thin, and decays. So this is a **bounded, kill-gated R&D bet**, run with full LdP discipline (purged CV, **PBO<0.2 + DSR>0**, **forward CLV** the cashability gate), not an open-ended fishing trip. Each workstream gets a **pre-registered hypothesis + kill criterion**; empty stones get killed, not rationalized.

> **🧭 STRATEGIC DECISION — market-aware modeling is now IN SCOPE for E13 (operator 2026-06-21).** The program has been deliberately **market-blind**; that's likely the single biggest reason the edge reads ~0 (predicting games from scratch vs a price that already embeds sharp money). E13 may build **market-AWARE** models. **Honest-framing implication:** a market-aware model's "edge" is the *residual it adds on top of the line* → it must be judged on **forward CLV / beating the close**, not accuracy, and the product story becomes "we find specific spots the market misprices, proven by CLV" (a *stronger*, more defensible narrative than "we predict games") — but ONLY once it clears forward CLV. Market-blind stays the default for the existing served point-models; market-aware is an E13 R&D exception, gated.

| # | Workstream | Idea | Kill criterion (pre-register) |
|---|-----------|------|-------------------------------|
| **E13.0** ✅ | `home_win/pre` forward-validation | ✅ **DONE 2026-06-21 — HOLD-for-deploy (verdict documented, challenger registered, deploy WITHHELD).** Pass-2 was **thin/ambiguous, NOT decisive**: completed seasons −0.0030 Brier (barely over the 0.002 floor), 2026 +0.0017 (≈ wash, slightly behind). home_win is the confirmed thin-signal target — a 3rd-decimal edge isn't worth bespoke forward infra. The only remaining tiebreaker (live retention on sparse-served features) needs real shadow-serve infra → build it GENERIC, not a one-off → **E13.5**. **🛑 POLICY: stop re-running the offline gate** — periodic re-gates share their decisive data → noisy ~PROMOTEs that CAN'T advance hysteresis (noted in the registry so no session re-gates expecting it to count). | — (closed) |
| **E13.1** 🔴 | **Market-anchored residual model** | 🔴 **KILLED 2026-06-21 (pre-registered criterion met) — 8,161 games / 3 purged seasons.** Both tiers fail the CLV gate: post_lineup CLV +0.03pp but ROI net of vig −1.48%; pre_lineup CLV +0.01pp, ROI +1.22% (outcome-noise). **DSR excess-over-drift = 0.000 both tiers** — doesn't beat always-bet-home; residual configs ~50% CLV-positive (coinflip). **🔑 FINDING: anchoring to the line + learning the residual is STRICTLY WORSE — it destroys signal.** The existing **market-blind point model is the best config on the CLV objective** (55.0%/53.7% CLV-positive, +0.24/+0.17pp) → replace-not-additive, but even *that* CLV (~0.2pp) is thinner than E4's already-dead ~0.5–0.9pp. No forward validation warranted. **⇒ H2H straight-bet edge is now dead from a 4th independent angle (E3.1, E4, E1.9, E13.1).** | met → killed. |
| **E13.2** 🔄 | Bottom-up PA-level game simulator | 🟡 **CONDITIONAL REOPEN — bake-off is a PRECONDITION (flagged 2026-06-24): Phase 1's null used ~1 learner (LightGBM), no Optuna/foils → it does NOT meet §0.5, so the current null is PROVISIONAL (architecture-robust by mechanism — 86% identity that log5 captures — but not bake-off-proven). The reopen triggers (E13.10 zone-matchup landing / a product-sim build) are the conditions under which it reopens; WHEN it does, Phase 1 MUST run the §0.5 learner bake-off + Optuna (LightGBM/XGBoost/CatBoost/NGBoost multiclass, feature-ablated, PBO/DSR) BEFORE any verdict — a reopen may NOT just re-run LightGBM. Until a trigger fires it stays parked + the provisional null stands. See Decisions & kills.** | Monte-Carlo the game from batter-vs-pitcher matchup distributions → native run distribution (feeds E2 totals); captures lineup/platoon structure aggregates blur. **🔼 REVIVED + REFRAMED (idea crowdsource, 2026-06-23): the 11-source review converges on the Singlearity blueprint — a context-aware PA-outcome model (Matchup = Batter × Pitcher × Atmosphere × base/out/score State; inputs incl. platoon, exit velo, weather, park) Monte-Carlo'd to a run DISTRIBUTION — as the ONE principled path to totals / run-line / NRFI / team-totals (our surviving DERIVATIVE lane per E13.8). NOT a moneyline attempt (that ceiling is saturated everywhere). The E13.10 zone-matchup signal is its flagship novel input. Prior deprioritization ("a better engine on a coverage-gapped mart") is lifted: the point is to inject NOVEL pitch-level signal the aggregates lack, aimed at the soft markets.** Compute-heavy → lakehouse, cost-gated. **▶️ NOW THE ACTIVE NEXT MODEL BET (2026-06-23): substrate E13.12 ✅ ready (1.96M PAs); prompt fleshed into phases. 🔒 PHASE 0 (baked in per operator) = make `mart_pa_outcome_substrate` + all heavy Statcast intermediates LAKEHOUSE-NATIVE (no recurring Snowflake build) — closes the E13.12 cost flag. **⚠️ NOT a conflict with W1-parked: W1 migrates EXISTING serving-coupled marts (careful/gated); E13.2 builds NEW off-serving models lakehouse-native (cost posture: new transforms default off-Snowflake). BUT the duckdb path reads `stg_batter_pitches`→S3 = W1's output (INC-10-fixed 6/23, parity-pending 6/25) → the heavy E13.2 TRAINING is GATED ON W1 PARITY for completeness (until then hold the heavy runs or validate S3-vs-Snowflake first; substrate correctness itself was validated off the Snowflake source).** Phase 1 = LightGBM/XGBoost multiclass PA-outcome model (point-in-time features, leak-clean); Phase 2 = MC game sim (base-out transitions → run dist); Phase 3 = calibrate (PIT/CRPS/calib_80) vs E2.1/E2.3 + vs the DERIVATIVE close (PBO/DSR). Heavy + multi-session; E13.10 zone-matchup folds into Phase 1 later (don't block on it).** **✅ PHASE 0 COMPLETE 2026-06-24 — Phase 1 UNBLOCKED.** Cost-hygiene: substrate set `enabled=(target.name=='duckdb')` → duckdb/S3 ONLY, can't be swept into a Snowflake build (closes the E13.12 cost flag). Parity: extended `parity_check_w1.py` (by-season row count + PK + natural-composite-key) → **PASS** (2015–2025 row-exact; total 0.0087%; all 7 `mart_pitch_*` pass). **The verify step (B) caught: (1) a +53K-row DUPLICATION** (stale Snowflake-export parquet overlapping the ingest partitions; evaded the old PK check b/c the two writers encode `pitch_sk` differently — md5-int vs sha256-hex → hardened the gate with a natural-key check); **(2) → INC-13 (Snowflake missing ≥2 completed 2026 games).** Gate REDESIGNED (correctness fix): completed seasons + total + PK = HARD, in-flight season = informational WARN (a live-revision-window season can't hit exact parity) — **also fixes the 6/25 W1 gate.** Fixed en route: `ref_players` duckdb-unresolvable → new `export_ref_players_to_s3.py` + `stg_ref_players` (real player names now emit). **PHASE 1 BAR (verified): 1,959,348 PAs; class-imbalanced (out 46%/K 22%/…/IBB 0.11% — rare tail matters for the sim); NO-SKILL baseline = 1.5074 nats marginal-prior log-loss (vs 2.3026 uniform) = the bar; core entering-state features 100% populated (0 null).** **✅ PHASE 1 COMPLETE 2026-06-24 → TRUSTWORTHY NULL, Decision C.** Leak-safe LightGBM multiclass PA model: real calibrated skill over no-skill (model 1.4838 vs marginal 1.5094 = −0.0246 nats) but **can't beat log5 above the 0.01 noise floor** — v1 +0.0033, v2 (+ EB-shrunk platoon/TTO split rates, the proven-signal features) +0.0043. **Mechanism (clean): feature importance = 86% batter×pitcher IDENTITY priors / 14% context / platoon+TTO ≈0 conditional → log5 already captures the dominant signal by construction; the GBM adds only a noise-floor sliver.** **The data-driven regime split (2023 rule changes, game-clustered bootstrap CIs) WORKED: 2023+ edge is genuinely real + growing (+0.0066 [+0.0060,+0.0071], CI excludes 0 AND the pre-2023 +0.0029; 2018 +0.0020→2025 +0.0078) BUT its whole CI sits BELOW the 0.01 floor → fails the floor leg → airtight null (too small to survive vig).** Per-class ECE **0.0026** (excellent). **Decision C executed:** Phase 2 MC sim PAUSED (cost-gated + product track parked); **`pa_outcome_v2.pkl` PRESERVED as a logged (non-served) research asset** — the calibrated foundation for a future product-sim + the natural consumer of E13.10 zone-matchup (the reopen trigger). No feature-chasing past the pre-registered shot. **⇒ 4th independent "no edge" confirmation this cycle (H2H ×5, main totals, E13.4 coverage, now PA-vs-log5) — coverage-conclusive: value = product-quality calibration + transparency, not cashable matchup edge.** CI 505 pass. | NULL ✅ recorded — Decision C; asset preserved. **⚠️ REOPEN = E13.2b feature-augmented re-test (operator 2026-06-24: "test everything we built — a null we didn't actually test isn't trustworthy").** E13.10 only tested the zone-matchup as a COLLAPSED SCALAR (null); the FULL per-cell profiles as model features is a genuinely different, not-yet-run test. So E13.2b tests BOTH built feature sets together: (1) the full zone-matchup PROFILE features (richer than the E13.10 scalar) + (2) `miss_distance` (post-INC-13 verify+capture). HARNESS-FIRST via E13.10's reusable `--feature-parquet` bridge (cheap, off-Snowflake); escalate to the heavy MC sim ONLY if the harness shows lift. Gated on INC-13 so both go in one re-test. **✅ E13.2b COMPLETE 2026-06-24 → NULL (6th no-edge confirmation; merged to main).** Zone PROFILE channels (the 7-channel rich decomposition the scalar collapsed) = NO-SHIP: perside_runs lift +0.0008 (below floor, PBO 0.427/DSR 0.837 fail), home_win −0.0015 (mildly hurts, DSR 0.073) — **orthogonal-but-INERT (max corr 0.28–0.38 = genuinely new info that lifts nothing → the strongest null; the channels the scalar threw away carry no incremental signal).** `miss_distance` = **UNTESTABLE-not-null: 2026-ONLY → the purged-CV walk-forward (needs ≥1 prior season) forms no valid fold (n_eval=0, degeneracy guard fired correctly); build is sound (100% cov, real variance) → DEFERRED to ≈2027** (build-ready, one command, expected null; documented in the builder docstring). **Decision: NO escalation — heavy MC sim stays parked; best_alpha=0; zone profiles preserved as a future-sim input only.** ⇒ the matchup axis is now EXHAUSTIVELY tested (scalar + full profiles, both null); the model-track edge search is CLOSED (6 confirmations). The only open edge threads are non-model: K-props/derivative MARKET-softness (E5.2/E13.13) + the 2027 miss_distance revisit. ⚠️ git-attribution: `test_dbt_runner.py` in the tree = INC-13's regression test, NOT E13.2b. |
| **E13.3** ✅ | Sub-model + meta-model re-eval on clean data | ✅ **DONE 2026-06-21 — TAPPED OUT (kill criterion met; PM-ratified).** Clean data **deleted a fake signal, revealed no new one.** Source audit: **10 of 12 sub-models touch zero bullpen-EB columns** → de-leak is a math no-op for them (verdicts unchanged: run_env + offense PROMOTE, all others defer/reject as before). Only bullpen was exposed — and on clean data the **bullpen Layer-3 signal (`bullpen_v2`) — the set's strongest, largest stacking weight 0.337/0.507 — COLLAPSED to REJECT (sign reversed: −0.029→+0.000)** → its entire prior "best signal" was the leak (the Layer-3 mirror of E2.1b, same leak at a 2nd consumer). Net promoted ensemble = **{run_env, offense} only** (both clean, unchanged). **Bayesian meta-model invariant** (consumes the logged live H2H prob, not bullpen/sub-signals; no champion swap → same fit) → meta re-fit is gated on a *served de-leaked v6 champion*, not this. **No live impact** (Layer-3 stack is unserved registry stubs; predict_today uses the monolithic champions). **⚠️ Loose end (non-blocking):** `stacking_weights.json` still gives bullpen 0.337/0.507 — harmless while unserved, MUST recompute if Layer-3 is ever revived (flagged in registry). Report `e13_3_submodel_meta_reeval.md`. 410 tests green; no changelog. **⇒ the sub-model/ensemble edge lever is exhausted** (with 30.2 Bayesian-leverage reject + E1.9 + E13.0/E13.1). | met → tapped out. |
| **E13.4** 🔄✅-audit | **Feature/signal COVERAGE audit + momentum/recency gap** | **✅ COMPLETE 2026-06-23 — ALL CANDIDATES NULL → "no betting edge" is now an EARNED COVERAGE conclusion, not "no edge given what we had."** Scoreboard (all leak-clean, purged CV + PBO<0.2/DSR≥0.95 + `is_cold_start` strat): **B1 TTO penalty = NULL** (orthogonal corr 0.09 but inert — per-side CRPS +0.0006 below noise, home_win logloss −0.0019); **B2 bullpen-fatigue × short-leash = NULL** (real interaction but GBMs already capture it via splits on the parents; PBO/DSR fail); **A FanGraphs in-season wRC+ = NULL at the fit-free pre-check** (corr(wRC+,wOBA)=0.9954 → fully redundant + unserveable); **B3 park×batted-ball = not run** (pre-gated on B1/B2 landing). **🔑 Headline correction (the payoff): the recency/momentum axis IS covered — heavily, via Statcast rolling windows (E2.1 = 308 feats) — the FanGraphs gap is redundant, not missing. So the model already HAD form/momentum and still came up thin.** **🛠️ DURABLE ASSET: `betting_ml/scripts/incremental_lift_eval.py`** — the real incremental-lift harness (the dossier's original command was fictional); holds the recipe fixed, swaps only the feature set, reports lift + PBO + DSR + orthogonality, per-side NegBin + champion backends, `--add-features` drop-in → **this is the harness E13.9/E13.10 run through.** 15/15 tests green. **⚠️ Found 2 silent-data bugs (now guarded): `::numeric` scale-0 truncation zeroed the TTO xwOBA (fix `::number(18,6)`); a new col on an incremental model is NULL pre-`--full-refresh`** → both now caught by dbt singular tests + a harness <50%-coverage degenerate guard (a null can no longer be banked from degraded data). **+ surfaced a SECOND dead feed → INC-11.** _(orig audit note ↓.)_ **✅ AUDIT/DOSSIER DONE 2026-06-22 (`E13_4_COVERAGE_DOSSIER.md`); candidate lift-TESTS pending operator.** **🔧 HEADLINE CORRECTION — the leading premise was half-wrong (and that's the value):** ✅ TRUE the FanGraphs in-season windows reach zero features (dead-end), but ❌ FALSE "no momentum axis exists" — **the recency axis is HEAVILY covered, just sourced from STATCAST not FanGraphs** (`mart_team_rolling_offense/pitching` = leak-guarded 7/14/30d team scoring + run-prevention form; starter/bullpen rolling form + velo/csw/mix drift; **E2.1 per-side is the repo's RICHEST contract at 308 feats with explicit trend/drift deltas**). ⇒ the FanGraphs gap is **largely REDUNDANT** with existing `off_woba_30d`/`off_xwoba_30d`/`off_barrel_pct_30d`. **So the models already HAD momentum and still came up thin → tightens the "no edge" read.** **Genuine non-redundant free leak-clean gaps are NARROW:** (1) ⭐ **times-through-order penalty** — `pitcher_times_thru_order` exists raw but feeds ZERO starter feature; orthogonal + plausibly market-underweighted (the standout candidate); (2) **bullpen-fatigue × expected-game-length** interaction (both inputs exist, no product term); (3) FG wRC+ form (test orthogonality first — expect redundant); (4) park×batted-ball (low). **➕ ADDED from the 2026-06-23 idea crowdsource (cheap candidates for this same harness): (5) opponent-SPECIFIC rolling stats (e.g. WHIP/run-prevention vs the specific opponent — Elfrink's top feature; thin MLB H2H samples → shrink hard, check redundancy); (6) pitcher PITCH-TYPE-MIX / arsenal-composition features (CMU NRFI — distinct from Stuff+ quality; test orthogonality); (7) EMA (exp-weighted) recency vs our SMA windows (cheap variant); (8) label-flip/sign-reverse augmentation to kill home bias (hygiene, not a signal). Note: sources DISAGREE on whether recency helps (Zheng yes / Elfrink no) → test, don't assume.** Deferred: per-player OAA (defense already closed no-signal), travel/battery-framing (hygiene). **⏭️ Operator: run the §5 pre-registered incremental-lift tests (purged CV + PBO<0.2 + DSR>0 + EB-shrinkage), per-side run-MEANS first then H2H (§6 run-order).** *(Original audit spec retained below.)* **EXPANDED + ELEVATED (operator 2026-06-22): stop re-modeling the same mart; find the MISSING signal axes first.** **Phase 1 — INVENTORY:** every column across (a) live contracts, (b) mart layer, (c) ingested-but-UNUSED staging, (d) source not yet ingested; classify by signal AXIS. **Concrete first find: FanGraphs 7d/14d/30d windows are ingested + preserved in `stg_fangraphs__hitting_leaderboard` but reach NO feature — only season-level Stuff+ was ever modeled (and it was the leaky/collapsed one).** **Phase 2 — GAP MAP across 3 categories** (operator 2026-06-22, broadened beyond momentum): **(A) TEMPORAL/recency** (lead — FanGraphs windowed hitting + Stuff+/velo TREND, team rolling run-scoring/prevention, bullpen recent workload); **(B) COMBINATORY/derived** — interactions/ratios/differentials we're NOT engineering from columns we already have, baseball-domain-rationaled (platoon = lineup wOBA × starter handedness split; park run-factor × team batted-ball profile; bullpen fatigue × expected game length) — hypothesis-driven, NOT blanket pairwise (curse of dim + E1 redundancy; trees capture some implicitly → matters most for the GLM lane); **(C) EXTERNAL/un-ingested** — domain checklist of what sharp MLB models use vs what we have (defense OAA/DRS, park dims/altitude, wind VECTOR vs park orientation, times-through-order, rest/travel/B2B, day-night, baserunning…), each flagged have/free-to-add/paid (paid = validate-free-first per spend discipline). **Phase 3 — RANK** by orthogonality × market-underweighting × leak-clean availability × cost. **Phase 4 — TEST:** leak-clean (`<game_date`) + **SHRINKAGE** incremental-lift over v6 under purged CV + PBO/DSR; ship winners into BOTH H2H and the E2 totals marginals (**E2.1 per-side means FIRST** — flattest, most form-autocorrelated). **🎯 THE BAR (operator): the deliverable is a COMPLETENESS DOSSIER so a null H2H/totals edge result is a TRUSTWORTHY "no edge," not "no edge given what we happened to have."** *Reframes E1 "more feats ≠ edge": that was redundancy WITHIN existing axes; this tests COVERAGE of missing axes.* **Honest:** momentum weak, combinatory often implicit-in-trees, externals often priced-in — but a clean null AFTER full-coverage is THE valuable result (it justifies pivoting the model track to product-quality + fantasy). | per-signal: fails leak-clean incremental-lift (purged CV + PBO/DSR + shrinkage) → drop; ship only winners. |
| **E13.6** ✅🔒 | **H2H prediction-quality: calibration / reliability audit + honest accuracy** | **✅ DONE 2026-06-21 — served v5 win-prob was badly OVERCONFIDENT (ECE 0.154; "75%" wins 56%; Brier 0.276 > no-skill 0.249; corr 0.07; uniform across ALL segments incl. the fully-dense post_lineup tier). REVERSES A2.9 identity (validated on the now-removed-leak 376-dim model).** **🔑 SHIPPED: spread-honest TemperatureCalibrator(T=6.30)** (fit on v5 served sample n=1138; live-only n=113 railed the optimizer so used the broad robust set) → **ECE 0.033, Brier 0.249, band ~0.43–0.57** (served 0.85→0.57). Class in `betting_ml/utils/calibration.py`; versioned `calibrator_temperature_v1.joblib`; promoted to live `calibrator.joblib`. **INTERIM — registry `calibrator_refit_required_after` guards a MANDATORY re-fit after E12/30.3 dense serving OR E1.9 v6** (band may widen, T→1). Harness `betting_ml/scripts/h2h_calibration_audit_e13_6.py`; report `ablation_results/h2h_calibration_e13_6.md`; changelog shipped; disclosure framing → app story **E9.24** (NOT E9.18 = the changelog accordion; session mislabeled → corrected). **PM RATIFIED the full-v5 fit-set** (n=1,138) over per-tier (per-tier deferred until the live sample grows). **⚠️ The ~zero discrimination held on the fully-dense post_lineup tier (corr −0.04) too → it's intrinsic, not 30.3 sparsity; expect little band-widening from the re-fit guard.** **(PM rationale retained:)** TEMPERATURE not Platt because the fit-set is 30.3-serving-skew-CONTAMINATED → 1-param monotone shrink is robust + trivially re-fit. Reasoning: a "75%"-that-wins-56% can't ship (kills the transparency wedge) → disclose-only rejected; but the calibration set is **contaminated by the 30.3 serving skew** (calibrating on sparse morning-served probs) so a 2-param Platt over-commits to the contamination → use the minimal-assumption monotone temperature shrink (ECE 0.154→~0.05, robust, one interpretable knob, trivially re-fit). The resulting **~0.48–0.56 band IS the honest truth** (de-leaked MLB H2H is near-coin-flip — surface an honest LEAN + the "why," not manufactured confidence). **MUST: version the calibrator + re-audit/re-fit AFTER the 30.3 serving fix** (the band may legitimately widen on dense features — don't permanently bake in the transient shrink). Served prob changes → changelog + app verify. Metric = Brier/log-loss/ECE, never CLV. | n/a (product-quality; ship calibration that reduces ECE). |
| **E13.5** 🆕 | Generic challenger shadow-serve + forward-scoring harness (infra; **runs after E13.1 exists**) | **NEW (2026-06-21).** ONE reusable path: persist a challenger as a shadow artifact → `predict_today` logs its prob alongside the champion (NOT user-shown) → a forward-scoring job accrues challenger-vs-champion metric (Brier / **forward CLV**) as games complete. Works for ANY v6 challenger (home_win/pre, run_diff/pre, totals re-prunes) **AND is the forward-CLV measurement E13.1 requires** → stand up once E13.1 has a residual model to shadow-serve. | n/a (infra) |
| **E13.7** ✅ | Unknown-pitcher cold-start fallback prior (data-quality / gated E13.4 eval) | **✅ CODE-COMPLETE & VERIFIED 2026-06-23 — the E13.4 lift-test blocker is CLEARED.** Quantified the problem: archetype NULL 6.6%(Mar)→15.1%(Jun 2026), peaks 23–27% in Sept of prior seasons; **87% (46/53) of 2026 NULL-archetype pitchers are true rookies** (genuine cold-start, NOT the INC-8/E11.8 freshness gap — that was separate + already closed). **Shipped (pure dbt, two phases):** Phase 0 scoping note (`E13_7_cold_start_scoping.md` — null inventory, near-term-vs-MiLB options, FREE-FIRST MiLB sourcing plan, E7/E8 Dynasty bridge); Phase 1 near-term fallback = rookie/call-up NULLs in archetype + Stuff+/arsenal + platoon now fill with a **leak-clean prior-season league baseline** (archetype → explicit `'league_baseline'` category = honest "profile unknown," not the modal cluster), and a new **`is_cold_start` flag exposed to the public surface** (`home_/away_starter_is_cold_start`). **Verified:** recent-window NULL 12.8%/15.1%→**0.0%** across all three blocks; flag matches the cold-start population exactly (107 May/97 Jun); established pitchers keep real labels. CI green (`dbtf compile` 1766/1766; `pytest` 451 pass/1 skip; new not_null/accepted_values tests pass). **⚠️ RETRAIN DEPENDENCY (the one caveat): filling these columns changes serving inputs for cold-start rows — the deployed champion sees `'league_baseline'` (all-zero one-hot) until retrained. OFFLINE E13.4 benefits NOW; production go-live of the new convention is gated by the promotion runbook + the next retrain (E1.9/E13.4 track) → schedule the retrain to consume the new convention + `is_cold_start`.** **✅ RESOLVED by E13.11 (2026-06-23): the v6 deploy WAS that retrain — fit on post-E13.7 data, so the served champion now uses the league-baseline convention; no all-zero-one-hot mismatch remains.** **First building block toward MiLB (E7):** the league baseline is a placeholder; the proper pitcher-specific MiLB-equivalent prior is the E7 follow-on (shared seam with E8 Dynasty), scoped in Phase 0. | ✅ near-term fallback shipped → cold-start NULL ~0 + flag exposed; CI green. |
| **E13.8** ✅ | **Market-accuracy benchmark — year×book line quality (H2H + totals)** | **✅ DONE 2026-06-23 — report `ablation_results/e13_8_market_accuracy_benchmark.md` + widened census analysis `dbt/analyses/e13_8_market_accuracy_benchmark.sql` (all ~50 oddsapi books, compile-only / zero warehouse cost).** 🔑 **What we're targeting:** (1) **H2H ceiling is thin** — Pinnacle (≈ the low-vig sharps betonlineag/lowvig/onexbet, within noise) closes at **Brier ~0.245**, beating the home-base-rate floor (~0.249) by only **~0.002–0.005 and not every season** (2023 & partial-2026 sit *at/above* the floor) → corroborates E13.6; **no headroom to beat the close.** (2) **Totals over/under PRICE is a coin flip** — *every* book incl. Pinnacle at **Brier ~0.250 = no-skill**, over-rate 0.49–0.51 (lines well-centered); all info is in the **NUMBER** — line **MAE ~3.4–3.7 runs / RMSE ~4.5**, irreducibly noisy, no book sharper than another beyond noise. (3) **Soft-book spread tiny** (2025 H2H Brier 0.243–0.249, mostly within ±0.002 sampling band) → real separator is **vig** (sharps 1.6–3% vs retail 4–8%), not calibration. (4) **No Brier year-trend**, but soft→sharp convergence shows in `dist_to_sharp` (Bovada H2H 0.018→0.006). ⚠️ **"Close" = last snap before game-day 00:00 UTC (~18–24h pre-game; E3.0b/E4.3 leakage guard preserved) → a conservative LOWER BOUND** on true closing sharpness. **Frame for the re-eval: target calibration-parity with the close; totals value (if any) must come from a sharper NUMBER, and §3B shows that's already near the variance floor.** Advisory/transparency only — no +EV claim. **🧭 PM DECISION (2026-06-23) on the ~24h-close caveat: ACCEPT the conservative close as the benchmark-of-record — don't build the true-close follow-on now.** Rationale: the ~24h close is the *appropriate* bar for our MORNING/pre_lineup serve (where the product competes) and a conservative *lower bound* for the dense tier; a weaker bar makes a model's job only EASIER, so a NULL against it is fully trustworthy and a real edge would survive. **The 24h→pitch gap only matters in ONE scenario — a model that CLEARS the conservative bar on the post_lineup (dense) tier** (a weak bar could let a non-edge look like one). **⇒ conditional follow-on E13.8b (NOT built now): a true minutes-before-pitch close benchmark — arm ONLY if a totals/H2H model clears the conservative dense-tier bar. Requires a commence-time timestamp absent from `odds_snapshots_historical` (scope then).** | n/a (situational awareness; benchmark report shipped). |
| **E13.9** 🆕 | **Selective "best-bet" model — abstention + betting-utility objective on the SOFT markets** | **NEW (operator 2026-06-23) — the "discriminate on the BEST bets, don't grade on predicting every game" idea.** Complement to E13.4: E13.4 = alpha-from-FEATURES; **E13.9 = alpha-from-SELECTION** — a policy that ABSTAINS on most games and fires only on a selected subset, with an objective that directly optimizes the **selected-subset payoff (expected CLV / betting utility) + an explicit abstention mechanism**, judged on **forward CLV on the bet subset, NOT Brier over all games.** **🔑 Distinct from the KILLED E13.1** (which anchored to the line + learned the residual, trained on all games): E13.9's *objective itself is selective* (optimize the chosen subset, not minimize all-games loss then threshold), and it targets the **SOFT markets — totals-derivatives (team/alt totals, data exists via E2.0) + props (once E5.1 lands)** — where E13.8 says the main line is efficient but the derivatives may not be. **Main H2H/totals lines = negative control only** (expect null per E13.8/E13.1). **⚠️ THE TRAP + GUARDRAILS (non-negotiable): "keep the games we'd have won" is a data-mining machine** → the selection rule + abstention threshold are PART of the model, pre-registered, fit IN-FOLD + validated OUT-OF-FOLD; judged on **held-out/forward CLV on the selected subset; PBO<0.2 + DSR>0** (DSR deflates for configs tried); purged/embargoed CV; subset size pre-committed/swept-in-fold, never chosen to flatter. **Revives E13.5** (shadow-serve + forward-CLV harness) as its measurement infra. **Honest expectation: likely NULL** (efficiency confirmed from E3.1/E4/E1.9/E13.1/E13.8) — but a genuinely distinct angle + a clean null completes the dossier; a soft-market hit = the program's FIRST real edge. **PRODUCT (separate from edge): even a null-edge selective layer powers a "highest-conviction / biggest-disagreement picks of the day" surface IF framed honestly (higher-variance, NO +EV claim unless it clears the CLV gate).** **Sequence:** AFTER E13.4 (use its best features) + E5.1 prop data (for props); can start on totals-derivatives sooner. Scoped now; not an immediate session. | forward CLV on the selected subset not positive beyond drift @ PBO<0.2/DSR>0 → NULL, archive, dossier entry (keep the honest "disagreement" surface only, no +EV claim). |
| **E13.10** ✅ | **ZONE-MATCHUP heatmap (batter hot-zone × pitcher location/arsenal overlay) — marketable VIZ (primary) + signal test (gated bonus)** | **✅ COMPLETE 2026-06-24 — Track A SHIPS, Track B TRUSTWORTHY NULL (5th no-edge confirmation).** (reframe: viz is the GUARANTEED deliverable; edge = likely-null bonus). Built lakehouse-native (S3/duckdb, NOT Snowflake; a 3-mo profile read ~8s): `betting_ml/scripts/zone_matchup/` (grid/shrink/lakehouse/profiles/overlap/viz) + CLI `build_zone_matchup.py {profiles,viz,feature}` + 13 unit tests. Leak-clean (`< game_date`) batter run-value/whiff/xwOBA-per-cell profiles (full-grid, HEAVY EB shrink toward tiered league prior) + pitcher usage-freq profiles (platoon-split, with measured mean location) + `is_cold_start` (E13.7 pattern). **Track A delivered (addendum 2026-06-24 — JSON is the product, not PNGs):** structured overlay JSON (schema v2.0; long-form `cells` per cell×{fastball,breaking,offspeed,all} → batter run value + pitcher usage-freq + measured pitcher_loc + strike-zone bounds + overlap scalar) → **S3 serving prefix**; committed contract example `e13_10_sample_overlay.json`; **PNG = research proof ONLY → S3 artifact prefix (NOT git, NOT user-served)**; app-render spec `e13_10_app_handoff_spec.md` (frontend design note: hero=All view + FB/BR/OS toggles + per-cell hover + plain-language caption, themed/responsive — frontend is the downstream E5.5-class story) + design doc `e13_10_zone_matchup_design.md`. **Track B (gated bonus) = TRUSTWORTHY NULL (operator-run 2026-06-24, both targets):** the E5.6 game-theory overlap scalar `home/away_zone_overlap` (per-game, prior-season windows ⇒ leak-clean) via the opt-in `--feature-parquet` E13.4-harness bridge — **perside_runs** lift −0.0006/−0.0008, PBO 0.588, DSR 0.298; **home_win** lift −0.0003/−0.0010, PBO 0.837, DSR 0.397; both NOT degenerate (coverage ~90%, std fine). The feature is genuinely ORTHOGONAL (per-side corr 0.158) but INERT → confirms matchup≈identity (E13.2). Dossier `ablation_results/e13_10_zone_overlap_lift.md` CLOSED; `pa_outcome_v2` keeps it as a future-sim consumer only. _(orig spec ↓)_ **NEW (operator 2026-06-23) — the flagship NOVEL signal from the idea crowdsource.** Build a per-batter×pitcher matchup-quality signal from Statcast PITCH-LEVEL data: batter value-by-zone heatmap (xwOBA/whiff per strike-zone cell × pitch type) ⊗ pitcher location/arsenal density (where + what they throw) → integrate over the (zone × pitch-type) grid into an expected-matchup-value (or learn it via a small CNN/embedding NN over the two profiles → PA-outcome distribution, the Singlearity blueprint). **🔑 GENUINELY UNEXPLORED: the idea crowdsource (11 sources) confirmed NONE use zone/heatmap/pitch-level matchup — they're all box-score aggregates that saturate at ~62–65% acc / AUC ~0.65 and NONE beat the market. So this is a real new axis + exactly the context-aware matchup signal a PA simulator needs.** **🔒 Leak-clean: all zone/arsenal profiles built strictly `< game_date` (point-in-time, trailing season/career) + HEAVY EB shrinkage (zone cells are sparse) toward league/handedness baselines + cold-start fallback for rookies (ties to E13.7 `is_cold_start`).** **Integration: lineup-aggregate → E2.1 per-side run-means (totals) + a core input to the E13.2 PA-simulator; AIMED at the DERIVATIVE/soft markets (E13.8: main line efficient).** Test via the E13.4 lift harness (purged CV + PBO<0.2/DSR>0 + shrinkage + `is_cold_start` strat). **Redundancy risk: must show INCREMENTAL lift over existing Stuff+/archetype/platoon matchup features** (they may already capture much of it). Compute-heavy (NN/MC) → build on the lakehouse (S3/duckdb), not Snowflake; sequence when cost headroom allows. | **Track A ✅ DELIVERED** (leak-clean profiles + structured overlay JSON product → S3 serving; PNG proof → S3 artifact; app-render spec). **Track B ✅ NULL** (no incremental lift over existing matchup feats @ purged CV/PBO/DSR on either perside_runs or home_win; orthogonal-but-inert; recorded in the dossier — the 5th no-edge confirmation). |
| **E13.11** ✅ | **De-leaked v6 PRODUCTION ROLLOUT (methodology-integrity, EDGE-AGNOSTIC) — reverses the E1.9/E13.0 deploy-HOLD** | **✅ SHIPPED 2026-06-23 (live; v5 retained as one-step rollback) — THE BULLPEN LEAK IS NOW GONE FROM PRODUCTION.** Swapped v5→v6 across all 3 targets × both tiers: `home_win` XGBoost(211)→**glm_elasticnet** (21/38 served), `run_differential` NGBoost(169)→ngboost_normal (15/126), `total_runs` NGBoost(113)→ngboost_normal (15/16). **✅ AC MET — explanation integrity: home_win drivers now led by `elo_diff` (+0.20, ~2.4× next) → `pythagorean` → de-leaked bullpen secondary w/ correct sign; leak-domination GONE.** Net-new code the recon flagged + 5 more gaps handled: **linear-SHAP path** for the glm (else explanations silently → "deferred"); `finalize_v6_champion.py` (v6 was never persisted, `artifact_path:null`); `PlattCalibratedLinearClassifier`; sidecar-from-post-imputation (CONTRACT-GUARD); a corrected pre_lineup parity invariant (zero lineup-gated feats, not ⊆-champion); the calibrator `--from-consensus` flag (avoids double-temperature). **E13.6 calibrator RE-FIT on v6: T 6.30 → 1.6441** (the de-leaked model needs far less shrink — the leaked v5 was wildly overconfident) + now loads from S3 (not a hardcoded local file). Tier-aware parity PASS both tiers (0 all-null, 0 strong-tier degraded); CI 489 pass; changelog committed (`f1ecc8b`, honest "leak-free, no win-rate claim" framing); kill/conviction windows reset to 6/23 (n=150/n=60 restart vs v6). **⏭️ FOLLOW-UPS:** (1) **pre_lineup tier calibrator still un-fit** — fit its own temperature once ~150 settled live games accrue (registry-noted interim); (2) **`bet_paused` field cleanup** on total_runs deferred (touches Epic 19 serving — separate decision; framing already deprecated); (3) v6 is edge-agnostic — kill-window now tracks it on real Bovada prices, no edge expected/required; (4) E13.4 had NO winners (all null) → nothing bundled; a future winner = fast-follow on the v6 base. **⚠️ served win-prob band shifts (T 6.30→1.64 = less shrink) — user-visible; E9.24 lean-framing context (parked) updates whenever it ships.** **🔧 CUTOVER CORRECTION (session #2, 2026-06-23): the deploy was STAGED, not "live in minutes" — read-path + backfill went live first, but LIVE DAILY v6 SCORING only began after the v6 merge to `main` (PRs #213/#214 ~22:00 6/23) + the DAGSTER PIPELINE REDEPLOY (self-healed 6/23 post_lineup → v6 at 03:27 UTC). Until then production (deploys from `main`) was silently still scoring v5 daily — which is exactly what made "Model Reasoning" show bullpen (the served row WAS v5; not a cache bug). NOW fully live on v6 + narratives backfilled 7d (v6 rows land with NULL `pick_narrative` until the Mistral op runs). 🚨 PROMOTION-RUNBOOK (capture so the next champion swap doesn't silently leave prod on the old model): a swap needs (1) merge to `main` + **DAGSTER PIPELINE REDEPLOY** (not just Lambda/Vercel — the model serves from the daily pipeline), (2) E13.6 calibrator re-fit, (3) bulk permanent-cache invalidation (E9.28), (4) `generate_pick_narratives` for the affected dates, (5) the serving-store rewrite, (6) verify served drivers/version on a live row.** _(orig spec ↓)_ **🚨 NEW + ELEVATED (operator 2026-06-23): DEPLOY v6 regardless of lift.** WHY: **production still serves the LEAKY v5** — its SHAP/driver explanations are bullpen-leak-dominated (`bp_eb_xwoba`), so EVERY game tells beta users "bullpen is the key driver." That actively misrepresents our methodology → a transparency-wedge integrity problem (same class as the E9.25 badge). **🔑 REVERSES the E1.9/E13.0 "HOLD-for-deploy (not worth it for a 3rd-decimal edge)" — that weighed EDGE only and missed the production-EXPLANATION-integrity cost. Re-decided: deploy v6 even at marginal/zero lift; the win is honest, non-leak-dominated explanations + removing the leak from prod.** **Gate = NOT edge:** (a) tier-aware serving parity (E12 harness — v6 serves complete per tier, no all-null strong feats); (b) deploying v6 TRIGGERS the MANDATORY E13.6 calibrator re-fit (registry `calibrator_refit_required_after`; band may shift, T→1); (c) VERIFY served drivers are now sensible (elo_diff/pythagorean rise — the signal E1.7 surfaced). **🔧 RECON CORRECTIONS (E13.11 session 2026-06-23): (i) FRAMING — v6 is de-leaked by REBUILDING bullpen point-in-time (E1.7), NOT removing it; the v6 contracts still contain bullpen cols → "bullpen no longer ARTIFICIALLY dominant," NOT "bullpen gone." Changelog frames LEAK-FIXED, not bullpen-removed. (ii) HARD PREREQ — v6 is `glm_elasticnet`; the served explainer is `shap.TreeExplainer` (XGBoost) which THROWS on a linear model → would serve "deferred"/NO drivers = strictly worse. So a LINEAR explainer (`shap.LinearExplainer` or exact `coef·(x−E[x])`) is MANDATORY net-new code BEFORE the swap (GLM is also inherently more interpretable — an upgrade). (iii) SCOPE = all 3 targets (home_win + run_diff + total_runs — run_diff feeds the H2H consensus the calibrator uses; total_runs serves leaked explanations even though bet-paused). (iv) E13.7 honored by FITTING ON post-E13.7 data — NO contract change / no `is_cold_start` input (option a; (b) reopening the derivation contradicts cost-first + E13.4's "more feats ≠ edge"). (v) v6 was NEVER persisted (artifact_path=null) → this is a RETRAIN-and-deploy needing a new `finalize_v6_champion.py` fit+persist+S3 script, not a registry flip. Registry = `model_registry.yaml`. Plan phases A(linear explainer)→B(finalize script)→C(operator retrain)→D(calibrator re-fit)→E(swap, v5 as rollback)→F(parity+explanation spot-check)→G(CI+changelog).** **Bundle into ONE retrain+deploy:** de-leaked v6 + the **E13.7 cold-start convention** (resolves the pending retrain dependency — the champion currently sees league_baseline all-zeros until retrained) + ~~any E13.4 winners~~ **(E13.4 ✅ COMPLETE 2026-06-23 — ALL candidates NULL, so there are NO winners to fold in → E13.11 is now UNBLOCKED to deploy = de-leaked v6 + the E13.7 cold-start convention only).** Tier-aware (pre_lineup + post_lineup). Served picks + explanations change → **changelog (user-facing)** + verify front-page/pick-detail no longer bullpen-dominated. | v6 served (both tiers) + leak gone from prod explanations + E13.6 recalibrated + parity intact; edge NOT required. |
| **E13.12** ✅ | **PA-grain outcome substrate (entering base-out-score state + PA result) — the E13.2 simulator's training data** | **✅ DONE 2026-06-23 — `mart_pa_outcome_substrate` (1,959,348 PAs, 2015–2026).** One row/(game_pk, at_bat_number): entering state from MIN(pitch_number) — `base_out_state` (24-state `'1-3|2'`) + `base_state` + runner booleans + outs/inning/`entry_bat_score_diff`; players + `batter_hand`/`pitcher_hand` + `platoon_matchup` + `batter_prior_pas_this_game` + `pitcher_times_thru_order_at_entry`; outcome from terminal pitch → `pa_outcome_label` {1B,2B,3B,HR,BB,IBB,HBP,K,out,other} + `woba_value`. Dedup validated (game 746167: 68=68); realistic dist (out 46%/K 22%/1B 14%/HR 3%); leak-clean (consumers filter `game_date < target`). Runs on duckdb (`+mart_pa_outcome_substrate`, S3 read-through) AND Snowflake. **⚠️ COST-HYGIENE FLAG (PM 2026-06-23): the recap says it was "deployed to Snowflake production" + builds on Snowflake by default — but a ~1.96M-row Statcast-derived mart whose ONLY consumer (E13.2) trains on duckdb/S3 should NOT recur on the Snowflake bill we're cutting. ⏭️ Confirm it is NOT wired into any recurring Snowflake build; make it lakehouse-ONLY (duckdb/S3) or ephemeral/excluded on Snowflake so it can't quietly rebuild there.** ⇒ unblocks E13.2 + feeds E13.10. _(orig ↓)_ **concrete prerequisite for E13.2/E13.10.** Audit confirmed the STATE already exists at PITCH grain: `mart_pitch_game_context` derives an 8-way `base_state` ("1--"/"123"…) + runner booleans + `count_state`/`count_leverage` + score context, off `stg_batter_pitches` (`outs_when_up`, `runner_on_1b/2b/3b_id`, balls/strikes, `bat_score_diff`, inning); `plate_appearance_event` is surfaced too. **GAP = no PA-grain model.** Build one row per (game_pk, at_bat_number): take the ENTERING state at `pitch_number=1` (base_state × `outs_when_up` = the full 24 base-out state; + score-diff/inning/count) + batter/pitcher/handedness + the PA OUTCOME (`plate_appearance_event` from the final pitch, mapped to a multinomial: 1B/2B/3B/HR/BB/K/HBP/out/other; + woba_value). Leak-clean (`< game_date` for any serving/eval use). **Build on the LAKEHOUSE (S3/duckdb) — `stg_batter_pitches` already reads lakehouse parquet — NOT Snowflake (cost posture).** Cheap, reusable. ⇒ unblocks E13.2 (PA-outcome model + MC simulation) and is a natural input table for E13.10 zone-matchup. | PA-grain table: 1 row/at-bat, entering 24-state base-out + score/count + outcome multinomial; leak-clean; lakehouse; DuckDB-readable; CI green. |
| **E13.13** 🆕 | **Derivative-market mispricing evaluation (Miller-Davidow framing) — find + TEST edge in the backfilled inning/derivative markets** | **NEW (operator 2026-06-23, from "The Logic of Sports Betting").** Operationalizes the E2.6 derivative gate now that the F5/NRFI/team-total/alt odds are backfilled (correct `*_1st_N_innings` keys). **THESIS (Miller-Davidow + E13.8): market-makers price FEATURED markets tight but DERIVATIVES loose (lower limits/effort) → the main line is efficient (E13.8) but the lazier derivatives may not be.** Four angles: **(1) extend the E13.8 benchmark to derivatives** — per-book per-derivative efficiency (closing Brier / line-MAE vs the realized derivative outcome, SETTLED from our pitch data: first-5 runs, 1st-inning runs) → find the least-efficient markets + soft-book spread + the (higher) derivative VIG; **(2) mechanical-derivation check** — do books derive F5/team-total/alt by a fixed rule off the main line? where the TRUE relationship deviates (e.g. ace-driven F5 suppression > the generic fraction) = the exploit; **(3) model-vs-market gate (E2.6)** — the E13.2 PA-sim / E2.3 distribution gives native derivative distributions → gate each vs its OWN close, **PBO<0.2 + DSR>0 + FORWARD CLV**; **(4) retail-bias** — directional over/favorite shading at soft books = a side to fade. **🔒 HONEST BAR (Miller-Davidow's own discipline): CLV is the measure; softer ≠ free — derivatives carry HIGHER VIG + LOWER LIMITS, so the edge must be forward-CLV-positive NET OF THE DERIVATIVE'S OWN VIG, not just statistically present.** Needs: derivative odds (backfilling now) + outcomes (our pitch data) + a model distribution (E13.2 sim or E2.3). **🟢 DATA UPGRADE 2026-06-24 (E2.0b-fix): LIVE forward F5/NRFI capture is now ON** — the cron was fixed to the correct `*_1st_5_innings` keys (16 books live: FanDuel/BetMGM/Pinnacle/Bovada/DraftKings/Caesars/…) + deployed; the earlier "F5 not offered live / backtest-only" belief was a wrong-key artifact across THREE probes. ⇒ E13.13 now has BOTH the historical backfill (train/backtest) AND a live forward F5-close feed → its **FORWARD-CLV leg can accrue PROSPECTIVELY on real captured closes** (the cleanest CLV test, not a retrospective proxy). Cross-ref E2.6 / E13.8 / E3.0b / E13.2 / E2.0b-fix. | a derivative clears forward-CLV-positive net of vig @ PBO<0.2/DSR>0 → ship; else trustworthy null → dossier (+ closes the betting-edge thesis after the efficient main line → product-quality + fantasy stands). |

**Priority (REVISED 2026-06-21 after E13.1 kill):** **E13.0 ✅ HOLD · E13.1 🔴 KILLED.** The two highest-leverage H2H *edge* bets came back empty → **H2H straight-bet EDGE is DONE; stop hunting it.** **BUT the H2H *prediction* is a non-negotiable product feature (see the 🔒 decision above) → E13.6 (calibration/prediction-quality) is now a standing model-track priority, judged on Brier/ECE not CLV.** Remaining *edge* search **pivots to TOTALS + props** (softer markets), not more H2H edge: **(1) E13.3** next — sub-models on clean data (operator's hypothesis; cheap, run to closure, LOW expectations + firm kill); **(2) pull E2.2/E2.3 forward** (the per-side totals *distribution* — the strategic read's actual named edge hope); **(3) E13.2** only as a **totals-distribution engine feeding E2** (not an ML-edge hunt); **(4) E13.4** scoped to **totals/run-environment signals** (team defense OAA/DRS, bullpen availability, weather×park) where there's unexplored structure. **E13.5 deprioritized** — its main consumer (E13.1 forward CLV) is dead; only revive for a totals/props challenger worth shadow-serving. **Strategic frame:** `best_alpha = 0` is now strongly evidenced on H2H from 4 angles — "delivering on liquid markets" = best-in-class honest projections + transparency/CLV (the GTM wedge), NOT a demonstrated ML edge. The only live betting-edge hope narrows to **totals-distribution (E2) + props (E5)** — softer markets with unexplored structure. Subscriptions fund the business (`../gtm_strategy.md` §0).

---

## Track 2 — Application build  *(one fresh session per story; beta requests prioritized)*
Ordered; the two **P1 correctness fixes lead** and can run in parallel (different surfaces). **Each story = one fresh session:** paste the **application-session bootstrap prompt** (`app_session_bootstrap.md`) first, then that single story block — build, report, end. Don't chain stories through one session.

> **🚨 APP TARGET (two live, one dead):** UI → **`frontend/` (Next.js) ONLY**; backend → **`app/backend/` (live FastAPI)**; ⛔ **legacy Streamlit UI (`app/streamlit_app.py`, `app/home.py`, `app/pages/`, `app/utils/`) — do NOT edit.** `app/` is half-alive (`app/backend/` live; rest dead). A session edited the Streamlit UI by mistake 2026-06-18; guard now in guide §0.2 + every `[App]` prompt. First action: `cat frontend/package.json` to confirm Next.js.

_This mirrors the guide's E9 backlog (§5F table). **Reconciled 2026-06-18** to add the E9 stories that were in the guide but missing here: **E9.3, E9.9, E9.10.**_

| # | Story | Title | Pri | Note |
|---|-------|-------|-----|------|
| — | **E9.15** ✅ | Fix "Model Skill — All Picks" double-counting | **P1** | ✅ **SHIPPED 2026-06-18** — one post-lineup production prediction per game (no dupes). |
| — | **E9.13** ✅ | Keep the pick write-up up to date | **P1** | ✅ **SHIPPED 2026-06-18** — write-up in sync with the served pick (+ 30.15 narrative-accuracy fixes deployed same day). |
| — | **E9.10** (A0.4.11) ✅ | Settings: profile + prefs (unblocked parts) | **P1** | ✅ **SHIPPED 2026-06-18** — new `GET/PUT /users/profile` (live; lambda deployed) backed by `credence-prod-dynamo-users`; editable `initial_deposit` persists; real email, gear-nav, sign-out-everywhere done; notif toggle = "Coming soon". **Unblocks E9.17.** |
| — | **E9.5** (A0.4.22) ✅ | Password reset flow | **P1** | ✅ **SHIPPED 2026-06-18** — validated end-to-end with a real non-sandbox email; branded SES template from `noreply@credencesports.com`; `POST /auth/verify-email` auto-verifies admin-created accounts (`CognitoEmailVerify` IAM policy + lambda redeployed). |
| — | **E9.4** (A0.4.18) ✅ | Welcome email + beta onboarding | **P1** | ✅ **SHIPPED 2026-06-18 — beta onboarding LIVE.** Branded invite + verification templates (atomic push, both-together to avoid clobber); SES bounce/complaint handling (suppression list + SNS `credence-prod-ses-bounce-complaint` → support@, config set), simulator-verified; e2e invite→login→dashboard confirmed; **beta users provisioned.** SES also unblocks E9.9's email path. |
| — | **E9.20** 🐞 ✅ | **Pick ↔ narrative side mismatch** | **P0** | ✅ **FIXED + SHIPPED 2026-06-18.** Root cause: the **narrative** flipped, not the chip — the prompt sent `calibrated_win_prob` (always P(home)) *unlabeled*, so Mistral attributed it to the picked team. **Chip was correct → the model backed BAL** (so the SEA bet was the wrong side — exactly why it was P0). Fix: prompt now labels probs by team + explicit "model backs X" line; edge uses `abs(cal−mkt)` (dropped the broken `h2h_edge=0.0`); **`_validate_pick_consistency()` guard** skips/ warns on side↔prob contradiction; QUALIFY dedup (one Cortex call/game); 14 regression tests. **Systemic scope: AWAY picks were affected** (home picks were coincidentally right). |
| — | **E9.2** (A0.4.34) ✅ | CLV meta-model confidence bar | P2 | ✅ **SHIPPED 2026-06-18 — H2H only** (totals excluded, low-information — per E9.23). Fixed the **NULL-meta-columns** serve-side gap (`arviz`+`h5netcdf`) + `MAX() OVER` propagation. **The weekly retrain (INC-1) is now ✅ resolved too** (pymc/h5py added), so the trace refreshes — the bar stays fresh, not stale. CLV = line-value leading indicator, not a bet rec. |
| 3 | **E9.24** ✅ | H2H pick presentation: honest "lean" framing (E13.6 disclosure layer) | **P1** | ✅ **SHIPPED 2026-06-25 (prod)** — `probability-bar.tsx` lean framing + hover legend (model/market/CI/50%-ref) + attributed-team numbers ("54% (51–57%) — STL · slight lean") + a11y. Bonus: per-book selector on the win-prob bar (synced to `selectedBook`; advances E9.11), CLV-bar tooltip (honest totals-AUC-0.445 copy), "Bovada"→"Market" relabel. ⚠️ caught: `bovada_devig_prob` serving field = consensus avg, not Bovada (UI fixed; field name still a misnomer → audit other consumers). Detail → archive. |
| 4 | **E9.1** | "+EV price range" (breakeven line) | P2 | Beta request; glue on A0.4.32. |
| 6 | **E9.14** | Add Fanatics to Book Comparison | P2 | Small; unblocks fuller best-price. **🔗 Bundle E9.22 (odds-freshness) here.** |
| 6b | **E9.22** | Book Comparison odds-freshness (stale "as of" / cadence) | P2 | ⬜ NEW — "as of 7AM / updated hourly" is stale vs the real odds-capture cadence; refresh book-odds serving at capture cadence + make the timestamp/label honest. **Bundle with E9.1 or E9.14** (same A0.4.32 surface). |
| — | **E9.23** 🅿️ | Totals pick-detail parity (CI + CLV confidence) | P2 | 🅿️ **DEFERRED (operator 2026-06-18) — leave totals bars OFF** until the totals model is recovered (E2.3). Both honest versions need model work that isn't ready (totals meta = noise; totals point model un-calibrated); empty is the honest state. Revisit at E2.3 → CI via E2.7 + real CLV bar via E9.2. **E9.2 ships H2H-only for now.** |
| 7 | **E9.16** | Paginate the Bet Log (~25/page) | P2 | Trivial UX win. |
| 8 | **E9.11** ✅ | Best price across top books | P2 | ✅ **SHIPPED 2026-06-25 (prod)** — best-price fields + `GET /picks/line-shopping` endpoint + serving `_compute_line_shopping_payload` (positive-edge plays, Pinnacle anchor) + green BEST badge on the book selector + EV-tracker "Line Shopping" tab. 14 tests; best_alpha=0 framing throughout. **Unblocks E9.12.** Detail → archive. |
| 9 | **E9.17** 🔁 | Bankroll BOOKKEEPING — multi-book balances + deposits/withdrawals + honest growth % | P2 | 🟢 unblocked (E9.10 `/users/profile`). **RE-SCOPED 2026-06-25 (operator): not just one editable deposit — a lightweight multi-book ledger** (per-book editable balances + recorded deposit/withdrawal events), because beta users run several books AND the growth % is only honest if it NETS OUT cash flows (a deposit ≠ growth, a withdrawal ≠ loss). Legacy `initial_deposit` migrates in. Prompt re-written; PHASE if too big. |
| 10 | **E9.3** (A0.4.31) 🟡 | Live scores → Railway PG | P2 (deferred) | 🟡 **DEFERRED + RE-SOURCED 2026-06-25 (operator):** drop the Odds-API `/scores` path (~1 credit/call = live-game credit burn); source from the **FREE MLB StatsAPI** instead, and **defer until we stand up a live in-game StatsAPI ingestion** (reuse `services/schedule_capture`, which already polls StatsAPI). The near-real-time in-game poller → PG → refreshing scoreboard is a NEW data path + overlaps other live-state initiatives → build it deliberately on the shared ingestion, not a one-off. Prompt re-written. |
| 11 | **E9.12** ❌ | Daily card | — | ❌ **DEPRECATED 2026-06-25 (operator)** — redundant with the Dashboard (today's picks + conviction + `qualified_bet` filter) + E9.11's Line Shopping tab (value plays + best price + Pinnacle anchor); its "bets to make" framing is gated on the un-cleared bet-permission gate (`best_alpha=0`). Revisit only if that gate clears, and then as a consolidation, not new capability. |
| 12 | **E9.9** (A0.6) | Push notifications (SNS + Lambda + Web Push) | P1 | ⬜ backend BUILD; **SES email path now unblocked (2026-06-18)** — no longer SES-gated; Dagster publishes post-`predict_today`. Completes E9.10's notif toggle. |
| 13 | **E9.18** 🔁 | Changelog accordion + Monday-bucket auto-fix & CI guard | P2 (bumped) | ⬜ RE-SCOPED 2026-06-25 — pure frontend accordion PLUS the **durable fix for the recurring week-bucket bug**: render auto-snaps any `week` date to its Monday + merges split blocks, and a CI guard FAILS the build on a non-Monday `week` key. (The written Monday→Monday convention is in §0.2 + CLAUDE.md + the app bootstrap and STILL gets ignored → make it programmatic.) Operator hand-fixed the 6/25 instance 2026-06-25. |
| 14 | **E9.21** | PostHog metrics in Admin (one-stop view) | P3 | ⬜ NEW — admin-only; embed a PostHog shared dashboard (iframe) in Admin → Analytics; no client-side secret; free-tier-safe (embed = no event cost). |
| 15 | **E9.30** ✅ | Public **"About Us"** page (editorial / principles-led) | P2 | ✅ **SHIPPED 2026-06-26 (v2, prod)** — editorial `/about` page (POV hero + serif origin + numbered principles 01–04 + who-it's-for + footer-only Penumbra; NO product UI). v1 (prose-wall, Penumbra-first) was REJECTED → redesigned ~70% shorter; #3 ("no false certainty") worded to not contradict the pick-of-the-day. Detail → archive. |
| 16 | **E9.31** 🆕 ⭐ | **Matchup heatmap viz** (frontend render of the E13.10 overlay) | P2 (marketable) | ⬜ NEW (operator 2026-06-25) — **prompt ready.** Closes the deferred E13.10 frontend gap: E13.10 already shipped the leak-clean batter-zone × pitcher-arsenal **overlay JSON (schema v2.0 → S3 serving) + app-handoff spec (`e13_10_app_handoff_spec.md`) + sample payload**; this builds the SVG/Canvas component (hero "All" + FB/BR/OS toggles + per-cell hover + caption, themed/responsive, `is_cold_start` empty state) on pick-detail / player pages. PROBE the serving read path first (S3 vs PG vs a backend passthrough). **Honest framing: explanatory matchup viz, NOT a bet signal (the zone signal tested null — E13.10 Track B).** The marketable "show-your-work" differentiator. |
| 17 | **E9.32** ✅ | Market **Hold** (vig/overround) on Book Comparison | P2 | ✅ **SHIPPED 2026-06-25 (prod, `936df9b`)** — per-book two-way hold (`1/dec_A+1/dec_B−1`) on the pick-detail Book Comparison + "LOW VIG" badge on the tightest book + cross-book hold summary (ML + Totals), honest "not a bet rec" footer. Pure client-side display math, no backend. Detail → archive. |
| 18 | **E9.33** 🆕 | Performance page — production-model **time-series** graph | P2 | ⬜ NEW (operator 2026-06-25) — a chart on Performance showing how the CURRENT production model performs OVER TIME. **🔒 HONEST FRAMING (critical): plot prediction QUALITY (rolling calibration/ECE, directional win%, Brier) over time — NOT a profit/equity curve** (best_alpha=0; an upward "winnings" line would be a false edge claim). **⚠️ the v6 deploy reset the kill/conviction windows 2026-06-23 → the "current model" series effectively starts ~6/23; older points were a different champion** — either start the series at v6 or clearly segment by model version. Data: extend the E9.26 performance metrics to per-day/week aggregates of settled picks. Needs a prompt. |
| 19 | **E9.34** 🆕 | Admin — update the **live model versions** shown | **P3 (low)** | ⬜ NEW (operator 2026-06-25) — the Admin section shows stale "live" model versions; update to reflect the v6 champions (E13.11: `home_win`=glm_elasticnet, `run_differential`/`total_runs`=ngboost_normal) + ideally read from `model_registry.yaml`/the served metadata rather than hardcoding so it can't go stale again. Admin-only → no changelog. Small. |
| 20 | **E9.35** 🆕 | Pick details — **quantify** the model-reasoning feature importances | P2 | ⬜ NEW (operator 2026-06-25) — the "Model Reasoning" section shows signed attribution bars; add the NUMERIC magnitude per driver (the SHAP contribution / % of total) so users see HOW MUCH each feature moves the pick, not just direction. The SHAP values are already served (E13.11 linear-SHAP path) → likely frontend-mostly. Honest framing: these are model ATTRIBUTIONS, not causal/edge claims. Needs a prompt. |
| 21 | **E9.36** 🆕 | Team details — clickable lineup/starters (new tab) + SP **last-3-starts** | P2 | ⬜ NEW (operator 2026-06-25) — on the team detail page, make each lineup batter + the starting pitchers CLICKABLE → open `/players/[player_id]` in a NEW TAB (`target="_blank"` + rel noopener); needs the player_id wired onto the lineup/SP rows. PLUS show the starting pitcher's **previous 3 starts** (date, opp, IP, K, ER/runs, result) — needs a per-pitcher recent-start game-log (derive from game results / Statcast; confirm the source). Frontend + a small serving/backend addition for the 3-start log. Needs a prompt. |
| 22 | **E9.37** 🆕 | Pick details — **line movement over time** per market | P2 | ⬜ NEW (operator 2026-06-25) — show each market's (h2h / totals, +derivatives if available) line MOVEMENT over time for the game on the pick-detail page (open→close trajectory, per book or consensus). **Data EXISTS: `mart_odds_line_movement`** (real `commence_time`). Reads the E9.27-guarded PRE-START snapshots only (freeze at last pre-game). Honest framing (timing/transparency context, not a bet rec); pairs with E9.11/E9.32 on the same surface. Needs a prompt + a serving payload of the movement series. |

**Trails its model dependency (likely beyond the window):** **E2.7** (distribution UX — needs E2.3) and **E5.5** (prop pages — needs E5.2/E5.3). The §0.3 app surfaces (E2.7, E4.6, E5.5) get their prompt emitted by the upstream model session.

**Parked but accounted-for (not lost — see *Out of scope*):** the **GTM/paid-tier launch sequence** (`../gtm_strategy.md`) = **E9.19 MFA (P1 security) → E9.7 OAuth → E9.8 Stripe**, where **E9.19 (MFA) is a HARD security gate before E9.8 goes live / takes paying customers** (account-takeover protection ahead of a billing relationship). **E10.1** (master 34.1, parlay *calculator*) = parked under E10 — but it's honest-value-now / zero-edge-required and beta-friendly, so it's a **candidate to pull into this window** if you want a differentiation win before the break (flag it and I'll slot it).

---

## Cross-track dependencies
- **E1.5 + PBO/DSR (Track 1) gate go-live** of every betting strategy app-surface (E9.12 daily card, any "+EV" framing) — the app builds the views, but they go live honestly only once the gate clears.
- **E9.12 (daily card)** consumes the decision gate + **E9.11** (best price) + **E9.13** (fresh write-up) → keep it after them.
- **E2.7** needs **E2.3**; **E5.5** needs **E5.2/E5.3** — app surfaces trail their model work.
- App-prompt handoff (§0.3): app surfaces marked 🧩 are unblocked only when the upstream model session has emitted their prompt.

## Out of scope for this window (parked until after the break)
- **⏰ DEADLINE EXCEPTION — MS.0: Multi-sport (NFL/NCAAF/NCAAB) HISTORICAL ODDS BACKFILL → S3, BEFORE 2026-07-17. ✅ DONE 2026-06-23.** Script `scripts/backfill_multisport_odds_to_s3.py` (h2h/spreads/totals, us+eu, 3 snaps/day → Hive-partitioned S3 Parquet; idempotent; `--mode probe`). **Full run: 8,058 calls, ~483K credits spent, 3.77M remaining** (huge surplus still in hand pre-7/17). **Landed all 6 seasons 2020–2025 (probe-confirmed 2020 data floor) for all 3 sports** — NFL 97,942 rows / NCAAF 225,378 / NCAAB 1,382,782; DuckDB-validated, row counts coherent (COVID-2020, CFP expansion). **🔴 KEY FINDING: The Odds API has NO historical scores endpoint** (`/v4/historical/.../scores` = 404; only live, ≤3 days) → **outcomes must come from FREE sources → MS.1.** **💰 CREDIT POSITION: the high-value sanctioned grabs are now COMPLETE** (MS.0 multi-sport odds + E2.0 MLB derivatives both done) → **UPDATE 2026-06-23: one more sanctioned grab added — MLB historical PLAYER PROPS (E5.1), elevated to a windfall grab before 7/17** (props = a named edge lane; ~0.5–1.1M cr fits the surplus; probe-first since The Odds API may not offer prop history). **UPDATE 2026-06-23b: WINDFALL MAXIMIZED — also add MS.2 (multi-sport NFL/NCAAF/NCAAB player props → S3, probe-first).** 💰 **SHARED CREDIT-BUDGET LEDGER (E5.1 + MS.2 draw the SAME 3.77M pool): probe each FIRST, project cost, allocate value-ranked (MLB props → NFL props → NCAAF props → NCAAB props), reserve a buffer so one grab can't starve another; STOP when the next candidate doesn't clear a value bar — leftover credits expiring is FINE (costs nothing), burning them on low-value data or starving a higher-value grab is the actual waste.** Pending (low-pri): 2025 end-dates are estimates (overshoot is safe — empty dates skip). Orchestration intentionally DEFERRED to pre-season (multi_sport_roadmap §8: NCAAF early-Aug, NFL mid-Aug, NCAAB mid-Oct 2026).
- **🚨 CREDIT BURN ALERT (operator 2026-06-24): the MLB props backfill is FAR more expensive than projected — ~2.88M credits burned, only at 2025-07-28 (partway through ONE season), 888,679 remaining (→ **563,437 after the NRFI grab 2026-06-24**: `totals_1st_1_innings` fully backfilled at ~49,680 cr — HALF the ~102K estimate, consistent with the double-write/single-region fix landing. ⏭️ confirm the double-write was actually resolved + which key-derivatives remain within the 563K vs the 7/1 refresh. S3 prefix = `mlb/` not `baseball_mlb/`).** Continuing drains the rest on partial-2025 props + grabs NOTHING else (no 2023–24 props, no MLB F5/inning DERIVATIVES = the actual edge lane + cheaper, no multi-sport). The value-rank discipline didn't hold (props ran first + ate the budget; derivatives — ranked higher — never started). Refills 7/1 (one more ~5M cycle, 5M-rate access lasts to 7/17, then 100K/mo) unless renewed. **⇒ DECISION (operator 2026-06-24): the running backfill IS the derivatives (+props) — the edge lane IS being captured (correction to the earlier "derivatives never started" read). And RENEW the 5M rate past 7/17 — TEMPORARILY — to finish the full historical grab, then drop to 100K/mo once complete.** Rationale: historical 2023–25 prop/derivative odds are a one-time acquisition (un-backfillable later at 100K/mo) + permanent once in S3; live capture is fine at 100K after. So: continue + complete the backfill; bounded renewal cost, permanent dataset. **Light hedge: still grab value-first (derivatives + recent props ahead of speculative deep history) so an interruption leaves the most useful subset.** ⏭️ track total credits spent so the renewal stays bounded to "finish history," not open-ended.
  - **⚠️ SUSPECTED DOUBLE-WRITE (operator log 2026-06-24): the same `market=X/.../date=Z/data.parquet` key is written TWICE per date with different row counts (spreads 562→262, spreads_1st_5 182→84, totals_1st_1 152→72) → likely us+eu (or open/close) pulls colliding on one S3 key with no `region=`/`snapshot=` partition → DATA LOSS (last write wins) + ~2× credit spend (paying for a clobbered pull) — plausibly a big part of the overburn.** Session tasked to diagnose + cut to a SINGLE region (`us`) + confirm the core F5 keys (`h2h_1st_5_innings`/`totals_1st_5_innings`) are actually captured (not in the sample log). Fixing this both saves data AND ~halves the burn.
  - **📋 PRE-7/1 NARROWED SCOPE (operator 2026-06-24): conserve the 888K → grab only the KEY DERIVATIVES (single region, full history): `totals_1st_5_innings` ⭐, `h2h_1st_5_innings` ⭐, `totals_1st_1_innings` (NRFI) ⭐ [+ `spreads_1st_5_innings`, `alternate_totals_1st_5_innings` if credits comfortably allow].** Session projects cost first, confirms it fits 888K + buffer, then resumes.
  - **📋 POST-7/1 RESUME QUEUE (with the fresh ~5M, 5M-rate renewed to finish): the DEFERRED set — secondary innings (`1st_3`, `1st_7`), full-game `spreads`/`alternate_spreads`/`team_totals`/`alternate_team_totals`, PLAYER PROPS (batter_*/pitcher_*), and ALL multi-sport (NFL/NCAAF/NCAAB props + derivatives, MS.2).** Idempotent skip means the pre-7/1 key-derivatives won't re-pull. This is the unambiguous resume scope.
  - **✅ AUDIT RESULT 2026-06-24 (S3 ground-truth, corrects the earlier "props deferred" read):** **PLAYER PROPS are COMPLETE, not deferred** — `pitcher_strikeouts`/`pitcher_outs`/`batter_hits`/`batter_home_runs`/`batter_total_bases` full 2023–2025 + 2026 current (1 day behind). **⇒ E5.2 (the K-props model bet) is UNBLOCKED — the data's already in S3** (was my error to say it waited on post-7/1). **TIER-1 KEY DERIVATIVES COMPLETE too**: `h2h_1st_5_innings` + `totals_1st_5_innings` + `totals_1st_1_innings`(NRFI) full 2023–2025 + 2026 current → **E13.13 derivative-eval data ready.** Only **partial/deferred:** the Tier-1 alts (`alternate_totals_1st_5`/`spreads_1st_5` stall 2025-08-11, no 2026) + all Tier 2/3 (`1st_3`/`1st_7`/full-game spreads/team-totals stall 8/11) + multi-sport. **DOUBLE-WRITE = RESOLVED** (confirmed in code: one write per market across snapshots; spot-checked 1 file/key). **Credits ~563,412 live (renews 5M on 7/1 ~7 days); live drain ~144 cr/day = negligible.** 💡 AUDIT RECOMMENDATION: **don't spend much pre-7/1 — wait for the 5M renewal** (only the trivial player-prop 1-day catch-up now); resume the deferred set post-7/1 (idempotent skip from the 8/11 checkpoint).
  - **🔴 E5.0 (live prop ingestion) = NOT STARTED.** No live prop capture of any kind exists. `odds-api-data-feed` captures only full-game h2h+totals → Snowflake. **`services/derivative_capture/` is built but NEVER DEPLOYED to Railway** (the `DERIVATIVE_CAPTURE_MARKETS` env var on `odds-api-data-feed` is DEAD config — wrong service; ⏭️ operator: remove it). **✅ CORRECTED 2026-06-24 (E2.0b-fix): the "HISTORICAL-ONLY" claim was a THIRD wrong-key probe artifact** — that session also used `h2h_h1`/`totals_h1`, not the correct `*_1st_5_innings` keys. Live probe 2026-06-24 with the correct keys returned **16 books including FanDuel, BetMGM, Pinnacle, Bovada, DraftKings, Caesars** → **F5 IS fully available live.** Code defaults fixed. **✅ DEPLOYED 2026-06-24 (operator): `services/derivative_capture/` created + deployed in Railway project `nurturing-embrace` (secrets copied from `odds-api-data-feed`; `DERIVATIVE_CAPTURE_MARKETS=team_totals,alternate_totals,h2h_1st_5_innings,totals_1st_5_innings,totals_1st_1_innings`).** ⏭️ Operator follow-ups: (1) after the next `*/30` cron fires, VERIFY non-zero F5 rows in `derivative_odds_raw` then `dbtf build --select stg_derivative_odds mart_derivative_closes`; (2) remove the DEAD `DERIVATIVE_CAPTURE_MARKETS` var on `odds-api-data-feed` (wrong service, low-pri). **⇒ live DERIVATIVE-market capture (F5/NRFI/team/alt) is LIVE.** The remaining E5.0 piece = live PLAYER-PROP capture (separate service; lower-pri — E5.2 trains on the historical S3 backfill, so live prop capture only matters for serving prop picks forward, i.e. the E5.5 surface).
- **MS.1 🆕 — Multi-sport historical SCORES/outcomes ingestion (FREE sources). ✅ SCORES + VALIDATION DONE 2026-06-24; crosswalk + dbt DEFERRED (fall).** 3 scripts (NFL/NCAAB=ESPN, NCAAF=CFBD), 2020–2025, idempotent + retry-hardened → S3. **JOIN COVERAGE (scores↔odds): NFL 77.6% · NCAAF 85.3% · NCAAB 27% ⚠️.** **🔑 NCAAB 27% = a team-NAME mismatch (NOT missing data): Odds API uses systematic abbreviations ("St"/"CSU"/"UConn"/"Loyola (Chi)") across 350+ D1 teams; the ~40-entry static crosswalk can't cover it.** FIX (scoped, deferred to the fall dbt session): replace the static dict with a DuckDB/Parquet xref (`{sport}/crosswalk/team_names.parquet`) auto-built via `build_team_crosswalk.py` (difflib ~0.80 cutoff + manual curation of unmatched), applied at QUERY time (no re-ingest). **⚠️ THIS IS GATING, NOT POLISH: 73% of the NCAAB odds we paid for are currently un-backtestable until the crosswalk lands** (NFL ~22% / NCAAF ~15% also stranded). **⚠️ TWO FLAGS for the crosswalk session: (1) NFL 77.6% is suspiciously LOW for 32 clean-named teams → likely a DATE-JOIN component (UTC-boundary/late games) + franchise renames (Washington 2020–22, Raiders→LV 2020) — check the date join, not just names; (2) CONFIRM period scores (1H/quarter/half) were captured — the multi-sport DERIVATIVE odds need them to settle later (if only final scores were stored, the derivatives can't be settled).** _(Note: the session said "blocked on MLB props" — minor mis-framing; MS.1 validates multi-sport scores vs the MS.0 multi-sport ODDS, already in S3 — it does NOT need the MLB props; the validation already ran.)_ 3 ingest scripts shipped (NFL=ESPN, NCAAF=CFBD, NCAAB=ESPN; idempotent/resumable/retry-hardened); NFL/NCAAF/NCAAB outcomes + period scores 2020–2025 in S3 + per-sport team-name crosswalks. The `--validate` join-coverage check (the success metric) is pending odds-in-S3. **⚠️ CHECK: validation joins scores to GAME-LEVEL odds → MS.0's main h2h/totals (already in S3) should suffice → MS.1 may be UNBLOCKABLE NOW against MS.0, NOT waiting on the credit-hungry props/derivative backfill — confirm before parking it as blocked.** **📐 STRUCTURE NOTE (confirmed 2026-06-24): MS.0 is COMPLETE in S3 (NFL 752 / NCAAF 987 / NCAAB 1,700 files, 2020→2026) BUT non-MLB sports store h2h/spreads/totals in ONE file per date (NO `market=` partition) — UNLIKE MLB props (split by market key). The MS.1 join + the fall multi-sport readers must handle the per-sport layout difference.** _(orig ↓)_ **🔄 IN PROGRESS (2026-06-23, Sonnet session) — gets the data fall-ready so the parallel multi-sport build can start modeling immediately (start dates: NCAAF early-Aug, NFL mid-Aug, NCAAB mid-Oct).** Capture FULL-GAME outcomes + PERIOD scores (1H/quarter for football, 1H/half for NCAAB) so the backfilled DERIVATIVE markets can be settled. 🔑 The hard part = the JOIN: book team names ↔ score-source team names → build a per-sport crosswalk + report join-coverage (% of MS.0 odds-games matched to an outcome). Prompt = MS.1 in `story_prompts.md`. The MS.0 odds are unusable for any backtest/benchmark without outcomes. Free sources identified: **NFL = `nfl_data_py`/nflverse · NCAAF = CFBD API · NCAAB = ESPN API / sports-reference** (separate per-sport scripts → S3, mirroring MS.0's partition layout). **Not urgent** (no credit clock, free, and multi-sport MODELING is the fall push) — do alongside the pre-season orchestration wire-up. Without it, nothing can be modeled or benchmarked (no multi-sport E13.8 equivalent) on the odds we just paid for.
- **⏰ MS.2 — Multi-sport (NFL/NCAAF/NCAAB + MLB) HISTORICAL PLAYER-PROP backfill → S3, BEFORE 2026-07-17. 🔄 BACKFILL RUNNING 2026-06-23.** All 4 sports probe-confirmed ✓: MLB 9 bkrs / NFL 10 bkrs / NCAAF 8 bkrs / NCAAB 7 bkrs; all expected markets returned; credit cost tracking as projected. Script `scripts/backfill_multisport_props_to_s3.py` — two-step events endpoint, idempotent, `--sport` flag for per-sport runs. Est. total credits: ~1.87M of 3.37M available. DATA-GRAB ONLY; needs MS.1 outcomes to be modelable. S3 layout: `{sport_label}/props/market={market_key}/season={season}/date={game_date}/data.parquet`. **⚠️ KNOWN GAP: snapshot at 17:00 UTC (= 1 PM EDT) means any game starting before that time (e.g. 12:05 PM doubleheader first games, holiday noon starts ≈ ~5% of game-days) is skipped by the leakage guard — no prop rows for those events.** **🔲 POST-BACKFILL ACTION (before modeling): run a game-by-game coverage audit.** Re-scan Step 1 (`GET /events`, 1 cr/date) for every date in the season ranges to get the full universe of event IDs → diff against event_id values in S3 Parquet → produce a gap report: event_id, game_date, commence_time, home_team, away_team, gap_reason (leakage_skip | no_bookmaker_data | network_skip). Report stored as CSV/Parquet so any event can be spot-checked by event_id. ~730 credits for MLB, trivially cheap for other sports.
- **(original MS.0 deadline note, now satisfied:)** **MS.0 ⏰ before 2026-07-17.** The Odds API quota drops **5M → 100K credits on July 17** (operator cut it) → use the current surplus to backfill multi-sport historical odds NOW (it'd be unaffordable at 100K/mo later). **DATA ACQUISITION ONLY — no models** (those are the fall push). **Land DIRECTLY in S3 Parquet, NOT Snowflake** (keeps it off the bill we're cutting + pre-stages the lakehouse for the fall). Credit-budget the depth (seasons × markets × books; historical endpoint ~10 cr/call) → prioritize the most useful depth within the surplus. Cross-ref `../../multi_sport_roadmap.md`. Prompt = MS.0 in `story_prompts.md`. _(This is the ONE sanctioned multi-sport exception to "baseball-only this window" — a use-it-or-lose-it resource grab, not a scope creep into multi-sport modeling.)_
- **Multi-sport** (NFL / NCAAF / NCAAB) — the fall push; see `../../multi_sport_roadmap.md`.
- **Fantasy / Dynasty vertical** (F1–F8) — baseball, but a separate vertical + the larger build; resumes after the break and is gated on **E7** (MiLB MLEs) for prospects. *(Pull it into Track 1 if you decide it's a pre-break priority — flag it.)*
- **E5.2–E5.6** (prop pricing/gates/DL), **E6.2–E6.5**, **E7** (MiLB), **E10** (parlay — **except E10.1/34.1, the honest calculator, which is a Track-2 pull-in candidate, above**), ~~**E11** (dbt→lakehouse migration)~~ **→ NOW ACTIVE (Track 1 rows 2–3): E11.2-T2 + E11.1-W1; see *Cost watch*** (only later E11 waves remain parked), **E9.19/E9.7/E9.8** (MFA → OAuth → Stripe — the **GTM/paid-tier launch sequence**, tracked in `../gtm_strategy.md`; **E9.19 MFA is the security gate before E9.8 takes paying customers**).

## 💰 Cost watch (baseline 2026-06)
Pre-revenue monthly burn (~$500–620/mo total), with the target obvious:
- **Snowflake — ~$287 → trending ~$359/mo (+25% MoM); ~60% of spend, GROWING. THE target.** (Last month $287.40; MTD 6/18 $225.90.) **🚨 6/22 AUDIT BOMBSHELL — the cost-opt was aimed at the wrong target: 96.7% of Snowflake credits are UNTAGGED.** Last-7-day run = **23.35 credits total; 22.58 (96.7%, 89,210 queries) UNTAGGED**; every E11.3-tagged job (all dbt builds + dagster jobs + crons) = **3.3% (~0.77 cr).** ⇒ the heavy Statcast dbt marts (W1's target) are tagged-and-TINY (~0.02–0.04 cr/run, once-daily incremental) → **W1 will NOT meaningfully cut credits.** **QUERY_HISTORY drill-down (6/22) identified it:** the "untagged" set is the **normal dbt/pipeline workload (`DBT_RW`/`COMPUTE_WH`) that isn't carrying QUERY_TAG** — `dbt_version: 2.0.0` = **dbt-fusion → E11.3's tag hook doesn't propagate to per-model queries** (not a hidden poll). **Real drivers: ⭐ `feature_pregame` CTAS rebuilt 1,861×/7d (~266/day) = #1; `ALTER ADD COLUMN IF NOT EXISTS` ×8,850 (DDL on every write); dead Parlay inserts (~3,570, self-resolving post-decommission); a small app-backend Snowflake read path.** **→ #1 cost action = E11.9: fix dbt-fusion tagging + cut the intraday `feature_pregame` rebuild frequency + the ADD-COLUMN-per-write waste + auto-suspend COMPUTE_WH.** W1 deprioritized as a COST lever (it's the rebuild FREQUENCY, not the marts).
- **Dagster — 🚨 UP, not down: $36.20 since 6/18 (~$8/day ≈ ~$240/mo), ABOVE the old $50–100 estimate despite E11.4 removing ~47.6% of run-MINUTES.** Why the minute-cut didn't cut the bill: E11.4 removed the *light* polling schedules, but the cost is dominated by **long-wall-clock dbt rebuilds metered via blocking-poll (the E11.0b gap)** — a Dagster op that triggers the runner + polls `/status` stays metered for the full dbt run. The Snowflake audit showed **`feature_pregame` rebuilds ~266×/day** → each metered on Dagster too (same root cause, two invoices). **And W1 just ADDED Dagster compute** — the `ingest_statcast_to_s3_op`/`run_w1_lakehouse_op` dead-end tail runs the DuckDB build *inside Dagster* (parallel window = paying for DuckDB build + Snowflake build). **→ Fix: E11.9 #1 (cut the feature_pregame rebuild frequency) cuts BOTH bills; RE-ELEVATE E11.0b (blocking-poll metering, now a big chunk); move the W1 DuckDB build to a Railway CRON (E11.4 pattern), not a Dagster op.** §6 principle holds: Dagster *coordinates*, Railway/DuckDB *executes* — heavy compute on the Dagster meter is the suspect.
- **Odds API $119/mo** — ~~consolidation candidate: SportsGameOdds~~ **KEEP (operator 2026-06-23): F5 IS available from The Odds API (`*_1st_5_innings` keys) → the SportsGameOdds-for-F5 swap rationale is gone; stay on the incumbent. Only revisit if F5 BOOK coverage proves too thin. — see E2.0c.**
- Vercel $30 · Railway ~$12 · **AWS ~$2–3/mo (negligible — MTD $1.34; serverless stack basically free at beta scale).**
- **Railway plan = Hobby ($5/mo, incl. $5 usage); sufficient at beta scale.** Compute is NOT the constraint (Hobby includes up to 48 vCPU/48 GB per service, 8/8 per replica — the always-on dbt-runner + DuckDB Statcast transforms fit). Watch items: the **5 GB volume storage** (Railway PG serving store + DuckDB local scratch for E11.1-W1 → keep DuckDB S3-first, cap local scratch, monitor volume). **Upgrade trigger (operator 2026-06-19): when monthly Railway *usage* crosses ~$20, move to Pro** — Pro's $20 base comes bundled with $20 of included usage credits, so at ≥$20 usage you pay ~the same either way and get Pro's perks (priority support, team workspace, larger credit) at ≈zero marginal cost. Below $20 usage, Hobby is cheaper; current ~$12 → stay on Hobby for now. (Per-service resource ceilings are the same on both tiers, so the upgrade is about credits/features, not capacity.)
- **🟢 Cost-opt ACTIVATED NOW (operator 2026-06-18) — "keep Snowflake at minimum until profitable."** E11 is **no longer parked**. **Done this wave:** E11.3 (tagging) ✅, E11.0 (dbt-runner substrate) ✅, E11.4 (intraday→crons, ~47.6% Dagster run-min off-meter) ✅, **E11.5 (engine decision: stay fusion) ✅**, **E11.2-T2 (state-aware daily builds) ✅ LIVE 6/19** (the dbt-fusion "wall" was just a YAML format issue — `config:`-nest freshness — fixed in E11.5; dbt-runner redeployed). **Next:** **E11.1-W1 (heavy Statcast marts → dbt-duckdb/S3)** = the PRIMARY remaining structural Snowflake lever (unblocked, runs on fusion); then E11.0b (residual Dagster dbt) + E11.6 (Parlay cleanup, low). ⚠️ **6/22 AUDIT — HONEST EXPECTATION (operator confirmed the intuition 2026-06-22): Dagster DOWN, Snowflake FLAT-to-slightly-UP.** Why: the only real Snowflake lever (**A2.15 = E11.1-W1**) is in its **parallel-validation window — marts NOT decommissioned, so the S3/DuckDB path runs ALONGSIDE the live Snowflake builds (double-run, possibly +Snowflake this week)**; T2's daily-skip savings are unverified + small; everything else shipped (E11.4 etc.) cut **Dagster**, not Snowflake. So **the 6/22 audit = "Dagster win banked (E11.4) + W1 not yet realized," NOT the Snowflake measurement.** **🎯 The REAL Snowflake checkpoint is AFTER the W1 decommission (~6/26+):** once parity passes 6/25 → external tables → drop the Snowflake `mart_pitch_*` builds → re-run the audit; THAT is when credits should fall. **Run the 6/22 audit:** `uv run python scripts/ops/snowflake_cost_by_job.py --days 7` (credit-by-job via the E11.3 QUERY_TAG) + the Dagster+ usage CSV, vs the 6/18 baseline. _(Master-guide reconciliation: A2.15 lakehouse = E11.1-W1 [parallel window]; A2.16 Dagster-compute-reduction = delivered via E11.4 [schedules removed]; 30.3 = E12 ✅; 12.10′ Pinnacle sharp-money = ❌ no-edge, +0.0095 vs +0.01 gate; 12.3.5 ✅ done, residual uk/au region-widen budget-gated.)_ Cost-opt also unlocks the F5 (E2.0c) budget.

## Maintenance
This is the persisted mirror of the Trello ordering. Update it when a story ships, a beta request lands (it enters Track 2 via the E9 backlog), or the gate verdicts change (e.g. if E4.3 comes back no-edge, the H2H edge story closes and the app track leans fully into transparency/CLV surfaces). Re-window after the All-Star break (multi-sport + fantasy come into scope then).
