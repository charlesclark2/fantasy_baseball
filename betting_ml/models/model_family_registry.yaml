nfl_fantasy__season_projection__nfl_fantasy_nf1_5_v1:
  served_version: nfl_fantasy_nf1_5_v1
  level_model_version: nfl_fantasy_fastpath_v1
  ordering_model_version: nfl_fantasy_nf1_5_v1
  rookie_model_version: rookie_slot_curve_v1
  rookie_selection_status: incumbent
  rookie_shrink_lambda: 0.0
  rookie_statistically_selected: false
  interval_model_version:
    rookie: nf1_8
    veteran: nf1_9
    kdst: nf1_6
  scoring_contract_version: nf_c0e
  projection_source: nf1_5
  projection_season: 2026
  artifact_uri: s3://credence-prod-s3-api-cache/fantasy/nfl/2026/
  fallback_artifact_uri: s3://credence-prod-s3-api-cache/fantasy/nfl/2026/
  previous_served_version: nfl_fantasy_nf1_5_v1
  validation_report: 'Held at incumbent: the rookie-RB interval-floor breach is a
    coin-flip at n=148 (a perfectly calibrated band fails this floor 50% of the time),
    so the honest remedy is a power-derived floor for structurally-thin groups — carded
    independently — not moving λ or the floor. NF-D16''s lift is deferred, not abandoned.
    [PM decision, NF-D21, 2026-08-05, Charlie (operator/PM); disposition=CONSTRAINT_REFUSED.
    Supporting measurements: ablation_results/nf_g0_d21_governance_publish.md section
    4 (the refusal) and ablation_results/nf1_9_interval_revalidation.md (the standing
    floors at the served lambda=0).]'
  promotion_status: champion
  promoted_at: '2026-08-01'
  reviewed_by: 'Charlie (operator/PM) — NF-D21 disposition 2026-08-05 (genesis promotion:
    operator (PM), NF1.5b re-land)'
  notes: 'GENESIS entry (NF-G0, 2026-08-04): the state NFL fantasy was ALREADY serving
    when this registry was created. It has no true predecessor, so `fallback_artifact_uri`
    points at ITSELF and `previous_served_version` repeats its own version — rolling
    the incumbent back means staying on the incumbent, which is honest rather than
    a placeholder. The first real promotion (NF-D21) will overwrite both with genuine
    predecessor values at stage time. ⚠️ `rookie_selection_status: incumbent` is the
    CURRENT truth and NOT a claim about NF-D21: NF-D16''s recalibration is ratified
    but its serving flip is held OFF (`rookie_publish_policy.SERVING_ENABLED = False`)
    because the interval-floor gate refused it at lambda=0.5 — see ablation_results/nf_g0_d21_governance_publish.md.
    RESOLVED 2026-08-05: the PM ACCEPTED THE NULL. NF-D21 is CLOSED as CONSTRAINT_REFUSED
    — not parked, because a story left open pending a floor fix is exactly the pressure
    that would bias that floor toward clearing lambda=0.5 — so `rookie_selection_status:
    incumbent` is now the SETTLED state rather than a pending one. This entry stays
    the champion and no challenger was ever promoted: the first real artifact routed
    through this registry was REFUSED by its own gate, which is stronger evidence
    the governance layer works than a clean pass would have been.'
  model_family: nfl_fantasy
  target: season_projection
nfl_fantasy__weekly_projection__nfl_fantasy_w2b_v1:
  served_version: nfl_fantasy_w2b_v1
  base_model_version: nfl_fantasy_nf_w1_v1
  per_position_spec:
    QB: inj_zero_leg
    RB: inj_both
    WR: inj_both
    TE: inj_zero_leg
  feature_bundle: weekly_projection.FEATURES + injury_rate + injury_report (= weekly_projection_w2b.FEATURES_W2B;
    per-arm legs per spec)
  scoring_contract_version: nf_c0e
  fallback_artifact_uri: repo:quant_sports_intel_models/football/nfl/fantasy/ablation_results/nf_w1_weekly_bakeoff.json
  validation_report: 'NF-W2b bake-off SHIP x4 (2026-08-09): pure player-level lift
    vs the marginal-rate-carrying foil QB +0.098 (11/12) / RB +0.135 (12/12) / WR
    +0.126 (12/12) / TE +0.059 (12/12) CRPS/wk; PBO <=0.14, DSR >=0.993, BH-FDR x4;
    the permuted arm''s lift over base_rate NEGATIVE at all four positions (the NF-W2
    refusal clause now passes two-sided). Record: quant_sports_intel_models/football/nfl/fantasy/ablation_results/nf_w2b_injury_rate_bakeoff.md
    (''Reading the result'').'
  reviewed_by: Charlie (operator/PM) — Decision 1 = Option A (per-position certified
    winners), 2026-08-09
  notes: 'STAGED CHALLENGER (NF-W2b, 2026-08-09) — inert at serve by construction:
    served_entry() returns champion-status entries only and no serving path reads
    this registry (guard-tested two-sided in test_nf_w2b_staged_registry.py). PROMOTE/PUBLISH
    BLOCKED until ALL of: (1) NF-C6 Phase 2 (the weekly serving path does not exist
    yet); (2) NF-W0a stamped injury forward-capture arming the family LIVE in-season
    (dark feed => serve the base champion; shadow-2025 measured the dark tax); (3)
    pre-flip RETENTION snapshot + PROJECTION snapshot taken before any promote (run_nf_serving_board_snapshot.py
    / run_nf_w2b_projection_snapshot.py). Nothing weekly has ever served, so there
    is no previous served version; fallback_artifact_uri is the NF-W1 base champion''s
    validated record — the spec the dark-feed serving rule falls back to. per_position_spec
    is read from W2B.POST_FLIP_SPEC (guard-pinned to the validated artifact); the
    RB/WR tie is not a licence to re-select (Option A explicitly rejects uniform inj_zero_leg).'
  artifact_uri: repo:quant_sports_intel_models/football/nfl/fantasy/ablation_results/nf_w2b_injury_rate_bakeoff.json
  promotion_status: challenger
  generated_at: '2026-08-09T05:34:33.358208+00:00'
  staged_digest: 87b1669bac7e448bf5f9af38251026d3b1b54f9534432996aca0ee4c2ce6ba1c
  model_family: nfl_fantasy
  target: weekly_projection
