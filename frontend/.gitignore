.env.local

# E9.45 — NFL fantasy draft boards are a PAID surface, served via the gated
# /fantasy/nfl/* API from S3, NOT as public assets. Never re-commit them here.
/public/data/nfl-fantasy/

# E9.63 — Playwright output. Traces/screenshots/reports are per-run artifacts; CI uploads them.
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
