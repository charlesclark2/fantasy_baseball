# Plan spec — NCAAF-P3.3b (MINI tier: the update-cadence stamp — data-derived, never copy;
# + the team-fixture re-capture)
# Authored by PM 2026-09-04 per the P3.3 closeout ruling ①. Frontend-only unless the vintage
# field is missing from the payload (then a small additive Phase-A echo, same P3.3 pattern).
---
apiVersion: planspec.io/v1alpha1
kind: Goal
metadata:
  name: ncaaf-p3-3b
  vertical: ncaaf
  card: https://trello.com/c/dpQKTbYd   # P3.3 record; P3.3b card linked at kickoff
  status: DONE
spec:
  description: >
    Close the product-expectation gap P3.3 measured and flagged: the
    ratings, bands, and ranks move only on the Monday 06:00 roll-forward
    (NCAAF_ROLL_FORWARD_CRON), so from Saturday kickoff to Monday
    morning a team can win by 26 while its rating and ranks sit
    unchanged beside the win in its own schedule — which reads as a
    broken product rather than a weekly one. RULED FIX: a stamp on the
    team page (and any surface printing the rating/rank), DERIVED FROM
    DATA — "ratings as of <the served artifact's rating vintage> · next
    update <the roll-forward's next fire>" — never hand-written copy.
    A stamp computed from the artifact and the schedule survives bye
    weeks, cancelled openers, and a cadence change by construction; a
    sentence does not. Cadence CHANGE is refused for now (a
    roll-forward is a model fit, not a copy job). best_alpha=0; no
    rating, rank, band, or serving-value change; NCAAF stays free.
  acceptanceCriteria:
    - description: >
        THE STAMP'S TWO HALVES ARE BOTH DATA: (a) "as of" reads the
        rating VINTAGE carried in (or derivable from) the served team
        payload — if the payload does not carry it, add it as an
        ADDITIVE field echoed by the serving-write from the artifact's
        own content timestamp (the INC-41 lesson: content, never
        mtime), Pydantic-declared (E9.41), with the operator box-run +
        curl runtime gate for that small Phase-A leg before the page
        consumes it; (b) "next update" derives from the roll-forward
        schedule, not a hardcoded weekday string. NO OVERCLAIM: the
        stamp states when the ratings last absorbed games and when they
        next will — it never promises the next update will move them
        (a bye week moves nothing and the stamp stays true).
    - description: >
        PLACEMENT: beside the strength/rank block on the team page —
        the surface where the staleness misread happens — using the
        claim-copy conventions (no forecast language; the
        rank-with-range guard and the recommendation-language ban are
        untouched and stay green). If the game-board or standings
        surfaces print the same rating, they carry the same stamp
        through ONE shared component/owner — never a second wording.
    - description: >
        FIXTURE RE-CAPTURE (P3.3 open item, folded in): re-capture
        ncaaf-team-68.json / ncaaf-team-2449.json now that standings
        exist (the self-announcing clause "the capture has acquired
        standings" is expected to fire — retire it by re-capturing,
        never by weakening); capture RAW from the live API per the
        Task-0 pattern. The pair stays a played/unplayed contrast so
        the stamp renders under E2E in both states.
    - description: >
        Guards: E2E asserts the RENDERED stamp on both fixture states
        (a real vintage prints; a missing vintage renders a stated
        absence, never a fabricated date — absent-vs-null discipline);
        RED-proven with unique mutation anchors (a hardcoded date
        string must go red; a stamp surviving a vintage change must go
        red) + baseline-pass/NOT-SELECTED in any Python harness.
        Changelog entry (user-facing). Frontend auto-deploys on main —
        verify the LIVE page post-promotion by content (the E9.60/#1075
        stale-head lesson: merge status and green CI are not evidence;
        the string on main is).
---
apiVersion: planspec.io/v1alpha1
kind: OverlapCheck
metadata:
  goalRef: ncaaf-p3-3b
spec:
  boardSearch:
    queries:
      - 'trelloSearch cards: "cadence" / "team stats" / "P3.3" (board Credence Sports, 2026-09-04)'
    duplicates: []
    adjacent:
      - "NCAAF-P3.3 (Done, live) — the parent; its rank-with-range guard, block wordings, and the #1081 cross-block guard stay green; this adds a stamp, it re-words nothing."
      - "Odds :00-collision card (lEgL0V5D, To Do) — market lane; disjoint; not touched."
      - "Operator Follow Ups card (Wyu3qZ80) — the Mon 9/7 roll-forward check; if that check FAILS, this story's 'next update' half is printing a schedule that isn't firing — coordinate through the PM, don't ship a stamp against a dead chain."
      - "NF-INJ4b / NF-INJ2c (IN FLIGHT) — disjoint verticals; changelog week-block shared (rebase)."
  triedLedger:
    files:
      - "P3.3 closeout (the Monday-cadence flag, the no-overclaim warning, the four-blocks-one-screen lesson — the stamp is a fifth voice on that screen; word it to compose)"
      - "frontend/components/ncaaf/team-page.tsx + the serving payload contract (does a rating vintage field exist? verify, don't assume — the spec-premise rule)"
      - "NCAAF_ROLL_FORWARD_CRON (the schedule the 'next update' half derives from)"
    recordedNulls: []
  supersedes: []
  dependsOn:
    - "dev→main merged (#1081 live) — do not stack a new frontend change behind an unpromoted one"
  feedsInto:
    - "The Sunday-morning reader seeing a weekly product behaving as designed, not a broken one; every future rating surface inheriting the stamp owner"
---
apiVersion: planspec.io/v1alpha1
kind: Plan
metadata:
  name: ncaaf-p3-3b-plan
spec:
  goalRef: { name: ncaaf-p3-3b }
  graph:
    nodes: []   # MINI tier — verify vintage field -> (optional additive echo + runtime gate) -> stamp component -> fixtures -> guards -> live verify
  closeout:
    verdict: >
      SHIPPED_WITH_ONE_HALF_REFUSED. The "as of" half is delivered in full and is
      data-derived end to end: an ADDITIVE Pydantic field on `NcaafTeamStrength`,
      echoed by the serving write from the strength artifact's own Delta commit
      timestamp (INC-41 — content, never an mtime), rendered by ONE shared
      component beside the strength/rank block. The fixture re-capture is done and
      the self-announcing standings clause was retired BY RE-CAPTURING.
      ⛔ THE "NEXT UPDATE" HALF IS REFUSED AS SPECIFIED, because its premise is
      false: `NCAAF_ROLL_FORWARD_CRON` does not re-fit P1.2, so printing its next
      fire would promise a ratings refresh that job cannot deliver — the exact
      overclaim criterion (a) forbids. The surface states the absence instead, and
      per this spec's own coordination rule that decision goes to the PM.
    followUps: >
      ① ⭐⭐ THE SPEC'S PREMISE IS FALSE, AND SO IS THE CLAIM IN #1081's COMMIT
      MESSAGE ("the P1.2 strength fit rolls forward weekly"). Measured 2026-09-04,
      three ways that agree. (a) STRUCTURALLY IMPOSSIBLE: the roll-forward job
      ingests `ROLL_FORWARD_SOURCES` and rebuilds the dbt marts;
      `team_strength_week` is not in that list and CANNOT be, because
      `ingest/sources.py` asserts at import that every entry is a free CFBD source
      and the ratings table is a derived model output written by
      `run_team_strength`. (b) STATED IN THREE PLACES: the roll-forward job's own
      docstring ("P1.2 must be re-fit ... those are the OPERATOR steps ... not
      wired here, because P1.2 is a once-per-season refit, not a weekly one"), the
      snapshot job's ("THE ONE QUALITY PREREQUISITE (operator, not code)"), and
      BOX_OPERATIONS §10 ("the season's ONE-TIME P1.2 re-fit ... operator laptop
      steps"); `grep run_team_strength pipeline/` returns docstrings and no call.
      (c) MEASURED ON THE LAKE, TWO-SIDED: `ncaaf/derived/team_strength_week` last
      committed 2026-08-18T06:16:36Z (v67), while `ncaaf/raw/games` and
      `ncaaf/raw/talent` last committed 2026-08-31T13:00:51Z / 13:01:13Z — Monday
      06:00 PT. THE ROLL-FORWARD FIRED AND THE RATINGS DID NOT MOVE: the chain is
      alive AND it does not touch them. ⇒ PM DECISION: either schedule a P1.2
      re-fit (then add its schedule NAME to
      `ncaaf_ratings_vintage.RATINGS_REFRESH_SCHEDULES` — one line, and the stamp
      starts printing the date with no frontend change), or ratify the stated
      absence. ⛔ Do NOT register the roll-forward: a guard refuses it by name,
      because every surface signal (weekly, Monday 06:00, NCAAF-named) points at
      it and the false claim is still in the git history.
      ② 🚨 BIGGER THAN THE STAMP, AND THE REAL FIX FOR THE P3.3 GAP: the served
      2026 ratings are FROZEN at the pre-season prior and nothing scheduled will
      move them. Both captured teams serve `as_of_week 1` with ONE week in the
      series on 2026-09-04 — six days after the 08-29 opener, one of them beside a
      completed game. So the Saturday-to-Monday staleness P3.3 measured is not a
      weekend gap; absent an operator re-fit it is the whole season, and the ranks
      and bands are the pre-season ones too. The stamp makes that VISIBLE and
      honest, which is what this story could do; it does not make the ratings
      current. Recommend carding "schedule the in-season P1.2 re-fit" as its own
      spec — it is a model fit, which this spec explicitly refused to change.
      ③ #1081's commit message will mislead the next reader (it is where this
      spec's premise came from). Not editable; the correction is recorded in
      `ncaaf_ratings_vintage.py`, in `test_ncaaf_p3_3b_ratings_stamp.py`, and in
      this file. A doc-level note on the parent card would help.
      ④ ⏭️ OPERATOR RUNTIME GATE (Phase-A leg, required before the page consumes
      it) + the dependsOn note: #1081 is merged to `dev` but NOT to `main`, so
      this PR stacks behind an unpromoted frontend change. The dev→main promotion
      will carry both; verify BOTH strings on main by content.
      ⑤ The Mon 9/7 coordination hazard this spec named is MOOT for this story:
      the roll-forward chain is alive (measured), and the stamp does not cite it
      either way, so a dead chain could not make the stamp print a schedule that
      is not firing. The 9/7 check still matters for the `talent` leg (NCAAF-RF1).
      ⑦ ⭐ THE DEPLOY ITSELF PRODUCED A FINDING, AND THREE AFFORDANCES WERE BUILT FROM
      IT — THIS IS THE JUSTIFICATION FOR THAT WORK, RECORDED SO THE PM DOES NOT HAVE
      TO TAKE IT ON TRUST. On 2026-09-05 the story deployed correctly (box CD, box
      writer, Lambda, frontend) and the acceptance nevertheless took three rounds and
      one WRONG diagnosis. Cause: every `/ncaaf/*` route answers
      `cache-control: public, s-maxage=900, stale-while-revalidate=3600` (G100-D1's
      public cache rule — the single biggest lever on the cost of a Saturday, so it is
      correct and is not going away). A single plain read of a team payload showed the
      new fields absent; that read as "the Lambda was not deployed", which was FALSE —
      the deploy was fine and the shared cache had not turned over. Isolating it needed
      a direct S3 read of the served blob to prove the writer had produced the field.
      ⇒ A CACHED READ AND A FAILED DEPLOY ARE BYTE-IDENTICAL, which is the repo's
      standing "a check whose failure state is indistinguishable from its healthy state
      has not verified anything" (G100-D1), arriving through the verification path
      rather than the product. Three gaps were measured, not supposed, and each is now
      closed: (a) the box run report was SILENT on the ratings vintage the run existed
      to gate — the writer computed it and only logged it, so a clean-looking
      acceptance JSON could not answer the question and the operator read S3 instead
      (E11.30 in miniature); (b) the `@live` suite — the ONE place that asks "does the
      SERVER still send X" — used plain, cacheable reads and had NO team-page clause at
      all, so the contract that had just shipped was the one thing it could not check;
      (c) "is the deployed build the one I think it is?" had no cheap answer, because
      this Lambda has no CD and `deploy.sh` packages the CURRENT WORKING TREE with no
      ref pin — G100-C0-MFA recorded that exact lesson once already, so this is its
      SECOND instance and now a permanent affordance rather than a per-story one.
      ⛔ The build marker is deliberately NOT a Lambda env var: that needs
      `update-function-configuration --environment`, which REPLACES the whole Variables
      map while `deploy.sh` only calls `update-function-code` — the E9.8-P2 wipe, on the
      day money started moving. It is baked into the package instead, and a guard
      refuses the env-var form by name. All three shipped in ONE follow-up PR with 11
      clauses and a 10/10 RED proof; that proof caught one of its own guards VACUOUS
      (the team-payload clause matched a substring that also appears inside an
      assertion's failure message), which is the fourth instance of that family in this
      story and the reason the harness exists.
      ⑥ NOT APPLICABLE, stated rather than silently omitted: the acceptance
      criteria ask for "baseline-pass/NOT-SELECTED in any Python harness". There is
      no §0.5 selection harness in this story (no model, no bake-off, no arms), so
      there is no baseline or NOT-SELECTED verdict to report about a SELECTION.
      The two controls of that name ARE implemented, in the sense that applies
      here: `ncaaf_p3_3b_red_proof.py` proves every named clause green on unbroken
      source before breaking it, and refuses to score a clause pytest did not
      select. 9/9 RED; the frontend harness 5/5 RED.
