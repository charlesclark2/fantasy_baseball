# Plan spec — NCAAF-P3.1b (MINI tier: serve the T-1 market snapshot; best-effort pre-8/29)
# Authored by PM 2026-08-25 per P3.2 closeout ⑤ (PM-funded — the highest-value non-blocking item).
---
apiVersion: planspec.io/v1alpha1
kind: Goal
metadata:
  name: ncaaf-p3-1b
  vertical: ncaaf
  card: https://trello.com/c/ikIt1qcm   # P3.2 record thread; P3.1b card linked at kickoff
  status: DONE
spec:
  description: >
    Serve the T-1 odds snapshot as the market line. The T-1 capture has
    been live since P0.6c (NCAAF_ODDS_CAPTURE_T1=1, schedules RUNNING,
    2020-2025 backfilled) but payloads._market() reads only close_* and
    hardcodes MARKET_SOURCE_CLOSE — so the one snapshot that is genuinely
    PRE-kickoff, the honest comparator for a pre-kickoff projection, is
    stored and ignored, and every opener card says "unavailable" when a
    real Friday line could stand beside the model. One serving change in
    P3.1's payload module + a deploy.sh Lambda deploy. best_alpha=0;
    transparency framing unchanged — a market line is context, never a
    pick. ⏰ Best-effort pre-Sat 8/29; if any gate wobbles, it ships
    post-opener instead — the deadline never relaxes a check.
  acceptanceCriteria:
    - description: >
        _market() prefers the T-1 snapshot when present and pre-kickoff,
        with market.source stamped honestly ("t1_snapshot" vs "close") and
        market.as_of carrying the snapshot instant — a reader can always
        tell WHICH line they are seeing and from when. Fallback order and
        the absent state (status="unavailable" + machine-readable reason,
        market_read_failed discriminator) unchanged. Response-model fields
        ADDITIVE only (NF-C0); every new field declared on the Pydantic
        contract (E9.41).
    - description: >
        LEAKAGE GUARD: a served T-1 line must be strictly pre-kickoff for
        its game (assert the snapshot instant < kickoff on every attached
        row; a violating row attaches NOTHING and logs loudly). Never a
        post-kickoff line labeled as pre-game.
    - description: >
        The client (P3.2 surface) already renders model-vs-market and the
        absent state; verify it renders the T-1-sourced line correctly
        WITHOUT client changes — if any client change is needed, STOP and
        flag (that's the P3.9 session's lane or a follow-up, not this one).
        Verify the as-of/source is surfaced or gracefully ignored per the
        existing contract.
    - description: >
        RUNTIME GATE: real box serving-write run + deployed-Lambda curls
        showing a real game payload carrying the T-1 line with source +
        as_of (OPERATOR-RUN steps, paste-ready, per the standing rule);
        the P3.1 positive-control-per-key-shape lesson applies — prove the
        date-keyed read specifically. deploy.sh is the ONLY deploy path;
        merged never means served.
    - description: >
        Guards RED-proven both ways (a post-kickoff snapshot must be
        refused; a valid T-1 must attach; absent stays absent). Changelog
        entry only if the user-visible line actually appears (declaration
        never outruns production).
---
apiVersion: planspec.io/v1alpha1
kind: OverlapCheck
metadata:
  goalRef: ncaaf-p3-1b
spec:
  boardSearch:
    queries:
      - 'trelloSearch cards: "T-1" / "odds" / "market" NCAAF (board Credence Sports, 2026-08-25)'
    duplicates: []
    adjacent:
      - "P3.1 (Done) — this edits its payload module only; the serving-write op, keys, and routers are untouched."
      - "P3.9 bundle (in flight, frontend) — disjoint files; if a client change proves needed, flag rather than reach into its lane."
      - "MLB-ODDS1 — MLB odds mart; disjoint store, but the SAME ingestion-honesty instincts apply (stamp source + as-of)."
      - "The market-line opener framing ruling (render stated absence) — this story UPGRADES that ruling where a T-1 line exists; the absence rendering stays for games with no snapshot."
  triedLedger:
    files:
      - "P3.2 closeout ⑤ (the finding + capture-state evidence); P0.6c record (T-1 capture live, credits measured); app/backend payloads module (MARKET_SOURCE_CLOSE hardcode)"
    recordedNulls:
      - "VAL1 CLV null untouched — a served market line is transparency, not an edge claim; no vs-close performance reading is produced here."
  supersedes: []
  dependsOn: []
  feedsInto:
    - "The opener cards carrying a real model-vs-market comparison (the brand directive's transparency asset)"
---
apiVersion: planspec.io/v1alpha1
kind: Plan
metadata:
  name: ncaaf-p3-1b-plan
spec:
  goalRef: { name: ncaaf-p3-1b }
  graph:
    nodes: []   # MINI tier
  closeout:
    verdict: SHIPPED_AND_SERVING
    verdictNote: >
      ⭐ AMENDED 2026-08-29 AFTER THE RUNTIME GATE. The line is LIVE. Measured on the
      deployed Lambda for the 8/29 opener slate: 8 of 8 games carry a real spread and
      total, market_lines_by_source={"odds_api_live": 8}, market_reasons={} (zero
      leakage refusals), every as_of strictly pre-kickoff. The original verdict is
      preserved verbatim below rather than rewritten — it was correct when written,
      and what changed is the world, not the reading.
      WHAT CLOSED IT was NOT followUp ①'s proposed manual Friday capture. It was the
      NCAAF-ODDS-LIVE story that followed: an ahead-of-kickoff live /odds feed
      (sports_ncaaf_odds_live_schedule, tiered 6h baseline / hourly inside 24h,
      measured 3 credits per capture against the /historical path's 30 per kickoff
      window). The premise-gap ① named was real; the remedy it proposed was the
      expensive one. Changelog entry added 2026-08-29, and only now — the
      declaration did not outrun the production.
      SUPERSEDED ORIGINAL (2026-08-25, verdict SHIPPED_CODE_NO_USER_VISIBLE_LINE_YET):
      "Every acceptance criterion is met in code and RED-proven, and the change is
      verified end-to-end against real captured data (2025). It produces NOTHING on
      the 2026 board today: the lake holds ZERO 2026 odds rows of either kind, so
      the opener cards still read 'No market line' unless an operator runs the paid
      capture on Fri 8/28. NO changelog entry was added — the user-visible line has
      not appeared, and a declaration must not outrun its production."
      Full readout:
      quant_sports_intel_models/football/ncaaf/ablation_results/ncaaf_p3_1b_market_source_readout.md
    followUps: >
      ① ⭐ THE PM PREMISE IS ONE CAPTURE-RUN SHORT, and this is the item that decides
      whether the opener carries a line. Measured: season 2026 has ZERO rows in
      odds_ncaaf_historical (2020-2025 carry 9,000+ close and 9,845 T-1 rows, so the
      backfill is real). The capture is the paid /historical CATCH-UP (P0.6b decision
      (A)), it only fetches a kickoff past its snapshot instant, and
      sports_ncaaf_odds_capture_schedule fires MONDAYS (0 8 * 8-12,1 1). The 8/29
      opener's T-1 instant is Fri 8/28; the next scheduled fire is Mon 8/31, after the
      games. ⇒ an OPERATOR-RUN capture on Fri 8/28 (T-1 only, ~1 week of kickoffs,
      command in the handoff) is what turns this story into a visible line. PM call:
      run it, or accept the opener ships with the stated absence and the line appears
      from week 2 onward. Neither is a defect; both should be a decision.
      ② ⭐ A GENUINE MIS-JOIN IN P1.4's CLV STAGING MART, found by this story's leakage
      guard firing on real data and DELIBERATELY LEFT UNFIXED (it is a mart P1.4 owns
      and it touches the vs-market/CLV evals, not just serving). build_clv_staging
      matches Odds-API team names to CFBD names by PREFIX with NO kickoff-proximity
      bound, so a rematch attaches the WRONG game's line: 8 of 876 2025 games (0.9%)
      carry a snapshot MORE THAN A DAY after their own kickoff (Texas Tech-BYU's 11/08
      game carries a 12/06 snapshot = the Big 12 championship rematch; Colorado-Arizona's
      11/01 carries 11/23). Those rows are inside the population VAL1/VAL3 read. Suggest
      a new spec: bound the join by kickoff proximity, then re-read whether any recorded
      vs-market figure moves.
      ③ A CFBD-vs-ODDS-API KICKOFF-CLOCK DISAGREEMENT, measured and worth knowing before
      anyone reads a coverage number: 64 of 876 2025 close snapshots (7.3%) sit 5-55 min
      after the CFBD startDate while being pre-kickoff by the BOOK's commence_time. The
      guard refuses them (fail-closed — at that distance a "close" may be an IN-PLAY
      price by CFBD's clock). The T-1 leg is structurally immune. Net cost to the served
      board over the whole 2025 season: 5 games of 876 (0.57%) refused, and those five
      are the ② mis-joins. Not an action item on its own; it IS the reason a close-line
      coverage figure will read ~7% lower than a naive count.
      ④ THE CAPTURED E2E FIXTURE NOW PREDATES THE CONTRACT. ncaaf-slate-2026-08-29.json
      is verbatim production bytes and does not carry the newly-declared market.as_of.
      Its round-trip test was RE-ANCHORED to tolerate a post-capture field only when it
      validates to null, and to NAME what it tolerated. Closing it properly is a
      re-capture through capture-fixtures.mjs AFTER the Lambda deploy — the fixture
      cannot lead the wire. Checklist item on the parent card.
      ⑤ NO E2E COVERS A T-1-SOURCED LINE WITH REAL BYTES. The market fixture's three
      rows are real 2025 CLOSES (that is what P3.2 recorded), so the T-1 render is
      covered by the mechanical no-client-change proof (the panel reads no source/as_of;
      two payloads differing only in source are identical where the panel reads) rather
      than by a browser assertion. Recording three real T-1 rows beside the closes is a
      ~10-minute follow-up once someone is at a laptop with lake creds
      (build_clv_staging(min_year=2025, with_t1=True)). Low priority — the render is
      provably source-independent — but it would close the last inference.
      ⑥ THE SERVING CHANGE IS NOT DEPLOYED BY MERGING. deploy.sh is the only path to the
      API Lambda; the box also needs the merged code for the serving WRITE. Both are
      operator steps in the handoff, and the runtime gate is only answerable after ① has
      put a 2026 line in the lake.
      ⑦ ⭐ ADDED 2026-08-29 — RESOLVED THE SAME DAY, option (b) built (see the tail of
      this list). ORIGINAL TEXT PRESERVED: THE CAPTURE IS HOURLY, THE
      RE-SERVE IS DAILY — so the SERVED as_of can trail the LAKE by up to 24h, and on a
      kickoff day that is the whole point of the line. sports_ncaaf_odds_live_schedule
      ticks hourly inside 24h of kickoff (that is what "the line is moving" means), but
      the served payload only changes when write_ncaaf_serving_store runs, and
      sports_ncaaf_serving_write_schedule is DAILY 06:00 PT (NCAAF_SERVING_WRITE_CRON
      "0 6 * 8-12,1 *"). ⇒ every line that moves after 06:00 PT on a Saturday reaches
      the reader only AFTER the games. This is NOT a defect of either story — each
      schedule is correct for the cadence it was written against, and the served line is
      HONEST at every moment because as_of is stamped (a reader can always see the line
      is from this morning). It is a CADENCE MISMATCH between a consumer and its newly
      faster producer — the INC-25 ordering family, on the refresh axis rather than the
      build axis. Three options, a PM call: (a) leave it — "as of this morning" is a
      defensible product, costs nothing, and the stamp keeps it honest; (b) add an
      in-window re-serve tier mirroring the capture's own tiering (the write is free:
      two Delta reads and a few dozen small puts, no credits, no warehouse); (c) chain a
      re-serve off the live-capture op when it actually captured, which couples the two
      and is the least reversible. Suggest (b) if the market line is meant to be a live
      comparator, (a) if it is meant to be a daily reference point.
      ⑦-RESOLUTION (operator chose (b), 2026-08-29): the re-serve is now HOURLY with the tier in
      the OP (`sports_ncaaf_serving_write_schedule` "20 * * 8-12,1 *" PT +
      `write_ncaaf_serving_store.should_reserve`). Four properties make it safe, each pinned by a
      guard and RED-proven 17/17: (a) ONE window — `should_reserve` shares the capture's
      `in_dense_window` BY IMPORT and passes no override, so tuning one cadence moves both (E9.61);
      (b) MONOTONE — the 06:00 PT baseline is preserved exactly, so every write the old daily
      schedule made still happens and the dense tier only ADDS refreshes; the guard pins the
      baseline against a LITERAL 6 (the retired cron's hour, a fact about history) rather than
      against the constant under test, which would have made the whole claim vacuous; (c) FAILS
      OPEN — an unevaluable tier falls through to the write, because a gate that failed closed
      would turn a transient lake read into a silently frozen board (NF1.7 (a) read the right way
      round: here "healthy" would be "skip", so the safe verdict is to serve); (d) the CHAINED
      `ncaaf_serving_write_after_snapshot_op` is deliberately NOT gated (INC-25). The tier reads
      kickoffs from the SNAPSHOT table, never CFBD — a paid key behind a HALT-tier serving write
      would let a missing credential stop the board refreshing, which is strictly worse than the
      staleness being fixed. ⏱️ Minute :20 vs the capture's :00 is the ordering mechanism between
      two separate jobs; it is a WEAKER guarantee than a graph edge (a capture running >20 min is
      still missed) and deliberately so — the write is idempotent and the next hour heals it.
      ⑧ PM ADDENDUM (1), `market.status` MUST STAY BINARY — SATISFIED BY CONSTRUCTION AND NOW
      PINNED. The cadence work changed WHEN the store is refreshed, not what `_market` can return,
      so no third status was introduced. Three guards now hold it that way (server emits exactly
      {available, unavailable}, read off the AST at MODULE scope because the unavailable branch is
      emitted by a helper `_market` delegates to; the TS union declares exactly those two; and the
      named call sites still test the field, so the list cannot rot silently). ⚠️ ONE CORRECTION TO
      THE ADDENDUM: there are THREE client call sites, not two — `game-card.tsx:133` (spread),
      `game-card.tsx:134` (total) and `market-comparison.tsx:119` (the panel's own gate). All three
      are named in the guard so a future story does not have to re-derive the set.
      ⑨ ⛔ PM ADDENDUM (2) CANNOT BE EXECUTED AS RULED — ITS PREMISE HAS EXPIRED, AND THE RULING
      DEPENDS ON IT. The addendum says "prod measured 8/8 unavailable on opener day", so a
      re-capture now would keep the fixture all-absent and owe zero re-anchoring. That was the
      PRE-DEPLOY reading. Measured against prod 2026-08-29 while writing this: 8 of 8 games are
      `available` / `odds_api_live` / `as_of 2026-08-28T01:38:24Z`. The live feed the operator
      enabled has already closed the window. ⇒ a re-capture NOW yields an all-AVAILABLE slate,
      which trips BOTH tripwires (`test_ncaaf_p3_2_surface.py:352`,
      `ncaaf-games.spec.ts:422`) — precisely the outcome the ruling was written to avoid. By the
      addendum's OWN second clause we are therefore in the ADD branch: a realistic-Saturday fixture
      is ADDED beside the all-absent one, never replacing it. That was NOT done here, deliberately:
      it is a fixture + mock-mode + spec change in the frontend lane the addendum itself reserved
      for the next story (item (1)), and it is NOT time-critical — every remaining Saturday
      reproduces these conditions, so the opener's bytes are not irreplaceable. Needs a PM ruling
      on which story owns the ADD. NOTE the existing `ncaafSlateWithMarket` fixture is SYNTHETIC
      (the ledger says so: "GENERATED, because at capture time there was nothing to capture") —
      real market bytes now exist for the first time, so the ADD would also retire that inference,
      which is followUp ⑤.
