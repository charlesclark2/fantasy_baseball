# Plan spec — NF-ROS1b (FULL tier, MODELING: the interval redesign — NF-ROS1's certified NO,
# answered at the mechanism it named)
# Authored by PM 2026-09-17. NF-ROS1 proved the UPDATE (Bühlmann rate×availability) beats the
# incumbent at every position, 6/6 folds — and refused to ship because the location-shift
# residual interval fails PIT calibration by SHAPE at every position. This successor changes
# ONLY the interval, keeps every bar, and is a fresh §0.5 registration, not a retry.
---
apiVersion: planspec.io/v1alpha1
kind: Goal
metadata:
  name: nf-ros1b
  vertical: nfl_fantasy
  card: https://trello.com/c/PMVQXY99   # ROS1's record; ROS1b card linked at kickoff by PM
  status: IN_PROGRESS
spec:
  description: >
    Certify an ROS value by fixing what NF-ROS1's refusal named: the
    interval cannot represent a player-specific chance of scoring
    zero, and its spread does not scale with the projection (measured:
    29.9% of top-third projections carried P5=0 while only 5.2%
    scored zero; lowest PIT decile ~2x full; the INCUMBENT's interval
    fails identically — shape, not overfitting). ⛔ FRESH REGISTRATION
    DISCIPLINE: the new interval family is declared FROM THE
    MECHANISM (a hurdle form — player-specific P(zero) x a
    conditional spread that scales with the projection — or
    ratio-scale residuals; the registration names ONE family and its
    parameters), never reverse-engineered from ROS1's fold numbers;
    the POINT form (eb_rate_avail as registered in ROS1) carries over
    unchanged as the location; every ship-bar clause C1–C9 carries
    over at the SAME values — this is not a softer retry of a refused
    bar, it is a different instrument measured against the same bar
    (the fix-the-spec honesty clause applies: ROS1's point-estimate
    superiority is an already-measured result — this story's fresh
    confirmation target is the INTERVAL, and C9 is where it lives or
    dies). ⛔ §13.4 stands: the PIT atom rule is correct and is not
    re-repaired. best_alpha=0; a second refusal is an acceptable,
    reportable outcome.
  acceptanceCriteria:
    - description: >
        §0.5 REGISTRATION before any fitting, inheriting ROS1's by
        reference and declaring only the deltas: (a) the interval
        family + parameters, from the mechanism; (b) the
        roster/injury-status-at-week-k covariate question DECIDED at
        registration (ROS1 noted the served prior has it, the
        historical prior does not — declare in or out with the
        train/eval asymmetry addressed, never added mid-run); (c) the
        two_pt term-set rule carried (score on the evaluated term
        set); (d) the same walk-forward (2020–2025, k=1–12, CRPS on
        the 19-level grid), same anchors + matched foil + width
        degenerates, same C1–C9 including PIT ≤ 0.05 and the
        power-floor; (e) reproduction pins; (f) the name-join rung
        TESTED on the 2026 board's 81 synthetic rookie ids this time
        (ROS1's followUp 4 — it has never run on real data). classify
        _null usage per ROS1's lesson: a null blocked by a binding
        calibration clause is reported as CONSTRAINT-bound, with the
        classifier's raw text kept but not acted on (the platform
        binding_clause improvement is carded separately — do not
        build it here).
    - description: >
        RUN + REPORT with ROS1's honesty mechanics verbatim:
        anchors scored every run; collapse-to-prior flagged as a tie;
        per-position verdicts; the decisive run reproduced at 0.0
        from committed code; RED proof with every-test-can-fail
        demonstrated (ROS1 caught two of its own vacuous guards —
        assume yours has some too and hunt them); runtime honesty —
        PROFILE before assuming the run fits in-session (ROS1's
        vectorization didn't help; two runs crossed the 2-minute
        line), and hand >2-min runs to the operator paste-ready.
    - description: >
        ON CERTIFICATION (any position clears all bars): build node
        4 exactly as ROS1's registration §8 specifies — the published
        artifact at fantasy/nfl/ros/<season>/<week>/{manifest,
        players}.json, contracted FROM BIRTH (validated dump +
        write-gate + served-bytes assertion + executing entrypoint
        smoke; the 0917B pattern), with the three distinguishable
        absences (not_certified / position_not_evaluated /
        no_preseason_prior), certified positions only, waiverValue
        only if its own registration clears, ROS stat line through
        STAT_FIELD (paid substrate — the derived paid-set diff comes
        to the PM before merge). The recurring publish cadence is
        DECLARED but its schedule enablement is an operator step.
        ON A SECOND REFUSAL: close as certified NO with the
        mechanism named, consumers keep their absences, and the PM
        decides whether a third form is worth funding — no in-story
        iteration past the registered family (E2.1-r).
    - description: >
        Standing: worktree; PR -> dev; no serving-page code; no
        WVR1/trade surface changes (they consume the artifact in
        their own stories); no Trello; roadmap frozen; >2-min
        operator paste-ready; findings to closeout.followUps; the
        weekly model's outputs remain out of bounds absent explicit
        registration; D/ST stays position_not_evaluated (team grain
        — its path is the ACC1/team-feed lane, not this story).
---
apiVersion: planspec.io/v1alpha1
kind: OverlapCheck
metadata:
  goalRef: nf-ros1b
spec:
  boardSearch:
    queries:
      - 'trelloSearch cards: "ROS" / "interval" / "calibration" (board Credence Sports, 2026-09-17)'
    duplicates: []
    adjacent:
      - "NF-ROS1 (Done, PMVQXY99) — the parent refusal; its registration, harness, anchors, and §13 findings are the inheritance; its point-form result is already-measured, never re-claimed as fresh."
      - "NF-WVR1 / trade engine — consumers; their stated absences lift only on certification; the never-substitute-fpPpr rule stands."
      - "NF-WK-ACC1 (IN FLIGHT) — disjoint (realized-scoring lane); shares STAT_FIELD — coordinate merge timing on any map change."
      - "Serving-writer census (cg5FRtch) — node 4, if built, is born contracted and never joins the uncontracted list."
      - "PLAT-CVP3 / cv_power binding_clause (ROS1 followUp 6) — platform improvement, separately triaged; not built here."
  triedLedger:
    files:
      - "ablation_results/nf_ros1_preregistration{,_amendment_1,_amendment_2}.md + §13 (the refusal's mechanism, the PIT-repair rejection, the runtime findings)"
      - "nf_ros1_walkforward.json (the before-side; the incumbent's identical C9 failure)"
      - "ros_value.py + run_nf_ros1.py (the harness this story extends; hash-pinned priors)"
  recordedNulls:
    - "Softening C9 (or any bar) — refused ex ante; the successor changes the instrument, never the bar."
    - "Re-repairing the PIT atom rule — §13.4, tested on synthetic truth, stands."
    - "Reverse-engineering the interval family from ROS1's fold numbers — the family comes from the mechanism."
  supersedes: []
  dependsOn:
    - "Nothing in flight; the stats ingest is verified running (ROS1's premise check: cron fire at 15:30:26Z)"
  feedsInto:
    - "WVR1's value column + cross-position waiverValue; the trade engine's core arithmetic; the first certified ROS artifact (node 4, contracted from birth); possibly the season board's own interval honesty (the incumbent fails C9 identically — a finding the board's owners inherit)"
---
apiVersion: planspec.io/v1alpha1
kind: Plan
metadata:
  name: nf-ros1b-plan
spec:
  goalRef: { name: nf-ros1b }
  graph:
    nodes:
      - id: 0-spec-commit
        desc: "Commit this spec (status IN_PROGRESS) before any other work."
      - id: 1-registration
        desc: "Delta registration: interval family from the mechanism; covariate decision; name-join test plan; bars carried at same values. Report to PM when committed."
      - id: 2-fit-evaluate
        desc: "Walk-forward per registration; profile first; >2-min runs to operator; decisive run reproduced at 0.0."
      - id: 3-verdict
        desc: "Per-position certification against C1–C9; a second refusal closes honestly with the mechanism named."
      - id: 4-artifact
        desc: "ONLY on certification: the §8 artifact, contracted from birth; paid-set diff to PM; cadence declared, enablement operator-gated."
      - id: 5-closeout
        desc: "Verdict + tables + the consumer unlock state (WVR1/trade) into closeout.followUps."
  closeout:
    # PM acks 2026-09-18: paid set (ack 2) and the R3 cadence supersession (ack 3) are GRANTED and
    # recorded in amendment 4.
    # Ack 1 READ IN 2026-09-18: RB-rookie C9 = 0.0810 > 0.05 -> STOP_TO_PM (prereg §15); the spread
    # read BROAD (§16) and the PM ruled disposition (ii) — rookie rows are a stated absence,
    # implemented in §17 (184 certified RB rows on the live build, was 197).
    # Verdict flips to SHIPPED-with-the-stratum-carve-out at the PM's acceptance of the final
    # recap, not before.
    # Proposed: SHIPPED (RB certified) · QB/WR/TE/K certified NO (CONSTRAINT_REFUSED).
    verdict: null
    followUps: >
      Findings land HERE. PM triages into: checklist item on the parent card |
      merge into an existing card (name it) | a new spec. Worker sessions never
      create Trello cards.
    results: |
      Record: ablation_results/nf_ros1b_preregistration.md §13 (+ amendments 1-3).
      Decisive run b8d98343 (operator), reproduced at 0.0; ROS1's full record
      reproduced at 0.0 on its final code (closes ROS1 §13.6's open item).
      RB CERTIFIED on C1-C9 (lift +1.53, 6/6, DSR 0.995, cov80 0.859/0.780,
      PIT 0.005 vs 0.096 under ROS1's construction on the same point).
      C9 passes QB/RB/WR/TE (0.034/0.005/0.009/0.008). Refused: QB C8a;
      WR C4+C8a; TE C4+C8a; K C2+C3+C4+C9. Field PBO 0.0.
      Consumer unlock state: RB ROS values exist in the node-4 artifact
      (fantasy/nfl/ros/<season>/<week>/) once published post-merge and the
      flag is enabled; every other position is a stated absence, so WVR1's
      value column and the trade engine unlock for RB only, and never as a
      cross-position ranking input (R4). waiverValue stays null
      (waiver_value_not_certified: §7 needs >= 2 certified positions).
    followUpItems:
      - "① (PM: platform, with binding_clause) C8a refuses QB/WR/TE because an MSE-fitted peeking oracle is judged on CRPS (the pre-registered ROS1 §5 divergence, made material by the hurdle halving the C1 lift the materiality bar scales with). Should a CRPS-judged arm be floored by a CRPS-fitted oracle (NCAAF-P2.5 (b))? A registration question for any successor."
      - "② (PM: platform, with binding_clause) cv_power.classify_null self-computes Sharpe/V: at QB its text says 'DSR alone needs 8 folds' beside a PASSING registered DSR 0.994."
      - "③ (story card; fund only if a consumer needs K) K refused again under the hurdle (PIT 0.082 -> 0.057, 4/6 folds). Mechanism note: not the zero atom (kickers realize zero on 0.9% of top-tercile rows) — likely the conditional-spread shape at a low-variance position."
      - "④ (followUp) Nowakowski: the name-join rung misses a TE-on-the-board / FB-on-the-realized-line player (FB normalizes to RB). Position-vocabulary disagreement, not a name failure. Served as join_unresolved only if TE ever certifies."
      - "⑤ (PM-owned) bare TOKEN env var is swallowed by delta-rs as the S3 session token (CLAUDE.md landmine added; s3io.storage_options fix carded by PM)."
      - "⑥ (PM-owned) status-at-week-k successor: its as-of substrate (nfl_pit_injuries forward capture) is accruing; unlock is calendar time, not machinery."
      - "⑦ (ledger, verbatim) ROS1's headline share figure ('29.9% vs 5.2%') was not, by itself, evidence of miscalibration — its PIT and lowest-decile readings were. A q05 = 0 share is not a calibration reading: a correct predictive puts q05 at 0 exactly when P(zero) >= 0.05."
      - "⑧ (ledger, verbatim) The CRPS value of an update depends on the interval it is scored with: every position's lift roughly halved once both predictives were calibrated (RB 3.50 -> 1.53)."
      - "⑨ (ledger) Controls + RED proof: positive 0.0092 vs pi=0 0.279 / location-shift 0.124; RED proof 27/27 (interval) + 15/15 (node 4). Catches: a vacuous foil-carry fixture (seed mapped the only playing player to himself), a mis-designed leak break (replaced, not excused), two new guards (the foil reads the PERMUTED pi on the quantiles it produced; the atom event is y <= 0), and the contract's own `ranking_note` field tripping the no-rank guard (renamed comparison_note)."
      - "⑩ (node 4, live finding) score_row applies ANY field a row carries regardless of the term's resolution verdict — the served 2026 board carries twoPt (no history board did), so the prior rate silently applied two-point conversions until the stat-line coherence gate caught it. Fixed in assemble (strip unevaluated fields). Sibling risk: any other surface that restricts a term set by verdict alone."
      - "⑪ (node 4; PM-RULED 2026-09-18 — carried on the served manifest as `upper_tail_note`, and inherited by WVR1 when it renders the value column) 9 of 197 certified RB rows (the top backs) carry a P90 above REALIZED_MAX_SEASON_PACE scaled to games remaining (e.g. Gibbs 664 vs 482); no point does. The registered ratio table's top cell spreads upward (RB over-covers, 0.859). Not a gate; a display consumer should know."
      - "⑫ (node 4) app/backend/models/nfl_ros.py is box-reached (widened deliberately in test_nf_wk_rc1_cadence). An app-only edit to it will not rebuild the box — the nfl_weekly hazard, one contract over."
      - "⑬ (pre-existing, repaired) test_nf_infra1_sleeper_hardening's exhaustive node set was already RED on dev (NF-WK-RC1 added nfl_realized_freshness_op without moving it; skipped on CI for lack of a manifest, so unseen). Re-anchored with no-dependency assertions for both new leaves."
      - "⑭ (node 4 design, flagged against R3; PM-RULED 2026-09-18 APPROVED — R3 is SUPERSEDED by ruling, recorded in amendment 4 §1, not quietly deviated from) the cadence rides sports_nfl_weekly_serving_job as an independent branch off its stats ingest (INC-25 by construction, the schedule is already RUNNING + heartbeat-checked) and is deploy-held by NF_ROS_PUBLISH_ENABLED, instead of a separate 09:30 default_status=STOPPED schedule (a STOPPED default cannot be heartbeat-checked — NCAAF-P1.2W/NF-CAP1)."
      - "⑱ (amendment 3 outcome, 2026-09-18) RB-rookie C9 = 0.0810 vs the 0.05 bar -> STOP_TO_PM. 1,464 rows / 122 player-seasons; cov80 0.7637 against a flat veteran control (C9 0.0120, cov80 0.8704). The decile shares ramp upward with the excess in the top two deciles (0.130, 0.181 vs 0.100), i.e. rookie outcomes land high in their own predictive. The magnitude sits inside amendment 3's fully-clustered range and outside its independent one, so the SHAPE is the discriminating evidence — amendment 5 defines the per-player spread the PM asked to decide on, gating nothing. PM disposition owed: a disclosed rookie-stratum note, or rookie rows as a stated absence."
      - "⑲ (amendment 5 outcome, 2026-09-18) The rookie tilt is BROAD, not a few breakouts: the top-decile mass comes from 54 of 122 player-seasons (44.3%), only 1 sits entirely inside it, and 62.3% of it comes from outside the ten largest contributors. The whole per-player-season mean-PIT distribution is shifted up — median 0.594 vs the VETERAN reference 0.435 (the calibrated reference for this statistic is the control, not 0.5), 63.1% vs 41.4% above half, z=+4.67 at the player-season level where clustering is handled by construction. Reproduced the fired read exactly (reproduction.identical true). No trimmed re-read was computed — re-reading a fired gate on a population chosen after the result is the post-hoc trim the program refuses."
      - "⑳ (successor candidate; sibling of ③) A ROOKIE-SPECIFIC INTERVAL TREATMENT is a registered-successor candidate, not an in-story patch (PM, 2026-09-18). The measured target: rookie outcomes land high in their own predictive — the hurdle's conditional-spread top cell is thin on the rookie upside even though RB over-covers overall (0.859) and finding ⑪ shows that cell already spreading above any pace on record."
      - "㉑ (process note, dated 2026-09-18 — a sequencing slip the architecture absorbed, NOT a precedent) PR #1173 merged at 01:33:15Z; the RB-rookie read it was gated on returned at 01:34:16Z, 61 seconds later, with STOP_TO_PM. Nothing published and no consumer read, because node 4 is deploy-held by NF_ROS_PUBLISH_ENABLED (off, deliberately not in env.required), so the disposition now gates ENABLEMENT — a boundary of equal strength to the merge gate it replaced. That is the argument FOR deploy-held flags; it is not a general licence, because the reason it did not matter is a property of THIS story's flag design that a future story might not share. Record commits were orphaned by the merge and carried by PR #1179."
      - "㉒ (PM disposition (ii), implemented 2026-09-18) Rookie rows at a certified position are served as absence `rookie_interval_not_certified` with NO value in either direction — no band and no point (serving a certified-looking number whose interval was refused splits one row's honesty in half). Keyed on the board's `rookie` FLAG, never the id shape (all 13 live RB rookies carry synthetic ids, so an id-shape rule agrees today and diverges silently on the first gsis-id rookie; the fixture carries one). The rookie reason BINDS over join_unresolved (fixing a join would not certify the interval), and join_unresolved_names now derives from the identity AUDIT so a rookie's broken join is still counted. Live build: 184 certified RB rows (was 197), 13 rookie absences, manifest carries the stratum note. Blast radius 13 rows today, growing as rookies accumulate games — growth that also adds rookie evidence for the ⑳ successor. RED proof 51/51."
      - "⑯ (census family; PM ack 2, 2026-09-18) `projection_fields.PAID_SCORING_FIELDS` was a HAND LIST (fpStd/fpHalf) inside the module whose own header explains why hand lists leak — so NF-ROS1b's six per-scoring ROS values (rosPts/rosP10/rosP90 x Std/Half) came out FREE while the season board's identical formats were paid, and the guard pinning the set held green because a membership pin cannot see a field nobody added. Ruled paid; re-expressed as SCORED_FIELD_STEMS x PAID_SCORING_PRESETS so a new per-scoring surface prices its formats by declaring one stem. Sibling risk for the census: any other pricing/entitlement set still spelled as names rather than derived."
      - "⑰ (pre-existing, repaired in passing) nf_inj_news_1_red_proof's `build_projection stops passing the overrides to the rookie half` break had been reporting ANCHOR MISSING (vacuous) on dev since NF-INJ4b inserted designation_games into the rookie call without re-anchoring the sibling story's proof. Re-anchored, substance unchanged; that proof is now 39/39 RED (was 38 + 1 vacuous)."
      - "⑮ (amendment 3) RB-rookie C9 design quantity: 1,464 rows / 122 player-seasons; P(>0.05 | calibrated) ~0.00 if rows independent, ~0.51 fully clustered — a reading above the bar is not by itself evidence of rookie miscalibration."
