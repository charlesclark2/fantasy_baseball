# Plan spec — NF-INJ2c (FULL tier: fresh registration of the assignment-rule fix)
# Authored by PM 2026-08-29 per NF-INJ2b rulings D1/D3/D4 + the §9#2 disposition ruling.
# RE-SCOPED by PM 2026-08-31 on the node-1 refutation — see `pmRescope` below, transcribed
# VERBATIM. The dispositions it resolves were declared BEFORE any scoring, so the re-scope is
# a choice between pre-declared branches, ⛔ not a post-hoc relaxation (E2.1-r).
---
apiVersion: planspec.io/v1alpha1
kind: Goal
metadata:
  name: nf-inj2c
  vertical: nfl_fantasy
  card: https://trello.com/c/Dsz8CROW
  status: IN_PROGRESS(the DSR field is RULED — PM 2026-09-01 declared the binding family; node 3's prereg is written on that basis; the decisive run is downstream of the operator's node-3b re-measure)
spec:
  description: >
    Fresh forward registration of the ASSIGNMENT-RULE fix — the mechanism
    NF-INJ2b's matched foils actually isolated (+0.4452 CRPS, 7/7 folds,
    p=0.0009, ordering held at all four positions; the rate fit-target
    read mildly harmful, refuting NF-INJ2's stated mechanism). NF-INJ2b
    nulled on (a) a mis-specified coherence gate (=0 demanded of a
    per-fold mean, not attribution-controlled) and (b) DSR 0.9325 vs 0.95,
    POWER_LIMITED at +1 fold. This is the fourth fix-the-spec successor
    (MARGIN2->3, W7->W7b, VAL3->VAL3b). HONESTY CLAUSE (verbatim, per the
    2b session's own seam): the assignment-rule result is settled; what is
    NOT settled is whether that arm can clear a correctly-specified
    coherence clause at all — 8.57 violations/fold is not an edge case a
    tolerance absorbs, and this story may buy a properly-specified record
    of a second refusal. Still funded: either outcome closes the question
    honestly. best_alpha=0; DEPLOY-HELD; SERVED_ARM stays incumbent until
    the gated ship path + operator approval.
  acceptanceCriteria:
    - description: >
        NODE 1 IS A HARD GATE — DIAGNOSE THE GAMES-FLOOR FIRST: establish
        or refute the hypothesis (assignment floors at max(games, 0.25)
        while the check uses real games; surviving evidence is only a
        count+position, 2019/QB/1 player — the row is gone). ⛔ IF REFUTED:
        STOP after node 1 and return to the PM — clauses below get
        re-scoped BEFORE the pre-registration is written, not after. Only
        a confirmed-or-refuted mechanism licenses a tolerance.
    - description: >
        THE PRE-REGISTRATION (committed before any scoring) carries:
        (a) the coherence clause declared FORWARD — attribution rule +
        tolerance, designed on the node-1 diagnosis, never "=0" of a
        per-fold mean; (b) primary arm = the assignment rule in point
        space (stratified's isolation), with the 2b field members carried
        by name; (c) the +1-fold question settled on DATA-FIDELITY grounds
        (the 2019-2025 window was inherited from NF1.5 score_from, not
        chosen — extend to 2018 only if data-honest, declared forward;
        admissible because classify_null read POWER_LIMITED with a
        reachable fold, not an NF-D18 trigger); (d) V-membership, BH
        family, pbo_application="field", and the positive control per the
        2b conventions (note PLAT-CVP2's CONSTRAINT_BLOCKED fix may not
        have landed — if BLIND fires on injection-invariant gates, carry
        the 2b annotation pattern).
    - description: >
        ⚖️ PM RE-SCOPE RULING 2026-08-31 (node-1 refutation) — TRANSCRIBED VERBATIM,
        PM-AUTHORED. It SUPERSEDES the disposition ordering in the clause below,
        which is left in place unedited because the branches it declares are what
        the ruling chooses between (E2.1-r: a pre-registration is not rewritten,
        it is resolved).

        "1. THE PRIMARY DISPOSITION (full coherence restoration) is RETIRED FOR THIS
        STORY, on the §4 bound as the licensing evidence: the envelope is a per-stat
        ceiling and every aggregate-permuting rule is structurally incapable of zero
        against it — points_rate_permute's own 1.09x residual proves it. Demanding =0
        of this family is the decision-in-a-gate's-clothing the program refuses. The
        full-coherence question moves to NF-COH2 (PM-carded: the per-stat mechanism +
        the new ordering mechanism it requires — options (a) and (c) merged,
        post-Week-1, bounded by §4).

        2. THE REGISTERED SHIP ROUTE is the STRICT-DOMINANCE disposition, alone:
        stratified vs the SERVED incumbent, forward margins on EVERY measure —
        violations/fold, worst-x, give-back %, CRPS, per-position ordering, interval
        floors — improve-or-tie everywhere, regress nowhere. Coherence is MEASURED AND
        REPORTED (both residual populations, per your §2 table), never gated at zero.

        3. THE GIVE-BACK RE-MEASURE IS A PREREQUISITE, exactly as you flagged: the
        82.70% -> 27.85% figures rode a board whose pin failed at 40.58/797 rows.
        Capture-pin a fresh board + market snapshot (the D3 convention) and re-measure
        the give-back and the coherence table on it BEFORE the prereg quotes any
        number. If the re-measured dominance no longer holds on any dimension, that is
        a NULL, not a margin to adjust.

        4. THE +1 FOLD is registered forward on the data-fidelity ruling already given
        (2018 admitted only if data-honest; state the finding either way). DSR at 8
        folds is then a real gate, not a formality — 0.9325 at 7 does not guarantee
        0.95 at 8.

        5. DISCLOSURE IS PART OF THE SHIP: the residual impossible lines (8.57/fold,
        incl. Easton Stick) remain covered by NF-INJ1-C's suppression and NF-RATE1's
        rate-line fix (in flight — coordinate only at the level of confirming its
        guards stay green on your counterfactual; no shared files). The operator packet
        states plainly: this HALVES the incoherence and the give-back, it does not end
        them, and NF-COH2 is the recorded successor for the rest."

        HONESTY FRAMING for the record (PM, verbatim): "if this clears, Week 1 ships a
        board that is strictly better on every measured dimension with its remaining
        defects disclosed — which is what the identity actually demands; if it nulls on
        the re-measure, the question closes at a specification nobody can argue with.
        Either way the thread ends honestly."

        ⛔ SEQUENCING THE SESSION ADDS, so ruling 3 cannot re-import the inversion it
        exists to prevent: the re-measure shows the ARM's numbers, and margins written
        after seeing them would be reverse-engineered from the result (E2.1-r). So the
        margin CONSTRUCTION RULE — how every forward margin is derived, from DESIGN
        quantities only (a measurement's own precision, a per-fold SE), never from an
        observed gap — is committed BEFORE the re-measure runs; the re-measure then
        fills in the SERVED INCUMBENT's baseline through that already-committed rule.
        Ruling 3's pre-committed NULL branch is what makes this safe either way.

        ⚠️ MARKED ANNOTATION ON RULING 4 (added 2026-09-01 by PM ruling; the ruling's text
        above is VERBATIM and STAYS STANDING, UNEDITED). Ruling 4's premise — that the +1
        fold would settle the gate, so "DSR at 8 folds is then a real gate, not a
        formality" — is MEASURED FALSE: node 3c established on three independent legs that
        2018 is not data-honest (structurally empty pool at the shipped `base_from`;
        already run there and INACTIVE at three of four positions; and appending it moves
        the winner's per-fold Sharpe 2.0101 -> 1.6418, DILUTING the statistic it was
        prescribed to strengthen). The settlement is the PM ruling of 2026-09-01 below.
        PM, verbatim on the shape: "a PM ruling whose premise a measurement later refuted
        is itself part of the record; correcting it in place would be the tidying-away
        this program forbids."

    - description: >
        ⚖️ PM RULING 2026-09-01 (decision request #2: the fold, the field, the record) —
        TRANSCRIBED VERBATIM, PM-AUTHORED. It SETTLES the fold question at 7 folds and
        DECLARES the binding deflation field. ⛔ Nothing above is edited; ruling 4 stays
        standing with node 3c as the correction beside it (see the marked annotation).

        THE FOLD (PM, verbatim): "The fold question is settled: the registration runs at 7
        folds. Your three-leg finding is accepted as the data-fidelity determination ruling
        4 asked for — stated either way, and it came out '2018 is not data-honest': the
        fold is structurally empty at the shipped base_from, its one prior run shows it
        inactive at three of four positions (an inactive fold is uninformative, never a
        pass — NF-D20), and appending it would dilute the very statistic it was prescribed
        to strengthen. Lowering base_from is refused for exactly the reason you gave: the
        window's authority is that it was inherited, not chosen, and re-cutting it after a
        gate missed is what the inheritance exists to prevent. Record the sole remaining
        8th fold as calendar-bound: the realized 2026 season — that is a genuinely
        reachable trigger and may be published as one (it is not an NF-D18 violation; the
        trigger names a date, not a purchase no design can make)."

        THE FIELD — the declaration, transcribed verbatim as the PM directed:

        "PM ruling (2026-09-01, NF-INJ2c prereg): the deflation gates' BINDING field is
        NF-INJ2c's own coherent family, declared on mechanism before any deflation
        statistic is computed: point-space assignment rules only — incumbent (reference),
        stratified, feasibility_clamp, and the two registered degenerates. The four
        rate-space arms are excluded as a DIFFERENT MECHANISM already refused on the
        ordering gate (NF-INJ2's CONSTRAINT_REFUSED — that refusal stands and is not
        re-read; carrying a refused mechanism's arms in V would tax this contest for a
        search it is not running: the MH2.5/NF-W6b-C V-inflation class). Provenance of the
        narrowing: PM ruling 2 on the 2b closeout framed this story's contest as
        stratified-vs-the-SERVED-incumbent — a point-space question — before any deflation
        figure at any field width existed; the family follows from that ruling, not from
        2b's 0.9325. V-membership per standing conventions: the incumbent reference arm
        ∉ V (MH2.1(a)); degenerates ∈ n_trials, ∉ V (DSR-CONV, opted into here, forward).
        Per NF-D14's two-sided rule, the INHERITED 10-arm field's DSR is ALSO computed and
        reported beside the binding figure, labelled NON-BINDING DIAGNOSTIC, declared as
        such here in advance — both numbers publish regardless of which looks better, and
        no third field is ever computed. If the binding family's DSR refuses, that is
        DEFLATION_REFUSED and the thread closes at that specification; the diagnostic does
        not rescue it."

        ON THE RESULT-AWARENESS CAVEAT (PM, verbatim): "Your result-awareness caveat is
        accepted and answered on its own terms: the defence is provenance, the provenance
        is mine and checkable, and the two-field-both-published structure removes the
        remaining degree of freedom — a reader can verify nothing was chosen by outcome
        because both outcomes are on the record and the binding one was named first."

        THE RECORD (PM, verbatim): "Ruling 4 stays standing, unedited, with node 3c as the
        correction beside it. That is the right shape — a PM ruling whose premise a
        measurement later refuted is itself part of the record; correcting it in place
        would be the tidying-away this program forbids. The annotation should say the
        premise ('the 8th fold settles the field question') is measured false and point at
        this ruling as the settlement."

        NODE 3a ENDORSED (PM, verbatim): "Node 3a is endorsed as written, including the
        margin-construction clause (R1/R2/R3, never an observed arm-vs-incumbent gap —
        correct, and it closes the inversion ruling 3 was exposed to). Both flagged measure
        definitions are APPROVED as declared: give-back as max(pct, 0) (a signed value
        banking credit for over-discounting would be a defect, not a virtue) and the
        attribution-controlled violation count carried despite measured inertness (an inert
        declared measure is a finding; an undeclared one is a hole)."

    - description: >
        REGISTERED DISPOSITIONS, in order (PM ruling on 2b §9#2, declared
        HERE forward — 2b's record is never re-read on this):
        PRIMARY: full coherence restoration under the correctly-specified
        clause + all standing gates -> the normal gated ship path.
        SECONDARY — STRICT-DOMINANCE-IMPROVEMENT: shippable ONLY if the
        arm improves or ties EVERY registered coherence + give-back
        measure vs the SERVED incumbent (violations/fold, give-back %,
        each declared with a forward margin) AND regresses NOTHING
        (ordering per position, CRPS, interval floors) — with the residual
        incoherence still disclosed on-surface (NF-INJ1-C suppression +
        chips remain load-bearing and their guards must stay green on the
        counterfactual). A dominance ship is still deploy-held to the
        operator with the full measurement.
        NULL: closes cleanly with its class; no re-test trigger unless
        genuinely reachable.
    - description: >
        Carried executions from 2b's rulings: D4 — the marked annotation
        on NF-INJ2's give-back figure (self-referential apply_2026
        baseline; the figure did not decide that verdict; pointer to the
        forward fix) — annotate, never re-run; D3 — the pin-against-
        capture landmine appended to CLAUDE.md in the staged wording
        (a repro pin over a live-snapshot-fed surface pins a captured
        artifact, not a re-pull); plus the 2b uniform-injection/PBO
        lesson if not already landed.
    - description: >
        Reproduction discipline per the D3 convention: capture the board
        + its market snapshot at study start and pin against the capture;
        record the freshness reading beside it. Standing rules: no bitwise
        comparisons (population-scoped 1e-9; rookie-band motion vs a
        >=5-draw same-commit envelope); RED-proofs with unique mutation
        anchors (the 2b harness lesson); >2-min commands to the operator
        paste-ready; run the owning CI shard for any shared guard touched.
    - description: >
        IF EITHER SHIP DISPOSITION FIRES: the level-adjacent path —
        counterfactual rebuild, diff, placement read + interval reval via
        --out stems, served give-back measured with the FIXED apply_2026
        (arm-independent baseline), operator packet with top-25 moves per
        config incl. superflex; combined read on the exact
        publish-candidate board; the publish decision is the operator's.
---
apiVersion: planspec.io/v1alpha1
kind: OverlapCheck
metadata:
  goalRef: nf-inj2c
spec:
  boardSearch:
    queries:
      - 'trelloSearch cards: "NF-INJ2" / "ordering" / "assignment" (board Credence Sports, 2026-08-29)'
    duplicates: []
    adjacent:
      - "NF-INJ2b (Done, PR #1048/#1051) — the evidence base; its record stands and is never re-read on the new dispositions."
      - "PLAT-CVP2 (To Do) — the BLIND/CONSTRAINT_BLOCKED instrument fix; consumed if landed, annotated-around if not."
      - "NF-INJ1-C suppression + NF-C8 chips (live) — load-bearing under the dominance disposition; guards stay green."
      - "Full-season-rate render card (To Do) — display; unaffected."
  triedLedger:
    files:
      - "ablation_results nf_inj2b record (PR #1051: the 2x2, the matched-pair isolations, the gate ledger, §9)"
      - "ablation_results/nf_inj2_rate_permutation.md (the refusal whose mechanism is now annotated as refuted-by-measurement; verdict untouched)"
    recordedNulls:
      - "NF-INJ2b NULL (branch 3) — this IS its sanctioned successor per ruling D1; what is new: the diagnosed coherence clause, the forward dispositions, and the fold ruling. Not a re-read."
      - "NF-INJ2 CONSTRAINT_REFUSED — verdict stands; its MECHANISM claim is superseded by 2b's measurement, recorded via the D4 annotation, never edited."
  supersedes: []
  dependsOn:
    - "PR #1051 merged to dev"
  feedsInto:
    - "Coherence + ordering + the give-back resolved on the flagship board — the terminal story of the injury thread's NF1.5 branch"
---
apiVersion: planspec.io/v1alpha1
kind: Plan
metadata:
  name: nf-inj2c-plan
spec:
  goalRef: { name: nf-inj2c }
  graph:
    nodes:
      - id: 0-spec-commit
        desc: "Commit this spec (status IN_PROGRESS) before any other work."
      - id: 1-diagnose
        desc: "Games-floor hypothesis: establish or refute. ⛔ REFUTED -> STOP, return to PM for re-scoping."
      - id: 2-carried-executions
        desc: "D4 annotation on NF-INJ2's record + the D3/2b CLAUDE.md landmines (separate commits)."
      - id: 3a-margin-rule
        desc: "PM re-scope seq: commit the margin CONSTRUCTION RULE + the pre-committed outcome branches BEFORE the re-measure, so no margin can be reverse-engineered from it (E2.1-r)."
      - id: 3b-remeasure
        desc: "PM ruling 3: capture-pin a fresh board + market snapshot (D3), re-measure the give-back + coherence table on it. Dominance failing on ANY dimension = NULL, ⛔ never a margin to adjust."
      - id: 3c-fold-fidelity
        desc: "PM ruling 4: the +1 fold on DATA-FIDELITY grounds — 2018 admitted only if data-honest; state the finding EITHER WAY. FINDING: NOT admitted (3 legs); SETTLED at seven folds by the PM ruling of 2026-09-01, which also records 2026 as the calendar-bound 8th."
      - id: 3-prereg
        desc: "The full registration — STRICT-DOMINANCE route alone (PM ruling 2); coherence measured + reported, ⛔ never gated at zero; SEVEN folds and the BINDING point-space field per the PM ruling of 2026-09-01, with the inherited 10-arm field published beside it as a NON-BINDING diagnostic; committed BEFORE scoring."
      - id: 4-run
        desc: "Decisive run (operator handoff if >2 min; capture-pinned board)."
      - id: 5-classify
        desc: "Gates + dispositions in registered order; decisive record."
      - id: 6-ship-path
        desc: "Only if the dominance disposition fires: the level-adjacent path + operator packet. PM ruling 5 — the packet states plainly that this HALVES the incoherence and the give-back and does not end them, names NF-COH2 as the recorded successor, and confirms NF-INJ1-C's suppression + NF-RATE1's guards stay green on the counterfactual (guard level only; ⛔ no shared files)."
      - id: 7-closeout
        desc: "Verdict + findings into closeout.followUps; operator summary."
  closeout:
    verdict: >
      NODE 1 REFUTED (2026-08-29) — the games-floor hypothesis is dead by a two-sided
      measurement, and the story STOPPED at its hard gate and returned to the PM as the
      spec requires. PM re-scope ruling 2026-08-31 resolved the fork to the STRICT-
      DOMINANCE route (see `pmRescope`, transcribed verbatim in the acceptance criteria):
      the PRIMARY full-coherence disposition is RETIRED on the §4 per-stat bound and moves
      to NF-COH2; nodes 3-7 proceed re-scoped. `best_alpha = 0`; DEPLOY-HELD; SERVED_ARM
      stays incumbent until the gated ship path + operator approval. PM RULING 2026-09-01
      then settled the two questions node 3c surfaced: SEVEN folds (2018 not data-honest;
      2026 recorded as the calendar-bound 8th) and the BINDING deflation field (NF-INJ2c's
      own coherent point-space family, with the inherited 10-arm field published beside it
      as a declared NON-BINDING diagnostic). Node 3's prereg is written on that basis.
    followUps: >
      NODE-1 FINDINGS, retained (they are the re-scope's licensing evidence).

      (1) The games floor is INERT on every fold: min proj_games 0.814-1.215 (3.3-4.9x the
      0.25 floor), zero non-finite rows, zero rows at or below it, zero rows the kernel
      floored — by BOTH the recorded `games_floor_binding()` definition and the wider one
      the kernel applies. The mechanism could not act (NF1.9 / NF-D20).

      (2) All 349 violating stat-rows, every arm and fold, are STAT MIX: the multiset a
      rate arm preserves is the FANTASY-POINT rate multiset, not each counting stat's own
      rate. Zero floor, zero clamp-lo, zero pre-existing. `rate_refit` 2019 = LAMAR
      JACKSON, rushAtt 12.44/g vs an all-time 11.73 (1.06x), on 16.5 expected games.

      (3) ⭐ THE §4 BOUND, now the licensing evidence for retiring the primary: the
      envelope is a per-STAT ceiling while every assignment rule permutes a per-PLAYER
      aggregate, so NO aggregate-permuting rule can be coherent BY CONSTRUCTION against
      it. Routed by the PM to NF-COH2 (Backlog), which also owns writing it into
      prod-state.

      (4) The two residual populations, which the ship route MEASURES AND REPORTS rather
      than gates: rate arms at worst 1.06-1.10x with zero rows under 2 expected games;
      `stratified` at 8.57 players/fold on 7/7 folds, worst 1.85x, 36.8% of violating rows
      under 2 expected games, against an incumbent residual of 19.14/fold, 2.51x, 34.2%.

      (5) `nf_inj2_rate_permutation.games_floor_binding()` counts `isfinite(g) & (g <
      FLOOR)` while the kernel floors `~(isfinite(g) & (g > FLOOR))`, so the recorded
      self-check cannot see a non-finite row the kernel does floor. Inert on every
      population measured. Routed by the PM into PLAT-CVP2 (item 2).

      (6) A fresh worktree silently REBUILDS the gitignored NFL feature caches from a live
      upstream, so a reproduction there is measured on a different feature vintage (51/56
      bare vs 55/56 after rsyncing the main checkout's caches). Landed in CLAUDE.md.

      ✅ RESOLVED BY PM RULING 2026-09-01 — THE FOLD AND THE FIELD (raised at node 3c,
      held, ruled). The block that stood here set out the two options and was answered:
        * THE FOLD: settled at SEVEN. Node 3c's three-leg finding is accepted as the
          data-fidelity determination ruling 4 asked for; lowering `base_from` is refused
          because the window's authority is that it was INHERITED, not chosen. The sole
          remaining 8th fold is recorded as CALENDAR-BOUND — the realized 2026 season —
          and the PM has ruled it publishable as a re-test trigger: "it is not an NF-D18
          violation; the trigger names a date, not a purchase no design can make."
        * THE FIELD: option (B), with structure. The BINDING field is NF-INJ2c's own
          coherent point-space family (`incumbent` reference + `stratified` +
          `feasibility_clamp` + the two degenerates); the four rate-space arms are excluded
          as a DIFFERENT mechanism already refused on ordering. The INHERITED 10-arm field's
          DSR is computed and published BESIDE it as a NON-BINDING DIAGNOSTIC, declared in
          advance per NF-D14's two-sided rule — both numbers publish whichever looks better,
          and no third field is ever computed. A binding-family refusal is
          DEFLATION_REFUSED and the diagnostic does not rescue it.
        * The session's result-awareness caveat was ACCEPTED and answered on provenance
          (the narrowing follows from the PM's own ruling 2, written before any deflation
          figure at any width existed) plus the two-field-both-published structure, which
          removes the remaining degree of freedom. No per-candidate-family DSR was computed
          before the declaration, per the NF-INJ3b-M rule.
        * RULING 4 STAYS STANDING, UNEDITED, with node 3c as the correction beside it —
          the PM's ruling on the shape of the record, not an oversight.

      ⚠️ FOR PM TRIAGE — ONE CONDITION THE REGISTRATION PUTS ON THE 2026 TRIGGER (node 3).
      The ruling permits publishing the calendar-bound 2026 fold as a re-test trigger ("may be
      published as one"). The prereg publishes it CONDITIONALLY: a fold trigger is only meaningful
      when `SR > SR0`, because `n` enters DSR through `√(n−1)` and can scale a positive gap but
      cannot create one (NF-W8-0d's lockstep invariant). If the decisive run reads DSR_UNREACHABLE,
      the trigger is WITHHELD WITH THAT REASON STATED rather than published, because publishing it
      would be the actively-misleading NF-D18 direction. Read as a standing-rule application of a
      PERMISSIVE ruling, ⛔ not a departure from it — flagged here so the PM can overrule it.

      🔴 NODE 3b RUN 1 = VOID (2026-09-01), and the cause is UNRECOVERABLE — full record in
      `ablation_results/nf_inj2c_node3b_void_diagnosis.md`. The pin failed at 84.72 over 797 rows
      against 0.05. Diagnosis, measured on both sides: ADP is IDENTICAL (same window, same 8161
      drafts) while the ECR cache is SIX DAYS stale (label 8/25 vs the served board's 8/31), and
      `market_rank = ecr.where(ecr.notna(), adp)` is ECR-PRIMARY and a feature at all four
      positions. Independent confirmation: the largest ECR mover (Josh Jacobs, rank 43 -> 145 over
      final cuts) IS the largest point mover, at exactly the 84.72 that failed the pin.
        * CAUSE OF THE STALENESS: `--market-refresh` names DIFFERENT SCOPES in two entrypoints —
          `run_nf1_5.py` re-fetches ADP/ECR, `run_season_projection.py` re-fetches the ADP census
          only (its own comment: "MVP-1 is market-blind by design"). The operator handoff
          prescribed the latter; the served board is the market-AWARE `nf1_5` board.
        * ⛔ BUT A CORRECTED COMMAND WOULD NOT HAVE SAVED THIS RUN: `apply_2026` hardcodes
          `market_refresh=False` at both call sites (correct — that is how an archived run is
          reproduced byte-for-byte), so the pin holds only if the ON-DISK ECR cache matches the
          served board's vintage, and nothing enforces that.
        * ⭐ STRUCTURALLY UNACHIEVABLE NOW: FantasyPros serves only the CURRENT snapshot (no as-of
          query) and the lake's ecr_benchmark asset is season-partitioned and overwritten, so
          2026-08-31's ECR exists nowhere reachable. A refresh today puts the local build on 9/01
          against a board built on 8/31 — differently wrong, not fixed. No timing and no re-run
          recovers it.
        * ⇒ FOR THE NEXT RUN both sides must sit on the SAME ECR day (FP rolls ~03-05Z; the board
          publishes ~14:18Z), with the cache refreshed to that day BEFORE the arms are built. The
          DURABLE fix, and the one worth building, is to extend the D3 capture to PIN THE MARKET
          CACHES BESIDE THE BOARD at capture time — capture-pinning the artifact is not sufficient
          when its INPUTS are unrecoverable daily vendor snapshots.
        * ⛔ No arm figure from the VOID run is quoted, compared or carried anywhere. VOID is a
          statement about the measurement, never about the arm.

      ⚖️ PM RULING 2026-09-05 (decision request #6: the control's null leg) — RECORDED.
        * D1 RULED. The session's diagnosis was adopted on both halves. The null leg's "no effect"
          hands back the REAL payload, so it is a noise test only for a study EXPECTING a null;
          this study's gate table IS its ship condition, hence `ship => a non-degenerate arm
          survives the null leg => VACUOUS` — the badge is ENTAILED by the outcome it exists to
          inform. THE TRUE FIX (an advantage-removed null construction + a declared study-shape
          input under which the current leg reports its own NULL_LEG_INAPPLICABLE) is CARDED as
          PLAT-CVP3, defect 5 of the instrument family. ⛔ THIS STORY DOES NOT WAIT FOR IT.
        * THE INTERIM IS A FORWARD DECLARATION, ⛔ NOT AN AFTER-THE-FACT ANNOTATION — committed as
          `ablation_results/nf_inj2c_preregistration_amendment_1.md` BEFORE PR #1084 merged and
          BEFORE the decisive run: (a) the VACUOUS badge is declared INAPPLICABLE to this family on
          the source-measured entailment, SCOPED so it never reaches a DEGENERATE survivor; (b) the
          control's BINDING substance is the INJECTED leg, and it CUTS BOTH WAYS — an undetected
          planted effect or a degenerate surviving either leg FAILS the control regardless of any
          badge, yielding `CONTROL_REFUSED`; (c) the badge is recorded VERBATIM beside it and §7's
          ban on re-running with `check_null_control=False` is untouched.
        * ⛔ (A) — "VACUOUS is disqualifying" — WAS REJECTED, and the reason is recorded so a later
          reader does not mistake it for the safe option: a control that structurally cannot
          support any shipping study of this shape is the UNACHIEVABLE-GATE family (E9.61's
          strict-pin boundary; NF-INJ4's oracle). The program refuses gates that cannot FAIL and
          gates that cannot PASS.
        * ⭐ THE AMENDMENT IS REFUSE-ONLY, and that is what makes an amendment written after a
          measurement admissible: every clause either leaves the disposition where it was or blocks
          it. It TIGHTENS node 3a §5 outcome 1 (which conditions SHIP on M1-M6 + the deflation
          gates and never mentions the control); §5 is ⛔ NOT edited.
        * D2 CONFIRMED. Passing `gate_classes=` carrying the partition §7 itself declared FORWARD
          is a change of DELIVERY, not substance. §7 stays VERBATIM with its measured-false
          PLAT-CVP2 premise beside it (NF-W7f). This also completes, with NF-INJ4b's registration,
          the two-caller trigger for retiring `cv_power`'s name-heuristic default (PLAT-CVP2's
          checklist item — ⛔ not this story's to build).
        * D3 CONFIRMED as standing, no overrule: the 2026 fold trigger publishes only where
          `SR > SR0` makes `n` a lever; `DSR_UNREACHABLE` WITHHOLDS it with the reason stated.
        * TRIAGE PLACED: the market-cache 5th NF-INFRA1 instance, the `market_refresh=False`
          live-pull fall-through on a cache MISS, and the two-scope `--market-refresh` flag are
          appended to the PLATFORM REPRODUCIBILITY card as accumulating evidence. The `parents[5]`
          off-by-one stays in this story's closeout followUps — the remaining flagged modules are
          swept by whichever story next touches them.
        * ⛔ The interim node-3b board figures stay UNCARRIED until the registered run produces
          them properly (PM, verbatim).

      🔴 NODE-4 PRECONDITION, MEASURED 2026-09-05 — THE DECISIVE RUN'S NUMBERS DEPEND ON WHICH
      CHECKOUT'S NF1.5 POOL CACHE IT READS, AND THE TWO ON THIS LAPTOP DISAGREE.
        * `artifacts/nf1_5_feature_cache/pool_base{b}.parquet` is GITIGNORED and `build_nf1_2_pool`
          REBUILDS a missing file from a live upstream with no error — so a checkout is scored on
          whatever vintage it happens to hold (the node-1 51-of-55 mechanism, and CLAUDE.md's
          "a cache keyed on the RANGE, not the query TEXT, serves a stale column set").
        * MEASURED: the worktree's cache (2026-07-31) and the main `dev` checkout's (2026-09-01)
          hold IDENTICAL ROW COUNTS for every base the seven folds need (2017-2024) and differ by
          FIVE COLUMNS the newer one has — `injury_games_served`, `injury_games_incumbent`,
          `fp_ppr_pg_sd`, `games_sd_raw`, `_formal_discount_applied`. Two of those are the
          NF-INJ3 / NF-INJ-NEWS-1 injury-cap columns, i.e. this story's own subject matter.
        * IT MOVES THE ARMS. Re-scoring folds 2023-2025 on each cache: the three SCORE-USING arms
          shift by up to 0.028 CRPS and the M1 margin by up to 0.032, while BOTH DEGENERATES are
          BYTE-IDENTICAL on all three folds (they ignore the fitted score) — which is what locates
          the difference in the scored arms rather than in the harness.
        * ⇒ THE DECISIVE RUN USES THE CACHE THAT MATCHES THE CODE THAT WRITES IT — the main `dev`
          checkout's. ⭐ The criterion is CORRECTNESS (the newer cache carries the columns the
          current `season_projection` emits; the older one predates them), ⛔ NOT the result: it
          happens to give `stratified` a LARGER margin on 2 of 3 folds, which is exactly why the
          criterion is stated first and independently (E2.1-r).
        * ⛔ NO number above is a finding about any arm — three folds, not the registered seven,
          and the run that produced them is a CODE-PATH smoke that wrote `_smoke` and was deleted.
        * NOW SELF-RECORDING: node 4's artifact carries a `feature_vintage` block with a CONTENT
          FINGERPRINT (row/column structure per file), ⛔ not an mtime — INC-41's lesson on a local
          file, and the difference above is invisible to an mtime by construction. RECORDED, ⛔
          never gated: node 3a §1 admits no band for it, and a guard pins it out of the verdict.

      🟡 NODE 4 = DEFLATION_REFUSED (2026-09-05) — STRICT DOMINANCE HOLDS; ONE GATE REFUSES.
      Artifact `ablation_results/nf_inj2c_decisive.{json,md}`, generated 18:53:02Z from commit
      7b21388c, which merged to `dev` at 18:50:32Z — i.e. the record was produced by COMMITTED,
      MERGED code and the ancestry is checkable (NF-INJ3b-M D3).
        * DOMINANCE = DOMINATES, on all six: M1 IMPROVES (+0.4551 CRPS, 7/7 folds, one-sided
          p=0.0012, band 0.0901) · M2 IMPROVES (-10.43 violating players/fold, negative on 7/7,
          band 2.2976; the `mvp1_null` attribution control was INERT exactly as 3a §3(b) declared
          in advance) · M3 IMPROVES (1.9947 -> 1.7467) · M4 IMPROVES (89.26% -> 30.02%) ·
          M5 TIES_OR_BETTER (QB +0.0087, RB -0.0013, WR +0.0026, TE +0.0014; nothing BH-significant)
          · M6 CLEARS every NF-D22 power floor.
        * ⭐ NF-INJ2's REFUSAL DOES NOT REPRODUCE. NF-INJ2 was CONSTRAINT_REFUSED at QB on the
          ordering constraint (rho 0.481 -> 0.350, BH-significant) because a POINTS-fitted ranker
          was handed a RATE multiset. Operating in POINT space, M5 regresses at NO position. That
          is the story's whole thesis, and it is the measured outcome.
        * DEFLATION: PBO 0.0 (field-level) · fold consistency 7/7 vs 6 required (the CALIBRATED
          clause, MH2 H8) · ⛔ ONLY `dsr` refuses, at 0.9306 vs 0.95.
        * POSITIVE CONTROL: badge DETECTED, binding verdict PASSES. The family DETECTED a planted
          effect over THIS field, so the refusal is evidence about the effect, ⛔ not about a blind
          gate family.
        * ⭐⭐ AMENDMENT 1 WAS INERT ON THIS RUN, AND THAT IS THE ENTAILMENT WORKING, NOT A WASTED
          DOCUMENT. §2 says `ship => a non-degenerate arm clears every gate on the real payload =>
          VACUOUS`. `dsr` refused, so NO arm cleared every gate, so the null leg had no survivor and
          the badge came back DETECTED rather than VACUOUS. `null_leg_declaration_applies` is
          recorded FALSE. ⛔ Recorded plainly rather than left to read as though the declaration did
          work: it was written for a branch this run did not take, and it stands for a future one.
        * FIELD-TRIM 2x2: STRUCTURALLY UNAVAILABLE (`V` has exactly two members) — pre-declared at
          §2.3(b) and stated A FORTIORI, ⛔ never as a trimmed number (NF-W7h).
        * LOCKSTEP (NF-W8-0d), run BEFORE any remedy was named: SR 1.908 vs SR0 0.8771, gap +1.031,
          lever OPEN, sign-invariant. `n` is therefore a real lever here.
        * VINTAGE: the run used the MAIN checkout's pool cache (fingerprint b6bd7b4a63b4edad,
          2026-09-01) — the precondition recorded above, satisfied and self-evidenced in the
          artifact.
        * Reproduction pin HELD at 0.05000000000000071 vs 0.05 (the representation-tolerant
          comparison from PM ruling #5).

      🔵 NODE 5 = POWER_LIMITED (`ablation_results/nf_inj2c_null_classification.{json,md}`).
        * `cv_power.classify_null`: the effect is positive and every gate is REACHABLE, but this
          design cannot resolve it — DSR alone needs 9 folds against 7.
        * RE-TEST TRIGGER **PUBLISHED**, on exactly PM ruling D3's condition (SR > SR0 makes `n` a
          lever), and stated in the unit that GROWS: **2 more folds**. ⛔ NOT reachable now — the
          window is SETTLED at seven (PM 2026-09-01; node 3c ruled 2018 out on data fidelity), so
          each further fold is a future SEASON: 2026 is the 8th, 2027 the 9th. ⚠️ The count is an
          ESTIMATE at this design's CURRENT SR and V, both of which move with new data — a horizon,
          ⛔ not a promise.
        * `field_remedy_admissible` = **False** (MH2.7, read as the MACHINE FLAG, ⛔ not the prose):
          five IS the declared minimum, so "use a smaller field" would mean dropping pre-registered
          arms because they lost — the MH2.2 laundering.
        * ⭐⭐ THIS IS **NOT** THE "THE FIELD DID IT" CASE, AND THE REFLEX WOULD BE WRONG HERE.
          Four prior stories in this vertical converted a DSR refusal into a ship via a fresh
          coherent re-registration (MARGIN2->3, W7->W7b, W6b-C, NCAAF-S1), which makes "re-register
          a narrower family" the reflex. It is MEASURED CLOSED here, two ways: the field is already
          the declared minimum (flag above), AND the NON-BINDING inherited 10-arm diagnostic field
          returns DSR **0.933** — within 0.003 of the binding 0.9306. Field composition is not what
          binds; per-fold EVIDENCE at seven folds is. This is NF-W7f's counterexample shape, and
          carding a coherent re-registration would burn a story on the wrong lever.
        * `V` declared 0.5409 vs whole-field 26.19 = a 48.4x inflation from the degenerates —
          DSR-CONV is doing real work, and the whole-field figure is a diagnostic, never the gate.

      ⛔ NODE 6 DOES NOT FIRE. The ship route is the STRICT-DOMINANCE disposition, and branch 1
      requires dominance AND the deflation gates. Branch 4 fired instead, so there is NO operator
      packet, NO publish, and `SERVED_ARM` stays `incumbent`. best_alpha = 0 throughout.

      REMAINING FINDINGS from nodes 3-7 land here. PM triages into: checklist item on the
      parent card | merge into an existing card (name it) | a new spec. Worker sessions
      never create Trello cards.
