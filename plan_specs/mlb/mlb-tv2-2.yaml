# Plan spec — MLB-TV2-2 (FULL tier: §0.5 — mixture-density head for the total_runs predictive SHAPE)
# Authored by PM 2026-09-01 per the TV2-0 SHAPE-BOUND routing (epic pa4ii3tQ, PR #1043).
---
apiVersion: planspec.io/v1alpha1
kind: Goal
metadata:
  name: mlb-tv2-2
  vertical: mlb
  card: https://trello.com/c/J9G2VHu8
  status: IN_PROGRESS
spec:
  description: >
    The funded shape fix. TV2-0 measured (served rows, post_lineup n=758,
    2026-06-23→08-23) that the marginal-shape oracle closes 100.3% of the
    printed P(over) error (gap +0.0726 → −0.0002) while ALSO improving
    CRPS + pit_ks — and that the FEATURE lever is inactive (scale-mixture
    oracle K=1 on 5/5 blocks; A2 per-game σ made CRPS worse; σ-scale
    channel 0.029). The served total_runs predictive is a symmetric
    Normal against a right-skewed target — the MH2.6/MH2.8/NCAAF-P2.5
    program-level finding, now confirmed in two sports. This story builds
    and registers a MIXTURE-DENSITY head (a K-component predictive whose
    likelihood is NOT flat at the symmetric start — the exact trap that
    collapsed MH2.8's skew-normal onto its own foil 5/5) to close the
    shape gap the oracle proved is there. best_alpha=0; bet_paused; a
    CALIBRATED honest totals distribution, never an edge play.
    DEPLOY-HELD: per MH2.1, a model-registry merge IS the deploy (no
    promotion gate exists) — nothing merges to main and no registry entry
    changes without the gated ship path AND explicit operator approval.
  acceptanceCriteria:
    - description: >
        FORWARD PRE-REGISTRATION (committed before any scoring) carries
        the FOUR epic obligations VERBATIM: (1) a LONGER-WINDOW
        REPLICATION leg of the shape gap — TV2-0's 2-month/1-champion
        read licenses FUNDING, not serving; the prereg names the wider
        served window (and its champion boundaries — the population is
        defined by the CHAMPION, not the calendar, per MH2.10) on which
        the gap must replicate before any ship clause can fire. (2) a
        TIE-WITH-FOIL guard: any arm that nests its foil (a K-component
        mixture nests K=1 Normal exactly as NB nests Poisson) must
        declare the collapse detector — a near-zero margin is a TIE that
        refuses to count, and the fit must NOT start from the symmetric
        point a flat likelihood can't leave (staggered/asymmetric
        initialization, with the MH2.8 positive control proving the
        fitter FINDS a planted skew). (3) the std_pred DISAMBIGUATION:
        two statistics share the name (validate_v2_gates.py:34
        mean-spread vs train_totals.py:121 predictive SD) — the prereg
        states which one every gate reads, by file:line. (4) the SCOPE
        statement: DISCRIMINATION (Var(mu)/Var(y)=0.0142 vs the >=2.0
        gate) is untouched by any shape fix; its attainability stays a
        named OPEN question on the epic — this story makes no claim on
        it and no gate reads it.
    - description: >
        FIELD DESIGN, declared coherent FORWARD (the MH2.8 lesson — its
        real skew arm cleared 9/10 clauses and died on a DSR bar one
        heterogeneous arm inflated; the sanctioned successor is a
        NARROWER coherent family, diagnostic ~0.79, NOT a foregone
        pass): mixture-density arms on ONE mechanism axis (K, and
        component-parameterization within the mixture family), the
        incumbent Normal as reference, the required degenerates, and a
        matched foil that keeps everything except the shape channel.
        Declare V-membership (incumbent reference arm and degenerates
        excluded from V per DSR-CONV/MH2.1(a) — named in the prereg, not
        adopted after), the BH family and which reading binds,
        pbo_application, and declared_field_size to classify_null.
        PLAT-CVP1 has LANDED: use the injected-effect positive-control
        HELPER (executed, not narrated), expect DEFLATION_REFUSED as a
        reachable state, and trust the lockstep computation over any
        "lower-variance design" instinct. Uniform-injection caveat
        (NF-INJ2b): report a field-level statistic as INERT when the
        injection cannot move the split winners.
    - description: >
        CALIBRATION INSTRUMENTATION per the MH2 lineage: any VARIANCE
        statistic sits in a SHAPE-MATCHED null, never a Normal-drawn one
        (MH2.10); PIT/coverage statistics keep the standard nulls; the
        leave-one-anomalous-season sensitivity (the 2.7% COVID season
        moved MH2.8's per-fold SNR ~5x) is registered and reported; and
        the negative control mirrors the SHIP RULE's margin, never a
        bare "closest arm" (MH2.8's second defect). The harness proves
        it CAN fail: clean-data false-positive rate + detection on a
        planted gross defect, both reported (the MH2.6 vacuity-floor
        discipline). Seed the GLOBAL RNG for any NGBoost/stochastic
        component (the pred-sigma reproducibility lesson); reproduction
        pins 1e-9.
    - description: >
        SERVED-ROWS DISCIPLINE (MH2.6: a backtest cannot see a
        serving-path defect): the decisive calibration read is over
        SERVED rows joined to realized finals, deduplicated to the
        served row, per tier — with the backtest window used only where
        the served population is underpowered, and each read labeled as
        which it is. Split any same-season backtest at the incumbent's
        fit date (MH2.1: the incumbent's held-out window is the only
        reading a same-season backtest supports).
    - description: >
        IF IT CLEARS — the gated ship path, ALL deploy-held, inheriting
        every MH2.1 promotion landmine by name: (a) audit every
        bundle-assuming consumer before a one-target swap
        (daily_model_predictions.model_version stamped from
        registry["home_win"]; the backfill idempotency key; any
        hardcoded model_version in marts); (b) SERVE THE OBJECT THAT WAS
        VALIDATED — persist exactly the mixture predictive the bake-off
        scored; the serving path calls pred_dist-style params, so a head
        without that surface needs its own serialized predictive object,
        never a re-derivation; (c) a registry change ships with the box
        image ON MERGE TO MAIN — so the merge itself is the promotion
        and happens only on operator instruction, with the operator
        packet carrying the served-impact read (P(over) at the posted
        line, before/after, per tier). Runtime gate: CI mocks IO — the
        real box run is an operator step, paste-ready.
    - description: >
        Standing disciplines: >2-min commands to the operator
        paste-ready with runtime/artifact/success criteria (the decisive
        fit almost certainly qualifies — time a smoke slice first,
        MH2.2's over-applied-tag caveat noted); RED-proofs with unique
        mutation anchors; no post-hoc trims (MH2.2); no gate re-reads
        (E2.1-r); E13.6b-B stays HELD and untouched — if this story has
        not cleared in ~6 weeks from kickoff, the epic tripwire fires
        and a SHAPE-AWARE isotonic serve-time calibrator gets its own
        registered interim read (a separate story, not a scope creep
        here); run the owning CI shard for any shared guard touched
        (cv_power pins live in cross-vertical shards).
---
apiVersion: planspec.io/v1alpha1
kind: OverlapCheck
metadata:
  goalRef: mlb-tv2-2
spec:
  boardSearch:
    queries:
      - 'trelloSearch cards: "TV2" / "mixture" / "total_runs" (board Credence Sports, 2026-09-01)'
    duplicates: []
    adjacent:
      - "EPIC TV2 (pa4ii3tQ, Backlog) — the routing authority; the four prereg obligations and the 6-week isotonic tripwire live there."
      - "TV2-1 — PREMISE-CONFIRMED, CLOSED (nothing to build on this population); not re-opened."
      - "E13.6b-B (HELD) — the serve-time calibrator; stays held; the tripwire is the only path that touches it, as its own story."
      - "MH2.8 (Done, INCUMBENT_STANDS) — the refused skew-normal recal; its record stands and is never re-read; this is its sanctioned narrower-family successor at the mechanism level."
      - "PLAT-CVP1 (Done) — the instrument this story's classification runs on; consume the helper, hand-record nothing."
      - "NF-INJ2c / NF-RATE1 (IN FLIGHT) — disjoint vertical and files; changelog week-block is the only shared surface if a user-facing change ever lands (it should not — deploy-held)."
  triedLedger:
    files:
      - "ablation_results/mlb_tv2_0_ceiling_diagnosis.md (the SHAPE-BOUND verdict + the oracle decomposition + the inactive feature lever)"
      - "MH2.6 / MH2.8 / MH2.10 records (the skew finding; the flat-likelihood collapse; the shape-matched null; the COVID-season sensitivity)"
      - "betting_ml train_totals.py + validate_v2_gates.py (the two std_pred statistics, by line)"
      - "cv_power post-PLAT-CVP1 (DEFLATION_REFUSED, lockstep compute, injected-effect helper)"
    recordedNulls:
      - "MH2.8 INCUMBENT_STANDS — stands as recorded; the E2.1-r line is absolute: this story registers a NEW coherent family forward, it does not re-cut or re-read MH2.8's field."
      - "TV2-0 feature-lever INACTIVE — not re-approached; no feature work anywhere in this story."
      - "MH2.10 morning-sigma partition ANTI-informative (rho<0) — inherited constraint: no per-game sigma partition work; the shape head is the lever."
  supersedes: []
  dependsOn:
    - "PLAT-CVP1 merged (landed — verify at kickoff with a one-line import check rather than assuming)"
  feedsInto:
    - "The served P(over) at the posted line becoming honest (the ~5.6pt over-statement retired); E13.6b-B's disposition; the epic's open discrimination question"
---
apiVersion: planspec.io/v1alpha1
kind: Plan
metadata:
  name: mlb-tv2-2-plan
spec:
  goalRef: { name: mlb-tv2-2 }
  graph:
    nodes:
      - id: 0-spec-commit
        desc: "Commit this spec (status IN_PROGRESS) before any other work."
      - id: 1-prereg
        desc: "Full registration: the four epic obligations verbatim, the coherent field, V/BH/pbo declarations, controls incl. the CVP1 helper — committed BEFORE any scoring."
      - id: 2-replication-leg
        desc: "Longer-window shape-gap replication on served rows. If the gap does NOT replicate, STOP after recording — the funding premise fails and the PM re-scopes."
      - id: 3-bake-off
        desc: "Decisive run (smoke-slice timing first; operator handoff if >2 min; capture-pinned inputs)."
      - id: 4-classify
        desc: "Gates + classify_null (declared_field_size) in registered order; shape-matched nulls for variance stats; the COVID sensitivity; decisive record."
      - id: 5-ship-path
        desc: "ONLY IF CLEARED: the MH2.1-landmine-audited promotion packet; merge = deploy = operator's call."
      - id: 6-closeout
        desc: "Verdict + findings into closeout.followUps; operator summary; epic card updated by PM (tripwire clock noted)."
  closeout:
    verdict: "DEFLATION_REFUSED. The mechanism is real and large; the pre-registered FIELD-LEVEL
      deflation gate refused the study. Node 2 REPLICATED (FULL_ERA n=851, gap +0.0734, outside its
      null band, MC p=0.0010, power 0.989). The vacuity floor CLEARED at 20 reps (positive control
      1.00 / clean 0.00 / gross METRIC detection 1.00). All four mixture arms clear C1-C6 and
      C8-C10; the winner mix3_full posts DSR 0.9789 vs the 0.95 bar and is sign-stable across all 8
      leave-one-block-out refits (0.0588..0.0742). NOTHING SHIPS because PBO is 0.771 vs the 0.20
      bar and ALL THREE registered readings fail (declared 0.221, eligible 0.771, two_arm 0.250),
      so the refusal does not hinge on which reading binds; classify_null returns DEFLATION_REFUSED
      with pbo_refusal_admitted=True. NO fold/season/row re-test trigger is published - no fold
      count moves a pre-registered gate POPULATION (NF-D18). NF1.8's discriminators, reported
      beside the refusal and gating nothing, say it is a TIE signature and not an unstable search:
      the four arms sit within a CONTENDER SPREAD of 0.00128 (2.33% of the best arm) and the flip
      mass over 16 buckets sits on two of them (7/7/2/0) - the coin flip is over WHICH of four
      near-identical parameterizations of ONE marginal shape wins in-sample, not over WHETHER to
      correct. DEPLOY-HELD throughout: nothing merged to main, no registry entry touched,
      deploy.sh not run. E13.6b-B untouched."
    followUps: >
      Findings land HERE. PM triages into: checklist item on the parent card |
      merge into an existing card (name it) | a new spec. Worker sessions never
      create Trello cards.

      (1) REUSE DEFECT, carried from the census -- `is_backfill = FALSE` DOES NOT
      ESTABLISH "SERVED". ~40,000 v0/v1/v2 rows carry that flag while sitting a
      MEDIAN 981-1009 DAYS after their own game date: a backtest wearing a served
      table's clothing, which MH2.6 forbids as a decisive population. TV2-0's pull
      (`mlb_tv2_0_ceiling_diagnosis.py`) filters on the flag and was safe only by
      ACCIDENT of its champion filter; it is under-specified for reuse by any story
      whose champion filter is wider. TV2-2 establishes served-ness by the INSERTION
      LAG and RAISES otherwise. Candidate: a shared served-row helper.

      (2) INSTRUMENT GAP -- the E7.14 sign-floor trap is not checked anywhere by
      default. At 5 folds the fold-SIGN floor 2^-5 = 0.031 sits ABOVE a 4-arm BH
      cutoff of 0.0125, so the multiplicity clause was STRUCTURALLY UNPASSABLE by an
      effect of ANY size, and nothing surfaced it -- `cv_power` HAS
      `folds_for_sign_certifiability` but no caller is obliged to consult it.
      Candidate: have `classify_null` (or a prereg-time helper) refuse a gate set
      whose sign floor exceeds its own BH cutoff, the way MH2.7 made the field-size
      remedy self-safe.

      (3) A PER-FOLD SERIES ON A CALIBRATION-IN-THE-LARGE STATISTIC CARRIES ~ONE
      EFFECTIVE OBSERVATION PER FOLD. The printed probability is a property of the
      block's LAW, so it is CONSTANT within a block; a Brier/|gap| fold series is
      then dominated by that block's realized-rate sampling noise. Measured: 4/6 vs
      6/6 gross-defect detection against a per-row CRPS series, both 0/6 on clean
      data. Generalises to any §0.5 leg whose primary is a pooled calibration
      quantity (NCAAF and prospect totals legs included).

      (4) DESIGN-POWER NOTE for the epic, stated in the unit that grows -- no
      materially LONGER served window exists for this champion: the era is bounded
      below by the fit date (2026-06-23) and above by today, so the widest window is
      +93 rows / +12.3% over TV2-0's, and it grows at ~93 rows per game-week. A
      genuinely INDEPENDENT replication is calendar-bound, not obtainable now.

      (5) E13.6b-B tripwire clock: kickoff 2026-08-31.

      (6) ⭐ THE SUCCESSOR, and the one thing it must NOT do. classify_null's own
      admissible remedy is a FORWARD-registered NARROWER COHERENT family chosen on
      MECHANISM - never a post-hoc re-cut of a field already scored (MH2.2). The
      field here is four parameterizations of ONE marginal shape, i.e. near-clones
      by construction, which is exactly why PBO reads as a coin flip. A successor
      that registers ONE form forward on mechanistic grounds makes PBO a two-arm
      decision (or inapplicable). ⛔ It must NOT register the arm that won here -
      that is discovering a family after scoring it, the exact laundering MH2.2
      forbids - and ⛔ this study deliberately computes NO per-candidate-family PBO
      menu, because publishing one would hand a successor a family selected on its
      own deflation statistic (NF-INJ3b).

      (7) ⭐ INSTRUMENT DEFECT in PLAT-CVP1, and it inverts a verdict silently.
      `injected_effect_positive_control` partitions a study's gates into
      deflation-class and metric BY NAME, and BLIND ("a null from this family is
      free") and DEFLATION_BLOCKED ("every metric gate fired; only deflation
      stopped it") are OPPOSITE readings. The intersection of its default names
      {cscv, deflated_sharpe, dsr, pbo} with this study's clause names was EMPTY,
      so the default call filed C7_deflation as a METRIC gate and returned BLIND
      for a family whose own blocking_gates showed every arm clearing every metric
      clause. Nothing warned. Candidate: have the helper RAISE (or report
      UNVERIFIED) when the caller's gate names intersect its deflation-class set in
      ZERO places, the way MH2.7 made the field-size remedy self-safe - a partition
      that matches nothing is the NF1.7 (a) vacuous-anchor class, one level up.

      (8) PROCESS: a renderer must be exercised against REAL producer output. The
      decisive run completed every computation and then died in write_report on a
      KeyError, because amendment 5 renamed a controls key and the rehearsal that
      was meant to de-risk the report path ran with controls=False - so the ONE
      section the amendment had changed never rendered. Recovery needed no re-run
      (the JSON is written BEFORE the markdown; --rebuild-report now makes that
      explicit). The guard added uses the COMMITTED decisive JSON as the renderer
      fixture: a hand-written one would restate the schema the renderer assumes.
