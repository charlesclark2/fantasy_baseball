# Captures land here

Git-ignored on purpose (see `.gitignore`). A capture is a structural summary of a real ESPN draft
room; it is handed over deliberately, not committed.

⚠️ **One capture cannot disconfirm** (NF-C0e — the Sleeper `fgm_50p` and ESPN yards-allowed-ladder
lessons: a fixture derived from the first payload inherits its author's assumptions). What a second
capture must differ in is recorded in `docs/nf_c_lda_0_espn_live_draft_spike.md` §5.
